# 班主任工作台：项目导航

## 最新执行入口（优先于下方历史基线）

2026-09-13：TASK-28已完成，周报与期末评语桌面/手机写入均等待服务器确认；失败保留本机内容和编辑上下文，周报补齐脏关闭保护，保存中防重复。M2完成，下一项TASK-30全量移除旧UI依赖。进度、测试和未覆盖边界以`docs/PROJECT_STATUS.md`与实施包tracker为准。

用户已确认UI彻底重构方案，当前已开始共享基础层及听写控制器/独立新视图开发，正式页面尚未切换。请从 `项目实施包-UI彻底重构与双主题-20260912/00-从这里开始.md` 读取；完整启动词在包内13号文档。先核对进度和当前源码，按33个任务执行。
下方20260911导航与旧规范作为历史补充；当前源码事实看PROJECT_STATUS，最新UI目标和具体操作看实施包。仅Git克隆不含用户参考图片，需本地完整资料包才能视觉实施。

最后更新：2026-09-11。

## 当前结论

当前唯一开发基线是本目录 `E:\文档\Codex\小红书\班主任工作台网站`。2026-09-06 的“两班各 50 人、学期期中”全流程审计后修复已进入当前源码；2026-09-09 已重新构建并生成部署包，但现有记录没有证明该包已经在服务器部署完成。

Git 仓库早已存在，但提交历史停在 2026-07-31；8 月至 9 月上旬的大量改动此前没有持续提交。本次将当前已验证源码登记为“历史补录基线”，从此按 `AGENTS.md` 强制维护状态、变更、部署和决策记录，并在测试通过后提交、推送。

用户已在 2026-09-11 明确要求按校园插画参考图完整重建页面构图和视觉层次。执行前必须读取 `docs/校园插画全站构图与视觉层次重建规范-20260911.md`；可直接复制给项目任务的消息见 `docs/给项目对话的校园插画全站重建指令-20260911.md`。这两份文件把 C1–C6 从普通风格参考提升为构图验收基准，旧资料中“不要求接近参考图”的表述不再适用于构图。

当前主代码远端为公开 GitHub 仓库 `https://github.com/a18502656182-arch/teacher`，本地 `main` 跟踪 `github/main`。旧的 `origin` 指向 `git.chatgpt-team.site`，因没有可用凭据只作历史保留，不作为默认推送目标。

## 目录用途

| 路径 | 用途 | 处理原则 |
|---|---|---|
| `app/` | 页面、工作台模块和 API 路由 | 当前有效源码 |
| `lib/` | 班级数据、认证和备份恢复公共逻辑 | 当前有效源码 |
| `db/`、`drizzle/` | SQLite 数据访问与迁移 | 改动前核对兼容与备份 |
| `worker/` | 辅助运行入口 | 当前有效源码 |
| `tests/` | 业务结构和认证集成测试 | 功能修改后必须运行相应测试 |
| `scripts/` | 布局审计、认证 UI 检查、数据库备份 | 支持工具 |
| `docs/` | 当前状态、变更、部署、决策和开发导航 | 新对话的主要记忆来源 |
| `data/` | 本地运行数据或知识索引 | 不新增提交班级数据/数据库 |
| `reports/` | 历史 QA 截图与报告 | 本地证据，不提交 Git |
| `*.tar.gz`、`*.zip`、`*.rar` | 部署包、备份和恢复包 | 本地保存，Git 只记录名称和哈希 |
| `对话恢复-*`、`对话完整恢复-*` | Codex 原始对话恢复资料 | 本地保留，不提交 Git |

## 新对话阅读顺序

1. `AGENTS.md`
2. 本文件
3. `docs/开发导航与状态-20260911.md`
4. `docs/PROJECT_STATUS.md`
5. `docs/CHANGELOG.md` 最新记录
6. 涉及部署时读 `docs/DEPLOYMENT_LOG.md`
7. 涉及架构和产品边界时读 `docs/DECISIONS.md`
8. 涉及校园插画全站重建时读 `docs/校园插画全站构图与视觉层次重建规范-20260911.md`
9. 需要直接启动本轮重建时使用 `docs/给项目对话的校园插画全站重建指令-20260911.md`
10. 需要追溯 2026-08-25 前后的细节时，再读 `对话交接文档-班主任工作台-20260825.md` 和相关专项计划
11. 需要一般性的新对话启动提示词时，使用该交接文档第 30 节“新对话启动提示词与最新交接索引”

## 当前常用命令

```powershell
npm.cmd run dev
npx.cmd tsc --noEmit --pretty false --incremental false
npm.cmd run lint
npm.cmd run qa:layout
npm.cmd run qa:strict
npm.cmd test
```

部署目录和历史命令见 `docs/DEPLOYMENT_LOG.md`。不能仅凭部署包已生成或 Git 已推送就宣称线上已经更新。
