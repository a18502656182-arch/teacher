# 班主任工作台认证与隔离部署说明

## 当前安全级别

当前部署暂不配置 HTTPS。只有显式设置 `AUTH_ALLOW_INSECURE_HTTP=true` 时才允许 HTTP 登录，Cookie 使用 `HttpOnly + SameSite=Lax`，但不能使用 `Secure`。此版本只能作为测试或过渡版本。

## 必需环境变量

```txt
AUTH_ENFORCEMENT_ENABLED=true
AUTH_ALLOW_INSECURE_HTTP=false
AUTH_TRUST_PROXY_HEADERS=true
AUTH_SESSION_SECRET=至少24位高强度随机值
REDEEM_CODE_SECRET=至少24位高强度随机值
ADMIN_KEY=至少8位管理员密码（HTTP 环境建议使用更长随机值）
ADMIN_ALLOWED_IPS=管理员实际公网IP
CLASSROOM_DB_PATH=data/classroom.db
DEEPSEEK_API_KEY=服务器已有密钥
DEEPSEEK_MODEL=deepseek-chat
# 可选：试卷图片/PDF 识别适配服务，仅部署在服务器端
EXAM_AI_ENDPOINT=https://受控的试卷识别适配服务
EXAM_AI_API_KEY=适配服务密钥
```

不要把真实密钥写进部署包、命令历史、URL、前端代码或日志。

## 可选：试卷识别服务

试卷与知识点分析会把老师选择的 JPG、PNG、WEBP 或 PDF（单份最大 12MB）提交到 `EXAM_AI_ENDPOINT`。该地址必须是受控的服务端适配器，接收 multipart 的 `file`、`examId`，并返回：

```json
{ "items": [{ "title": "第3题", "subject": "语文", "questionNo": "3", "knowledgePoint": "句型转换", "questionType": "填空", "maxScore": 5, "confidence": 0.92 }] }
```

本项目只负责鉴权、文件大小/类型校验和人工核对流程，不自行将 PDF 转图或替代第三方 OCR/大模型实现解析。没有配置这两个环境变量时，界面会明确提示未配置，不会生成伪造的识别结果。不要使用“随便填”的密钥；密钥只应由服务器环境变量提供，且不应把工作台访问令牌转发给识别服务。

## 部署顺序

1. 停止班主任工作台服务。
2. 备份 `/www/classroom/data/classroom.db` 并验证备份。
3. 将部署包上传到 `/www`，解压到 `/www/classroom`。
4. 在 `/www/classroom/.env.local` 设置上述环境变量。
5. 在 `/www/classroom` 执行构建并重启服务。
6. 访问 `/admin` 生成首个兑换码，使用测试手机号验证第一和第二浏览器进入、第三浏览器被拒绝。
7. 验证 `/w/demo` 可读但保存返回 403。

认证表和 `workspaces.owner_user_id` 会在首次访问认证接口时幂等创建。部署包不包含 `data`，不会覆盖服务器数据库。

## 未来切换 HTTPS

证书和 443 配置完成后：

1. 将 `AUTH_ALLOW_INSECURE_HTTP` 改为 `false`。
2. 配置 HTTP 到 HTTPS 跳转。
3. 仅在应用端口不对公网暴露、且反向代理由自己控制时，设置 `AUTH_TRUST_PROXY_HEADERS=true`，并确认代理传入可信的 `X-Forwarded-Proto=https` 和 `X-Forwarded-Host`。直连部署必须保持该项为 `false`，避免客户端伪造转发头。
4. 重新登录，确认认证 Cookie 带有 `Secure`。

不需要重写认证系统或迁移用户数据。
