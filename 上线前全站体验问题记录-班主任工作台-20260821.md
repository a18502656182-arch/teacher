# 班主任工作台上线前全站体验问题记录

记录日期：2026-08-21

项目名称：班主任工作台

审查范围：桌面端与手机版 15 个业务页面、主要详情弹窗、批量操作、长期数据场景、保存链路、响应式布局与上线前可用性。

## 1. 文档用途

本文用于保存本轮 Impeccable 全站审核发现的问题，作为后续修改、回归测试和验收的固定依据。问题按事实、风险推断和设计判断区分，避免把主观偏好误写成确定缺陷。

配套执行计划见：`Impeccable全站体验改造执行计划-班主任工作台-20260821.md`。

## 2. 审核结论

- 当前状态：可用于公开演示和受控试用，但当前令牌即权限的模式不具备商业销售所需的用户隔离能力。
- 设计健康度：24 / 40。
- P0 商业上线阻断问题：1 项。
- P1 高优先级问题：3 类。
- 自动化结果：`npm.cmd run build`、`npm.cmd run qa:layout`、`npm.cmd run qa:strict` 均已通过。
- 响应式结果：15 个页面的桌面端与 390px 手机版未发现正向横向溢出。
- 静态设计检测：Impeccable detector 返回空数组，不代表没有体验问题，只说明没有命中其确定性反模式规则。
- 浏览器控制台：发现 favicon 404；未发现影响主流程的脚本错误。

## 3. 已确认的产品边界

- 保留现有 workspace 业务数据 JSON 格式；允许为用户隔离增加所有权字段、认证表和认证接口。
- 不修改已有页面路由。
- 不删除真实业务能力。
- 不新增 npm 依赖，不执行 `npm install`。
- 手机端不能只是电脑版纵向排列，必须围绕老师的实际操作重新组织。
- 课程日程的周一到周五按钮条保持原状，不改成下拉。
- 值日岗位的周一到周五按钮条和台账状态按钮条保持原状，不改成下拉。
- 作业详情继续保留学生高亮选中能力，不恢复大头像框。
- 期末评语继续保留“学生列表 / 保存后的评语”两个入口结构。
- 页面需要承载 32 名学生、8 个小组、30 条以上记录、多个考试和多个学期。

## 4. P0 商业上线阻断问题

### P0-01 当前访问令牌同时充当身份和权限，无法形成真正的用户隔离

证据类型：代码事实。

事实：

- 当前 `/api/workspace/[token]` 的 GET 和 PUT 只校验 workspace 令牌、状态和到期时间，没有用户 session、设备状态和 workspace 所有权校验。
- 任何获得正式 `/w/{token}` 地址的人，都可能在令牌有效期内读取或修改对应班级数据。
- `workspaces` 当前没有 `owner_user_id`，系统不能回答“这份数据属于哪个用户”。
- `/api/knowledge/[token]` 同样主要依赖 workspace token 判断访问权限；AI、资料库和后续真实数据接口都需要统一纳入所有权校验。

结论：这不是单纯的登录页缺失，而是权限模型缺失。只在首页增加兑换码弹窗、但不修改数据接口，不能实现用户隔离，也不能安全售卖。

必须达到：

- 正式 workspace 的读取和保存同时满足：session 有效、session 关联设备有效、workspace 所有者等于当前用户。
- `/w/{token}` 只作为页面定位符，不能继续作为授权凭据。
- `/w/demo` 保持公开，但必须只读并与正式数据彻底隔离。
- 管理员可生成和禁用兑换码、禁用用户、查看并解绑设备。
- 现有正式 workspace 必须通过迁移兑换码归属到具体用户，不能在上线迁移中丢失历史数据。

设备边界说明：网页不能可靠识别物理手机或电脑，只能绑定一个持久的浏览器设备令牌。清除 Cookie、换浏览器、使用无痕模式或重装浏览器后，会被视作新设备。这一限制必须在管理员支持流程中通过“解绑旧设备”解决，不能承诺识别真实硬件。

## 5. P1 高优先级问题

### P1-01 保存语义不一致，存在老师误判已保存的风险

证据类型：事实 + 高可信风险推断。

事实：

- 全站真实持久化入口集中在 `ClassroomApp` 顶层 `save()`，通过 `PUT /api/workspace/{token}` 写入数据。
- 大量模块内部按钮只更新 React workspace 状态，并提示“已保存，记得保存”或“已更新，记得保存”。
- 期末评语、考试反思、作业、沟通记录等页面均存在模块级“保存”按钮，同时页面顶部还有全局“保存更改”。
- 当前未建立离开页面前的未保存拦截，也没有模块保存失败后的恢复入口。

风险：老师点击“保存评语”“保存记录”等按钮后，容易认为数据已经写入服务器；如果关闭网页或网络中断，可能丢失本轮编辑。

影响页面：全站所有可编辑页面，期末评语、家校沟通、考试反思、成绩录入、作业追踪风险最高。

上线标准：任何显示“已保存”的反馈都必须代表服务器已确认写入；未写入只能显示“待同步”“尚未保存”或同等明确状态。

### P1-02 信息架构仍然是功能目录，不是班主任实际工作流

证据类型：事实 + 设计判断。

事实：桌面侧栏近 15 个模块权重接近；手机版底部导航、首页入口和“更多”中存在重复入口。

设计判断：老师通常从“今天谁需要处理”“某个学生最近发生了什么”“本周或本学期要输出什么”开始，而不是先判断应进入成长档案、家校沟通、考试反思还是期末评语。

风险：新用户需要记忆功能归属；同一学生问题跨多个模块时容易重复记录或不知道从哪里继续。

上线标准：首页必须优先呈现今日待办、异常学生和可继续处理的工作；低频配置模块不能与高频操作等权展示。

### P1-03 手机版高频批量处理仍然偏重

证据类型：真实页面观察 + 用户场景推断。

事实：作业、成绩、值日等页面已经具备搜索、筛选、多选和批量操作，但 32 名学生场景下仍有较多逐行按钮和重复点击。

风险：课间几分钟内，老师难以快速完成“全班正常，只处理少数异常”的常见任务；单手操作时顶部保存、长筛选区和行内多按钮会进一步降低效率。

上线标准：作业、值日优先支持“一键全正常，再改异常”；成绩优先显示未录和异常；批量操作固定在拇指可达区域，并只在已选学生后出现。

## 6. P2 重要体验与可靠性问题

### P2-01 弹窗和底部弹窗的无障碍行为不完整

证据类型：代码事实。

- `MobileInfoSheet` 重复使用固定的 `mobile-info-title`，同时打开嵌套弹窗时会产生重复 ID。
- 未发现统一的焦点锁定、Esc 关闭、关闭后焦点恢复和背景 inert 处理。
- 部分可点击列表行使用自定义按钮结构，需要继续验证键盘访问顺序和可见焦点。

影响：键盘用户和读屏用户可能失去当前位置；嵌套弹窗可能被读屏错误关联。

### P2-02 桌面端长文本被压缩或隐藏

证据类型：浏览器测量事实。

- 通用学期字段出现 `clientWidth=95`、`scrollWidth=215`。
- 成绩建议字段 `score5-advice` 出现 `clientWidth=275`、`scrollWidth=397`，同类共 10 处。
- 班干部任期文本存在多处横向溢出。

影响：老师无法在不点击或横向移动的情况下确认完整内容；长学期名和较长建议不适合单行输入框。

### P2-03 部分页面仍存在框套框和层级过多

证据类型：结构代理指标 + 视觉判断。

- `section section section`：成长档案深度 3、班级周报深度 2、期末评语深度 4。
- `[class*="card"] [class*="card"]`：学生名单 20 处、考试反思 1 处、期末评语 1 处。
- 这些数字只是风险信号，不等于每一处都必须删除；需要结合真实截图逐项确认。

影响：页面层级显得机械，边框密度过高，主操作不突出，容易产生“通用后台模板”观感。

### P2-04 色彩仍以蓝白为主，颜色语义不稳定

证据类型：视觉判断。

事实基础：已有页面增加了绿色、琥珀色等颜色，但部分颜色按列表位置或装饰需要使用，没有稳定对应业务状态。

问题：整体仍接近通用蓝色管理后台；同一种蓝色同时承担导航、选中、信息、按钮和状态，视觉层级不够清楚。

上线标准：颜色按业务语义固定映射，不能为了“丰富”随机染色。建议映射为：信息/跟进蓝、完成/成长绿、待处理/异常琥珀、危险红、归档灰、学期输出靛青。

### P2-05 主操作、次操作和危险操作层级不稳定

证据类型：真实页面观察。

- 手机版值日岗位单行仍可能出现三个接近等权的操作。
- 复制、导出、配置、停用等低频操作在部分页面与新增、完成、保存等高频操作并列。
- 危险操作在部分详情区仅依赖确认弹窗区分，按钮视觉权重与普通操作接近。

影响：老师需要反复阅读按钮，误触成本高，主任务不够明确。

### P2-06 表单验证与错误恢复不足

证据类型：代码观察 + 风险推断。

需要补强的字段：

- 成绩分数：满分范围、小数、空值、批量覆盖确认。
- 日期与学期：格式、开始结束顺序、跨学期提示。
- 家长电话：允许空值，但输入后需要基本格式反馈。
- 座位行列与小组数：边界、人数容量、自动调整说明。
- 积分分值：零值、正负号和批量提交前确认。
- 长文本：推荐字数、超长提示、草稿恢复。

当前问题：多数错误只有禁用按钮或通用提示，缺少字段旁说明、重试和保留草稿。

### P2-07 帮助信息过度依赖 placeholder

证据类型：代码事实。

问题：日期格式、内容示例、后续跟进等说明主要放在 placeholder 中，用户输入后说明消失。

影响：长表单中途返回时难以确认格式要求；错误发生后没有持久提示可参考。

### P2-08 状态只靠颜色表达的风险仍需复核

证据类型：待复核项。

已有多数状态带文字，但需要逐页确认选中、异常、完成、停用等状态在灰度、高对比度和色弱场景下仍能识别。

## 7. P3 上线精修问题

### P3-01 图标和入口语言仍有原型感

部分模块使用“名、作、分”等单字块作为图标。它们可识别，但更像占位符，降低产品完成度。若不新增图标依赖，可使用统一的文本导航、现有符号或 CSS 简洁图形，不应继续混用多套图标语言。

### P3-02 打印、复制、配置入口在部分页面过于显眼

这些功能有价值，但通常属于低频操作，应进入“更多”菜单、详情区或页面二级工具栏，避免与保存、新增和完成抢占首屏。

### P3-03 favicon 404

这是低风险技术问题，但属于上线完成度问题，应补齐 favicon 或移除无效引用，并确认桌面浏览器标签页显示正确。

### P3-04 文案仍有管理系统和 AI 模板语气

需要重点检查：“本页概览”“完整规则库”“当前已接入”“用于 AI 帮写”等解释型文案。页面应直接呈现任务和状态，减少说明产品功能的自我描述。

## 8. 分页面问题记录

| 页面 | 已确认问题 | 优先级 | 应保留的有效部分 |
| --- | --- | --- | --- |
| 公开首页与进入流程 | 当前没有用户身份、设备绑定和 workspace 所有权；正式链接可被转发 | P0 | 首页只保留“进入我的工作台 / 查看演示”，不增加复杂注册登录页 |
| 今日工作台 | 模块入口仍多，今日待办与异常学生不够绝对优先；手机首页与底部导航有入口重复 | P1 | 今日待处理、作业未完成、待跟进、今日值日 |
| 学生名单 | 框套框代理信号较多；批量模式与普通浏览模式需要更清晰切换 | P2 | 搜索和小组筛选同一行、圆形选择控件、紧凑列表 |
| 作业追踪 | 32 人场景逐行操作仍重；详情顶部状态块仍可能抢占学生列表 | P1 | 科目/时间/内容筛选、多选高亮、批量改状态 |
| 成绩分析 | 需要异常和未录优先；桌面建议字段长文本溢出；批量覆盖需要防错 | P1/P2 | 多选后批量录分、考试和科目筛选 |
| 积分评价 | 记分表单仍可继续压缩；学生和规则选择需要减少来回切换 | P2 | 只保留记分和常用规则、不横向滚动 |
| 积分规则 | 桌面筛选区仍像通用后台且有框套框；状态和分值层级不够鲜明 | P2 | 搜索、分类、状态筛选和详情编辑 |
| 成长档案 | 首屏筛选多于主操作；添加记录与最近证据应先于自动摘要 | P1/P2 | 学生详情弹窗、按学生沉淀证据 |
| 班级周报 | 结构代理显示有嵌套；复制和编辑入口需要稳定分级 | P2 | “概览 / 周报库”模式可作为手机版正向样板 |
| 课程日程 | 月份/周次区仍偏表单；配置按钮权重偏高 | P2 | 本周课表时间轴、周一到周五按钮条 |
| 座位分组 | 座位图需要始终是主视觉；自动排座、轮换、配置需要分级 | P2 | 删除底部搜索座位、点击座位编辑 |
| 值日岗位 | 单岗位多个按钮等权；默认完成与异常返工应更高效 | P1/P2 | 周一到周五与台账状态按钮条保持原状 |
| 班干部 | 任期文字溢出；新增、复制、停用等动作层级需统一 | P2 | 新增班委/小组长等宽按钮组 |
| 家校沟通 | 长表单依赖 placeholder；日期、电话、跟进状态验证不足 | P2 | 列表优先、详情弹窗、学生与时间/内容/跟进分组 |
| 考试反思 | 模块保存与云保存语义混淆；状态统计仍可压缩 | P1/P2 | 多考试切换、学生状态、删除薄弱科后的流程 |
| 期末评语 | 32 名未填写容易形成压力墙；桌面框套框；AI 与保存层级需整理 | P1/P2 | 学期切换、学生列表/保存后的评语、点击学生直接编辑 |

## 9. 用户角色风险

- 高频老师：批量功能已经存在，但跨模块跳转和重复点击仍偏多。
- 第一次使用的老师：可能不清楚同一学生问题应记在成长档案、家校沟通还是考试反思。
- 手机版老师：单手操作时，顶部保存、长筛选区和多行按钮降低效率。
- 键盘或读屏用户：弹窗焦点、重复 ID 和背景可交互问题可能阻断操作。
- 长期使用者：多个学期、30 条以上记录、长期草稿积累后，列表和筛选必须继续保持可扫读。
- 付费用户：需要确认自己的班级数据不会因链接转发被其他人读取或修改，并且换设备时有清晰恢复方式。
- 管理员：需要处理兑换码发放、错误手机号、设备名额耗尽、禁用账号和旧 workspace 认领。

## 10. 正向样板与不应推翻的部分

- 课程日程的本周课表时间轴已形成较好的信息层级，可作为列表节奏参考。
- 班级周报的“概览 / 周报库”双入口符合手机版信息分层，可作为期末评语等长期数据页面参考。
- 作业和成绩的多选后批量操作逻辑方向正确，应优化效率，不应重新做成独立无上下文按钮。
- 成长档案已把学生详情移入弹窗，方向正确；下一步调整弹窗内部主次，而不是恢复页面下方详情。
- 手机版搜索和小组筛选同排、学生紧凑列表、不使用大头像框，均应保留。

## 11. 上线前统一验收口径

- 未登录用户不能读取或修改任何正式 workspace。
- 登录用户不能通过更换 URL token 访问其他用户 workspace；此类请求统一返回不暴露资源存在性的 404 或通用无权访问结果。
- 已禁用用户、已撤销设备和已过期 session 不能继续访问。
- `/w/demo` 可以公开查看，但任何 PUT 或真实保存操作都不能写入正式数据。
- 兑换码、session token 和设备 token 不以明文存入数据库或日志。
- 所有“已保存”都代表服务器写入成功。
- 离开未保存页面时有明确拦截或自动恢复。
- 390px、768px、1280px 均无横向滚动和内容遮挡。
- 32 名学生、8 个小组、30 条记录、3 次考试、多个学期下仍可高效筛选和操作。
- 每个页面首屏只有一个明确主任务。
- 多选后才出现批量操作；未选择时不占据页面空间。
- 复制、删除、配置、导出不与主操作等权。
- 所有弹窗支持 Esc、焦点锁定、关闭后焦点恢复和唯一可访问标题。
- 所有状态都有文字或图形辅助，不只依赖颜色。
- 长文本可查看完整内容，不被输入框或表格列无提示截断。
- 构建、布局、严格 QA、浏览器截图和关键弹窗交互全部通过。

## 12. 2026-08-24 复检与修复结果

本轮按用户要求暂不处理以下三项：HTTPS/传输保护、首次兑换工作台仍带演示学生数据、兑换码订单/批次/渠道/退款销售闭环。它们仍属于正式商业上线前的明确待办，不计入本轮完成项。

本轮已修复并验证：

- 工作台保存增加单调 `revision`、原子条件更新和 409 冲突响应；多设备同时保存只允许一个版本成功。
- 冲突时保留本机草稿，提供“导出当前草稿”和“载入服务器最新版本”，不再形成无效重试循环。
- 服务器保存前自动保留旧版本，每个工作台保留最近 20 个可恢复版本。
- 本机未成功同步的草稿写入浏览器存储；同版本重新打开时自动恢复。
- 家校沟通、成长档案、周报和期末评语改为 `classId + studentId` 关联，姓名只作为旧数据兼容，解决同名学生和跨班串记录。
- 课程日程和座位配置改为按班级保存，切班不会互相覆盖。
- 删除学生和班级时同步清理作业、积分、成长、成绩、反思、评语、沟通和值日关联数据。
- 桌面与手机版作业状态、成绩录入和考试删除行为统一；禁止删除最后一次考试。
- 管理后台增加用户/兑换码搜索分页、手机号修正、操作审计、完整数据导出和手机号二次确认的永久删除。
- 禁用用户时保留设备令牌；管理员恢复后原浏览器可重新建立会话。
- AI 请求增加 20 秒超时，失败时退回当日额度并保留老师原草稿；授权可在编辑场景关闭。
- 用户可导出和恢复完整 workspace JSON；新增一致性校验的 SQLite 在线备份脚本和迁移版本记录。
- 新增隐私与数据说明页，说明数据范围、AI、导出、删除和保留期限。
- 补齐 ESLint 配置并清除全部错误与警告。
- 新增真实认证集成测试：首次兑换、第二设备、第三设备拒绝、禁用恢复、跨用户 404 和并发保存冲突均通过。
- 15 个页面在 390×844 和 1440×900 运行时复检通过；新增可见容器越界检测，防止 `overflow:hidden` 掩盖裁切。

验证结果：

- `npm.cmd test`：通过，13 项源码/结构断言与认证集成流程全部成功。
- `npm.cmd run lint`：0 错误、0 警告。
- `npm.cmd run qa:layout`：通过。
- `npm.cmd run qa:strict`：通过，15 个页面桌面与手机均无横向溢出、隐藏裁切或按钮文字挤压。
- Impeccable detector：`[]`。

已知非阻断工程债：`ClassroomApp.tsx` 仍是约 592KB 的大型前端模块。它不影响本轮功能验收，但后续新增页面前应按业务模块拆分，以降低维护和编译成本。
