# 班主任工作台全站功能与商业兑换流程审核

审核日期：2026-08-24  
审核范围：桌面端、手机端、管理后台、兑换码、账号与设备、SQLite 数据隔离、保存、到期续费、AI、长期数据承载  
审核方式：代码审查、真实生产构建、隔离 SQLite 数据库接口测试、15 个页面多视口布局 QA、Impeccable 静态检测

## 1. 结论

当前版本可以继续作为内部测试或小范围试用版本，但**不建议按商业正式版直接上线售卖**。

兑换码创建账号、设备数量限制、用户数据隔离、到期只读和管理员续期等主干已经存在，且关键接口实测可以工作。真正阻断商业上线的不是页面是否精美，而是以下三类数据可信问题：

1. 两台设备可以用旧数据互相覆盖，最后保存者会把另一台设备的新修改抹掉。
2. 批量替换学生名单可能跨班级清空积分、干部数据，并留下大量孤立记录。
3. 当前仍允许 HTTP 认证，兑换码、手机号、会话和学生数据缺少传输加密。

此外，新付费工作台会直接带入 32 名演示学生、保存成功提示早于服务器确认、禁用后恢复可能无法重新进入、手机号输错后无法修正。这些问题会直接造成用户对“是否保存成功、是不是自己的数据、还能不能进入”的不信任。

## 2. 审核评分

| 维度 | 得分 | 说明 |
| --- | ---: | --- |
| 可访问性 | 3/4 | 弹窗大多具备 Esc、焦点锁定和焦点恢复；仍有少量仅依赖 placeholder 的输入框，背景 inert 不完全统一。 |
| 性能与长期容量 | 2/4 | 初始数据量可用，但所有编辑都提交整份 JSON，核心组件和 CSS 过大，长列表缺少统一分页或虚拟化。 |
| 响应式与移动体验 | 3/4 | 15 页无横向溢出，核心功能已覆盖手机；多个页面在数据量增加后仍会形成 2400-3800px 长页。 |
| 视觉与主题一致性 | 3/4 | 色彩和组件已较统一，静态反模式检测为 0；成长档案、周报、评语等仍有少量嵌套表面和重复信息结构。 |
| 实现完整性与数据可信 | 1/4 | 认证隔离主干可用，但存在多设备覆盖、批量名单破坏、桌面与手机写入逻辑不一致、保存确认失真。 |
| **总分** | **12/20** | 视觉层面基本可用，商业数据可靠性仍需重点整改。 |

商业上线判定：**NO-GO，先解决 P0 与核心 P1。**

## 3. 已验证可用的完整流程

以下不是代码推测，而是在隔离数据库和生产构建中真实调用接口验证：

- 管理员登录并生成兑换码成功。
- 新用户通过兑换码和 11 位手机号创建用户、工作台、设备和会话成功。
- 同一浏览器可恢复会话。
- 第二个浏览器可绑定，第三个浏览器正确返回设备上限错误。
- 用户 A 无法读取或保存用户 B 的 workspace token，返回 404。
- 禁用用户后会话立即失效。
- 到期 10 天进入只读宽限期：允许读取，禁止保存。
- 到期 40 天禁止进入；管理员续期后恢复。
- 缺少合法 Origin 的写请求被拒绝。
- `/w/demo` 可读但不能保存，演示 AI 返回静态内容，不调用外部服务。
- 同一兑换码并发提交只有一个成功，SQLite 事务锁生效。
- `npm.cmd test` 构建成功，现有 11 项测试通过。
- `npm.cmd run qa:strict` 通过，15 个页面在手机和桌面视口无横向溢出。

## 4. P0 上线阻断问题

### P0-1 多设备与同设备并发保存会丢数据

工作台以一整份 JSON 读取和写入。电脑和手机同时打开后，两边都基于旧版本编辑，后保存的一方会完整覆盖前一方。真实测试中，电脑先保存的字段在手机随后保存后消失。

同一浏览器内也可能出现两个自动保存请求重叠：新请求先完成、旧请求后完成时，服务器最终可能留下旧数据，而前端仍显示已保存。

影响：老师在手机记录沟通、电脑录成绩时可能无提示丢失另一端数据。  
建议：workspace 增加 `revision`，GET 返回版本，PUT 必须携带版本并使用乐观锁；冲突时禁止覆盖并提供重新加载或合并。

### P0-2 批量替换名单会破坏跨班级数据

桌面端“替换名单”没有明确的破坏性确认，会清空整个 workspace 的积分事件，并按当前班级名单过滤全局干部数据；同时没有完整清理成长、沟通、成绩、反思、评语、值日等关联记录。

影响：编辑一个班级可能清掉其他班级数据，同时保留无法对应学生的孤立记录。  
建议：禁止前端直接拼接删除；建立按 `classId + studentId` 的服务端事务，并在执行前给出影响预览和可恢复备份。

### P0-3 HTTP 认证不能作为安全商业正式版

当前明确允许非 Secure Cookie。即使权限代码正确，HTTP 链路仍可能泄露兑换码、手机号、会话和学生资料。当前管理员凭据也过于可预测。

影响：账号、学生信息和管理权限存在可被截获或猜测的风险。  
建议：正式售卖前必须启用 HTTPS、Secure Cookie、强管理员凭据和管理员登录限流。证书是否覆盖二级域名只影响证书选择，不改变必须加密的结论。

## 5. P1 高优先级问题

### P1-1 新付费用户会获得演示学生数据

首次兑换创建 workspace 时直接写入 `defaultClassroomData`。真实测试的新账号包含 32 名演示学生及家长电话示例。

影响：用户会误以为兑换到了别人的班级；若直接编辑，演示记录可能混入正式数据。  
建议：正式 workspace 使用空白数据模板，只保留必要配置；演示数据只能存在于 `/w/demo`。

### P1-2 页面提示“已保存”早于服务器确认

多处业务操作在只更新 React 状态后立即提示“已保存”，真正的自动保存约 900ms 后才发往服务器。断网、权限过期或服务器错误时，老师已收到错误的成功反馈。

影响：保存可信度失真。  
建议：统一为“正在保存 / 已保存 / 保存失败”，只有服务器确认后才能显示成功；失败时保留草稿和重试入口。

### P1-3 没有可恢复的本地草稿

保存失败后的内容只存在当前页面内存。浏览器崩溃、刷新或关闭后无法恢复；离开提示只能警告，不能恢复数据。

建议：按 workspace 和模块建立本地恢复草稿，服务器确认后清除；下次进入提示恢复或丢弃。

### P1-4 禁用再恢复可能锁死两台设备用户

用户被禁用后，浏览器收到 401 并清除本地 session 和设备令牌，但数据库中的设备仍为 active。恢复用户后，重新输入兑换码会因为两个设备名额仍被占用而返回设备上限。

建议：恢复时允许数据库中的同一设备重新认领，或在禁用/恢复流程中明确处理设备令牌；管理后台提供“恢复并重置设备”。

### P1-5 首次手机号输错后无法修正

只要手机号格式正确，即使实际输错也会绑定用户。兑换码随后变为已使用，用正确手机号重试只得到“兑换码或手机号不正确”。管理后台没有修改手机号或重新分配兑换码的能力。

建议：首次进入增加手机号二次确认；后台增加修改手机号、重置兑换关系，并写入管理员审计日志。

### P1-6 多班级模型不完整

作业、考试等部分实体有 `classId`，但家校沟通只存学生姓名，课程日程和座位配置是 workspace 全局。删除班级时又按姓名删除沟通记录，同名学生可能跨班误删或混入。

建议：所有班级业务实体统一持有 `classId`；所有学生业务记录统一使用稳定 `studentId`，不再用姓名关联。

### P1-7 电脑端与手机端保存逻辑不一致

桌面端修改作业状态时没有同步当前班级 roster；重新加载或切班后，学生摘要可能恢复旧状态。桌面端录入成绩只更新考试，手机端还会更新学生平均分，导致成长档案、首页和 AI 依据因录入设备不同而出现不同结果。

建议：业务更新逻辑从两个 UI 中抽离成唯一领域函数，桌面和手机只调用同一入口。

### P1-8 没有真正完整的数据导出与恢复

只读宽限期声称可以查看和导出，但当前只有部分页面的 CSV、打印或复制，没有完整 workspace 导出、导入、历史版本和管理员恢复。

影响：老师到期迁移、误删恢复、客服处理都缺少最后保障。  
建议：提供完整 JSON/ZIP 导出、校验后的导入、每日数据库备份和按 workspace 恢复。

### P1-9 兑换码售卖运营闭环不完整

当前能批量生成兑换码，但没有订单号、批次、渠道、购买人、备注、退款、发放状态等关联；使用新兑换码也不能为已有账号续期，只会返回“用户已有工作台”。

影响：兑换码卖出后无法可靠追踪来源，也无法形成标准续费商品。  
建议：增加兑换批次和订单关联；区分“新购码”和“续费码”；支持核销、撤回、补发和操作审计。

### P1-10 管理后台缺少审计与权限分级

后台使用一个共享密码，没有管理员身份、角色和操作日志。生成码、禁用、续期、解绑设备均无法追溯到操作者。

建议：至少增加命名管理员、登录限流、操作时间线；后续再区分客服、运营和超级管理员。

## 6. P2 体验与长期使用问题

### P2-1 长列表在手机上仍然过长

布局检查没有横向滚动，但部分页面高度明显偏长：成绩约 3880px，成长档案约 2657px，考试反思约 2649px，学生名单约 2426px。学生、成绩、反思状态等列表仍会一次性映射全部数据。

建议：10-20 条后分页或渐进加载；筛选状态固定；返回列表时保留滚动位置和筛选条件。

### P2-2 整份 JSON 自动保存不适合长期数据

每次编辑都在 900ms 后上传整份 workspace JSON，服务端只判断 `data` 是否存在，没有请求体大小、结构版本和严格 schema 校验。错误结构可能在下次读取时导致页面崩溃。

建议：短期增加 schema 校验、大小限制、版本字段；中期按模块或记录拆分持久化，常用查询建立表和索引。

### P2-3 前端包和维护面过大

`ClassroomApp` 客户端脚本约 419.7KB 原始、95.9KB gzip；框架约 57.7KB gzip；CSS 约 342.5KB 原始、46.5KB gzip。单个核心组件同时承载 15 个模块，手机也加载大量桌面逻辑。

建议：按页面动态加载，将领域操作、弹窗和移动/桌面展示拆开；先做代码边界，再做性能微调。

### P2-4 管理后台无法承载大量用户和兑换码

用户与兑换码接口一次返回全部记录，后台没有搜索、分页和批次筛选，并会解密全部兑换码。用户增长后页面、数据库和隐私暴露面都会扩大。

建议：服务端分页、按手机号/状态/批次搜索，只在必要时查看完整兑换码。

### P2-5 设备生命周期会制造续费后的客服问题

浏览器设备 Cookie 最长一年，数据库 active 设备不会自动过期。用户续期后 Cookie 可能消失但设备名额仍占用，重新进入可能被当作新设备并触发上限。

建议：设备令牌滚动续期；设备表增加最近活跃过期策略；后台显示最后使用时间并支持一键替换旧设备。

### P2-6 考试和其他长期记录缺少完整维护能力

考试支持新增和编辑，但没有明确删除或归档机制。长期使用后错误考试、测试记录和过期配置会不断累积。

建议：统一提供归档、恢复、永久删除二次确认，并显示关联数据影响。

### P2-7 AI 额度、失败和隐私控制不完整

AI 调用在请求外部服务前就扣除每日额度，失败或超时也可能消耗次数；后端请求没有明确超时控制。已有首次数据范围确认，但用户端没有持续可见的关闭/撤回入口，也没有完整隐私说明。

建议：成功后计费或对失败返还；增加超时和取消；设置页提供 AI 开关、数据范围、服务商和保留说明。

### P2-8 可访问性仍有细节缺口

部分搜索框仅靠 placeholder 描述，复杂编辑表格的输入控件缺少稳定可访问名称；弹窗焦点锁定较完整，但背景 inert 的实现并未全站统一。

建议：补齐 label/aria-label、错误关联、键盘表格操作和统一的 `DetailSheet` 可访问实现。

### P2-9 ESLint 质量门禁当前失效

`npm run lint` 实际退出失败：ESLint 9 找不到 `eslint.config.js/mjs/cjs`。因此当前只能确认构建和现有测试通过，不能声称 lint 通过。

建议：迁移到 ESLint flat config，并把 lint、真实接口集成测试和 UI 冒烟测试纳入部署前命令。

## 7. 管理员到老师的完整旅程审核

| 阶段 | 当前状态 | 用户风险 | 建议目标 |
| --- | --- | --- | --- |
| 生成兑换码 | 可批量生成 | 无订单/批次追踪 | 绑定批次、订单、渠道和有效期 |
| 售卖发放 | 系统外完成 | 无法确认发给谁、是否退款 | 发放状态、客户备注、退款/作废 |
| 首次兑换 | 流程短，可创建账号 | 手机号无校验且误输不可改；带入演示学生 | 二次确认手机号，创建空白工作台 |
| 日常进入 | session/设备恢复可用 | 设备 Cookie 与数据库名额生命周期不一致 | 无感恢复，旧设备替换清晰 |
| 双端使用 | 支持电脑+手机 | 整份 JSON 互相覆盖 | 版本冲突保护与模块级保存 |
| 到期 | 有效期、只读宽限、阻断已实现 | 没有完整导出；续费码未实现 | 到期提醒、完整导出、续费码核销 |
| 客服处理 | 可禁用、续期、解绑 | 恢复锁死、手机号不可改、无审计 | 账号修复工具和管理员审计 |
| 数据退出 | 未完整实现 | 无删除、迁移、恢复政策 | 导出、删除申请、备份恢复和保留周期 |

## 8. 测试覆盖缺口

现有 `npm test` 的 11 项检查更接近源码模式断言，认证视觉脚本也会模拟接口。它们不能替代真实数据库流程测试。

应固化为自动化集成测试的场景：

1. 同码并发兑换只有一个成功。
2. 两设备成功、第三设备失败、解绑后可重新绑定。
3. 禁用和恢复后原设备能重新进入。
4. A 用户所有读取、保存、AI 请求都不能访问 B 的 token。
5. 两端同时编辑必须检测冲突而非静默覆盖。
6. 到期前、宽限期、宽限结束、续期后的权限矩阵。
7. 新正式 workspace 必须为空，不得含演示学生。
8. 所有“已保存”提示必须晚于服务器成功响应。
9. 班级删除、学生删除、名单替换不能影响其他班级。
10. 数据库备份可以实际恢复并重新打开 workspace。

## 9. 建议实施顺序

1. 先修数据安全：revision 乐观锁、保存确认、名单替换事务、空白正式 workspace。
2. 再修账号售后：手机号修正、禁用恢复、设备替换、完整导出与恢复。
3. 补齐售卖运营：兑换批次、订单关联、续费码、管理员审计。
4. 统一业务模型：所有记录使用 `classId + studentId`，桌面/手机共用领域更新函数。
5. 再做长期容量和体验：模块级保存、分页、归档、按页动态加载。
6. 正式售卖前完成 HTTPS、强管理员认证、隐私说明和真实集成测试。

## 10. 审核边界

- 本次没有修改业务代码或数据库结构。
- 破坏性账号测试全部在临时 SQLite 副本完成，没有操作正式数据。
- 没有检查线上 Nginx、云服务器防火墙、数据库文件权限和实际备份任务，因此这些不能视为已通过。
- 本报告不构成法律意见；涉及学生和家长信息的隐私条款、数据保留和跨境 AI 处理需要单独进行合规评估。
