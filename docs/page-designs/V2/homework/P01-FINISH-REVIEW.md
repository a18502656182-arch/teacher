# 作业 P01 独立 finish review

审查日期：2026-09-15。角色：impeccable_finish_reviewer。只读审查源码及执行者提供的截图/采集记录；只新增本报告。用户确认的是 C01 方向，本报告不构成用户 D、单页设计批准、R、正式接线或部署批准，也不要求旧监工 PASS。

## 1. Direction fidelity

P01 保留了已确认的主要方向：暖白任务栏、浅蓝学科工作面、青蓝主操作，先选作业再处理连续学生行；手机独立任务列表进入 Drawer；四态同时显示文字与明确选中态。1536 实测任务栏 310px，与方向合同一致。实际打开了 candidates 下 desktop、mobile-list、mobile-workspace 三张 C01，以及 round2 的 1536-normal、390-normal、390-mobile-detail、1536-long-title-note、390-long-title-note、390-selected、390-validation 七张真实图。

这不是像素复刻。候选的大面积水彩书本及学科字样改为更小的通用文具局部；手机抽屉插画更淡。实稿字体、导航、行高、任务内信息顺序及学生列组织均有可见差别，整体更紧凑且偏操作表单。候选导航错误、额外切班箭头与过小四态按钮按 brief 明确豁免，不应要求复刻。字体使用实际 Microsoft YaHei，不能称为已匹配候选手感。上述差异需如实交用户判断。

## 2. Findings / evidence

- 源码 HomeworkDesign.tsx 复用真实 useHomeworkController 类型与回调，含新建/编辑、筛选、分页、四态、公共 note、批量、待跟进、删除确认及关闭草稿提示。未发现为视觉删除关键入口；本次没有完整逐项回调执行审计。备注直接绑定 student.note，标签明确“学生公共备注”，没有虚构作业独立备注。
- 直接状态编辑没有额外手工保存按钮；编辑作业对话框的“保存修改”属于提交表单。独立 preview 的保存、失败及 409 由内存和定时器模拟，并有合成数据说明；不能据此证明真实持久化或冲突恢复正确。
- 桌面首屏学生四态标签完整、选中态清晰；CSS 为四个选项保留 min-height:44px 和 nowrap。final/capture.json 共 40 个截图条目（20 状态 × 2 视口），所有条目 squeezed 为空，记录的页面宽度均未超过视口。这只证明其检测范围内的几何，不证明完整可访问性。
- 手机默认详情的学生区始于约 y=463，首屏约见 1 个完整学生行及下一行开头。390-selected 中批量条占约 290px，学生行落到首屏以下。它仍保留清晰动作且可滚动，但比 C01 显示更少学生、批量后须增加滚动；这是可见的效率代价，不能以“已无溢出”替代说明。
- 桌面长标题改为 64 字摘要及“查看完整作业内容”；长备注仍为可滚动/调整高度的 textarea，没有从数据中删减。截图表明当前摘要与输入框不重叠；未亲手展开完整内容或调整文本框。
- 390-validation 展示了明确字段错误与恢复所需输入，底部取消/新增完整可见。源码只读禁用写入入口，但本次未动态验证每一条只读路径。
- 检测器记录属于全局 DESIGN 外的局部色彩、字号/字体、圆角 advisory。它们不是已证实视觉缺陷；应由本页 DESIGN.md 说明方向选择，不能把 28 条 advisory 写成“无任何偏差”。

## 3. Material fixes

在已实际查看的截图及源码范围内，未发现足以要求重做 C01 方向或修改共享壳层的已证实阻断项。无产品代码修改要求。

收尾仍需本页 DESIGN.md 记录局部视觉选择、上述真实差异以及模拟边界。手机批量条较高可作为后续经用户反馈后的局部优化，当前不把审美偏好升级为强制返工。

## 4. Unverified checks

- 原 round2 手机长内容/105 人详情覆盖不足，已由补充证据关闭：本审查者实际打开 final/390-long-title-note.png、final/390-students-105.png、supplement/390-long-expanded.png、supplement/390-students-105-scrolled.png。最后页图可见学生105、5/105人及6/6分页；展开图可见完整长内容。supplement/capture.json 的5项执行者检查均通过，覆盖普通/105人/长内容分页可达、105末页以及展开文本与fixture原文一致。没有冒称本审查者亲手点击。
- 没有本审查者的浏览器交互证据：键盘 Tab/焦点环、Esc 与焦点恢复、嵌套 Drawer 背景不可交互、移动键盘/真实触控未亲验。滚动分页已取得上述执行者补充证据。手机图可见外侧及抽屉滚动轨道，不能凭静态图断言背景滚动锁有效或失效。
- 本次只看 1536×1024 和 390×844；中间断点、文字缩放、其他系统字体、全部 hover/disabled、精确颜色对比、reduced-motion 未复验。
- 本审查者未重跑 build、qa:strict、lint 或业务测试。已读取 interactions/capture.json：30项双端交互均通过，覆盖四态、跨任务公共备注、搜索、分页、批量、跟进复制反馈、新增校验、草稿关闭、删除和只读；它属于执行者测试记录。已核实 P01-strict.txt 报 Runtime audit failed: TypeError，querySelectorAll 访问 undefined，故 qa:strict 未完成，不能宣称全站QA通过。执行者另报告生产构建通过、34项关联测试通过、受控源码 ESLint 0 errors，而原 npm lint 扫描缓存扩展报10 errors；本报告不将这些扩展为独立全站验证。没有真实后端保存/权限/409、正式入口集成或部署验证。

## 5. Verdict

有条件可交用户审看：所检范围内未见已证实的 material blocker，核心方向得到保留，但不等同像素一致或用户审美批准。手机长内容与大班额分页补充证据已到位；补齐本页设计记录，明确手机首屏容量差异及严格QA中止边界后，展示桌面/手机真实图与候选差异，由用户决定是否接受 P01。保持隔离稿，不据此接入正式业务或进入下一门。

