# 作业追踪 · 独立设计准备

模式Operate。先选作业，再处理异常学生。C4是桌面参考，手机独立设计。本轮准备C01候选，计划桌面、手机任务列表、手机作业处理抽屉各一张；实际生成结果以record为准，仍无可运行定稿及D/R批准。

桌面拟以300–330px任务清单和右侧学生四态工作面为主，学科与选中任务明确，统计收紧，不让两层指标/只读说明推低操作面。手机先任务再处理Drawer，选择后出现批量条。材料文具横幅参与学科上下文，保留四态文字的完整宽度。

已有实现HomeworkView/Controller/read-model/operations均保留。C4里的“保存更改”、任务归档状态和两个新增按钮不能直接复制：现有一般编辑走workspace自动保存，月份筛选不等于归档动作。学生备注是学生公共note，不是每次作业独立备注。

当前截图：桌面工作面、手机待跟进空名单Drawer。手机截图由旧审计脚本在交互后采集，不能命名成任务默认态。真正默认手机、30任务/105人、编辑/失败/409将在候选确认后的隔离设计阶段补齐。

下一允许动作：展示作业C01并等待用户确认方向，然后制作隔离可运行稿；不得直接改正式作业接线。

## C01实际图核对

桌面图是构图候选，不是完整状态或像素定稿。保留任务选择、学科上下文和四态操作的独立工作面；统计20+3+5+4=32、完成24/32=75%，待处理8人符合示意数据。图只画7条学生却标20/32，属于示意，真实稿须使用实际分页数据。

已发现：左导航出现文字/图标生成误差，左下水彩侵入导航，不能作为正式壳层修改依据；实际壳层必须复用现有实现。生成图侧栏与任务栏尺寸也不是合同值。操作按钮高度、文字对比度、所有菜单和空/错误/批量状态仍待可运行稿实际验证。不得将C01视作上述问题已解决。

手机候选分为任务列表和处理抽屉，均为独立高分辨率概念图，真实尺寸见record，不冒充390×844浏览器截图。列表多画了班名下拉箭头，是否切班仍按真实壳层入口，不增虚构回调。抽屉四态一行的方向清晰，但按钮按目标手机比例折算不足44px；第四行漏画选中态，筛选被排在同一行也需真实控件复核。可运行阶段必须处理这些问题，保持完整标签和真实状态。

三图已展示；此次初始生成连接失败，无有效文件，后续重试成功。提示词、输入来源、原尺寸、哈希保存在candidates和assets-candidate.json。当前等待方向确认，没有新增原型或D/R批准。

## 2026-09-15：C01确认，P01待审

用户“确认/继续”只绑定C01候选方向。P01组件、操作状态、字体和素材已实现，真实几何及证据见record/geometry/assets。保留310px任务栏、浅蓝当前任务和四态色义；手机两步流程。实际导航保留、插画更小更淡、系统字体及44px操作令手机首屏更少学生，批量条高度较大，未将这些差异视为用户已接受。长标题可展开完整内容，学生公共备注可滚动/调整高度。受控源码及独立稿检查通过，全站qa:strict运行时异常中止，详见日志。下一允许动作是等P01单页D审核，不接业务。

## 2026-09-15用户接受

P01基本接受；桌面/手机输入与筛选框比例偏大，用户允许后期缩小，当前未实施。designApproval仅本页，后期清单见POSTPONED-VISUAL-OPTIMIZATIONS.md。
