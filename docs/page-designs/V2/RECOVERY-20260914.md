# V2接手核对与暂停点 · 2026-09-14

唯一开发目录为本仓库；接手HEAD 9a0babd0286414ca6c8c72a2fd70ba248bd7295b，github远程为a18502656182-arch/teacher。无.codegraph目录，没有解压交接包或另建开发基线。四项预存差异及哈希见recovery/source-state.json，作者不能从现场确认，本轮不修改或提交它们。

## 已核对事实

- 按指定顺序恢复导航、架构/保存边界、最新进度及相关历史、决策、部署日志、产品/设计、旧33项保留依据、视觉账本及history、未实施优化、学生返工与旧检查点、新逐页包指定全文/inventory/templates。参考原图及Git副本哈希已核对，实际打开C3等相关参考、旧实际/对比图及本次三页截图。没有继承被否决的旧完成。
- 旧33项完成是功能/结构历史。V1/23fcdc5批准仅限首页；POSTPONED-VISUAL-OPTIMIZATIONS中的事项未实施。学生399a5c4整体否决仍有效，范围包含构图、粉笔绿、状态颜色、身份/记录层级、概览比例和手机按钮。依据为主账本、PROJECT_STATUS及V2-STUDENTS-C3-REALIGNMENT-PROPOSAL。
- 新包05覆盖强制监工PASS；本轮schema 3实现分阶段兼容，保留旧历史及schema 2校验。测试通过不构成审美认可。
- 真实入口ClassroomApp.tsx使用app/components/workbench/shell/WorkbenchShell.tsx，旧文档较短壳层路径已过时。听写正式批改是dictation/Grading.tsx，features/dictation/GradingView.tsx不是当前正式入口。workspace草稿/保存/冲突、operations和controller业务未改。
- 学生在途TSX布局类缺少配套样式，手机状态列竖排已看图确认。混合时间排序、0错词语义色和0分展示边界留待功能回归，不能借视觉重写保存。作业备注是共享student.note；听写已有安排复习动作，不能凭旧计划虚构自动复习开关。
- DEPLOYMENT_LOG只读：历史曾有用户部署反馈，当前线上版本未核验。本轮采集、打包历史及Git推送都不等于部署。

## 当前交付和缺口

学生candidates内保存桌面/手机C01、提示词、尺寸和哈希，均未批准。实际偏差见students/brief.md；图中虚构控件和矛盾状态不能冻结。三页都有brief、function-map、states及record；作业/听写仅盘点，未生成本轮候选。没有可运行原型、geometry定稿或D/R批准。未发现阻断候选阶段的必需历史文件缺失；真实字体、冻结fixture hash、浏览器版本及完整关键状态证据尚未产生，属于后续验收缺口。

## 实际预览与验证边界

接手时3006/4180/4207未监听；layout-audit --serve临时启动127.0.0.1:4180/w/demo，使用隔离QA数据库路径和合成demo，采集后关闭。1536×1024、390×844，DPR 1截图及原始报告在recovery/。

报告有两项失败：桌面overview strip缺失、手机Drawer hasFacts=false。后者含旧文案/结构断言，不能据此断言用户事实丢失；手机状态列拥挤为看图确认的问题。当前截图属于9a0babd加预存差异，不是干净提交。fixture日期未冻结，浏览器版本/解析字体及fixtureDataHash未取得。作业手机图是QA打开的跟进空态Drawer，听写demo无任务，未覆盖批改。evidence-index.json记录限制。没有新全量构建/认证验收，不继承399a5c4全量通过。

## 检查命令

- node scripts/visual-baseline-audit.mjs：记录与历史证据完整性，不等于放行。
- 同命令 --gate V2 --stage design-review：三页可运行证据能否交D。
- --stage integration：三页是否已有有效D批准。
- --stage result-review：真实接入证据能否交R。
- --stage next-batch：三页是否已有有效R批准。

当前默认检查通过，integration必须失败。19项针对性测试通过tests/visual-baseline-audit.test.mjs入口执行，包含新测试；未修改共享package.json。检查器不验证合同文案完整性或评判审美，仍需人工看图/操作。日志单独持久化，不覆盖runtime失败。

## 下一允许动作

等待用户确认学生C01桌面和手机方向；确认后制作隔离可运行稿及关键状态，后续接入直接复用同一组件。作业/听写仍需各自候选和设计流程。三页D前不接业务，三页R前不进V3；家庭教育排除，不部署。候选提交号不能填入prototype.commit，辅助检查不能代替用户批准。
