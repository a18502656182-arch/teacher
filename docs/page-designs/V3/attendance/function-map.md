# 考勤功能与状态映射

2026-09-15更新：P01/d7450bb已按下表实现隔离界面并完成44项交互。每项桌面/手机实图在record.prototype.evidence，105人与长备注完整滚动另见additionalEvidence；实测日志tests/interactions.json。当前无正式接入，所有集成验证仍待办。手机日期、搜索、筛选、选择label、备注及月历目标的360/390实测均不小于44px（容差0.1px）。后文C01盘点保留为历史。

核对源：app/w/[token]/Attendance.tsx；features/attendance/operations.ts；ClassroomApp.tsx桌面316/手机379。历史保留依据为旧实施包trackers的F04/O04/A04；旧技术通过不替代用户功能验收。

| 当前行为 | 设计与后续验证合同 |
| --- | --- |
| 日期切换和月历 | 日期保持班级范围；月历仅依据已保存记录，无记录不能说正常 |
| 四态正常/迟到/请假/缺勤 | 每行完整连体四态；异常同时用文字及颜色，不靠颜色独传达 |
| statusFor无当天记录回退student.attendance | 现有回退语义需保留并明确，不能把回退状态说成当天已登记；候选32人均有当日合成记录 |
| 搜索姓名/学号/小组/共享学生备注/状态 | 提示姓名或学号为主要入口；不宣称能搜当日备注 |
| 状态、小组筛选 | 手机收纳进筛选展开，不能删除；无小组也能选学生 |
| 桌面50、手机20分页 | 105人覆盖；全选针对当前页可见ids，旧桌面“当前筛选”文案与实际范围不同 |
| 单行状态即时保存；当日备注独立保存 | 保留保存时机，不能改成没有必要的全局保存；当日备注不是共享学生note |
| 选中后批量状态和统一备注 | 留空不覆盖原备注，不修改状态是合法选项；保存成功才清选中/草稿 |
| 一键全员正常 | 作用于当前班全体，不仅筛选结果；保持现有语义 |
| dirty导航/日期切换保护 | 失败保留输入和选择；beforeunload及路由切换保护需实测 |
| 只读 | 写控件/选择禁用，不能改真实班级数据 |
| 历史日期、离班学生 | 更新历史日期不覆盖当前摘要；历史记录保留但当前名单不凭空显示离班学生 |
| 请假附加模型字段 | period/leaveType等由operations保留或默认，当前无请假详情编辑UI；旧任务卡存在超出当前源码的描述 |
| workspace保存/冲突 | 现有业务保持；未来隔离稿用模拟保存/失败/409，不重写正式workspace/operations |

本轮只读盘点与两张基础候选，不构成功能验收。P01最低覆盖：普通、选中批量、备注、月历、历史、无记录、空班、无搜索结果、105人、长文、只读、保存中、失败、409和脏稿离开。
