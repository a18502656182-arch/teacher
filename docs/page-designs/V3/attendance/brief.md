# 当前：P01可运行稿待用户设计审核

用户“方向确认”已绑定考勤C01两图hash。隔离P01源码d7450bb已完成，36张主状态、8张窄屏、6张完整滚动图及44项交互通过；月份待同步与手机44px问题经独立复核关闭，结论“可交用户审核”，尚未获P01/D/R批准。核心operations和正式入口未改，原4项修改hash保持；听写继续暂缓，学生/作业单页批准保留，作业控件比例仍待优化。生产/隔离构建、类型、5项考勤operation测试、scoped lint通过（1条img提示）；qa:strict仍因旧runtime querySelectorAll错误中止，不称整站通过。不部署。交付入口docs/page-designs/V3/attendance/record.json和preview/README.md，下一步等用户审P01。

实际差异：原共享壳层完整复用；字体为Windows实测微软雅黑，水彩素材独立生成而非裁候选；手机概览、工具、三段学生行与原底栏共用首屏，候选935×1683不是390×844真实图，P01以真实截图为审核对象。桌面名单内部滚动与分页保留32/105人，不照抄只画8人。小月历附注可读性观察仍开放。

以下为C01阶段历史记录，后续状态以上节和record为准。

# 考勤与请假 C01 独立设计

2026-09-15：用户明确听写功能未验收，允许跳过先做其他页。当前仅提前开展考勤独立设计；V2/V3正式接入与R门仍关闭。

当前源码基准ed373ed，Attendance.tsx和features/attendance/operations.ts未改；四项既有学生相关修改保留。C4仅提供课堂文具风格语境，没有独立考勤手机原图。

主流程：选日期，检查四态名单，逐人或批量修改状态/当日备注，保存；按月回看已登记日期。名单为主体，桌面右侧月历，手机日期→名单，批量工具选中后出现。输入与筛选区收紧比例，手机实现仍须44px触控目标。

## 本轮边界与待修正

- C01两张为ImageGen候选，未获方向确认，没有P01可运行设计或D/R。
- 桌面仅展示前8行，不能当作完整32人列表；P01必须按真实桌面50、手机20分页且可达全部学生。
- 手机最终候选前三行四态独立成行，第四行仍被生成器排到姓名旁，属于明确不采纳的偏差。实现统一所有行结构，不能照搬第四行。
- 手机候选遗漏“全选本页/一键全员正常/人数”工具行，需在可运行稿恢复；筛选展开必须有状态与小组；不是删功能。
- 候选实际像素比例以record为准，不是390×844真实截图。字体、颜色渐变残留、控件几何及壳层导航均未冻结；共享壳层直接复用，不据生成图重写。
- 关键状态只完成盘点，未生成完整批量/历史等状态稿，也未做交互验收。
- 旧卡“请假记录编辑”与当前页面不符：存在模型字段但无详情编辑窗，不能声称已保留一个不存在的交互。

## 当前组件现场

整站dev曾因QA Chrome Cookies-journal文件watch EBUSY中止；本地生产预览4180返回200但停在“正在读取班级资料”。改用4209只读合成数据直接导入真实Attendance组件及原主题/页面CSS，截图不含共享壳层，不是正式路由截图或线上版本验证。第一次漏载页面变量的截图已被重拍替代，不作为证据。

核对可见现状：桌面搜索框占满工具栏剩余空间；手机日期、全员正常和筛选区堆叠，占据首屏较大比例。本轮只建立候选，不改正式组件。

下一允许动作：向用户展示C01并等待方向确认，再制作隔离P01及关键状态；听写继续暂缓。没有部署。

P01最终源d7450bb补齐隐藏标签定位范围；完整滚动证据已重拍，独立复核第三项关闭。全部三项问题均关闭，无用户D/R批准。
