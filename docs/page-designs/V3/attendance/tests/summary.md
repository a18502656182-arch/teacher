# P01范围检查结果

设计源码：d7450bba192d5a0124cd2e642c49a8127d53c94d。

- 最终主状态36张、窄屏8张、完整滚动6张，截图及对应capture/supplement JSON已持久保存；无页面横溢出、挤字按钮、缺图、运行异常记录。
- 最终交互44/44通过，覆盖日期/搜索/筛选/105人分页/批量范围与备注/当日备注/历史与离班保留/只读/dirty守卫/失败重试/409/月历待同步和确认/焦点返回。保存为模拟，不能当真实后端验收。
- 初次焦点返回测试使用程序click而未设置触发器焦点，39/40；改用先focus再click及标准Escape键后2/2，最终全44通过。未为该测试改共享弹窗。
- node --test tests/attendance-operations.test.mjs：5/5通过，原operations未改。
- 类型检查、正式构建、最终隔离构建通过；ATTENDANCE-C01合同在独立构建HTML中保留。没有上传或部署。
- scoped lint：0error、1条img提示。内置ImageGen PNG约1.8MB，暂无图片管线转换，固定尺寸已声明。
- 机械检测27条均advisory，局部色彩/字号/圆角已交独立复核与局部DESIGN记录，未为清空提示修改全局token。
- qa:visual-baseline最终通过；qa:strict因已有运行器TypeError（querySelectorAll）中止，完整日志保留，不声明全站技术通过。
- 独立review-P01.md：三项修正已resolved，可交用户审核；不能替代D/R。实测微软雅黑为系统字体，不重新分发；实际软键盘、屏幕阅读器整流程、200%缩放和服务器保存不在本轮已验收范围。
- 原4项预存修改hash与接手一致。正式Attendance、operations、ClassroomApp和共享壳层/主题未改；听写暂缓状态保持。

完整滚动补查发现隐藏辅助标签越过内部滚动范围，d7450bb仅给学生行增加定位上下文。桌面文档从2457/2541恢复到1128；手机从3830/3891恢复1195（含预览82px底边距）。新50图已按最终源提交重拍。fullPage图中固定导航仍保持原视口位置，只用于边界/分页证据，首屏构图看36张主图。44项功能交互在9cd504b完成，d7450bb未改逻辑；最终隔离构建通过。
