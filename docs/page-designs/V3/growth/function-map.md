# 源码功能/状态盘点

来源：ClassroomApp.tsx:2652 Growth、1743 MobileSecondaryPage growth分支；features/growth/operations.ts、time-range.ts；旧实施包F-08/O-08/A-08，历史报告growth-health-records-implementation-20260913.md。旧通过仅为功能保留证据。

| 功能 | 现场行为 | 设计位置 |
|---|---|---|
| 学生选择 | 姓名/学号/分组/成绩积分搜索；分组、状态、记录覆盖与排序；无分组也须可用 | 桌面窄目录；手机列表+筛选层 |
| 100+人 | 桌面列表内部滚动；手机每页10人 | 可搜索分页，不能全班select |
| 档案 | 身份、成绩、积分、作业率、沟通数、班干部 | 身份带；次要信息摘要 |
| 四类事实 | 老师补充、沟通、积分、作业；时间倒序，来源和日期必须可读 | 主时间线 |
| 记录筛选 | 全部/7天/30天/本学期；四类；桌面8条/手机6条分页 | 紧凑时间/来源工具条 |
| 人工补充 | 日期、类型、标题40字、事实500字、后续300字；校验、只读、忙碌、失败草稿 | 添加模态/手机编辑层 |
| 自动摘要 | 固定规则按成绩/积分/作业/考勤派生优势跟进，不是教师观察或AI结论 | 查看摘要、复制反馈 |
| 导出素材 | window.print调用打印；无文件生成后端 | 摘要内次要入口 |
| 手机详情 | 当前有档案层、摘要层、记录详情层、新增层 | 完整保留返回/滚动/焦点路径 |

人工事实当前没有编辑/删除入口，F-08也明确此点；旧任务卡的“编辑”不能写成已实现。只读恢复器update无操作、save返回false，不访问workspace API。

数据边界：growthEvidenceForStudent核对当前班学生ID与classId，排除source=家校沟通防重复归集；沟通沿recordBelongsToStudent；作业允许无classId旧数据。积分筛选当前只有studentId，需在P01验证跨班ID假设。作业时间线用student.homework回退，但目录计数只算有逐生status的任务，两者可能不一致，应真实披露/验证后决定，不擅改业务。

时间：手工记录growthTimestamp优先createdAt，可能和所填日期不同；范围筛选依赖真实时钟/学期配置。P01需固定合成日期覆盖补录/旧日期/无日期，不把日期伪造为新事实。

源码风险待P01验证：选学生关闭编辑器却不清draft，可能混淆草稿归属；save返回false后再次add可能重复；未捕获save抛错；桌面遮罩仍可直接关闭（旧O-08宣称保存时禁关不能直接继承）。保留workspace草稿/冲突契约，隔离稿验证对策后再审核；本轮只记录，不改正式业务。

待覆盖状态：正常、无学生、无事实、搜索无结果、组合筛选/重置、105人、长记录全文、事实/学生分页、四类来源/跨班/沟通去重、时间范围、摘要复制成功失败、打印、编辑器必填/边界、加载、只读、保存中、失败重试、409、脏草稿离开与换学生。全部尚未P01验收。

## P01实现核对

app/designs/growth/read-model.ts从当前Growth函数派生，原文件未改；保留旧时间范围、作业fallback和计数、积分studentId语义。新增仅限隔离controller：草稿绑定学生，pending重试不重复add，异常恢复busy，保存中禁关、脏草稿确认，持续409时弹窗内导出/载入确认。真实workspace接入仍需另审。原业务风险不因合成测试通过而消失。
