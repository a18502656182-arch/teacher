# 成绩 P01 独立 finish review

## 1. 证据

2026-09-15，审查范围为隔离成绩设计稿。实际读取 AGENTS.md、craft-floor.md、new-work.md 的 finish 要求、brief.md、function-map.md、ScoresDesign.tsx、ScoresDesign.module.css、useScoresDesignController.ts 和 ScoreTrendsDesign.tsx；实际通过 view_image 查看 candidates/scores-desktop-c01.png、scores-mobile-c01.png，以及 .qa-shots/scores-design/round2 下 1536-normal、390-normal、1536-trends、390-trends、390-analysis、390-save-failure、390-paper-review 七张图。读取 interactions/interactions.json，其已有检查项 passed，failures 为空；66 项及其他补充检查以执行线程最终日志为准。本审查没有浏览器，不把日志转述为亲手操作，也没有执行 Git 或修改产品文件。构建、qa:strict 与服务器持久化不是本报告独立验证结论。

## 2. C01 对应

桌面保留了标题与文具情境、浅绿当前考试带、三个工作区标签、紧凑搜索筛选和连续录分台账；手机保留考试上下文、分项成绩、未录与零分区分、学生录分入口及底部导航。实际稿继承完整共享 shell 和绿色命令色，未继承候选的假教师账号、额外口号及蓝色命令，这符合已确认边界。候选与实际稿并非像素复刻：桌面更密、手机展示完整 shell 和页面标题；首屏学生数量少于候选。当前不要求整页重建，这些差异应随真实双端主图交用户审核，不可宣称已获 P01 或完整 D 批准。

## 3. 可用性与手机

指定正常与分析实图没有可见文字互压、按钮截断或连续装饰卡片。桌面分数可直接编辑，手机学生逐项读数明确；未录入采用文字与底色双重标识，0 分仍显示为 0。手机趋势筛选能换行，试卷分析主操作和核对入口可见。390-save-failure 中问题、保留状态和重试按钮均位于弹窗内；390-paper-review 中确认操作可见。手机部分辅助字为 11–12px，密度与首屏内容量属于用户主页面审阅点。本次截图未覆盖完整滚动到底、键盘焦点实际轨迹或所有对比度计算，因此不据静态图声称完整无障碍验收。

## 4. 功能与恢复

源码保留考试库、批量、分数区间、重点、学生建议/关联反思、历次趋势、分析项与候选确认入口。controller 的 sync 有 try/catch/finally，pendingRef 将重试与再次创建区分；脏输入切换有 guard，冲突有导出及确认载入入口。未发现需以弹窗外观为由阻断的功能缺失。两项趋势语义需修复：F1，ScoreTrendsDesign.tsx:38–46、60，coverage 以至少录过一科的学生人数计算；全班各录 1/3 科时仍为100%，当前“平均录入覆盖”易被理解为成绩项完整率。保留算法，改成“平均有录入学生占比”，并说明至少一科即计入。F2，ScoreTrendsDesign.tsx:20、63，横坐标依 index 等距计算，却标“横轴按时间”；两次间隔一天与一年占相同宽度。保留算法，将说明改成“按考试日期排序，场次等距”；勿让用户按图的斜率理解每单位时间的变化速度。缺失点分段已在源码用 M 重启路径，并在桌面趋势图中实际可见，没有再次连缺失点的问题。

## 5. 处置及未审范围

处置：NEEDS-FIX（两项限定的趋势文字口径；不要求重建）。F1、F2 当前为 open，修复后以同一趋势双端截图与源码回传本审查评分；主录分页面无已识别的阻断视觉缺陷。detector 的 border-accent-on-rounded 指向标签选中下划线，实际 .tabs button 为 border-radius:0，非厚侧边装饰卡，属于误报；其余颜色、圆角、字号属于 DESIGN/token 登记建议，不自动等于机械缺陷。brief 和项目状态仍有“C01未确认/尚无P01”的旧文本，应由执行/文档线程更新到本轮真实授权，不以旧文本否定新授权。本结论不批准弹窗审美、未查看状态、整页 D/R、真实业务接入、真实上传/AI、网络并发或服务器保存。正式业务未接入、未部署；所有合成数据验证只支持隔离设计稿。最终仍须用户审核 P01 主页面。

### 最终 verdict（同一 reviewer，2026-09-15）

保留以上首次 NEEDS-FIX 历史。本次只复核 F1/F2，不重新开展缺陷搜索。再次读取 ScoreTrendsDesign.tsx:60、63，实际 view_image 查看 reviewer-fix/1536-trends.png、390-trends.png、1536-long-history.png、390-long-history.png 四张补拍图。

| 项目 | 判定 | 证据 |
|---|---|---|
| F1 有录入学生占比口径 | resolved | 第60行已标“平均有录入学生占比”，同时说明“每人至少录入一科即计入”；四张图均能读到，手机正常换行，没有裁掉口径。 |
| F2 等距横轴说明 | resolved | 第63行已改“按考试日期排序，场次等距；纵轴为得分率”，两张桌面图显示正确；手机首屏尚未滚到图表，本次对手机该段结论依据共用源码，不伪称截图展示了图表说明。超过36场的截取提示分支也没有继续声称时间比例轴。 |

最终处置：PASS（仅本次隔离 P01 交用户主页面审核的 finish review）。两项必修均已解决，open material findings 为0。执行线程报告隔离构建、tsc 通过及四图无 overflow/runtime errors，本审查未独立运行这些检查。长历史补拍当前选择“最近12场”，因此图片证明50场考试库下的12场视图，不证明完整50场曲线。未审范围与首次报告相同；本 PASS 不代表用户主页面批准、弹窗审美批准、完整 D/R、正式接入或部署许可。
