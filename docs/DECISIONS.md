# 长期决策记录

## 2026-09-15 家校沟通C01确认与P01边界

用户“确认”仅确认已展示家校沟通C01方向。P01默认按待跟进/已跟进/归档分组，不根据自由文本followUp生成到期/逾期统计。复制通知不外发；按原操作规则空回执保存为“已确认知晓”，UI明确说明；再次复制仍保留回执文本。旧记录时间与reflectionId兼容在隔离层，正式业务尚未接入。弹窗视觉继续后置但不可绕过接入前补审。P01待用户主页面审核，不能自动开始成绩页。

## 2026-09-15：成长主页面范围认可，照护C01待方向确认

用户同意弹窗与未覆盖内容后置补审并继续下一页。成长P01/4f7637c仅新增mainPageApproval，整页designApproval仍false；学生/作业/考勤原批准历史保留，四页均新增overlayReview待补审条件，不扩大为弹窗/完整内容批准。专项清单docs/page-designs/OVERLAY-FOLLOWUP-20260915.md，正式接入前补齐；指标语义延期仍开放。

健康与照护已核对真实HealthCare与operations，完成2张只读合成现状截图和2张ImageGen独立C01候选；入口docs/page-designs/V3/health/record.json。候选尚未确认，未制作P01，未改正式组件/共享shell。列表规模与保存/脏关闭风险记录在function-map，不能算解决。听写仍暂缓，原四项修改保留；不部署。下一步等照护两图方向确认。

## 2026-09-15：成长C01风格获准，P01准备设计审核

用户确认页面风格并授权实现，同时明确红框成绩/积分/作业整组指标后期重新设计：成绩和作业属于某次任务，不能作为档案常态指标。P01身份带先移除整组，仅保留姓名学号分组；这不等于统计语义重设计完成。旧摘要/状态/排序口径未改，摘要及复制载荷带局限提示。延期项GROWTH-PERIOD-METRICS与用户标注图hash已登记。

新增app/designs/growth隔离实现与4212/growth.html合成预览，复用原growth操作；未改ClassroomApp、正式features或共享shell。完成38张主状态+8张补充、34项双端DOM交互及7项既有成长/照护操作测试；类型、scoped lint（1条img提示）、隔离与生产构建通过。复制说明与持续409弹窗内导出/确认载入恢复路径已修正，独立复核两项均resolved，技术/呈现结论PASS，不能替用户D。qa:strict复跑仍因旧querySelectorAll运行错误失败，不称整站通过。当前无P01/D/R批准。听写继续暂缓；原4项修改保留；不部署。下一步向用户交付成长P01供单页设计审核。

## 2026-09-15：考勤P01单页获准，成长C01待方向确认

用户“可以 继续下一页”绑定考勤P01/d7450bb，36张主证据与14张补充证据hash已登记，仅单页D。当前HEAD核对0d6bcd6，原4项未提交改动保留。成长真实组件只读合成恢复3图，独立生成桌面、手机列表和手机详情C01，入口docs/page-designs/V3/growth/record.json；未制作成长P01、未接正式业务。实际源码无人工记录编辑/删除，与旧任务卡差异及草稿/日期/计数风险已盘点。候选的分页、日期示例和字体/控件比例待真实实现校准，不能当已实现。听写继续暂缓，作业控件比例及考勤月历小字仍为未实施优化；V3整批D/R未批准。下一步等待成长三图方向确认。没有部署。

## 2026-09-15：考勤独立P01的复用与同步语义

C01方向批准不等于P01批准。正式接入必须复用AttendanceDesign及局部控制器/素材，宿主unsynced接真实工作区未确认状态；月历在busy/unsynced时标待同步，不能用乐观本机数据声称服务器已登记。当前只做隔离适配，没有重写正式workspace/operations。手机今天移入月历，全部四态独立行，触控44px；保留小输入区的视觉目标。新候选未冻结的实际字体/壳层/插画差异交用户审P01，不自动升级批准。

## 2026-09-15：听写暂缓与设计顺序例外

用户明确要求先跳过尚未验收功能的听写页，先做其他页面。记录为可提前开展考勤等后续页面独立设计，保留听写功能验收、候选和D/R待办；不是将听写移出项目，不算已通过，也不开放正式接入。工作流检查器原有正式接入/下一批产品门槛继续保留。作业P01基本接受，用户允许输入/筛选框比例后期收紧，列为未实施优化。

## 2026-09-15：作业候选批准与唯一展示组件

用户“确认/继续”只接受作业C01方向。HomeworkDesign接受现有controller，后续正式接入必须复用该组件；预览host仅负责合成数据及保存模拟。任务长内容通过原文展开保留，备注继续绑定student.note，不新增每任务备注模型。共享shell按实际复用，候选误字不继承；字号、插画、触控行高及手机版密度差异交P01审核，不从候选批准推导D。


## 2026-09-14：学生P01接受范围与V2批次边界

用户在学生P01交付后表示“可以 继续下一步”，接受该单页可运行稿97d2121及所披露的手机构图差异。designApproval仅绑定学生记录和证据，作业/听写仍需独立候选及设计批准；本批三页D未齐，不允许提前接入。所有R批准仍空，V3不开放。

## 2026-09-14：隔离稿保存唯一展示实现，候选确认不升级为D

用户“方向确认”只绑定学生C01两个候选hash。学生P01放在app/designs/students，正式入口不导入；后续D批准后直接复用同一组件，将现有controller/data/update注入。独立预览只模拟宿主响应，不能证明业务保存。为避免依赖在途未提交接口，显示层兼容既有字符串和结构化近期状态，排序选择仍归controller。

新素材与局部规则跟随该组件，避免未接入资产触发共享首页样式回归或覆盖共享设计。当前手机插画横带是P01实际差异，尚待审看；本批三页D和R门槛不变。

## 2026-09-14：schema 3承载新审核，旧字段保留历史含义

主账本recordPath/recordSha256链接逐页record；旧page.status、productCommit、currentEvidence及history不自动成为D/R批准。V1已有用户批准保留原范围。schema 2保留历史校验，schema 3不强制监工PASS；新门槛命令必须明确design-review、integration、result-review或next-batch阶段。候选图不替代可运行设计，本批三页D是接入前提，三页R是下一批前提。

检查器核对提交、证据/记录哈希、必要合同文件和变更路径，不评判好看，也不证明合同内容与关键状态完整。共享路径规则不等于完整依赖图，人工审核仍须核对字体、素材、交互及受影响页面。本次恢复demo截图元数据不足，不用于批准。

## 2026-09-14：每页独立设计，设计与接入分开批准

每页桌面、手机和关键交互均需独立可运行定稿，首页或公共组件不能代替。每批D设计批准后复用同一视图代码接入，R实际批准后进下一批。ImageGen仅做候选，真实组件版需再获认可；不承诺生成图跨环境逐像素复刻。监工为可选技术辅助，不承担审美与强制页间放行。保留已有批准历史，当前V2先设计。新包05定义旧账本迁移而不是覆盖已有记录。

## 2026-09-14：V1–V7持续对齐验收，而非一次性四区修补

每次视觉修改必须检查网格、上下模块列分界、列间距、标题/图标/表格/内容起点、内外边距、按钮基线与容器表达；在桌面、手机、空态、长内容实际渲染中核对。无依据的错位和分离/连体混用须修正，功能性不对称须说明用途并提供证据，不能以未经验证的“故意设计”辩解。共享布局变更复查受影响页面及已批准页面。

不靠等高大片留白、裁切内容、删真实数据或整页缩放追求整齐。自动测试不代表对齐与构图通过。首页本次共用外围双列，内部保留时间列、事实头像和靠右词卡；标题区共享基线，正文按内容自然高度。永久操作要求见AGENTS、VISUAL_BASELINE及资料包04证据规范，独立监工和用户批准规则不变。

## 2026-09-14：采用单执行者加独立监工

每页需要独立监工检查；门槛结束另需用户批准。监工PASS不能代替人工审美确认。报告绑定产品提交与证据，监工不改源码或Git索引。工具缺失时退化为共享文件检查点并在页间等待，不能声称永久后台监督。功能性色彩分区和参考构图优先于旧通用表格偏好。未提供独立参考的页面按已批准母版与逐页卡延展。

## 2026-09-14：视觉纠偏采用七个人工门槛，聊天记忆不再承担基线

- C1-C6原图以SHA-256锁定，并在Git保存高质量WebP副本；任何新对话、上下文压缩或视觉修改都从`VISUAL_BASELINE.md`与机器账本恢复，不依据旧聊天摘要猜测。
- 教师19页分为V1-V7：V1单独首页，此后每三个页面一门。每门必须展示桌面/手机实际图和参考对照并等待用户明确确认；未确认不得进入下一门。
- 自动测试只负责功能和可测构图约束，不能代替人工视觉判断。页面只有具备持久对照证据、机器门槛和用户确认后才能记为`user-approved`。
- 共享CSS、主题、插画、壳层或断点修改会使已批准门槛进入回归复核，避免后期整理覆盖前期成果。家庭教育排除在本轮视觉纠偏之外。

## 2026-09-13：正式 UI 退出旧生成链，性能结论只记录实测边界

- 正式入口不再生成或加载旧全局 UI：兼容源、隔离产物、legacy 根、探针与 `homework-bootstrap-shell` 已删除。现有路由级 CSS Module 是迁移后消费者规则的临时集中点，后续只能按真实页面族继续拆分。
- 校园主题与内部预留 glass 均使用实色、无 blur；glass 仍无公开入口或插画资源，不能将预留定义写成已交付主题。
- 低频教师模块按需加载，主客户端 JS 与构建 CSS 的下降已经由生产构建实测；手机访问过页面的隐藏保活仍会累积 DOM，是否改为快照恢复要等低端真机数据，不凭主观流畅度决定。
- 当前产品优先级是班主任工作台。家庭教育保留历史能力和证据，但不属于 TASK-30 至 TASK-33 的修改、验收或交付声明范围。

## 2026-09-13：作业追踪以任务为主线，异常处理优先于长期筛选

- 桌面作业页是一张连续批改工作纸：左侧固定任务主列表，右侧集中当前任务统计、学生异常筛选、逐生状态、完整备注和选择后批量动作；不能重新拆成卡片墙或多个无上下文页面。
- 首要路径是“选任务—看异常—批量处理”。月份、日期、学生状态和小组等长期条件进入次级筛选Drawer，批量条只在有选择时出现，成功本地应用后清空选择。
- 手机不缩放桌面双栏，而从任务列表进入单个任务的学生详情Drawer；返回后保留任务筛选和分页上下文。桌面与手机共享同一controller和读取模型。
- 作业必须按显式classId限定活动班，学生集合来自当前班名册；学生搜索可含姓名、学号和班务备注，但不含家长电话。演示/到期只读保留查看、筛选和复制，所有写动作禁用。
- 页面本地应用仍只是workspace草稿，不等于服务器确认。网络失败与409继续由共享保存状态保留dirty并提供重试/冲突处理，不能以页面提示宣称云端已保存。

## 2026-09-13：学生名单只呈现班务识别信息，敏感资料进入单学生流程

- 桌面学生页以连续名单和右侧当前学生事实组成一个工作面；手机从线性名单进入单学生Drawer。两端共享同一controller和当前班名册，不复制业务树。
- 名单主表只显示家长电话是否维护，不展示完整号码；完整电话、监护人与照护资料只能在单学生编辑或受控档案内出现。CSV导出仍是教师主动触发的当前班数据导出，不等于公开展示。
- 新增与导入学生不预填虚构积分或成绩；0表示没有已录结果，页面按“未录入”表达。删除必须先确认并按studentId清理当前班关联，不能用姓名或行号推断关系。
- 演示和到期只读状态禁用学生写入口但保留搜索、详情和导出。一般正式编辑仍先进入workspace本机草稿，网络失败和409由共享保存状态承接，页面本地成功不等于服务器已确认。

## 2026-09-13：首页只汇总可追溯班级事实，家庭入口降级但不删除

- 首页是班主任当天工作面，不是第二套业务编辑器；只汇总现有模块事实并导航，写入、失败重试和冲突继续由目标模块与共享工作区控制器负责。
- 所有数字先以活动班和当前名册求交集。学生提醒只接受带日期和来源的考勤异常、待跟进沟通或未完成作业；无时间来源的汇总字段不能包装成“最新情况”。
- 当天课程、活动、个人事项按真实本机日期与星期合并；无当天事项不等于无课程配置，空工作区也不得补造岗位、任务或学生。
- 主要目标用户仍是班主任。家庭学习保留可达和数据隔离，但当前不扩展；后续完整迁移需要用户重新确认优先级。

## 2026-09-13：共享壳层只有一个导航与场景所有者

- `WorkbenchShell`负责桌面顶栏、四组19模块导航、手机返回/底栏/更多和保存状态；`useWorkbenchShellController`负责活动模块、URL、浏览器返回、模块轨迹及离开守卫。业务页不再自建第二套全局导航或场景切换。
- 手机高频底栏固定四个模块加“更多”；更多只包含余下15项，不能为了“完整”重复一级入口。听写与家庭页面保留业务子页，不重复班级/家庭全局场景按钮。
- 返回需要恢复教师的工作上下文。访问过的手机模块短期用隐藏保活保存筛选与分页，滚动由壳层按模块记录；最多19页的DOM/effect成本必须在TASK-31量化，不能把短期正确性当成无限扩展方案。
- 新壳层位于next主题边界，未迁移业务主体继续位于legacy根。旧`workspace-chrome.css`只因其他遗留选择器尚有消费者而保留，不能再作为新壳层的验收来源；TASK-30按消费者删除。
- 自定义返回当前用`replaceState`避免历史增长；与浏览器原生Back交替的完整语义仍需后续集成测试，不宣称已覆盖所有混合序列。

## 2026-09-13：首批校园情境图采用母样家族与独立手机资源

- C1–C4只提供媒介、配色和空间组织参考；生产图必须独立构图，不复制人物、动物、口号、UI或参考图内文字。
- 一个视觉母样约束水彩笔触、晨光和暖纸底；首页、听写、学生档案、作业按业务物件分别生成，避免一张桌面图重复充当所有语义角色。
- 桌面和手机登记独立文件与实际裁切，不靠同图硬裁。业务文字只由HTML提供，任何生成的尺规数字、分数或疑似文字都必须移除。
- 带完整暖纸背景的全幅横幅允许使用不透明WebP；透明边缘是独立摆件/贴图要求，不能机械套用于全幅场景。

## 2026-09-13：旧CSS由AST生成隔离产物，不直接进入根布局

- 迁移期保留旧CSS源作为未迁移页面的真实样式，但正式入口只加载生成产物；每个最终命中元素必须属于legacy根，next子树是停止边界。
- 选择器通过解析器修改，不按逗号切分或对整文件做字符串前缀。`html/body/:root` 主体必须人工收敛；生成器发现其重新出现时立即失败。
- 原生top-layer不改变DOM祖先关系，因此新Dialog仍受next停止边界保护；旧portal若未来改挂到body，必须显式建立所属根，不能假设CSS Module会阻止全局命中。
- 生成产物不提交，标准dev/build/QA流程前自动重建；修改旧CSS的长驻开发服务需要重启或手动运行生成命令。TASK-30仍负责逐消费者删除旧DOM、源规则和最终生成链。

## 2026-09-13：共享浮层使用原生顶层，百人选人保持线性名册

- Dialog与Drawer共享一个原生顶层生命周期：打开时聚焦明确子元素，背景不可交互，嵌套层关闭只恢复所属父层或触发器，不能由全局Escape处理器连带关闭父层。
- dirty只是状态，不等于自动保护；关闭请求必须进入明确的放弃确认。busy期间拒绝关闭，异步失败保留表单和选择上下文。
- 100+学生选择使用搜索、分页和线性行，不使用按钮墙、卡片网格或一次渲染全部105人。跨页选择保存在提交草稿中，只有确认成功后才交给调用者。
- 旧StudentLookupDialog暂保留为兼容适配器；旧页面全局CSS和其余浮层只可在逐消费者验证后删除。

## 2026-09-13：全站视觉迁移设置三个用户可浏览门槛

- TASK-07完成后检查基础控件与主题语言，重点看按钮、输入、菜单、加载、空态及手机可操作性；它不能替代整页构图验收。
- TASK-11完成后检查共享壳层、导航、顶栏和手机底栏，判断旧后台结构是否已经退出。
- TASK-12完成后检查首页桌面/手机、满数据/空态及插画构图；只有该代表页方向得到确认，才继续大批量迁移后续业务页面。
- 在每个门槛等待反馈时保持Git提交、测试和进度文档闭环，不把页面停在半迁移、半删除旧CSS或未完成保存时序的状态。

## 2026-09-13：主题定义是新UI token与插画资源的唯一来源

- 每个主题必须完整提供语义颜色、表面、字体、间距、圆角、阴影、动态和能力；页面与共享控件只消费生成的 `--wb-*` 变量，不自行建立颜色主题。
- `ThemeBoundary` 只提供definition和CSS变量，不持有业务状态、不使用React key。主题值改变不得重挂载页面controller或清空表单。
- 插画使用语义role。旧slot仅是迁移适配，不能维护独立路径；资源缺失时返回空，glass不得回退campus。
- 玻璃主题必须同时声明实色与增强表面。不支持backdrop-filter时使用玻璃自己的实色面；减少动态由能力声明和系统媒体条件共同处理。
- 公开解析固定campus，glass保持planned且没有公开入口。只有资源、各页面状态和全站视觉验收全部完成后，才能改变公开解析策略。

## 2026-09-13：工作区控制器保持一般保存与听写确认提交两种语义

- `useWorkspaceController` 统一拥有工作区加载、草稿恢复、dirty、自动保存、保存队列、revision、409冲突、备份读取和离开守卫；页面不再直接复制这些ref和effect。
- 一般 `updateData` 先应用本机数据并写草稿，`save` 再以服务器revision提交；听写 `commitWorkspace` 只有服务器确认后才把批改计入正式结果。两者不能合并成一个无条件成功接口。
- 待提交听写必须进入完整备份并阻止退出；409后不得继续自动覆盖，用户只能先导出草稿或载入服务器版本核对。
- `/w/demo` 是只读演示。任何共享一般写入口都必须与到期只读一样拒绝修改，不能用“不会持久化”替代只读。
- 页面自己的成功提示、关闭编辑器和清空选择需要等待相应提交确认；共享controller完成不代表所有旧页面已经符合该时序，须在页面迁移任务中逐项验收。

## 2026-09-13：正式空工作区与旧字段缺失使用不同兼容语义

- 显式 `homeworkTasks: []` 和 `weeklyPlan: []` 表示用户的真实空状态，规范化不得为页面丰富度写入演示作业或周计划。
- 只有旧备份完全缺失 `homeworkTasks` 字段时保留一条兼容任务，避免旧学生汇总失去可编辑入口；周计划缺失直接保持空，因为没有可靠来源可恢复具体内容。
- 规范化、读取派生和写操作分层维护。工作区/班级投影位于 `workspace/normalize.ts`，页面所需日程、成绩和成长读取模型位于对应feature，写入校验继续由operations负责。
- 纯函数抽取必须固定验证缺失值、显式空值、真实零值、同名学生和跨班映射；减少大组件行数本身不代表页面迁移或视觉验收完成。

## 2026-09-13：页面迁移使用可重复合成夹具而非生产数据副本

- E0至E11由 `tests/fixtures/workspace-fixtures.mjs` 按当前数据形状生成；夹具只能包含明确的测试内容，不读取或复制正式数据库、备份、环境变量、学生或家长资料。
- 大名单、家庭孩子、任务数量、词数和容量使用显式variant；每次调用返回独立对象，避免一个场景的修改污染另一个场景。
- 版本1/2备份和听写归属必须调用当前产品校验器，全部场景至少能在系统临时SQLite序列化回读。临时数据库测试结束删除，不能写入仓库 `data/classroom.db`。
- 只读、保存失败和409夹具描述输入与响应契约；对应页面只有在真实认证、请求失败和并发流程执行后才能通过验收，不能用静态对象冒充交互证据。

## 2026-09-13：管理员展示层独立于工作区且继续依赖服务端权限

- 管理员页面放在 `app/admin/features/admin`，以页面级CSS Module、controller和请求层维护；旧 `admin.css` 及单文件内联展示退出。展示拆分不改变 `/api/admin/*`、管理员会话、同源检查、IP白名单或审计职责。
- 用户详情、设备、手机号和永久删除共用一个原生对话框。续期、禁用恢复、设备解绑和删除在对话框内明确确认；手机号脏表单关闭需要放弃确认，失败响应保留上下文。
- 汇总数字表示完整数据集，不随当前筛选结果缩小；表格在手机转为带字段标签的列表，不复制第二套状态或请求逻辑。
- 管理员页不是工作区，因此不套用演示/到期只读写入规则；它只能在独立管理员会话后调用服务端动作，并可读取用户的工作区状态。
- `admin` 插画通过主题语义槽提供。校园主题使用已有原创校园物件，玻璃主题保持空映射，资源和全站材料完成前不公开切换。

## 2026-09-13：账户、班级与备份共用一个受工作区状态约束的入口

- 桌面与手机共用一个 `AccountCenter` 和同一工作区数据，不再各自维护账户、班级或备份状态。账户接口只返回当前用户、工作区期限和当前设备；管理员数据与续费能力留在管理员权限边界内。
- 班级名称、年级和学期的编辑直接进入工作区本机草稿，关闭账户中心不丢修改；删除班级使用纯操作层清理目标班关联和班级配置映射。至少保留一个班，家庭孩子及其听写历史不属于班级删除范围。
- 当前设备退出必须先处理未同步草稿；保存失败时取消退出。退出接口只撤销当前设备会话，不能在文案中暗示其他设备同时退出。
- 完整备份恢复继续先校验5MB上限、版本和数据身份，再显示替换摘要。账户中心在打开系统文件选择器前关闭，避免恢复确认与原生对话框形成不可见的嵌套顶层。
- 对话框内的删除和退出确认使用内联危险状态；通用原生对话框每次打开将滚动位置复位到顶部，保证手机重开时先看到上下文而不是上次停留位置。

## 2026-09-13：公开入口按认证契约和页面级样式边界维护

- 首页已有会话检查、兑换码与手机号验证、服务器错误和跳转路径属于认证契约；展示重构不能在客户端推测账户状态，也不能把验证成功文案提前于服务器响应。
- 入口、隐私和管理员虽然都在工作台外，但不再共用一份按页面类名覆盖的布局CSS。入口与隐私由 `features/entry` 和 CSS Module 负责；管理员当时保留的独立 `admin.css` 仅是迁移过渡，后续已由 `features/admin` 的CSS Module替代。
- 入口插画通过 `entry` 语义槽接入同一主题系统。校园主题可复用已有原创教室场景；玻璃主题保持空映射，资源未完成前不显示切换入口，也不复制认证页面和业务状态。
- 代价是公共页有自己的轻量壳层和焦点管理；收益是工作台旧CSS无法改变认证页面布局，手机表单和隐私正文可以独立验收。

## 2026-09-13：家庭孩子以稳定ID隔离，归档不改写学习历史

- 家庭孩子不属于任何班级名册；姓名和年级只是展示字段，任务、结果、错词与统计必须按稳定 `childId` 读取。允许重名，不能用姓名合并档案或关联历史。
- 归档孩子只暂停新建、复听和批改，既有任务、参与者与材料快照继续可读；服务端校验必须同时阻止绕过界面的新增写入。已归档任务不计入待处理数量，但历史统计和错词来源保留。
- 家庭页面可以拥有独立的校园构图和插画槽，但任务编辑、词库、批改、版本冲突与备份仍复用听写核心。未来玻璃主题通过同一语义槽替换材质，不复制家庭业务树。
- 代价是家庭页与共享听写组件之间需要明确边界，旧公共CSS不能一次性删除；收益是多孩子切换、重名、归档、复习轮次及未来主题都不改变业务身份和历史。

## 2026-09-12：班干部岗位按班级和明确小组保存

- `CadreRole.classId` 是岗位班级归属，`groupNumber` 是小组长岗位的明确小组。旧数据只在学生能够唯一归属时补班级，小组号可由旧岗位名或已任命学生迁移；新写入不能继续只靠展示文字推断。
- 班委可以先建待任命空岗，小组长必须选择当前班已有小组及该组学生。正式空工作区保持空白，不能为了页面丰富度自动补示例干部；待任命复制文本也必须准确标注状态。
- 删除学生时只清理目标班岗位关系，显式属于另一班的同ID记录保留。成长档案读取班干部经历同样先校验班级，避免历史备份中重复ID导致跨班串读。
- 代价是旧备份归一化要兼容无 `classId` / `groupNumber` 的岗位；收益是多班切换、非连续小组、缺岗统计、候选人范围和历史复制都有稳定依据。

## 2026-09-12：值日配置按班级保存，检查记录保存当日负责人快照

- `classDutySettings[classId]` 是岗位和轮换序号的权威映射；根 `dutyJobs` / `dutyOffset` 只用于活动班兼容和无映射旧备份迁移，新班不得继承上一班固定学生或岗位。
- 值日教学日继续读取课程日程，但每个教学日必须换算当前自然周的真实本地日期。自动轮换只使用有学生的实际组号；未分组学生明确排除，不能被隐式塞入其他组。
- 完成或返工时保存岗位、负责人、组号和分配来源快照；后续轮换不追改历史。恢复自动分配只更换负责人快照，不能删除既有状态和教师备注。
- 代价是旧备份首次归一化要同时维护根字段和班级映射；收益是多班切换、历史追溯、学生删除和完整备份恢复都有稳定边界。

## 2026-09-12：线上快照与开发基线并存

用户导出包的受检运行源码与0691aba一致；后续开发以该事实为参照并保留本地增量。导出残留但无引用的文件不等于当前运行依赖，不重新引入。归档仅留本地忽略目录，不公开Git或作为编译输入。保存控制器提取保留ref与两种提交语义；服务端确认后仍dirty的本机草稿必须记录新的服务器revision及已合并数据。

## 2026-09-12：共享数据操作不得冒充通用服务器提交

学生/作业新旧视图共用纯数据操作，权限、目标选择、确认、选择清理与持久化仍归controller/workspace。当前commitWorkspace在并发修改时只合并dictation，是听写专用语义，不能直接用于学生/作业。迁移保留一般编辑先本地应用再服务器保存的区别；不得把save的无操作true当写入成功。两端筛选及全选能力先记录并集再设计新入口。

## 2026-09-12：迁移中的批改逻辑共用与接线门槛

先将已有批改状态、草稿、重试及服务器确认后切人的逻辑原样抽取，再让旧视图和新视图共用。独立UI探针必须标明模拟，不访问工作区API。CSS Modules不能自动抵御旧全局CSS；完成隔离和真实保存验证前不切生产入口。插画使用主题语义角色，缺少玻璃资源时不冒用校园插画；当前原创PNG为candidate，资源压缩仍待上线门槛。

## 2026-09-12：新组件基准使用独立本地入口

- tools/ui-preview使用现有Vite/React依赖，只监听127.0.0.1，无API代理/数据库/生产路由和旧CSS。独立基准只证明控件自身，接入前仍必须验证新旧混合环境与真实保存契约。
- 原生Dialog作为候选公共浮层，关闭意图统一交controller，native top layer负责基础inert/焦点；接入旧页面时必须排除旧全局DialogAccessibility重复处理，同一浮层不能有两套管理器。
- 公共SVG注册表在新旧世代共用；旧名字映射只供迁移兼容，最终页面不使用旧结构类。玻璃切换仅在本地工具，新页面默认校园。

## 2026-09-12：允许重建全部展示层，旧UI兼容必须有退出终点

- 用户明确接受保留所有业务逻辑和功能、清除重构UI/DOM/CSS以适配校园及未来玻璃。仅换色/加图/保留所有旧结构不满足要求。
- 保持当前API、数据归属、保存/草稿/409、听写快照和备份契约；先盘点两端操作，再抽取controller与展示层。
- 未迁移页面可短期隔离使用旧CSS，迁移页必须无旧依赖；最终移除workbench-repair.css与homework-bootstrap-shell。旧文件体积本身不是删逻辑理由。
- C2三段式是任务材料、当前学生、批改的信息关系，不强制三条全高竖列；默认中央任务情境+批改、右学生队列，材料细节可展开，避免挤压词区。
- 未来glass保留theme/token/artwork/material降级接口，仅内部验证；首版不公开切换，不复制业务树，不改变草稿和备份版本。
- 详细执行唯一入口为项目实施包00。资料包制作时仅完成计划资料，不代表实施已完成；代表页视觉门槛通过后再全面铺开。

本文记录会约束后续实现的长期决定。新决定追加日期、背景、选择、原因和代价；不要把临时修复或纯样式调整写成架构决策。

## 2026-09-12：座位配置和撤销快照必须绑定班级

- `classSeatingConfigs` 是多班级座位布局的权威映射；根 `seatingConfig` 只承担活动班兼容字段和无映射旧备份迁移，不能作为新建班级的默认继承来源。
- 新班级按自己的名册人数建立默认布局。排座、换座、轮换、分组、条件和组长操作只修改目标班；撤销快照同时校验班级ID和学生ID集合。
- 页面允许教室画布与条件表在各自容器内横向滚动，但不能造成整页溢出。100人以上通过扩展行数承载，当前上限为20排×10列；超过200人必须明确拒绝，不能静默丢人或复用座位。
- 代价是旧备份首次归一化仍需保留根配置迁移逻辑，班级切换层也要维护根兼容字段；收益是新增班级不会继承另一班的空间安排，撤销不会跨班覆盖。

## 2026-09-12：主题通过语义槽接入同一业务树

- 校园主题使用 `ThemeArtwork` 语义槽和共享 SVG 图标，页面选择 dashboard、roster、homework、assessment、planning 等业务语义，不直接绑定某一张参考图或复制两套页面。
- 桌面与手机可以使用不同壳层和交互布局，但读取同一份工作区数据、调用同一保存与冲突处理流程；响应式差异不能产生两套学生、任务或听写结果。
- 浅蓝紫玻璃主题保持 `planned` 配置和空素材映射，不显示切换入口。只有素材、组件状态和全站代表页面完成后才能对用户开放。
- 未迁移模块可暂时保留 `homework-bootstrap-shell` 兼容作用域以维持稳定布局；每次迁移一个页面族时删除对应旧依赖并补充浏览器回归，不能以一次性删除旧选择器换取表面上的文件整洁。

## 2026-09-11：校园参考图成为构图验收基准

- 用户明确要求按 C1–C6 完整重建页面构图和视觉层次；参考图对全局壳层、页面区域比例、视觉重心、插画体量、主从布局和操作优先级具有约束力。
- 允许偏离的范围限于稳定字体、统一图标和参考图中的错字/假数据/生成瑕疵；真实业务数据与长名单必须适配进目标构图，不能据此保留原有后台模板布局。
- 桌面和手机共用业务数据与状态，允许使用不同交互外壳；19 个模块继续存在，但导航按任务组和频率分层。
- 视觉验收必须包含 C1–C6 同尺寸截图对照，自动布局和功能测试不能替代。完整规范见 `docs/校园插画全站构图与视觉层次重建规范-20260911.md`。
- 代价：需要重建共享壳层、标杆页面和页面族组件，并清理旧 DOM/CSS 依赖，工作量显著高于换肤；收益是后续模块可以在稳定的视觉和结构系统上扩展。

## 2026-09-11：共享控件与弹窗行为

- 桌面和手机首页、班级切换、通用学生选择与复制能力共用实现，主题样式按控件和页面族拆分，避免重复业务树。
- 工作台弹窗由共享管理器按激活顺序负责焦点、Tab/Escape、背景inert与恢复；入口和管理员页保留各自管理器，避免同一弹窗由两个管理器竞争焦点。
- 视觉验收区分初始页面、编辑状态、实际交互与自动布局检查，代表页通过不能替代全站通过。

## 2026-09-11：听写持久化与双主题边界

- 校园主题优先，共用业务树、页面结构、SVG和主题资源插槽；玻璃主题仅预留，不展示未完成入口。
- 听写数据作为工作区可选version:1扩展，沿用账户所有权、版本冲突与完整备份；导出备份版本2，仍接收版本1。
- 每个任务固定材料和参与者快照，确认结果存状态、错词索引、时间和备注；复听新建任务。家庭孩子使用独立context，不进入班级名册。
- 保留5MB边界，预检4.8MB；200次、每次50人20词的合成听写数据约1.43MiB。全工作区与长期存储仍受容量约束，归档不缩小数据。未来规模超过上限时应迁移增量记录，不能静默删除历史或单纯提高阈值。
- 听写必须收到服务器确认才更新正式结果和跳转；未批改、请假、未参加不作为错误或全对。自动报词、OCR、AI批改与外部消息不在此次默认范围。

## D-001：用仓库文档和 Git 作为跨对话项目记忆

- 日期：2026-09-11
- 决定：新对话按 `AGENTS.md` 固定顺序读取导航、状态、变更、部署和决策；每次修改后同步文档并在验证通过后提交推送。
- 原因：Codex 超长对话可能压缩或折叠显示，聊天界面不能作为唯一项目记录。
- 代价：每次任务增加少量文档和 Git 维护成本，但能保留可审计的代码差异和进度。

## D-002：Git 不存部署包、运行数据、密钥和对话日志

- 日期：2026-09-11
- 决定：压缩包、数据库、`.env.local`、真实班级数据、QA 报告和 Codex 恢复资料仅本地保存；Git 只记录包名、大小、哈希和验证状态。
- 原因：避免仓库膨胀、隐私泄露和把运行状态误当源码。
- 代价：部署包需要额外本地/服务器备份，不能只依赖 Git 恢复。

## D-003：正式工作区空白，演示数据只留在演示入口

- 日期：2026-08-25（历史重建）
- 决定：正式账号首次进入不生成虚构学生、成绩或健康资料；演示数据只用于只读演示工作台。
- 原因：防止老师误把示例当真实记录并保存。

## D-004：健康信息按最小必要行动提示管理

- 日期：2026-08-25（历史重建）
- 决定：产品名称使用“健康与照护”，仅记录日常安排所需提示，不展示诊断细节，不作为公开学生标签，也不替代学校医疗和应急流程。
- 原因：减少隐私暴露和标签化风险。

## D-005：长名单操作必须支持搜索、筛选、批量处理和状态清空

- 日期：2026-08-29 至 2026-09-06（历史重建）
- 决定：学生、作业、考勤等全班名单按 50 至 100 人规模设计；不能依赖长下拉框，批量状态操作完成后清空选择。
- 原因：符合真实班主任高频操作，降低误操作和重复取消选择的成本。

## D-006：值日教学日跟随课程日程配置

- 日期：2026-08-29（历史重建）
- 决定：值日岗位不固定为周一至周五，必须读取课程日程定义的一周教学日，支持六天或七天教学安排。
- 原因：避免两个页面维护互相冲突的星期配置。

## D-007：AI 结果必须经过教师确认

- 日期：2026-08-25（历史重建）
- 决定：试卷识别只生成候选题号、题型、知识点、分值和置信度；老师确认后才进入正式统计。AI 密钥和文件处理留在服务端。
- 原因：OCR/模型输出不具备天然准确性，不能直接形成学生评价或正式教学结论。

## D-008：桌面与手机共享业务数据，但允许不同交互外壳

- 日期：2026-08 至 2026-09（历史重建）
- 决定：电脑和手机功能能力同步，数据来源一致；布局可分别优化，但不能产生重复写入、不同字段或一端缺功能。
- 原因：两端使用场景不同，但业务事实必须一致。

## D-009：GitHub 作为持续项目记忆的主远端

- 日期：2026-09-11
- 决定：默认将当前分支推送到 `github`（`a18502656182-arch/teacher`）；旧内部 `origin` 只作历史保留。项目状态、变更、部署和长期决策与源码一起提交。
- 原因：内部远端没有可恢复的用户凭据，而本机 GitHub Credential Manager 已具备可用授权。独立 GitHub 仓库能在切换 Codex 对话后继续读取完整项目历史。
- 风险：该 GitHub 仓库当前为公开仓库，必须持续排除真实学生数据、数据库、密钥、部署包和对话恢复资料；如项目不希望公开，应由仓库所有者改为 Private。
# 2026-09-12：学生删除按学生关系清理，不按同班考试整体清理

删除学生只能移除目标studentId的考试反思；不能因反思引用同班考试就删除其他学生反思。听写任务只在本次删除确实移走其最后一名班级参与者时删除，原本为空、家庭场景和其他班任务必须保留。此边界由relations.ts纯操作与合成数据回归约束。
# 2026-09-12：工作日期使用本地年月日

考勤等按老师所在地理解的“今天”必须用浏览器本地年月日生成，不能用toISOString的UTC日期截取。考勤批量状态未显式提供备注时保留已有备注；状态操作不能隐式清空教师记录。
# 2026-09-12：听写任务编辑保留历史身份

编辑任务只能改变允许编辑的任务内容；原id、createdAt、results、sourceId和archived必须保留。复习来源和归档是历史语义，不能因表单重建对象而丢失；已批改任务继续由assertDictation禁止修改场景和材料。

## 2026-09-15：健康与照护C01确认的实施边界

用户“确认”许可隔离P01，维持逐页候选/真实设计/实际接入三版本及后置浮层补审边界。照护登记是持续的必要行动提示，列表不改成一次性完成任务或健康评分；联系人按需展开，不铺满主清单。P01原操作层复用，失败时保留已变更的本机状态，重试仅同步同一变更；此方案尚未接入真实workspace保存，不推断服务端验证完成。详见health/record及function-map。

## 2026-09-15：照护主页面批准与V4设计顺序例外

用户明确认可照护主页面同时指出弹窗有问题，允许继续下一页。仅绑定mainPageApproval/6e19189及双端主图，整页D/弹窗补审不放行。下一页家校沟通仅独立候选/后续隔离设计，可先于前批R推进设计；不能推断允许V4正式接入。通知草稿复制不等于外发，跟进为文本约定，候选不新增自动到期判断。

## 2026-09-15 成绩C01口径约束

用户曾否定将某一次成绩作为档案常态指标；本页将考试标题/日期/科目/满分绑定展示。C01不捏造平均、排名或趋势；原分均值与标准化比率必须区分，趋势subject实际筛选范围在P01前核对。保留旧operations，任何正式口径迁移另作明确兼容判断。本轮家校沟通仅主页面大体认可。

## 2026-09-15 成绩P01隔离实现口径

C01方向已由用户“确认”，P01待审。原分/原始平均与标准化得分率分开，不用于学生常态评价。趋势科目只筛考试；学生覆盖定义为至少一科有录入，横轴按日期排序但等距，缺失场次断线。隔离组件复用原operations及业务数据结构，真实student.score派生值行为未重写。桌面暂存录分显式保存；原AI服务不调用，仅合成候选与手工确认。所有弹窗/未审状态仍需接入前补审。
