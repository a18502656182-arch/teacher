请作为班主任工作台视觉重构的独立监工，监督另一个执行对话。
项目：E:\文档\Codex\小红书\班主任工作台网站。
读取AGENTS.md、VISUAL_BASELINE.md、trackers/visual-baseline.json及docs/重构执行与监工资料包-20260914中的00至04。检查supervision/checkpoint.json。
你只修改supervision/reviews内的检查报告，不改产品代码、主账本、Git索引或分支；不要与执行者并行运行会写构建产物的命令。以实际源码、git diff、提交和打开的真实图片为依据，不相信“测试通过＝视觉通过”。
先发现实际可用的跨对话和定时工具。执行对话链接由我提供后登记在报告中；有工具则用紧凑进度快照监督，有变化再深入。无工具时明确报告“共享文件检查点模式，尚未启用自动监控”，检查已有文件即可，不假装读到另一对话。允许向指定执行对话发送检查意见和停止纠偏通知。
每页完成检查范围、构图、插画面积/裁切、字号、颜色分区、手机重排、功能状态、证据版本和未决问题。报告含checkpointId、reviewedProductCommit、PASS/CHANGES_REQUESTED/BLOCKED/STALE、具体证据、下一动作。
V1首页完成、V2至V7每三页完成都必须暂停让用户看图。你的PASS不能替代用户批准；检测到下一门未经批准开工，立即要求执行者停止并通知我。
若我启用周期监督，优先用当前产品提供的定时机制返回本对话，每10分钟检查HEAD/checkpoint变化；无变化保持静默，仅新问题、检查完成或需要人工决定时通知。没有可用调度机制时不要声称后台已运行，也不要无限sleep占用一轮。
现在先做启动检查并告诉我当前门槛、最近产品提交、缺失证据和实际监督模式。
