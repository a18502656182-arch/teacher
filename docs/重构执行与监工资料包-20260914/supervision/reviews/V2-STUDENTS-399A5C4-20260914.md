# V2 学生名单独立检查

- checkpointId: V2-STUDENTS-399A5C4-20260914
- reviewedProductCommit: 399a5c48b538b4cab8177d2bccb82011671db763
- observedHead: 9bfe147930867714b2f38f92776bb85fb6bcdf5b
- checkedDate: 2026-09-14（北京时间）
- verdict: CHANGES_REQUESTED
- gate/page: V2 / students
- executorThread: codex://threads/01a09e19-8379-7f63-b537-7c54965c2044
- humanPauseRequired: false（本页修复并 PASS 后可继续 V2 内下一页；V2 三页结束必须暂停）
- approvalMessageReference: null（未批准 V2）
- supervisionMode: 共享文件检查点模式，尚未启用自动监控

## 结论

整体名单主区/详情栏、颜色分区和手机 Drawer 方向成立，不要求重构或继续精修插画。按用户“小问题记录后通过”的标准，装饰差异可后置；但手机可用的“新增学生”主按钮文字几乎不可辨，需要局部修复后复核。本次仅一项 P1 阻塞，不把所有瑕疵升级为阻塞。

## 必修 S1 / P1：手机新增按钮文字被页头样式覆盖

Render：实际打开 empty/referenceMobile-students.webp，右上按钮为实心青色，新增学生文字为暗灰色，明显难辨。该场景为可编辑零人名单，并非只读 demo 中合法禁用的灰按钮。不能用 demo 的禁用截图证明正常按钮可读。

Source：StudentsView.module.css:3 的 `.mobileHeader span{color:var(--wb-muted);font-size:12px}` 不仅命中班名，也命中共享 Button 的 label span。Button.tsx:13 在按钮内包裹文字 span；controls.module.css:11 的 primary 白色设置在 button 上，被子 span 自身颜色覆盖。校园主题 definitions.ts:36–37 的 muted=#526a7b、primary=#14778a，按这些静态色值计算对比约 1.09:1（非现场浏览器 computed-style 测量）；源码链与真实图相符。

下一动作：仅收窄学生页班名选择器或增加专属班名类，恢复主按钮自身的文字颜色与字号；优先局部修，不改共享 Button。补可编辑 empty/large 手机图、实际新增入口打开结果以及 label 的 computed color/background/font-size；保持 demo 禁用状态与正常状态可区分。不要借此重做名单布局。

## 非阻塞问题

### S2 / P2：近期状态残缺时间

demo 桌面和手机多行出现 `:20 家访准备`、`:20 成长记录`、`:45 作业跟进`。StudentsView.tsx:80/90 直接展示 recentStatusById；read-model.ts:56 对 latest.date 无条件 slice(5)，未区分输入日期格式。当前 read-model 本轮未改，故这是审查发现的既存问题，不称为本轮引入。

建议先核对实际记录日期格式，按类型格式化，无法确定日期时明确兜底，不伪造年月日。还需核对不同格式参与 latest 排序是否合理，不能仅用 CSS 遮住残片。可登记为 V2 收尾前的独立数据展示修复，不阻塞本页 S1 修复后的视觉放行；若涉及数据语义修改，先说明范围，不顺带改保存/API。

### S3 / P3：长姓名下“完整档案”尾字单独换行

large 桌面详情和手机抽屉均可见“完整档/案”，文字完整且抽屉底部另有清楚的完整档案主入口，因此不阻塞。后期局部为短动作保留合理宽度，让长姓名自然换行；不强制压小所有字号。

插画与 C3 的具体文具造型、右栏淡绿面积、手机页头留白等精细差异可继续后置，不新增像素级还原任务。

## 逐项证据与覆盖

| 项目 | 结果 |
| --- | --- |
| 范围 | 产品学生视图与局部 CSS、对应 QA/测试；另有视觉门槛脚本更新。未见作业/听写/V3 产品开工，未改家庭或保存/API |
| 构图/对齐 | 名单主区、右侧详情和连续概览带成立；普通与105人桌面图列边界、工具区起点清楚，表内滚动与分页保留，不靠删到几个人伪造密度 |
| 插画面积/裁切 | 右栏顶部为横幅场景，书本/纸笔可辨；没有首页式孤立小图回归，不要求换资产；空态另用小插画有提示作用 |
| 字号/颜色 | 标题、表头、名单与动态形成层级，蓝色表头/概览及当前行有区分；主按钮 S1 未通过，长姓名动作 S3 后置 |
| 手机重排 | 普通名单改为行列表，打开真实 Drawer 截图可见概览、动态、返回与完整档案；large 长标题可读，empty 有0人/分页与新增入口；未亲自操作浏览器 |
| 功能状态 | 已打开32人、105人及0人图；保留只读禁用、分页与抽屉入口。测试通过不代表生产新增/导入/保存或批量操作经监工亲自验证 |
| 版本 | index 中33次带 SHA 文件引用全部匹配，strict 与三场景 JSON 均绑定399a5c4；strict115项、三场景各3项，failures 均空。未宣称打开全部图片 |
| 门槛 | 共享 checkpoint 与账本登记了 V1 对23fcdc5的明确批准原文；无跨任务读取工具，不能独立验证消息原件。未发现与登记相矛盾的证据。本轮只读 gate V1 返回通过，不等于 V2通过 |
| 未决证据 | 1057px布局主要依赖源码/审计几何，未打开该宽度实际图；缺手机到底/分页、可见批量操作条的持久图，S1复核时宜补少量定向图。物理设备、屏幕阅读器、真实弱网/409及生产保存未独立验证 |

实际打开六张：C3桌面对比；demo手机名单；demo手机Drawer；large桌面名单；large手机Drawer；empty手机名单。比较图保留原 C3 1536×1024；C3无独立手机原图，未把旧/新手机图当原始设计参考。

证据根目录为 docs/visual-baseline/current/V2-STUDENTS-399a5c4，索引为 docs/visual-baseline/logs/V2-STUDENTS-399a5c4/evidence-index.json，提交说明为 docs/visual-baseline/V2-STUDENTS-399a5c4-review.md。

QA注意：students-visual-qa.mjs 在 Drawer 打开后直接通过 DOM `.click()` 点击背景名单 checkbox，不能证明用户能在模态遮罩下完成该操作。不要为了通过检查改变模态行为；改为先正常关闭抽屉，再选择学生并检查实际批量条，保存定向证据。此为证据路径问题，不直接指控产品批量功能坏了。

门槛脚本改为页面局部失效加共享失效，避免学生页修改自动废除首页，方向合理；该检查依赖路径映射，不是完整依赖图，新增跨页依赖需补映射。未做全量检查器安全认证，也未运行会写构建产物的命令。

## 下一动作

执行者读取本报告并登记 CHANGES_REQUESTED，只修 S1 并补对应正常/禁用状态、手机滚动及批量流程证据；S2/S3有记录地后置。保留已经成立的构图、颜色和插画。新产品提交绑定截图和日志后交监工定向复核；本页 PASS 后才继续 V2 的 homework，再 dictation。V2 三页结束向用户展示图并暂停，未经批准不得进入 V3。

本轮按 impeccable 界面维度与 anti-ui-slop 的源码/成图/判断分层，优先主要入口可读性，未因审美细节阻塞。不声称完成全面可访问性评分。监工仅新增本报告，未改产品、账本、checkpoint、Git索引或分支；未直接联系执行任务，仍需用户转发。
