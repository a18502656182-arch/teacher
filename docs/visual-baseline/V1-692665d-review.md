# V1返工提交说明

审核产品：`692665d8ad928c3ae90a52188623e2367212d0d2`。本次界面实质修改在`fc86f2e`，随后`0b1dc1f`及`692665d`补强采集器等待目标加载、滚动稳定和合成空岗位夹具。这三个提交合为本次可审查提交组。

上轮独立结论为CHANGES_REQUESTED，绑定`25950b7`及`docs/重构执行与监工资料包-20260914/supervision/reviews/V1-DASHBOARD-25950B7-20260914.md`。本说明由执行者填写，是提交复核的事实记录，不是独立监工报告。用户未批准V1，V2保持blocked。

## R1–R5处理与证据

| 监工项 | 源码处理 | 本轮验证与边界 |
|---|---|---|
| R1 听写空态正文挤压 | 首页局部同时约束picture和img；空态插画112×88，正文拥有剩余空间。有任务的picture也有明确宽高和最小宽度 | demo与empty验证无任务；large验证105人、200词、长任务名。有任务手机沿用原有窄屏隐藏插画规则，为完整正文和统计留空间 |
| R2 日期断词 | 月日与星期分别渲染为不拆分的语义组，15px文字保持 | 默认9月14日、合成1月5日、合成11月23日均为星期一；桌面与390px手机记录文字和计算矩形 |
| R3 纵向主从与下层 | 桌面待办标题/说明同行，压紧组内间距；课程名称/节次同行；场景picture脱离固有比例对舞台高度的撑开 | 未缩放整页，未删真实待办，移除日程前7项截断。demo保留5项，large展示8项；固定视口连续滚动至底部，保留常用操作、值日、事实提醒与底栏。真实内容增多时页面自然变长，不保证任何数据量都能装入首屏 |
| R4 原始日志与状态 | 采集器记录版本、时间、URL、设备信息、数据夹具、计算样式、滚动位置、快捷入口结果及底部安全区 | `logs/V1-692665d/evidence-index.json`登记原始日志、截图与哈希。加载页不作为交互完成图；额外记录目标模块、可见标题及内容 |
| R5 V0负例 | public资源、字体、构建插件、依赖锁文件、视觉配置均参与版本失效；证据路径规范化后去重 | 10项检查器测试包含`./`、重复分隔符、反斜杠等价路径及资源/配置提交后失效。使用Git NUL分隔输出保留中文文件名，并纳入未跟踪文件。仍不宣称检查器能替代独立读图或验证真实用户批准 |

主按钮额外修正为白字，计算样式为白色对`rgb(20,119,138)`，对比度约5.20:1。“记录班务”快捷入口改为与实际目标一致的“值日安排”。共享根CSS本轮没有再次修改；当前无已批准页面，严格QA仍回归19个教师页。

## 环境与数据场景

- 浏览器插件连接失败：运行时缺少`C:\Users\hx\.codex\plugins\cache\openai-bundled\browser\26.908.40834\scripts\browser-service.mjs`。使用项目既有Chromium/CDP巡检，独立临时浏览器配置，Windows、Node v24.19.0；具体浏览器路径和userAgent见日志。
- `demo`：真实本地只读演示入口`/w/demo?page=dashboard`，未替换其学生、待办、课程或日期。首页图反映原有32人示例数据；它不证明保存成功。
- `large`：仅测试浏览器内拦截`/api/workspace/qa-visual`响应，合成甲班105人、乙班1人、长姓名、200词听写、8节课程、带日期的考勤提醒和明确值日岗位。固定日期2026-11-23，先甲班、再通过可见班级选择器切乙班并返回。乙班没有甲班任务，当天空课。
- `empty`：同样隔离的合成工作区，甲班2人，无听写/作业/沟通/值日，保留周二课表，固定周一2026-01-05验证空课日。未把“没有今天的课”误写成“没有课表”。
- 合成可编辑场景的工作区写请求只在浏览器内返回模拟确认，绝不进入服务端/数据库；用于测试切班与UI状态，不能当作真实保存证据。演示模式本身禁止更新班级状态，因此跨班测试使用隔离可编辑场景。
- 四个手机快捷入口逐一点击、等待目标内容加载、记录标题/URL和截图，再通过底栏返回首页。值日和听写仅验证到达，不扩大到V2或后续门槛的页面改造。

## 测试日志与版本关系

`logs/V1-692665d/tests.txt`保存`fc86f2e`上完整`npm test`原始输出：生产构建、270/270 Node测试、认证集成通过。`test-reuse-proof.txt`记录fc86f2e至692665d的相关输入差异为空：app、lib、db、drizzle、worker、public、build、tests、依赖和构建配置完全未变；仅QA采集器的加载/滚动等待、合成夹具和验证命令筛选有变化。原始日志保留真实源提交号，未改写成新版本执行。

类型检查与19页六档严格QA在0b1dc1f执行；该版至692665d仅改变截图滚动和合成夹具分支，严格QA没有启用这两类分支，结果可复用。ESLint与demo/large/empty采集在692665d执行，各自保存原始输出及JSON报告，见证据索引。标准门槛检查仍需独立监工PASS，当前不放行。

本次保存相关证据来自同一业务源码的现有workspace操作测试与认证集成：前者覆盖失败保留、重试和冲突锁定，后者在临时SQLite/合成身份环境执行版本冲突请求。没有重新实测物理手机、真实弱网、屏幕阅读器或生产服务器，也没有用模拟确认替代这些结论。

## 如何看完整图

`current/V1-692665d/<scenario>/referenceDesktop-dashboard.webp`和`referenceMobile-dashboard.webp`分别是1536×1024和390×844首屏。`*-dashboard-scroll-XX.webp`为相同CSS视口连续滚动帧，滚动位置在索引与原始报告中。`desktop-continuous.webp`/`mobile-continuous.webp`仅把完整原尺寸帧加标题并拼成阅览图，保留重叠区，不能当成单次全页截图。

两张`comparisons/V1-692665d-*.webp`完整保留C1/C5参考，并标注各自原生像素及实际视口。C5原图948×1659不是手机CSS视口，不拉伸到390×844。

设计判断：参考要求的校园场景、黄/蓝/绿功能分区和标题层级继续作为视觉依据。彩色区域与四个同类快捷操作是本产品有意保留的结构；修复重点是文字可读性、图片尺寸和纵向主从。本轮不以设计检测器或测试总数抵消独立读图发现。

下一动作：监工按新检查点复核R1–R5及完整图；随后用户看图决定是否批准V1。没有独立PASS或用户批准记录，不开始V2。未部署。
