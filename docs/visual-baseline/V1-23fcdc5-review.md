# V1 R8插画局部适配返工提交

- 产品提交：`23fcdc582575ccd50f2edeba0db9e9e2d64673db`（实现提交`d46eba5`，断点样式优先级修正`23fcdc5`）。
- 结论来源：最新监工报告`V1-DASHBOARD-04E5ABF-20260914.md`为`CHANGES_REQUESTED`；`V1-DASHBOARD-351880E-20260914.md`作为问题历史一并核对。
- 当前只提交独立复核候选：监工`pending`、`humanApproval=null`、V2 `blocked`，未部署。

## 逐项回应

| 报告项 | 本轮实现与证据 | 当前状态边界 |
| --- | --- | --- |
| R6 图标与动作重量 | 保留04e5abf已关闭的彩色任务图标、四区图标及“查看/处理/进入”动作文字；未重做任务结构 | 已关闭项只做回归，不申请重新解释 |
| R7 工作面与学科色 | 保留暖白内容面、浅蓝/浅绿/浅金分区和语文/数学/英语/体育/科学学科色 | 已关闭项只做回归 |
| 四区对齐 | 保留统一双列分界、22px列距、标题/图标/内容起点、内边距和独立容器；手机仍为单列自然高度 | 已关闭项只做回归；不填空白、不删数据 |
| R8a 教室图 | 原`classroom-morning.webp`为960×640（3:2），桌面画框约613×287（2.14:1）。直接`contain`必然产生两侧宽留白，直接以原图`cover`会牺牲前景。现从同一原图生成首页专用960×450局部适配副本，保留窗、黑板、讲台和前排桌椅；桌面在约2.14:1画框内使用`cover`，摘要仍是独立58px区，stage仍约345.34px | 这是针对固有宽高比冲突的局部适配，不以`object-fit`或填充率代替视觉结论；主体辨识、占比和融合仍交监工读图 |
| R8b 听写文具 | 继续使用既有`word-cards.webp`，不改业务文案。桌面图像盒由216×144提升为340×226.66，手机由180×120提升为285×190；通过模块内定位和下沿出血增加可见主体，主词卡、铅笔和橡皮完整，流式容器仍为桌面144px/手机120px | 不强制撑高卡片，不挤正文；最终视觉重量仍交监工读图 |
| 断点回归 | 二次渲染发现601–900px共享`.campus-art { object-fit: contain }`会覆盖首页局部规则；最终提交在场景元素上绑定局部`cover`，桌面、平板、手机计算样式均已核对 | 该修复只作用首页场景，不改共享资产映射或其他页面 |
| 业务边界 | 读模型、导航回调、保存、只读、API、数据库、真实数据和家庭模块均未修改 | 家庭教育继续排除，V2未开始 |

## 同产品版本实际图

全部86张图、测试命令、采集时间、实际URL、视口、产品提交和SHA-256见`docs/visual-baseline/logs/V1-23fcdc5/evidence-index.json`。

- 桌面/手机首屏：`current/V1-23fcdc5/demo/referenceDesktop-dashboard.webp`、`referenceMobile-dashboard.webp`。
- 桌面/手机连续滚动：`current/V1-23fcdc5/demo/desktop-continuous.webp`、`mobile-continuous.webp`，包含常用操作、下层提醒和手机底栏安全区。
- C1/C5完整对比：`comparisons/V1-23fcdc5-desktop.webp`、`V1-23fcdc5-mobile.webp`。
- 教室同尺度局部：`comparisons/V1-23fcdc5-scene-desktop.webp`。旧、新均按原生像素并排，不缩放；旧源为04e5abf实际首屏裁区，新源为23fcdc5实际DOM局部。
- 听写文具同尺度局部：`comparisons/V1-23fcdc5-stationery-desktop.webp`、`V1-23fcdc5-stationery-mobile.webp`。两侧均为完整模块原生像素局部，不缩放。
- demo、large、empty均有桌面/手机首屏、连续滚动、scene/tasks/agenda/dictation/duty/attention局部和四区证据；large包含105人、200词、8节课，empty包含无课程/无听写。

实际人工打开核对了最终桌面与手机首屏、教室前后局部、桌面和手机文具前后局部。其余图片由同一采集器生成并经路径、产品提交和哈希校验，不冒称逐张人工阅读。

## 测试、数据与交互证据

- `types`、`lint`、`npm test`均退出0；`npm test`包含生产构建、270/270 Node测试和认证集成。
- `qa:strict`为115项、0失败；demo、large、empty各3组结果、0失败。
- 三场景手机入口students/dictation/homework/duty均实际点击并保存状态图；日程入口也由布局审计点击验证。
- demo为已有只读演示（5节课、无听写任务）；large为隔离合成105人/200词/8节课/跨班切换；empty为隔离合成2人/无课程/无听写。合成请求被本地浏览器截获，没有真实写入。
- QA明确检查场景最终计算`object-fit`、桌面专用横图宽高比、文具图像盒尺寸、正文宽度、日期完整、四区对齐、横向溢出、长内容、空态、连续滚动末项和底栏遮挡。
- V0资源/视觉配置失效与规范化路径去重负例继续由全量测试覆盖；检测器输出`[]`。
- 普通基线检查退出0。V1门槛退出1是预期结果：尚无绑定23fcdc5的独立监工PASS和用户批准。

尚未覆盖物理手机、屏幕阅读器、真实弱网/409或生产保存；这些不能由当前合成证据外推。

## 下一检查点

`V1-DASHBOARD-23FCDC5-20260914`：请独立监工先复核R8a/R8b及已关闭项是否保持；之后再由用户看图明确批准。监工PASS不等于用户批准。在两者完成前，V1不放行、V2保持blocked。
