# V1 R6–R8 返工候选

产品提交：`351880ee957aca334324f681be64348ce5dfc0ba`。执行者提交材料，不是监工结论。692665d报告顶部13:30更正优先：PASS已撤回，CHANGES_REQUESTED仍有效；旧R1–R5的已核实修复事实保留。用户未批准V1，V2 blocked。

## 逐区色彩角色对照

| 区域 | C1/C5参考角色 | 692665d差距 | 本次局部处理 |
|---|---|---|---|
| 待办标题与任务 | 暖金标题；蓝、暖金、绿色图标与柔和动作落点 | 图标、箭头几乎都为青色 | 金色标题；日程绿色、作业暖色、家校金色、听写蓝色；箭头仅使用小面积浅色衬底。青色实心主按钮仍唯一，不把每行变成强主操作 |
| 今日课程 | 蓝色外层、暖白内页、小面积学科标签 | 蓝底铺满课程正文 | 桌面浅蓝外层，暖白连续课程内页；语文金、数学蓝、英语珊瑚、体育/科学绿，按真实课程名称判定，未识别课程/个人事项为中性色。标签保留学科文字，不依赖颜色识别 |
| 听写 | 绿色标题、白色任务面，下方文具融入模块边缘 | 大片空绿、孤立小矩形 | 绿色标题与低强调入口、暖白内容面；有任务显示完整标题/日期/统计，无任务只显示真实空态。文具放在独立的下方空间，与正文不竞争宽度；没有增添假任务或家庭数据 |
| 值日及提醒 | 下层班务浅暖外层、白色事实面，强调减弱 | 金黄背景上的图标仍为青色 | 值日标题金色，岗位在连续暖白表面；提醒绿色标题、原事实条目与日期保留 |
| 手机常用操作 | 蓝/绿/金等少量图标落点，白色工作区 | 四个图标同青色 | 名单蓝、听写绿、作业金、值日深墨，名称与目标不变 |
| 页头 | “今天的班务”为大标题，日期辅助 | 日期最大最重 | 桌面32px任务主题，18px日期并排；手机保留“今天”与完整月日/星期语义分组，不改日期数据 |

珊瑚色在这里是学科/作业类别色，配有文字标签，不是错误状态；没有使用危险按钮或错误提示语义。未全局提高饱和度，未整页染黄，未修改共享主题token。

## 插画评估与范围

实际打开C1/C5原图及现有classroom-morning、home-scene-v3、dictation-stationery-v2资产。现有home-scene-v3更接近桌面文具近景；classroom-morning包含窗、黑板、讲台、前排课桌的纵深，适合主场景。首页本地组件只将桌面选为已有entry.scene资产，手机保留home.scene的mobile源，data角色仍是首页场景；共享definitions、Artwork、ThemeArtwork映射不改。没有新生成素材，也没有把概念页作为背景。

中层复用dictation.grading文具资产，在独立下方装饰区显示，以边缘遮罩融合。原112×88 picture/img约束仍保留在局部CSS；本次进一步让文字工作面不含横排位图，有/无任务正文都不受插画固有尺寸挤压。装饰区有明确高度和overflow边界，不覆盖入口。主体与裁切应由监工看图评价，填充率不代表视觉通过。

只改DashboardView.tsx/CSS及首页QA采集字段；read-model、API、数据库、保存/冲突、权限、共享壳层和家庭教育无改动。仍显示全部真实日程。当前没有任何已批准页面；严格QA回归19个教师页只是检查共享影响，不表示开始V2。

## 测试与证据说明

本次types、lint、npm test（含生产构建及认证集成）、strict、demo/large/empty全部在351880e重新执行，不复用旧版测试输出。原始命令输出、源版本、时间、退出码和SHA见`logs/V1-351880e/evidence-index.json`。三份场景JSON新增visualRoles，记录标题/日期字号、实际主场景资源、各模块背景/图标色、课程标签和文具区域矩形；用于验证实现，不是量化审美评分。

保留首次失败证据：认证测试服务首页持续404，随后.next/dev/types自动生成文件存在截断拼接，导致类型检查失败。将本轮可再生成缓存移至归档后恢复；中途归档放在.qa-shots触发ESLint扫描生成代码，随后移到已排除的.next/archived-r678-failed-cache，保留文件未删除。没有停止原3006预览服务，没有为此修改认证或业务源码。三份失败日志与最终结果分别保留，不隐藏失败尝试。

设计检测器返回DESIGN.md规格差异advisory（包括旧字号/圆角和本轮局部颜色），不是零告警：32px标题来自当前V1方案；学科色、暖白内页及边缘色是本次R6–R8要求的局部角色，遮罩中的黑色只表示透明度。上述属于有明确参考依据的候选例外，未批量改全站DESIGN或关闭规则，交监工核查。

d3a4401存在1条未使用eslint-disable警告；351880e删除无用注释并修正三个学科文字对比度，最终ESLint无错误或警告。对比度修正不改布局，重新采集证据并运行全部测试。

浏览器插件调用失败，原始错误为缺少`C:\Users\hx\.codex\plugins\cache\openai-bundled\browser\26.908.40834\scripts\browser-service.mjs`。按项目既有隔离Chromium/CDP QA执行，Windows/Node v24.19.0，1536×1024桌面与390×844手机固定CSS视口。运行URL、浏览器userAgent、具体日期见JSON。

数据场景沿用R1–R5隔离夹具：demo为只读32人演示，不替换真实示例；large为合成105人、200词长听写、8节课程、长姓名及带日期事实，跨班切换确认隔离；empty为合成2人、空听写/岗位/当天课程，保留其他教学日课表。模拟工作区写入不进入服务端，不当作真实保存成功。四个手机快捷入口等待实际目标内容加载后截图并返回。

连续图为原视口逐段滚动至底部的拼图，保留重叠，绝非缩小整页。C1/C5对照完整保留原参考尺寸；另有旧版/新版同视口桌面对照，辅助检查R6–R8。未重新验证物理手机、真实弱网、屏幕阅读器或生产服务器；未部署。

设计技能只帮助识别色彩/白面/标题关系和源码、渲染、判断三种证据，不用通用反卡片偏好删除参考的功能分区。本轮按规定两次桌面/手机批量检查，首轮发现图标选择器优先级与主图object-fit冲突后集中修复，最后发现标签对比度不足4.5后，仅修正文字色并补做可读性确认，停止外观微调。

下一步：监工先读取新检查点及本说明，独立复核R6–R8；监工审核后再交用户确认V1与产品提交。不得自填PASS/user-approved，不启动V2。
