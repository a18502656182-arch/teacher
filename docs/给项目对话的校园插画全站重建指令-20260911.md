# 给项目对话的校园插画全站重建指令

把下面整段作为项目任务的新消息发送。它已经包含本轮授权和关键验收边界；项目任务仍需读取仓库中的完整规范，不能只凭这段摘要实施。

---

请在当前“班主任工作台”项目中执行一次完整的校园插画风格构图重建。用户已经明确授权你修改项目代码；不要只给建议、计划或继续在现有页面上换颜色。先读取并遵守：

1. `AGENTS.md`
2. `项目导航.md`
3. `docs/PROJECT_STATUS.md`
4. `docs/CHANGELOG.md`
5. `docs/DECISIONS.md`
6. `docs/校园插画全站构图与视觉层次重建规范-20260911.md`

本轮的核心要求是：把 `项目资料包-听写与双主题-20260911/references/campus/` 中的 C1–C6 作为构图和视觉层次约束，重新建立桌面端和手机端页面。旧资料中“参考图只作方向、不要求接近”的表述不再适用于页面构图。允许偏离的只有三类：项目稳定字体、统一图标系统、参考图中的错字/假数据/生成瑕疵。真实数据、长名单和复杂功能需要适配进参考构图，不能成为继续保留当前后台模板结构的理由。

先核对当前代码和实际页面，确认 `.codegraph/` 是否存在；存在就先用 CodeGraph。使用 `anti-ui-slop`、`impeccable` 和浏览器工具完成设计审计与实现。不要把参考图当背景图，也不要注入正式假数据。

实现顺序如下：

1. 建立现状截图和“19 模块 → 页面族 → 参考图 → 特殊业务约束”映射。
2. 重建共享壳层：桌面全局顶栏、分组侧栏、主内容层次、手机页头、手机底部导航、真实保存状态。19 个模块保留，但不能继续全部等权铺在侧栏。
3. 先完成四个标杆页面：
   - 首页严格参考 C1 / C5 的场景插画、今日任务和班级概览层次；
   - 作业严格参考 C4，桌面改成左侧任务列表、右侧学生状态详情的主从布局，手机改为列表进入详情；
   - 学生严格参考 C3，桌面改成名单与学生档案主从布局，手机为名单进入单学生；
   - 听写严格参考 C2 / C6，桌面为任务材料、学生、批改三段式，手机一次处理一名学生并固定“确认保存并下一位”。
4. 标杆页达到参考图的构图和视觉重心后，把其余 15 个模块按规范中的页面族迁移。不要复制业务状态树。
5. 入口、账户和管理员页面共用新字体、色彩、控件与状态语言，但保持适合管理任务的信息密度。

必须保留现有认证、账户隔离、正式空白工作区、只读演示、搜索、筛选、100+ 人长名单、批量操作、分页、备份恢复、保存失败、409 冲突、听写服务器确认和隐私约束。桌面与手机可以使用不同外壳，但业务数据和写入逻辑必须共用。玻璃主题仍只保留架构接口，不显示切换入口。

这次要改 DOM 和组件结构，不要只在 `workbench-repair.css` 后继续追加覆盖规则。迁移页面时同步删除已经失效的旧选择器和旧布局依赖，并拆分过大的 `ClassroomApp.tsx`、`workbench-repair.css` 到真实页面族与共享组件边界。不要为了赶进度把原文件机械切段。

视觉系统以暖白画布、天空蓝主色、粉笔绿完成态、温暖黄提醒、低饱和珊瑚红风险和深海军蓝文字为主。首页使用大幅校园场景，业务页使用与模块语义匹配的情境插画。扩充现有插画注册表；不允许 19 个页面继续复用同一张 `study-desk`。统一使用一套正式图标，不用 emoji。控制卡片数量，相关内容合并为完整工作面，用色块、分隔线、留白和排版建立层级。

每个标杆页完成后，在 1536×1024 桌面基准和 390/360px 手机 CSS 视口截图，把实现图与对应 C1–C6 以相同显示尺寸并排检查。至少完成两轮视觉修正。自动布局脚本通过不能代替视觉对照；如果无顶栏、19 项等权侧栏、标题后直接表格、小插画点缀等旧构图仍在，就继续修改，不要解释为“正式数据太多”。

功能验证至少包括 TypeScript、ESLint、生产构建、业务测试与 `qa:strict`，并覆盖空工作区、30–50 人、100–105 人、长文案、保存失败、冲突、只读和移动键盘/安全区。视觉结果、功能结果和未覆盖项分别报告。

请直接实施到完整范围，不要停在计划阶段。完成后更新 `docs/PROJECT_STATUS.md`、`docs/CHANGELOG.md` 和必要的 `docs/DECISIONS.md`，提交并推送到 `github/main`。未经我另行明确要求，不要部署服务器，也不要把 Git 推送写成线上部署。最终给我：主要结构变化、C1–C6 对照截图路径、测试结果、仍有的限制、提交号和推送结果。

---
