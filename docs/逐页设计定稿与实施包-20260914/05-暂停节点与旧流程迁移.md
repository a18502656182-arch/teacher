# 暂停与现场迁移
## 各批内容
| 批次 | 页面 | D设计审核 | R实际审核 |
|---|---|---|---|
| V1 | 首页 | 若重新设计则先审其独立桌面/手机及状态 | 已有23fcdc5历史批准，保留；共享改动后回归 |
| V2 | 学生名单、作业、听写 | 三页逐页候选确认＋三页真实稿定稿后暂停 | 三页接入完成后暂停 |
| V3 | 考勤、成长、照护 | 三页全部定稿后暂停 | 三页接入后暂停 |
| V4 | 沟通、成绩、反思 | 同上 | 同上 |
| V5 | 课程、工具、座位 | 同上 | 同上 |
| V6 | 值日、班干部、规则 | 同上 | 同上 |
| V7 | 积分、周报、评语 | 同上 | 同上 |
V2–V7共12个固定D/R审核点，另有候选图确认和返工检查点；不是承诺只需用户回复12次。任何批次都不能用一个页面替整批获批。
暂停是交付事件，没有固定钟点。未回复不视为通过。只有该批全部页面的设计批准才允许正式接入；全部实际批准才可进入下批。

## 迁移任务（执行者开工后实施）
1. 现场主账本trackers/visual-baseline.json已经比旧包复杂，先阅读当前schema、scripts/visual-baseline-audit.mjs及其测试。不要覆盖回旧schema或重复V0已完成修复。
2. 在主账本保留历史页面、参考、产品commit、人工批准和监工报告；追加designApproval/resultApproval或等价分阶段字段，指定schema升级与兼容读法。
3. 本包inventory.json只负责静态范围；templates/record在页面设计目录填写。主账本记录每页recordPath/hash作为索引，避免两份批准状态分别维护。
4. 将强制supervision PASS改为可选报告，人工D/R批准仍必须。新检查器区分“待人工设计审核”“允许集成”“待人工实际审核”“允许下一批”，不能用一个qa:visual-gate通过代表全部。
5. 追加真实负例测试：仅首页批准不能解锁学生；生成图获批但可运行稿未批不能接入；本批只批2/3不能接入；过期设计hash不能放行；产品变更后旧结果批准无效；无监工报告不应成为新流程独立阻塞理由。
6. 若旧脚本暂时仍要求监工，报告为迁移未完成，修脚本；不伪造PASS以绕过。本次资料交付未运行或完成这些代码迁移。
7. 学生页现存未提交修改由执行者核对来源，保存其差异，作为待评估候选而非已批准实物；不要reset或直接批量提交。
