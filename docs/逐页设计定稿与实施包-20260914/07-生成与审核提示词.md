# ImageGen输入模板

## 2026-09-15 独立子界面补充规则（优先）

每个页签、卡片入口、独立记录库、阅读详情和不同分析对象先做源码入口盘点，任务或构图不同须分开出桌面/手机候选。出图覆盖及可运行证据检查按 docs/逐页设计定稿与实施包-20260914/09-独立子界面出图与覆盖检查.md 执行；不得让用户审核时替执行者查遗漏，主页面批准不扩展到其它界面。历史明确暂缓弹窗范围保留。

用途：<页面ID>完整UI候选，非风格示意图。
输入A为当前实际页面（功能与文案依据）；输入B为用户喜欢的参考（校园视觉依据）；输入C为已选同页桌面/手机稿（一致性依据）。
目标端：<desktop 1536×1024构图 / mobile 390 CSS宽的独立重排；图像工具实际像素尺寸另记>。
逐字文案：<从当前真实UI盘点填写>。
必有区域和动作：<功能ID映射>。关键状态：<本张stateId>。
保持：真实信息结构、功能可达、校园插画和明确主次；参考中的示例姓名日期不进入正式数据。
改变：<本页经过分析的构图/色彩/尺度问题>。
不要：省略字段、编造按钮或统计、随机新增导航、把桌面缩进手机、用装饰挤压主工作区。
桌面与手机分别出图，输入同一套已选素材和配色；记录版本并实际检查。生成文字不准的地方记入修订清单。
生成图不是可运行稿，不能凭图片通过机器功能验收。

# 可复制：方向确认
我认可<页面/候选版本>的视觉方向，请制作该页完整可运行设计稿，包括手机和关键状态。此确认不授权正式业务接入。

# 可复制：D设计批准
我已查看<批次>以下每页的桌面、手机和关键状态可运行设计：<页面列表＋版本/提交>。批准这些具体设计用于本批接入。未列出的页面不视为批准；本批接入完成后暂停让我看实际结果。

# 可复制：R实际批准
我认可<批次>的实际产品版本<commit>，对应设计版本<commit/list>。可以进入下一批的逐页设计；下一批设计定稿及接入完成仍分别暂停。

# 可复制：否决
<页面/版本>未通过。问题是<具体区域/外观/交互>。回到本页设计或接入修复，不把差距留作后期优化，重新展示可审查结果。

# 可复制：上下文恢复
按新包00恢复，读取当前页record及主账本D/R批准和git差异。区分候选确认、设计批准、实际批准，不继承监工审美判断。继续本批尚未完成且已获授权的阶段。
