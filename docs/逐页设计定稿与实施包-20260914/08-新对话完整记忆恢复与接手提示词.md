请接手“班主任工作台网站”。先恢复项目记忆、核对当前进度，再按最新计划继续。不要让我复述历史，也不要把聊天摘要或压缩包当作当前源码。

一、唯一项目与授权
唯一开发目录：E:\文档\Codex\小红书\班主任工作台网站。
所有源码核对和修改均在这里。压缩包只作交接快照，不解压覆盖工作区、不另建一个开发基线。先读目录内AGENTS.md；存在.codegraph时按其规则定位代码，没有则不用索引。
本轮授权：恢复记忆、盘点当前批次、制作独立候选图与隔离可运行设计稿，并完成必要的审核流程兼容迁移。正式业务接入须等该批设计获我批准；不得部署。

二、必须恢复的项目记忆（按顺序）
1. AGENTS.md、项目导航.md。
2. docs/开发导航与状态-20260911.md：读取架构与数据边界，里面的旧进度不能覆盖新记录。
3. docs/PROJECT_STATUS.md、docs/CHANGELOG.md：先读最新进度，再读与当前页相关的历史；被后续否决的旧“完成”不能继承。
4. docs/DECISIONS.md：产品、架构、保存、视觉与审核决策。
5. docs/DEPLOYMENT_LOG.md：只读了解历史，区分本地构建、打包、Git推送、上传、实际部署。用户曾反馈部署后看到预览；日志未同步时只能记线上版本未核验，不能断言从未部署。
6. PRODUCT.md、DESIGN.md、app/components/workbench/DESIGN.md：产品与视觉上下文；遇到旧表格偏好与最新逐页设计要求冲突，按最新用户要求处理。
7. 项目实施包-UI彻底重构与双主题-20260912/00-从这里开始.md，以及trackers/progress.json、tasks.csv；按当前模块读取feature-preservation.csv、overlay-inventory.csv、acceptance.csv。这是旧33项功能任务的历史与保留依据，不是重新执行列表。
8. VISUAL_BASELINE.md和trackers/visual-baseline.json：读当前页面状态、历史版本、批准来源、证据、问题、共享回归要求，区分当前字段与history。
9. docs/visual-baseline/POSTPONED-VISUAL-OPTIMIZATIONS.md：尚未实施的优化不能算已经解决。
10. docs/visual-baseline/V2-STUDENTS-C3-REALIGNMENT-PROPOSAL.md、V2-STUDENTS-399a5c4-review.md；旧资料包supervision/checkpoint.json以及主账本所引用的最新报告，作为返工经过与证据，不作为强制监工放行。
11. 完整读取docs/逐页设计定稿与实施包-20260914/00-从这里开始.md、01-可直接执行的总体计划.md、02-19页独立设计任务卡.md、03-状态覆盖与功能映射.md、04-逐页定稿合同与代码交付.md、05-暂停节点与旧流程迁移.md、07-生成与审核提示词.md、inventory.json及templates。
12. 实际打开本批参考图、最新实际截图与对比图。图片路径和hash以主账本为准，不能只读文字描述。

读选定文档被截断时继续读取缺失部分。大历史文件按标题检索与当前任务相关段落，不盲目塞入全部日志。文件不存在时先在项目定位；找不到就标出具体缺失与影响，不编造记忆。无需恢复与本任务无关的私密聊天原文。

三、历史事实提示（仅供定位，必须与最新现场核对）
- 旧33项功能/结构重构已经完成过；视觉质量后来被用户质疑，技术通过不等于审美通过，不能重做旧任务或将全站说成视觉已完成。
- V1首页产品23fcdc5曾获用户批准；仍有插画细腻度等未实施事项。保留其批准范围，不自动重做首页，也不把首页批准扩展到全站。
- V2学生名单399a5c4被用户否决，返工涉及整体构图、粉笔绿详情色面、近期状态色、身份与学习记录层级、概览带比例及手机按钮可读性；不是只修一个小问题。
- 编制启动词时最近计划提交为9c2c8f0；这不是产品视觉完成提交。现场有StudentsView.tsx、features/students/read-model.ts、useStudentsController.ts及旧监工报告的未提交修改。此列表可能变化，接手时以git status和diff为准，不能丢失、覆盖或混入自己的提交。
- V2作业和听写尚无本轮逐页设计批准，V3以后仍须分批。家庭教育暂不做。
- 用户已否定“首页母版延展＋监工审美放行”，改为每页独立设计后批准再接入。旧文档中的强制监工PASS规则被新包取代。

四、源码与运行核验
先运行只读git status、git log -10 --oneline、git remote -v，检查当前任务相关diff；记录HEAD、未提交文件及来源能否确认。不stash/reset/checkout清掉他人工作，不无差别git add。
核对真实入口ClassroomApp.tsx、共享shell/theme/primitives及当前页组件；学生页重点查看features/students及其controller/read-model。现有workspace保存/草稿/冲突和operations逻辑保留，不因视觉重做而重写业务。
本地预览是否运行、端口和数据模式需实际核验；历史localhost:3006/4180不能当成一定在运行。安全预览可自行启动，视觉测试用演示或合成隔离数据，不操作真实班级记录。

五、新流程与人工暂停
每页：功能/状态盘点 → ImageGen独立桌面和手机候选 → 我确认方向 → 完整可运行设计稿及关键状态 → 本批D设计审核 → 复用同一组件接入业务 → 本批R实际审核。
候选图、可运行设计和集成结果是三种不同版本。首页或任意一页不能替其它页定稿；至少38个基础设计槽位只是计划数，不是已完成数。
当前先从V2学生名单候选设计校准，继续本批作业/听写的独立设计准备。三页可运行设计都获批准前不接入本批正式业务；三页实际结果获批准后才进V3。
批次：V1首页；V2学生/作业/听写；V3考勤/成长/照护；V4沟通/成绩/反思；V5课程/工具/座位；V6值日/班干部/规则；V7积分/周报/评语。V2至V7每批都有D、R两次固定人工审核，另有候选确认和返工暂停。
监工可选，不能替我判断好看、不能代我批准。旧脚本若强制PASS，按新包05兼容迁移，保留历史，不伪造PASS。
ImageGen用于候选和素材，不能保证整页逐像素精确。最终冻结真实字体、素材、组件、状态和截图；接入直接复用该实现，不能二次猜着重写。

六、恢复后的首条实质性反馈与继续
先给出简洁“接手核对结果”：当前HEAD/未提交差异、已完成与明确未完成、上一有效批准及其范围、当前否决问题、新流程覆盖旧规则、实际下一步、缺失资料。每个重要结论引用项目文件或提交。
这是事实核对，不必等我再说“继续”才做已经授权的盘点和候选设计；有实际缺失阻断时明确说明。完成学生页候选展示后停下等我确认方向。
不要一开始改正式页面，也不要凭旧摘要声称已读全部历史。

七、后续长期记忆
每个阶段同步PROJECT_STATUS、CHANGELOG、必要DECISIONS、主账本及逐页record；记录设计/产品commit、截图路径/hash、fixture/viewport、实际测试日志、未解决问题、批准来源和下一允许动作。
上下文压缩后重新读最新记录和当前差异，不能从头重做。通过相称检查后只提交自己的文件并推送github；保留无关未提交改动，推送不等于部署。
