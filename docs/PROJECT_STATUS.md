# 当前项目状态

## 2026-09-14：V1四区对齐及R6/R8续修（待独立复核）

- 候选4b16fef的完整测试通过后，源检查补上901–1180px摘要独立区域及同排纵向起点保护；最终产品版本另行绑定全部日志和截图，不把前版日志冒记为新版。

- 开工HEAD为0e137d0；与351880e之间无产品差异，故351880e监工CHANGES_REQUESTED仍有效，已登记并退回needs-fix。
- 首页四区统一等分双列、22px列距、标题/内容起点和独立分区边界；手机统一单列与16px区间距。各区按内容自然高度，保留时间列、头像等功能差异。
- 任务图标本体增大，增加匹配既有回调的浅色“查看/处理/进入”动作区；不嵌套按钮。保留R7工作面、学科色和R1–R5修复。
- 教室图完整适配独立画框，摘要与原图分开；听写复用现有完整词卡资源，不改共享资产映射、业务、API、保存、只读和家庭模块。
- AGENTS、VISUAL_BASELINE及04证据规范写入V1–V7持续网格/对齐检查。QA补坐标、图标本体、完整主体适配、局部原尺寸截图。
- 当前仅提交返工候选；新版证据待生成，监工未审、用户未批准，V2 blocked。无跨任务通信工具，采用共享检查点；没有部署。

## 最新进度：V1按13:30更正返工R6–R8（2026-09-14）

最终返工候选产品351880e（主体d3a4401，标签文字对比度修正351880e）已重新完成类型、lint、生产构建、270项测试、认证集成、19页六档严格QA和三场景采集。44张持久图、原始日志及失败重试记录见docs/visual-baseline/logs/V1-351880e/evidence-index.json；逐区色彩角色见docs/visual-baseline/V1-351880e-review.md。dashboard现为ready-for-review，仅表示提交候选，监工pending；旧CHANGES_REQUESTED及撤回记录保留。先监工、后用户确认，V2 blocked，未部署。

监工撤回692665d的PASS，同名报告顶部更正为CHANGES_REQUESTED，不是产品变化引起的STALE。V1/dashboard已退回needs-fix，V2 blocked、用户未批准。保留R1–R5修复事实及证据。

本轮只改首页视图/局部CSS：任务和模块分类色、课程暖白工作面与真实学科标签、听写白色内容面及下方文具情境、“今天的班务”主标题和辅助日期。首页局部复用classroom-morning资产，手机仍用原mobile源；不改共享主题映射、壳层、业务保存、数据和家庭模块。新产品及三场景证据完成后先交监工，再请用户确认；未部署。

## 最新进度：V1按监工R1–R5返工（2026-09-14）

独立监工对产品25950b7给出CHANGES_REQUESTED，报告已登记；V1/dashboard退回needs-fix，用户未批准，V2保持blocked。上一条“V0完成”的标题仅保留历史，资源/视觉配置与规范化路径去重仍需本轮负例及独立复核，不继承其完成结论。

本轮局部修复听写picture/img尺寸、手机日期语义分组和桌面纵向密度；日程不再静默只显示前7项，所有真实日程保留。新候选产品为`692665d8ad928c3ae90a52188623e2367212d0d2`，dashboard为ready-for-review（仅表示提交复核，不是通过）。旧版needs-fix及CHANGES_REQUESTED保留在账本reviewHistory，新版监工pending、用户未批准、V2 blocked。

生产构建、270项Node测试、认证集成、类型检查、ESLint与19页六档严格QA通过；完整测试源提交与当前版本之间只有QA采集分支修改，原始日志保留源版本和等价差异证明。三场景截图及交互在692665d重新采集，42张持久图覆盖桌面/手机、连续滚动、空态、105人200词长任务及快捷入口。详情：`docs/visual-baseline/V1-692665d-review.md`；日志及哈希：`docs/visual-baseline/logs/V1-692665d/evidence-index.json`。独立监工及用户必须看图复核；共享检查点已更新并停下，不继续V2，没有部署。

## 最新进度：V0完成、V1首页候选等待双重审核（2026-09-14）

V0已将视觉账本升级为schema 2，并补齐证据元数据与哈希、路径安全和去重、产品提交绑定、提交后产品/共享样式变化、独立监工结论及用户批准双向绑定。新增8项检查器正反例，避免把同角色截图、游离证据、旧提交或自填批准误判为放行。

V1首页候选实现已完成：桌面恢复黄色待办与大幅校园场景组成的首屏主舞台，中段使用蓝色课程与绿色听写形成彩色功能分区，下段改为连续信息面；手机改为日期校园横幅、纵向待办、课程/听写、常用操作和提醒的内容重排。路由级`.campus-art`不再固定220×130，页面专用构图可以实际铺满。既有读模型、跳转、保存及只读边界未改，家庭教育仍排除。

六档严格布局、TypeScript、ESLint、生产构建、268/268 Node测试及认证集成已经通过；这些只能证明结构与稳定性，不能代替视觉批准。候选已绑定产品提交`25950b7d9649d0e01d64749544b8dcfaa6ce9540`，1536×1024桌面、390×844手机实际图及保留完整C1/C5参考的对比图已生成并登记SHA-256。独立监工仍为pending、用户批准仍为false，因此V1门槛命令按设计拒绝放行；V2保持blocked，未部署。

## 最新进度：执行与独立监工完整交接资料已编制（2026-09-14）

资料位于 `docs/重构执行与监工资料包-20260914/`，包含具体重构方案、19页任务卡、监工通信和权限协议、证据规范、启动/审核/恢复提示词及初始检查点。V1仍未实施或批准。检查器的提交后回归检测、文件匹配、证据绑定等缺口登记为V0待做，不假报已解决。监工与执行对话尚未连接，定时监督未启用。本次只交付资料，无页面或部署变化。

## 最新进度：视觉基线与七个人工审核门槛已落地（2026-09-14）

33项功能与结构重构仍保持完成，但2026-09-13的C1-C6“视觉通过”结论已撤销，不能继续作为参考图还原证据。当前新增`VISUAL_BASELINE.md`、机器账本、Git内C1-C6视觉副本、固定视口采集器和门槛审计；V1首页纠偏为当前活动门槛，状态`needs-fix`。

V1单独审核首页；V2至V7每三个教师页面暂停一次，全部必须等待用户明确确认后继续。家庭教育不纳入本轮。当前只落地防跑偏机制，没有修改页面视觉、业务、API或数据库，也没有生成新部署包或操作生产环境。

## 最新进度：33项重构计划完成并形成受控交付包（2026-09-13）

TASK-33已完成，33/33项实施任务闭环，M5完成。教师工作台业务源码以`45f72d7`为最终打包基线；本任务只补齐记忆、追踪和交付记录，因此随后文档提交不改变包内运行源码。

本地受控包`classroom-deploy-teacher-workbench-ui-complete-20260913.tar.gz`为9,450,169字节，SHA-256为`1D3DC54F61098CE68EC01D48FD6767132A71AAC23463A19E8AF2206F67E3167B`，共446个条目。路径安全、禁止项、解压及五个核心入口检查通过；包不含项目文档、测试、脚本、报告、数据库、运行数据、环境密钥、`node_modules`或`.next`。

最终质量证据仍是生产构建、260/260自动化、认证集成、TypeScript、ESLint及115项严格QA通过。当前未上传、未部署、未操作生产数据库；家庭教育保留历史能力但按用户要求不属于TASK-30至33修改和当前验收。物理手机、键盘/屏幕阅读器、真实弱网、线上Web Vitals、正式AI/OCR及逐页真实409仍是明确未覆盖边界。

## 最新进度：教师工作台最终业务与视觉验收（2026-09-13）

TASK-32已完成。生产构建、260/260自动化、认证集成、TypeScript和ESLint通过；静态架构项加19个教师导航页×6档视口共115项严格QA失败0。acceptance当前201项pass、6项not-applicable、0项not-run；家庭9项仍保留原历史提交，按用户要求未改动也未纳入本轮验收。

技术审计16/20（Good）：P0/P1均为0；P2为大型共享CSS/主组件和手机保活DOM成本，P3为历史设计token漂移。当前完成32/33项，按数量97.0%，M4完成。未操作生产；物理手机、屏幕阅读器、真实弱网和线上Web Vitals仍未覆盖。下一项TASK-33只更新记忆、提交并生成受控部署包，不部署。

## 最新进度：主题架构与性能检查（2026-09-13）

TASK-31已完成。公开校园主题与内部预留glass均使用实色材质，应用CSS无blur，公开主题仍固定校园且主题变化不使用React key重挂载。低频教师页面改为按需加载；主客户端JS由598589降至332249字节，CSS由401636降至309349字节，构建大chunk警告消失；听写批改插画由1886556字节PNG压缩为75016字节同尺寸WebP。

105人、200词和接近容量上限的纯计算/本机SQLite基线通过；390×844学生页按需加载后无横向溢出或错误日志。当前完成31/33项，按数量93.9%。手机访问四个保活页时DOM峰值约2570节点；真实低端手机、3G和线上Web Vitals仍未测。按用户要求未修改或验收家庭教育。下一项TASK-32执行最终教师业务与视觉验收。

## 最新进度：旧UI依赖全量退出（2026-09-13）

TASK-30已完成。正式入口不再加载或生成旧全局CSS，`workbench-repair.css`、八份campus兼容CSS、`legacy-scoped.css`、生成器、隔离探针、legacy根边界与`homework-bootstrap-shell`均已退出。仍有消费者的页面规则经源码筛选后限定在工作台内容区CSS Module中，不再污染共享壳层。

19个教师导航页面、六档视口共114个运行态组合通过；静态审计、ESLint、生产构建和50/50聚焦回归通过。当前完成30/33项，按数量90.9%，M3完成。按用户要求没有修改或验收家庭教育；物理手机、真实弱网及生产环境未验收。下一项TASK-31处理主题架构和构建已揭示的超500kB客户端分块。

## 最新进度：周报与期末评语保存闭环（2026-09-13）

TASK-28已完成。桌面与手机周报、评语写入均等待workspace服务器确认；失败保留本机内容、当前周次/学生/依据和编辑层，不再把本机应用误报为服务器成功。周报桌面切周、换版本、打开归档及手机关闭补齐脏草稿保护；两页保存中防重复，只读写入口原生禁用。

1280×720桌面及390×844手机实查，两页六档严格布局、42/42聚焦回归、全量ESLint、生产构建、264/264自动化及认证集成通过。当前完成29/33项，按数量87.9%，剩余工作量粗估约96%。M2完成；共享`weekreport/comment5/mobile`旧样式集中移除归TASK-30。未操作生产环境，物理手机软键盘、真实弱网、正式AI和双标签409未在本项浏览器验收。

## 最新进度：座位、值日与班干部保存闭环（2026-09-13）

TASK-27已完成。座位配置与调整等待workspace确认，失败保留安排、选人上下文和撤销快照；值日轮换、检查、指定、岗位和台账备注统一确认，岗位编辑补齐脏关闭保护；班干部新增、编辑、删除失败保留本机修改和编辑表单。三页保存中均防重复。

1280×720桌面及390×844手机实查，三页各六档严格布局、74/74专项、全量ESLint、生产构建、263/263自动化及认证集成通过。当前完成28/33项，按数量84.8%，剩余工作量粗估约94%。死旧样式集中移除归TASK-30；未操作生产环境。M2继续TASK-28。

## 最新进度：课堂工具保存闭环（2026-09-13）

TASK-26已完成。随机点名保持不落库的瞬时课堂工具，继续排除当天请假且本轮不重复；临时分组等待workspace服务器确认，失败保留本机结果与历史选择，保存中防重复。演示/只读仅禁用持久化分组，点名、重置和复制仍可用。

1280×720桌面及390×844手机实查，六档严格布局、49/49专项、全量ESLint、生产构建、262/262自动化及认证集成通过。当前完成27/33项，按数量81.8%，剩余工作量粗估约92%。共享旧工具样式集中移除归TASK-30；未操作生产环境。M2继续TASK-27。

## 最新进度：课程与个人日程保存闭环（2026-09-13）

TASK-25已完成。桌面及手机课程、活动、每日重点、个人事项和工作留痕统一等待workspace服务器确认；失败保留编辑器、本机修改和当前上下文，保存中防重复。演示/只读模式显式禁用写入口；跨月、1—7教学日、周外数据保留、跨班隔离和完成留痕幂等保持。

1280×720桌面及390×844手机实查，六档严格布局、50/50课程专项通过；全量ESLint、生产构建、自动化与认证集成通过。当前完成26/33项，按数量78.8%，剩余工作量粗估约90%。共享`courseplan/agenda`及手机旧样式集中移除归TASK-30；未操作生产环境。M2继续TASK-26。

## 最新进度：考试反思保存与脏草稿闭环（2026-09-13）

TASK-24已完成。桌面与手机反思保存等待workspace服务器确认，失败保留当前考试、学生和五项输入；桌面切换学生/考试、手机关闭编辑层前检查未保存草稿。完成仍只生成可追溯的内部家校沟通留痕，不宣称外部发送；演示/只读输入和归档按钮原生禁用。

1280×720桌面及390×844手机实查，六档严格布局通过；ESLint、生产构建、48/48专项、262/262全量自动化及认证集成通过。旧审计“只读仍可强行输入”前提已改为核对原生disabled，不是放宽断言。当前完成25/33项，按数量75.8%，剩余工作量粗估约88%。共享`reflection5`/手机旧样式集中移除归TASK-30；未操作生产环境。M2继续TASK-25。

## 最新进度：成绩分析保存闭环（2026-09-13）

TASK-23已完成。桌面与手机成绩录入、考试管理、满分区间、重点跟进和试卷分析接入workspace保存结果；新增/编辑/删除考试、批量录分和分析项等关键提交仅在服务器确认后关闭或清空选择，失败保留本机修改与当前上下文。演示/只读模式保留查询、趋势和已有分析查看，但禁用成绩及试卷写入口；空白、真实零分、不同满分和AI候选需教师确认的语义保持。

1280×720和390×844实查成绩录入、趋势、试卷分析及手机学生详情，成绩录入/试卷分析各六档严格布局通过；ESLint、生产构建、51/51专项、262/262全量自动化及认证集成通过。当前完成24/33项，按数量72.7%，剩余工作量粗估约86%。共享`score5/MobileScores`旧样式集中移除归TASK-30；未上传真实试卷或操作生产环境。M2继续TASK-24。

## 最新进度：积分与规则保存闭环（2026-09-13）

TASK-22已完成。桌面和手机保留现有积分/规则校园工作面，积分提交改为服务器确认后才清学生选择和说明，失败保留本次上下文；撤销同样等待确认。规则新增、编辑、启停、复制和删除接入保存结果，失败不关闭编辑内容，历史积分继续使用事件快照且不被规则改动追改。

1280×720和390×844实查两页，六档严格布局通过；ESLint、生产构建、45/45专项、261/261全量自动化及认证集成通过。当前完成23/33项，按数量69.7%，剩余工作量粗估约84%。共享`pointdesk/ruledesk`样式集中移除归TASK-30；浏览器仅使用只读演示数据。M2继续TASK-23，未操作生产环境。

## 最新进度：成长、健康与家校沟通保存闭环（2026-09-13）

TASK-21已完成。没有重做已成形的三页校园布局；本轮将桌面/手机成长人工事实、健康照护、沟通记录、通知草稿与回执接到workspace保存回执。编辑层仅在服务器确认后关闭，失败保留字段和对象选择；只读模式直接禁用写入口。通知复制仍可用但不冒充系统外发，照护继续只保留日常安排所需信息。

1280×720和390×844实查三页，六档严格布局通过；ESLint、53/53专项、生产构建、260/260自动化和认证集成通过。当前完成22/33项，按数量66.7%，剩余工作量粗估约82%。共享旧样式集中移除归TASK-30；现有产品没有人工成长事实编辑/删除，记录100+虚拟化尚未验证。M2继续TASK-22，未操作生产环境。

## 最新进度：考勤连续名单完整迁移（2026-09-13）

TASK-20已完成。桌面与手机统一使用Attendance控制器和局部CSS Module；日期/月历、搜索筛选、四态、逐生备注、分页多选、批量状态/备注及一键正常都留在连续名单。写操作接入workspace保存回执，仅在服务器确认后清选择与草稿，失败保留本机内容；105人重复批次按每名学生最后一次修改去重，演示/只读模式禁用全部写入口。

浏览器实查1280×720与390×844，严格布局另覆盖六档视口；ESLint、42/42专项、生产构建、259/259自动化及认证集成通过。当前完成21/33项，按数量63.6%，剩余工作量粗估约79%；M2继续TASK-21。没有独立请假详情编辑入口是当前产品边界，不在本次虚构。未操作生产环境。

## 最新进度：家庭学习完整迁移复核（2026-09-13）

TASK-19已完成。既有孩子切换、今日任务、档案、个人错词和共享人工批改不重做；本轮将家庭30天统计统一到听写公共窗口，并修复孩子档案保存中离开守卫被卸下的问题。家庭页头移除装饰渐变，TASK-18删除旧听写全局CSS后仍保持局部样式完整。

1280×720与390×844检查零孩子只读场景、五个子页和班级/家庭隔离；ESLint、19/19专项、六档`qa:strict`、生产构建、258/258自动化及认证集成通过。当前完成20/33项，按数量60.6%，剩余工作量粗估约77%；M2继续TASK-20。未操作生产环境。

## 最新进度：听写词库、错词与历史完整迁移（2026-09-13）

TASK-18已完成。任务、词库、错词和记录统计退出全局`dictation.css`，改由四张局部CSS Module承载；任务创建保留文本/词库/课次/历史错词/参与者与快照，词库新增脏离开保护和失败草稿保留。近30天窗口抽为可测试读取函数，明确包含首尾自然日并排除未来和第31天。

浏览器在1280×720与390×844检查四个子页、空态、单页头与统计重排；ESLint、47项专项、六档`qa:strict`、生产构建、257/257自动化及认证集成通过。当前完成19/33项，按数量57.6%，剩余工作量粗估约75%；M2继续TASK-19。未操作生产环境。

## 最新进度：首页、学生、作业核心业务联调完成（2026-09-13）

TASK-17已完成。TASK-12至14的新代表页不重做，本轮逐项确认首页跳转、学生/作业桌面与手机浮层、只读、workspace本机草稿、网络失败重试、409和跨班隔离。浏览器真实点击验证首页到日程/作业/家校/听写/学生/家庭入口，390×844学生和作业Drawer可达。

联调修复作业历史数据同ID的跨班风险：编辑、删除、状态和跟进名单现在只操作当前班唯一目标，同班重复ID拒绝歧义写入。专项58/58、ESLint、六档`qa:strict`、生产构建、256/256自动化及认证集成通过。当前完成18/33项，按数量54.5%，剩余工作量粗估约72%；M2继续TASK-18。未操作生产环境。

## 最新进度：C1–C6 标杆视觉门槛与 M1 完成（2026-09-13）

TASK-16已完成。首页、学生、作业和听写批改按C1–C6做六组同面板并排对照；浏览器复核使用1280×720与390×844，`qa:strict`另覆盖1536×1024等六档视口，不把较小截图冒充1536。首页行动/场景校准为约52%/48%，学生名单改以近期真实状态取代电话维护和长备注常驻列；短听写任务补上校园语义插画。

生产构建、ESLint、253/253自动化、认证集成和六档`qa:strict`通过。当前完成17/33项，按数量51.5%，按剩余工作量粗估约69%；M1完成，M2从TASK-17开始。按用户最新指令不中途暂停，连续推进至TASK-28；未操作生产环境或生成部署包。

## 最新进度：听写 C2/C6 中央批改工作面完成（2026-09-13）

TASK-15已完成。听写批改改为中央逐词核对与右侧学生队列，材料收为单一可展开横幅；手机使用共享学生Drawer和底部固定保存区。旧批改三栏、重复banner及其全局专属规则退出；三态、错词、备注、显式核对、本机草稿、失败不跳、成功下一位和错词复习保持不变。

生产构建、ESLint、252/252自动化、认证集成及六档`qa:strict`通过；1536×1024与390×844隔离合成预览覆盖200词末项、失败保留、成功推进、手机无横向溢出与学生Drawer。当前完成16/33项，按数量48.5%，按剩余工作量粗估约67%；M1下一项是TASK-16标杆视觉门槛。按用户最新指令不中途暂停，连续推进至TASK-28；未操作生产环境或生成部署包。

## 最新进度：作业追踪 C4 连续批改工作面完成（2026-09-13）

TASK-14已完成。旧`Homework`/`MobileHomework`展示退出，桌面使用330px任务主列表与右侧逐生处理工作面，手机从任务列表进入学生详情Drawer；新增编辑删除、历史筛选分页、双搜索、已交/未交/待订正/已复查四态、完整备注和待跟进名单由`features/homework`统一控制。批量操作仅在选择后出现，演示/只读保留搜索、筛选、查看和复制但禁止写入。

生产构建、ESLint、251/251自动化、认证集成及六档`qa:strict`通过；1536×1024与390×844真实浏览器检查通过，独立复审`PASS`且无剩余material finding。当前完成15/33项，按数量45.5%，按剩余工作量粗估约64%；M1下一项是TASK-15听写构图基准。按用户临时规则每个任务完成即暂停，收到继续指令后再开始；未操作生产环境或生成部署包。

## 最新进度：学生名单 C3 主从构图完成（2026-09-13）

TASK-13已完成。旧`Students`/`MobileStudents`退出，桌面使用连续名单主表与350px右侧单学生事实区，手机使用名单到单学生Drawer；新增、单人编辑、批量、导入预检/追加/替换、CSV、关联删除及完整档案由`features/students`统一控制。名单主表不显示完整家长电话，只显示维护状态；完整电话仅进入单学生编辑或受控档案。演示/只读状态禁用写入口，档案脏关闭增加放弃确认。

生产构建、ESLint、246/246自动化、认证集成和`qa:strict`通过；1536×1024与390×844真实浏览器检查通过，独立终审0 P0/P1。当前完成14/33项，按数量42.4%，按剩余工作量粗估约61%；M1下一项是TASK-14作业构图基准。按用户临时规则每个任务完成即暂停，收到继续指令后再开始；未操作生产环境或生成部署包。

## 最新进度：班主任首页构图与事实读取完成（2026-09-13）

TASK-12已完成。桌面与手机共用新的首页工作面：当天待办优先，课程、教师事项、作业、家校沟通、听写、值日和学生事实按当前班/当前名册读取；学生提醒必须带日期与来源，并能进入指定学生成长档案。家庭学习入口保留但降为次要，未扩大本阶段范围；旧`Dashboard.tsx`与`dashboard.css`已经退出。

生产构建、242/242自动化、认证集成、ESLint和115项六档视口`qa:strict`通过；1536×900、390×844、360×800实看无横向溢出，四个主要待办和学生成长上下文导航通过，独立复核为`ship`且0 P0/P1。当前完成13/33项，按数量39.4%，按剩余工作量粗估约58%；M1仍在进行中，下一项是TASK-13学生构图基准。按用户要求暂停在真实本地首页；未操作生产环境或生成部署包。

## 最新进度：共享工作台壳层完成并进入用户浏览暂停点（2026-09-13）

TASK-11已完成。新`WorkbenchShell`集中桌面顶栏、四组19模块连续导航、班级/家庭场景、真实保存与冲突状态；手机提供明确返回、四个高频入口与只含其余15模块的“更多”Drawer。模块轨迹支持连续返回，访问过的手机页和各自滚动位置得到保留；听写内部重复场景入口和旧`WorkspaceChrome`外壳已经退出。

生产构建、237/237自动化、认证集成和115项六档视口`qa:strict`通过；1280×720与390×844实看无横向溢出，独立复核为`ship-with-notes`且无blocker/major。当前完成12/33项，按数量36.4%，按剩余工作量粗估约55%。按用户要求暂停在真实本地页面，确认后才执行TASK-12完整首页；未操作生产环境或生成部署包。

## 最新进度：首批校园插画母样与四角色变体完成（2026-09-13）

TASK-10已完成。基于C1–C4的媒介、配色与构图方向独立生成校园晨光母样，以及首页、听写、学生档案、作业四个语义角色的桌面与手机候选资源；业务文字全部保留为HTML，图中不含人物、动物、UI或可读假文字。全幅暖纸横幅采用不透明WebP，透明边缘只适用于后续独立摆件。

1280×720实际加载1200×800桌面图，390×844实际加载900×560手机图；四个容器均无横向溢出或控制台问题。TypeScript、相关ESLint、生产构建、234/234自动化、认证集成和115项`qa:strict`通过。当前完成11/33项，按数量33.3%，按剩余工作量粗估约52%。下一步TASK-11建立共享壳层；未操作生产环境或生成部署包。

## 最新进度：新旧样式正式隔离完成（2026-09-13）

TASK-09已完成。正式根布局不再直接加载十份旧工作台CSS，而是由AST生成器在开发、构建和QA前将旧规则限定到 `data-ui-generation="legacy"`，并在 `data-ui-generation="next"` 子树停止；8处文档级旧规则已人工收敛。未迁移旧页面继续保留原声明，新ThemeBoundary和原生浮层不再被宽泛旧选择器命中。

专项6/6、TypeScript、ESLint、生产构建、233/233自动化测试、认证集成、静态审计及115项六档视口`qa:strict`通过。浏览器在1280×800和390×844实测加载完整旧CSS：旧输入变化、新输入计算样式不变、无横向溢出或控制台问题。当前完成10/33项，按数量30.3%，按剩余工作量粗估约49%。下一步TASK-10制作校园插画首批母样；未操作生产环境或生成部署包。

## 最新进度：共享浮层与105人搜索选人完成（2026-09-13）

TASK-08已完成。新界面基础层新增统一原生顶层、桌面侧抽屉/手机底部抽屉、草稿关闭确认和线性学生选择器；旧 `StudentLookupDialog` 保留调用接口但已转接共享实现。105人名单支持搜索、分页、跨页多选，异步失败保留当前选择和上下文；嵌套层关闭后只恢复正确父层或触发器焦点。

TypeScript、全量ESLint、生产构建、231/231自动化测试、认证集成及全量`qa:strict`通过。隔离预览在1280×800和390×844实际验证105人、脏关闭、失败重试和嵌套焦点，无控制台错误或横向溢出；仅有既有大chunk警告。当前完成9/33项，按数量27.3%，按剩余工作量粗估约47%。下一步立即执行TASK-09新旧样式隔离；未操作生产环境或生成部署包。

## 最新进度：基础控件闭环并进入用户浏览暂停点（2026-09-13）

TASK-07已完成。共享基础层补齐带保留文字进度状态的Button、批量上下文SelectionBar、可键盘操作的Menu/MenuItem，以及不伪造业务结果的LoadingState/EmptyState；Field和StatusSegment继续使用真实标签、错误和按下状态。所有新增样式只消费主题变量和CSS Module，未接入旧全局控件CSS，也未提前删除仍有消费者的旧规则。

独立浏览器检查器验证桌面及390×844内嵌CSS手机视口：长按钮完整换行、文档无横向溢出、菜单方向键循环和Escape焦点恢复、空态/加载态可读、内部主题探针切换后输入保持且玻璃不回退校园插画。TypeScript、全量ESLint、生产构建、229/229自动化测试、认证集成和115项六档视口`qa:strict`通过，仍有既有500KB以上chunk警告。

当前完成8/33个任务（TASK-01至07及提前完成的TASK-29），按数量24.2%；考虑已迁移的入口、账户、管理员、家庭、座位、值日和班干部等工作，按工作量估计约45%。这不是精确工时统计。按用户要求暂不启动TASK-08，先在本地预览检查基础质感；TASK-11共享壳层和TASK-12完整首页将作为下一批量迁移前的两个视觉门槛。本轮未操作生产环境或生成部署包。

## 最新进度：统一双主题契约与语义资源manifest（2026-09-13）

TASK-06已完成。校园与玻璃主题都完整声明语义颜色、工作表面、字体、间距、圆角、阴影、动态、资源和能力；共享转换器生成 `--wb-*` 变量。玻璃主题拥有明确的实色后备面和支持blur时的增强面，但状态仍为planned，资源表为空，公开解析固定campus，产品中没有主题切换入口。

原 `components/campus/theme.ts` 不再维护第二份插画路径，只保留旧slot到17个语义role的迁移适配；所有调用最终读取同一manifest。校园资源记录真实固有尺寸、裁切/焦点、安全区和无回退策略，玻璃缺图直接不渲染，不借用校园图。独立浏览器探针实际确认切换材质后输入不丢、颜色和blur变量改变、玻璃插画节点为0，控制台无错误。

TypeScript、全量ESLint、生产构建、224/224自动化测试、认证集成及115项六档视口`qa:strict`通过，仍有既有500KB以上chunk警告。TASK-06只关闭主题架构门槛；全工作台ThemeBoundary在TASK-11，玻璃素材及视觉仍未实现。下一步TASK-07基础控件。

## 最新进度：工作区保存、草稿与冲突控制器抽离（2026-09-13）

TASK-05已完成。工作区加载、同revision本机草稿恢复、dirty与保存状态、900ms自动保存、排队保存、乐观revision、409冲突锁定、载入最新版本、备份读取和离开守卫集中到 `workspace/useWorkspaceController.ts`；`ClassroomApp.tsx` 删除相应状态、ref、effect和闭包。一般本地编辑与听写服务器确认提交继续保持两种语义，听写失败不计入正式结果，待提交听写会阻止退出并可进入完整备份。公开演示的一般写入口同时修正为真正只读。

隔离浏览器用临时SQLite和两班各50人夹具实际完成E7到期只读、E8断网保留与恢复重试、E9双标签真实409，并验证演示考勤点击不改变数据；正常检查控制台错误和警告为0，测试服务及临时数据已清理。TypeScript、全量ESLint、生产构建、220/220自动化测试和认证集成通过，仍有既有500KB以上chunk警告。

本项没有DOM/CSS变动，不计作视觉迁移。考勤等旧页面的模块内“已保存”提示仍可能早于服务器确认，须在相应页面迁移中逐项调整；物理手机、真实弱网、HTTPS和线上环境未验收。M1继续进行，下一步完成TASK-06主题契约和TASK-07基础控件。

## 最新进度：纯业务读取模型从大组件抽离（2026-09-13）

TASK-04已完成。工作区规范化与按班配置投影移至 `workspace/normalize.ts`，日程日期和课程派生、成绩逐生读取模型、成长时间范围分别移至独立feature模块；学生关系清理和各模块归属继续复用既有操作层，没有另建重复算法。复核Git内容后确认 `ClassroomApp.tsx` 由5740行降至5327行；此前记录的5574/5191为转录错误。它仍是后续需要继续拆分的大组件。

核验发现正式空工作区的显式空作业会被补成“今日作业”，缺失周计划会被补成五条示例数据；当前只有完全缺失作业字段的旧备份保留兼容任务，显式空作业和空周计划保持空。规范化同时保留零分、零积分、同名学生显式班级和班级课程/座位/值日映射。

新增10项聚焦回归；TypeScript、全量ESLint、生产构建、217/217自动化测试和认证集成通过，仍有既有500KB以上chunk警告。本轮没有DOM/CSS变动，因此没有把构建通过记成视觉验收，也没有操作生产环境或生成部署包。活动任务进入TASK-05通用保存与草稿控制器抽取。

## 最新进度：E0–E11隔离夹具与M0完成（2026-09-13）

新增可重复的合成工作区夹具，覆盖正式空白、两班各50人、单班105人、两个重名家庭孩子、0/3/30条作业、8/12/60/200词听写、长字段、只读、保存失败、409、旧版/当前备份和4.2–4.8MiB容量边界。夹具不读取真实数据库、正式备份或环境变量，所有身份和内容均为明确合成数据。

10项夹具测试通过当前听写归属校验、版本1/2备份预检和系统临时目录SQLite写入/回读；临时路径明确不指向 `data/classroom.db`。完整 `npm test` 的生产构建、207/207自动化测试和认证集成通过，仍有既有500KB chunk警告。E7至E9提供测试数据与响应契约，后续页面验收仍要实际触发认证只读、路由失败和双客户端409，不能因夹具存在直接标pass。

源码基线、F/O全量账本和隔离夹具三个条件均满足，M0关闭；活动任务进入TASK-04纯业务计算核对。没有启动生产服务、修改正式数据库或生成部署包。

## 最新进度：首页账本回补与实施顺序纠正（2026-09-13）

F-01/O-01已按当前源码完成盘点，TASK-02的23个功能范围和23个浮层范围现已全部展开。桌面与手机首页共用 `Dashboard`，已有作业/考勤/听写导航、教师事项、听写摘要、值日、学生近况、家庭及账户入口；首页没有自有写入或对话框。

盘点同时确认首页尚未完成正式迁移：今日安排没有课程表，待处理作业未按当前名册二次过滤，沟通待跟进未按当前班过滤，学生近况缺少事实日期和所点学生上下文；页面仍使用全局 `dashboard.css`，A-01九类场景保持未运行。TASK-19至TASK-28在实施台账仍未满足完成门槛，因此不能跳到TASK-30直接删除旧CSS。

下一步依赖是TASK-03建立E0至E11隔离夹具。M0在夹具可重复生成并验证前仍为进行中；本次为账本与状态修正，没有操作生产环境或改变业务数据。

## 最新进度：管理员工作台独立迁移（2026-09-13）

管理员页已从单文件内联页面迁到 `app/admin/features/admin`。管理员登录、用户与兑换码检索、分页、手机号、设备、续期、禁用恢复、数据导出/永久删除和审计均保留原服务端接口与权限边界；危险操作改为同一原生对话框内联确认，写入失败不关闭编辑器或清空已加载列表。旧全局 `admin.css` 已删除，页面使用局部CSS Module、统一SVG图标和 `admin` 主题插画槽；玻璃主题仍为空资源映射且没有公开切换入口。

隔离浏览器已验证1280×720桌面、390×844手机、错误密码、空态、30条兑换码分页、复制反馈、账户详情、手机号校验、设备解绑取消、续期/禁用恢复确认、导出、永久删除确认输入、审计和网络失败保留。手机表格转为字段列表，对话框主体可滚动且页脚可达，正常路径控制台错误和警告均为0。

生产构建、197/197自动化测试、扩展认证集成、全量ESLint及115项六宽度 `qa:strict` 通过。认证集成实际覆盖设备解绑、重复手机号409、改号、续期、导出、永久删除、兑换码禁用和审计；所有写入均使用临时数据库和合成用户。真实物理手机、HTTPS、外部网络和线上环境尚未验证；本轮没有操作服务器、正式数据库或生成部署包。TASK-29已完成，下一步回补首页F-01/O-01账本后进入TASK-30旧UI依赖清理。

## 最新进度：账户、班级与备份统一迁移（2026-09-13）

账户信息、班级管理、完整备份和当前设备退出已迁到 `features/account`。桌面顶部与手机首页/“更多”打开同一个 `AccountCenter`；原桌面 `AccountDialog`、侧栏 `ClassSwitcher`、手机账户简报和独立班级层已经退出。账户读取保留失败重试，只显示手机号、期限和当前设备，不提供未完成的续费、退款或订单入口。

班级切换、新建、编辑和删除由纯操作层统一处理。两班各50人的切换与编辑不串班，新建班级不复制名册或听写；删除班级清理目标班关联和课程/座位/值日映射，保留其他班及家庭历史。编辑立即进入本机草稿并自动同步；断网时字段仍保留。退出当前设备前会先保存脏数据，失败则取消退出；确认退出只撤销本设备并返回公开入口。

隔离浏览器已验证1280×800桌面、390×844手机、空班、长班名、添加/切换/取消删除/确认删除、无效备份、有效备份预检取消与确认、正式到期只读、演示只读、断网保存失败和当前设备退出，正常流程控制台无错误。生产构建、196/196自动化测试、认证集成、TypeScript、全量ESLint及115项六宽度 `qa:strict` 通过。真实物理手机、软键盘、弱网、HTTPS和线上环境尚未验证；本轮没有操作服务器、正式数据库或生成部署包。下一步迁移管理员页面F-23/O-23。

## 最新进度：公开入口、验证浮层与隐私页独立迁移（2026-09-13）

公开入口和隐私页已迁到 `app/features/entry`，认证接口和账户绑定逻辑保持原契约。首页只有正式工作台与只读演示两个主要去向，校园构图使用真实用途说明和 `entry` 语义插画槽；隐私页保留原有数据范围、AI授权、导出删除及30天宽限内容。玻璃主题仍只有配置与空资源位，没有公开入口。

验证浮层统一处理焦点、背景 inert、滚动锁定、提交中防重复及未提交离开保护。无效/过期/网络失败不清空输入或关闭表单；过期码返回统一认证错误。入口和隐私不再依赖旧全局布局规则；当时原 `public-entry.css` 收缩为管理员专用 `admin.css`，管理员后续迁移已将该过渡文件删除。

TypeScript、全量ESLint、生产构建、191/191自动化与认证集成、115项六宽度 `qa:strict` 通过。应用内浏览器实际验证1280×720桌面与390×844手机入口、表单、失败保留和隐私页，控制台无错误。真实物理手机键盘、弱网、HTTPS和线上环境尚未验证；本轮没有操作服务器、正式数据库或生成部署包。下一步继续账户中心与管理员页面迁移。

## 最新进度：家庭学习专属校园页面与孩子隔离（2026-09-13）

家庭学习已从听写页的条件化选择框和平铺任务表迁到独立 `FamilyScene` 页面和纯操作层。孩子切换、今日任务、词库、个人错词、记录统计、孩子档案、自定义听写和逐词人工批改均保留；桌面与手机复用同一任务编辑、材料快照、批改及保存逻辑，没有复制家庭版业务树。页面新增原创家庭学习物件插画和 `family` 主题槽，玻璃主题仍为空资源映射且没有切换入口。

重名孩子通过稳定ID保持各自任务、结果、错词和统计；家庭任务只接受与 `childId` 相同的单个参与者，班级学生不能混入。归档只改变档案状态并保留历史，服务端拒绝为归档孩子建立新任务，页面同时禁用新建、复听和批改。未批改不算全对，请假/未参加不计错误，复习建立新任务且不覆盖原记录。孩子档案与任务编辑均有未保存离开保护；真实409后全局冲突入口和内联草稿同时保留。

新增8项家庭操作回归；生产构建、190/190自动化测试、认证集成、TypeScript、全量ESLint和全站115项六宽度 `qa:strict` 通过。隔离浏览器实际验证两个重名孩子、长姓名第三个孩子、归档/恢复、错词来源与复习、自定义任务创建、服务器确认批改、零孩子只读、390×844、360×800及双标签409。共享任务编辑/词库/批改仍使用 `dictation.css`，无消费者旧根规则待TASK-30统一清理；物理手机、软键盘、弱网和线上环境未验收。没有操作生产环境或生成部署包。

## 最新进度：班干部共享校园页面与班级归属（2026-09-12）

班干部已从 `ClassroomApp.tsx` 的旧桌面/手机双实现迁到同一 `Cadres` 页面、CSS Module和纯操作层。班委与小组长分流、当前班岗位搜索/筛选、任命/待任命、职责、任期、状态、评价、小结、单岗/全部复制和删除均保留。手机在900px工作台断点内使用岗位列表到详情再编辑的单列流程，360px长标题和768px返回入口已修正。

岗位通过 `classId` 按班隔离，小组长通过 `groupNumber` 保存明确小组；班委候选来自当前班，小组长候选只来自目标组。正式空工作区保持零岗位，不再填入三条示例干部；班委可建立待任命空岗，空任期保留为空，复制文本也不会把待任命写成已任命。页面新增原创透明水彩岗位素材与主题槽，玻璃主题仍为空槽且没有切换入口。

新增10项班干部回归；生产构建、182/182自动化测试、认证集成、TypeScript、全量ESLint和全站115项六宽度 `qa:strict` 通过。隔离浏览器实际验证甲班105人、乙班50人、第105名学生搜索、第27组组长、跨班隔离、长职责、空任期、脏取消、只读、保存失败和双标签真实409。共享 `StudentLookupDialog` portal、根CSS中的无消费者班干部规则、通用服务器确认式保存、物理手机、软键盘、弱网和线上环境仍待后续。没有操作生产环境或生成部署包。

## 最新进度：值日岗位共享校园页面与检查快照（2026-09-12）

值日岗位已从 `ClassroomApp.tsx` 的旧桌面/手机双实现迁到同一 `Duty` 页面、CSS Module和纯操作层。课程教学日、岗位编辑/停用、非空小组轮换、固定学生、按日临时指定、完成/返工、负责人快照、本周复制及台账搜索/筛选/备注均保留。教学日使用当前自然周的真实本地日期，支持六天或七天和星期别名；未分组学生会明确排除，不再造成轮换异常。

岗位与轮换序号通过 `classDutySettings` 按班级隔离，根字段只作活动班和旧备份兼容。检查记录区分自动、固定和临时来源；轮换不追改历史快照，恢复自动会保留检查状态与备注。完整备份替换时旧班映射覆盖导入设置的问题也已修正。页面增加独立水彩清洁角素材和 `duty` 主题槽，玻璃主题仍为空槽且没有切换入口。

新增12项值日回归；生产构建、172/172自动化测试、认证集成、TypeScript、全量ESLint和全站115项六宽度 `qa:strict` 通过。隔离浏览器实际验证105人长名单、两班、6教学日、未分组、固定/临时分配、轮换、检查、台账备注、只读、保存失败重试和双标签真实409。空班纯操作已覆盖但独立视觉未运行；岗位编辑脏关闭、共享学生选择portal、旧值日死CSS、物理手机、软键盘、弱网和线上数据仍待后续。没有操作生产环境或生成部署包。

## 最新进度：座位分组共享页面与完整操作核验（2026-09-12）

座位分组已从 `ClassroomApp.tsx` 的旧桌面/手机双实现迁到同一 `Seating` 页面、CSS Module和纯操作层。教室行列、过道、1至12组、选人换座、拖放、智能排座、前后排轮换、按座位分组、一步撤销、打印、组长、身高、方位、避让和固定座均保留。配置上限扩为20排×10列；重复学生、重复/越界座位和无效小组显式提示，固定座及可满足的特殊条件在智能排座后保持有效。

修复了新班级继承上一班座位布局的跨班污染。座位选择、提示、选人器和撤销记录绑定班级；撤销还会拒绝变化后的名册。一般修改仍先写本机草稿再由工作区自动同步，页面不冒充服务器确认；隔离浏览器中临时关闭本地保存路由后，换座结果保持并出现失败/重试，恢复路由后同步成功。

新增11项聚焦回归；生产构建、160/160自动化测试、认证集成、TypeScript、全量ESLint和全站115项六宽度 `qa:strict` 通过。桌面与390×844手机实际验证105个长姓名、18×6与12组、智能排座、固定座、换座撤销、空班、跨班和只读。共享 `StudentLookupDialog` 的最终主题隔离、真实浏览器双客户端409、物理手机、弱网、打印版式和根CSS死规则清理仍待后续；没有操作生产环境或生成部署包。

## 最新进度：课堂工具班级边界与随机流程核验（2026-09-12）

课堂工具已接当前班共享操作层，统一本机日期、当天请假排除、无重复点名、非空临时分组、班级历史和复制。修复了 UTC 日期、切班串入上一班本轮、请假计数包含已移除学生、抽取顺序显示错误，以及人数少于分组数时生成空组。临时分组明确受只读限制并显示正在同步；随机点名仍只是当前页面临时结果，不写积分或学生评价。

新增9项聚焦回归；生产构建、149/149自动化测试、认证集成、TypeScript、全量ESLint和全站115项 `qa:strict` 通过。本地 Chromium 以两班隔离合成工作区验证桌面/390×844手机的全轮抽取、人数不足、多组、历史复制、切班、只读和全员请假；甲班2条、乙班1条分组记录隔离，积分未变化，页面无横向溢出或控制台异常。生产页面仍是旧 `.classroom-tools`/`.tool-*` 结构，独立校园构图、旧CSS退出、操作级服务器确认、物理手机和弱网仍未完成；`A-16-*` 保持未运行。没有操作生产环境或生成部署包。

## 最新进度：课程日程与教师留痕操作核验（2026-09-12）

桌面与手机课程日程已接同一当前班操作层，统一学期教学日、周课表、班级活动、每日重点、个人事项和工作留痕的校验。修复了保存当前周会丢失周外活动/重点、倒置学期、跨班学生关联、只读假成功、重复完成产生多条留痕，以及手机与桌面默认月份不一致。完成事项现在只生成或更新一条带稳定 `agendaId` 的内部记录，不宣称外部发送。

新增10项聚焦回归；生产构建、140/140自动化测试、认证集成、TypeScript、全量ESLint和全站115项 `qa:strict` 通过。真实 Chromium 检查覆盖桌面和390×844手机只读编辑层，并在合成可写账号/临时数据库上验证6天教学周、长课程、活动、重点、个人事项、幂等留痕、刷新持久化及手机改课后桌面读取。生产页面仍是旧 `courseplan-*`、`agenda-*`/手机公共结构，独立校园视图、旧CSS退出、操作级服务器确认、100+/物理手机和完整极端状态仍未完成；`A-15-*` 保持未运行。没有操作服务器、正式数据库或部署包。

## 最新进度：期末评语班级边界与草稿真实性核验（2026-09-12）

期末评语桌面与手机已接同一当前班操作层，统一班级/学生归属、同学生同学期同语气唯一性、字段清理和稳定更新。学期选项、已保存/待填写数量与学生状态不再读取另一班评语；旧无班级字段的记录只在学生确属当前名册时兼容。

本地草稿只整理教师当前勾选的沟通、积分、考试反思和老师补充。无依据时保持空白并提示，不再根据当前成绩/作业汇总或缺失记录编写学生表现；切换证据不再覆盖手工内容。只读保存保留编辑器和草稿，桌面对象切换与手机关闭增加未保存确认。正式 AI 请求在扣配额前校验学生与依据，结果仍只进入草稿；演示响应明确标为流程示例。

新增 8 项聚焦回归；生产构建、130/130 自动化测试、认证集成、TypeScript、全量 ESLint和全站 115 项 `qa:strict` 通过。真实 Chromium 六宽度实际打开手机编辑器和二级依据浮层，验证依据变化及只读保存后草稿保留。生产页面仍使用 `comment5-*`/手机公共大组件，独立校园视图、旧 CSS 退出、100+ 名册/软键盘、真实 DeepSeek 和操作级服务器确认仍未完成；`A-14-*` 保持未运行。没有操作服务器、正式数据库或部署包。

## 最新进度：班级周报证据范围与草稿/归档核验（2026-09-12）

班级周报桌面与手机已接同一当前班操作层，手机现在明确区分保存草稿和完成归档。同班同周同版本稳定更新，跨班、重复目标、空正文、无效日期和只读操作都会保留编辑内容。旧无班级字段的周报在载入时兼容归到当时活动班，随后严格按显式班级隔离。

自动汇总改为只使用所选周内可核实的逐生作业状态、带日期积分、沟通/成长、考试和考勤；不再用学生当前汇总填补历史，也不再虚构表扬理由。考试按各科满分换算得分率，未录不当零分。小组积分仍是当前累计口径，界面已明确标注“当前”。新增10项聚焦回归；生产构建、122/122自动化测试、认证集成、TypeScript、全量ESLint和全站115项 `qa:strict` 通过。

真实 Chromium 六宽度检查实际打开周报编辑器、输入草稿并触发只读保存；草稿、提示和两个长文本字段均保留。人工看图另发现并修复手机粘性动作重叠、半宽主归档按钮及提示覆盖。生产周报仍是旧 `weekreport-*`/手机大组件，统一controller、脏关闭、操作级保存失败恢复、班级化 `weeklyPlan`、独立校园视图和旧CSS退出尚未完成；A-13仍为 `not-run`。没有操作服务器、正式数据库或部署包。

## 最新进度：考试反思归属与完成语义核验（2026-09-12）

考试反思桌面与手机已接同一当前班操作层，统一学生/考试/旧记录归属、主要问题校验和本机日期。草稿不会提前清除重点跟进或生成沟通记录；完成后才移除当前学生，并通过可选 `reflectionId` 生成或更新一条内部家校沟通留痕，重复完成不再不断追加。反思页也已区分完整成绩、部分录入和未录入，手机切班会同步有效考试，只读操作保留输入和编辑器。

新增8项聚焦回归；生产构建、112/112自动化测试、认证集成、TypeScript、ESLint和全站115项 `qa:strict` 通过。真实 Chromium 在六个宽度自动打开编辑器并验证五个长文本字段、只读提示和草稿保留；本轮未用可写正式账号验证服务器确认。页面仍是旧 `reflection5-*`/手机大组件结构，切换对象的脏关闭、具体浮层失败恢复、统一 controller、校园构图和旧 CSS 退出仍未完成。未操作服务器或正式数据库。

## 最新进度：成绩分析数据语义与操作核验（2026-09-12）

成绩分析已统一当前班考试、录分、人工分析项和试卷候选操作。核验发现旧逻辑会给未录成绩预写零分，同时又把真实零分排除在录入人数之外；本轮改为明确区分空白与零分，只有全科完整学生进入总分分布，各科平均只使用实际录入值。正式空工作区不再用学生综合分构造假考试，新考试和新增科目保持空白，录分可清空，批量成功后两端清除选择；删除最后一场考试后，考试反思也显示真实空状态，不会写入占位考试。

新增11项聚焦回归，覆盖50人空考试、105人批量、真实零分、跨班拒绝、考试删除、旧数据、本机日期、人工分析项和AI候选确认幂等。生产构建、103/103测试、认证集成、TypeScript、全量ESLint和115项 `qa:strict` 均通过；应用内浏览器在演示数据上验证桌面空考试、零分/清空、两人批量以及390×844手机逐生编辑/考试切换，无横向溢出或控制台错误。当前生产页面仍是旧 `score5-*`/手机结构，两端选择的当前考试也还没有共享 controller；CSS隔离和校园视觉迁移尚未完成。真实OCR、单弹层失败恢复、调低满分的历史成绩策略、分析项删除确认与 `Student.score` 的未录表示仍是明确缺口。未操作服务器或正式数据库。

## 最新进度：家校沟通与通知草稿操作核验（2026-09-12）

桌面与手机沟通记录已接同一操作层，保存、状态和删除均校验当前班及稳定学生ID，同名学生不会再因为姓名匹配写错对象。通知草稿支持并保留真正的多人对象，拒绝跨班收件人；编辑保留原日期、状态和手工回执，复制仍只表示文本已进入剪贴板，没有外部发送。

核验发现成长时间线原本已直接归集沟通台账，旧新增逻辑又额外写入一份成长事实，造成重复以及编辑/删除后的过期副本。本轮停止双写并在展示层过滤历史副本，不删除旧备份原数据。新增8项聚焦回归；完整生产构建、92/92自动化测试、认证集成、TypeScript、全量ESLint和 `qa:strict` 通过。生产页面DOM/CSS尚未迁移，记录分页、脏关闭、失败后保留编辑上下文及浏览器多人/长文本/100+名单验收仍待后续阶段。未操作服务器或正式数据库。

## 最新进度：成长档案与健康照护操作核验（2026-09-12）

成长人工事实统一校验当前班并立即保存班级快照；显式属于另一班的记录不会混入当前学生。健康新增/编辑/删除新增班级和原记录归属校验，默认日期改用本机年月日，监护人同步按当前班主联系人和紧急顺序选择；学生档案也补齐班级成员与私密记录归属校验。

8项新增聚焦回归覆盖跨班、家庭隔离、字段清洗、旧记录、照护删除和监护人选择；完整生产构建、84/84自动化测试、认证集成、TypeScript、全量ESLint和 `qa:strict` 通过。当前成长模块实际只有新增人工事实，没有编辑/删除入口；这已作为规划缺口记录。页面DOM/CSS、浏览器长内容/100+名单/嵌套浮层和失败保存上下文仍待后续阶段验证。未操作服务器或正式数据库。

## 最新进度：积分评价与规则库操作统一（2026-09-12）

桌面和手机积分入口已接同一纯操作层，新事件立即保存班级与规则引用；批量写入和撤销都校验当前班，兼容旧事件且不触碰另一班或家庭数据。桌面原“本周”指标实际统计全部历史，现改为准确的累计口径。

默认规则目录已移出主组件。新增首次修改标记后，旧备份空规则仍显示默认目录，用户明确删除最后一条后则能保留真实空库；规则编辑、停用和删除不改历史事件快照。6项聚焦回归覆盖两班50人、105人、跨班拒绝、撤销、旧备份和历史引用；完整生产构建、76/76自动化测试、认证集成、TypeScript、全量ESLint和 `qa:strict` 通过。页面DOM/CSS尚未迁移，普通保存失败仍不能保留操作级选择/编辑上下文；浏览器完整状态验收待新页面阶段执行。未操作服务器或正式数据库。

## 最新进度：线上包核实、保存操作提取（2026-09-12）

线上导出受检88文件与0691aba对应源码一致，另有2份未引用旧CSS残留；本地2891b8f增量均保留。线上源码隔离存放，未覆盖本地。详见实施包evidence/online-baseline-and-save-20260912.md。

已抽取一般编辑/save/听写commitWorkspace操作，ref和初始载入仍由原组件持有。新增8项回归，并修复听写成功时并发一般编辑的本机草稿未更新revision问题。54项测试及构建通过。页面迁移和CSS隔离未完成，继续推进，不需要用户重复授权。

## 最新进度：学生/作业共享数据操作（2026-09-12）

两端学生批量编辑和四个作业状态入口已接入features内共享纯操作；保留原一般编辑/服务器保存语义。新增7项回归并加入npm test，TypeScript、ESLint、46项测试、构建、认证集成及qa:strict通过。没有替换本轮页面DOM/CSS，听写新视图仍在独立预览。

已展开学生/作业27项源码核验，记录两端搜索、全选范围、旧任务兼容和新增关联差异；保存层核验确认commitWorkspace仍是听写专用并发合并，不能直接用于学生/作业。详见实施包evidence/students-homework-operations-20260912.md。TASK02/05/09仍未完成；下一步继续档案/导入导出/浮层，再处理保存层与旧CSS边界。未操作服务器。

## 最新进度：听写控制器保全与独立新视图（2026-09-12）

已将原听写批改状态/草稿/保存处理原样抽成共享hook；正式页面继续使用原视图，新校园GradingView仅在本地独立预览。新增语义插画槽、原创无字词卡素材、105人/200词/家庭夹具。逐动作核验19项的证据与未测边界见实施包 evidence/grading-controller-20260912.md。

类型、ESLint、构建、39项测试、认证集成及qa:strict通过；浏览器验证模拟失败重试、脏状态保护、105人搜索、只读、手机200词确认及底栏布局。修复家庭空列、手机名单焦点及错词色级联。新视图仍待旧CSS隔离/真实接线/完整视觉门槛；不宣称整个听写或全站迁移完成。学生与作业仍需继续逐handler审计。未生成部署包、未操作服务器。

## 最新实施进度：已开始共享基础层，页面尚未迁移（2026-09-12）

- 用户已授权按实施包实际开发。TASK01源码/Git核验完成；TASK02交互候选扫描已覆盖28文件，1282交互与70浮层候选尚需逐handler核验，不能当完整保全审计。
- 新增workbench主题边界、语义token、基础按钮/表单/状态分段/选择条/原生Dialog；SVG注册表共用。独立Vite组件预览只监听127.0.0.1:4207，不加载旧CSS、不访问数据库，保存均为标注的模拟。
- TypeScript、ESLint、构建、39项既有测试及qa:strict通过。桌面与390×844内嵌手机视口检查通过的范围限基础表单；主题替换保持输入、模拟失败保留草稿、嵌套返回与关闭保护已检查。局部finish review可阶段提交。
- 全站C1–C6新页面、新插画、controller抽取、完整选人/弹窗改造与旧CSS移除均未完成。旧页面目前仍在；不将本轮基础层写成全站换新或部署完成。
- 恢复入口：实施包trackers/progress.json，当前TASK02。详细阶段记录在evidence/implementation-start-20260912.md。没有新部署包、生产数据库或服务器操作。

## 历史阶段：实施包制作完成，当时尚未开始重构

用户认为9月12日版本视觉仍未达到校园参考，已接受保留全部业务、重建整个UI并为玻璃预留架构的方案。本轮仅生成详细实施资料，不修改app/lib/db运行源码。
入口：`项目实施包-UI彻底重构与双主题-20260912/00-从这里开始.md`。包含33任务、23逐页卡（19模块及家庭/入口/账户/管理员）、功能和浮层保全账本、207场景、12张原参考、主题/保存代码样例。
下一步从TASK-01核验，再展开逐动作账本；全部任务初始planned。旧CSS只可作迁移期兼容，最终退出，不以旧DESIGN和历史QA通过作为本轮视觉完成证据。
公开Git保存原创规范和样例，用户参考/历史资料副本/素材副本只在本地包。新的资料包不是部署包，服务器状态未改变。

完整本地ZIP：`项目实施包-UI彻底重构与双主题-20260912.zip`，16,472,464字节，SHA-256 `E01FE1C0F1582E3529B5999D6DC028F0129D67B603C04A63B523BBF825F7EB1C`。共121文件；包哈希/解压/引用检查、任务依赖与范围检查、5项独立样例测试及ESLint通过。16个关键源码文件与制作基线哈希一致；这不是新UI业务或视觉验收。

最后更新：2026-09-12。

## 上一版实施记录：校园构图调整（用户已要求进一步彻底重构）

- 已建立新的共享工作台壳层：桌面使用 68px 顶栏、约 240px 分组侧栏和连续工作区，手机使用紧凑班级/场景头部及五项底部导航；19 个模块保留在同一业务树中。
- 首页按 C1/C5 重做为暖黄任务区与宽幅教室场景，学生名单和作业追踪采用 C3/C4 主从工作区，听写批改按 C2/C6 使用桌面三栏和手机单学生固定保存区。
- 校园语义插画槽覆盖首页、名单、作业、教学分析、计划、照护与沟通页面族；新增四张独立 WebP 素材，导航统一使用 SVG。`legacy-theme.css` 已删除，弹窗、输入框、按钮和焦点外观由共享校园控件接管。
- 终审发现并已修正：首页场景图体量、手机首屏过高、作业零选择时常驻批量按钮、桌面听写保存动作落出首屏和无日程时的推断性文案。完整映射与验收边界见 `docs/校园插画全站重建映射与验收-20260912.md`。
- 听写的材料快照、逐生错词、确认保存、失败不跳人、复听保留历史，以及班级/家庭身份隔离未改变；真实浏览器在隔离工作区验证了选错词后“保存并下一位”，完成数由 5 变为 6 并进入下一名学生。
- 最终 TypeScript、ESLint、生产构建、39/39 自动化测试与认证集成通过；`qa:strict` 对 19 个模块、6 个宽度生成 115 项结果和 117 张临时截图，失败为 0。构建仍报告前端 chunk 超过 500KB，属于已记录的弱网性能风险。
- 本轮仅操作本地源码和系统临时目录中的合成测试数据库，没有连接或修改正式数据库，也没有在服务器上执行命令。新的部署包与校验信息在本轮完成后记录到 `DEPLOYMENT_LOG.md`；生成本地包不代表服务器已部署。

## 最新本地完成：校园全站样式清理与弹窗核验

- 用户报告已部署9月11日校园听写包，但旧样式残留明显；本轮查看线上界面后，在临时合成数据上继续改造。
- 共享首页、班级切换、学生选择、弹窗焦点和复制组件已提取；统一标题插画、手机列表及弹窗控件，修正班干部草稿、分组历史和弹窗关闭后的交互缺陷。
- 入口与管理员页面同步校园样式；学生档案字段补齐标签，考勤分段按钮、值日连续列表、周报指标与手机编辑工具栏完成统一。
- TypeScript、ESLint、生产构建、37/37业务测试、认证集成通过。19模块在1440、1280、1057、768、390、360宽度的qa:strict共115项通过；长文本框最小高度新增真实DOM回归，六宽度加静态检查共7项通过。认证集成曾被本地预览占用开发目录阻断，关闭预览后重跑通过。
- 人工浏览器检查覆盖全站桌面/手机、成绩编辑与筛选、学生档案、健康、成长记录、家校沟通、期末评语与嵌套依据、值日、座位、班干部、考试反思、周报、听写及管理员电话/设备弹窗。独立视觉复审的五项修正均已复核通过。
- 本轮没有重新手工穷尽所有失败、空值、日期及设备组合；先前版本的失败保存、409冲突与草稿恢复浏览器证据见下节，不能算成本轮重新执行。旧CSS布局规则仍有保留，不宣称历史CSS已全部删除。
- 设计检测器单次扫描返回14条警告与799条建议；警告对应的字体及装饰色条已按源码清理，建议中仍有旧文件的字号/圆角/色值维护债务。按设计技能的单次检测流程未重新运行，因此未取得检测器零提示证明。
- 本轮清理包已在本地生成并校验，尚未上传或部署；线上仍是用户此前部署版本，Git推送不会自动改变服务器。

## 前一版本：校园主题与听写复习

- 已新增共享校园主题变量、SVG组件、独立插画资源和听写模块；玻璃主题仅保留配置类型与资源插槽，没有切换入口。
- 听写接入班级/家庭隔离、词库、材料快照、逐生批改、复习历史、加权统计、首页和学生档案；原18模块保留。
- TypeScript、ESLint、生产构建、37/37测试（原29项与新增8项）、认证集成测试均通过；qa:strict共39项结果通过（静态检查及19模块桌面/手机渲染）。
- 浏览器验证了50人任务、105人名单搜索、两个家庭孩子隔离、确认后保存并下一位、409冲突停留并保留草稿、网络失败后草稿恢复、个人错词创建任务和只读演示。正式听写结果仅在服务器确认后更新，未批改不计全对，请假/未参加不计错误。
- 实际查看组件样板与校园参考图；桌面名单、手机批改完成独立视觉复审，修正指标卡堆叠、标题层级、插画比例和词数换行后通过。360/390/768/1280宽度检查无横向溢出，1440/390覆盖全站严格渲染；不等于所有真实设备与线上环境均已验收。
- 两班各50人、100教学日、每日20词的独立听写备份实测1,496,331字节；不代表现有其他模块与无限历史都能容纳。保留5MB服务上限，听写提交在4.8MB预检。
- 本地功能预览需使用Node/Next.js：当前默认vinext开发预览的Workers运行时不能执行既有node:sqlite写入。未改动生产运行配置。
- 该版本开发验证阶段未操作服务器或正式数据库；随后生成的包及用户部署反馈见下方当前基线和DEPLOYMENT_LOG。玻璃主题、真实OCR、自动报词与外部发送均未实现。

## 当前基线

当前本地源码包含 2026-09-06 “两个班、每班 50 人、学期期中”全流程审计后的 P1/P2 修复，并在 2026-09-09 完成桌面/手机严格渲染审计，2026-09-11 再次完成代码、构建与自动化回归。由于 2026-07-31 后 Git 提交曾中断，本次状态建立属于历史补录基线，不代表这些功能是在 2026-09-11 一次完成。

最近已生成包：`classroom-deploy-campus-rebuild-20260912.tar.gz`（6,604,961字节；SHA-256 `2D2D3B377955FC71C4FAC13125BF6146A086F4D6240D56DFE8C4965A78CDA4BE`），业务源码对应提交 `0691aba`。包已在本地完成路径安全检查、解压和核心入口核对，不含数据库、环境变量、依赖目录、测试报告、备份或资料包；尚未上传或部署。

上一份本地清理包：`classroom-deploy-campus-ui-fullscan-20260911.tar.gz`（5,772,847字节；SHA-256 `A9903E7DC63EA2F61CF4C90A3689BD2220C8619B5CA9376C102F415DC4BE94E2`），业务源码对应提交 `1fc8359`。

此前用户部署的包：`classroom-deploy-campus-dictation-20260911.tar.gz`（5,564,753字节；SHA-256 `F80CDFAFC2EBB074587D9F5B7714AF5DE0A381F1D070381D7A21AB213EF46236`）。用户报告已部署，浏览器可见听写与插画；不代表完整服务器和线上验收。

上一份历史包：`classroom-deploy-midterm-audit-fixes-20260909.tar.gz`。

- 大小：5,335,946 字节
- SHA-256：`F6D448BFDB59C9C02CC540AC7EC217688BF983A2F4941259ACDDFA20CCE1B016`
- 包内不含 `data`、`.env.local`、`node_modules`、测试报告和对话恢复资料
- 当前记录只能证明包已生成并校验，尚不能证明它已经部署到服务器

Git 记录状态：

- 历史补录基线已在本地提交为 `41325fa chore(project): capture verified classroom baseline`
- 2026-09-11 已新增 `github` 远端 `https://github.com/a18502656182-arch/teacher.git`，并将完整 `main` 历史首次推送成功
- 本地 `main` 现在跟踪 `github/main`；后续默认使用 `git push` 同步 GitHub
- 旧 `origin` 仍指向 `git.chatgpt-team.site`，因没有可用凭据只作历史保留
- GitHub 仓库当前为公开仓库，源码、项目文档、历史部署压缩包和知识索引均可被公众读取；不得向其中提交真实学生资料、数据库或密钥

交接状态：

- 当前主对话为 `codex://threads/01a03731-f077-7bf0-b823-9c1e463f0e48`，上一段主对话为 `codex://threads/019fff2c-2036-7251-a6a7-ceefa99414db`
- 可复制的新对话启动提示词已写入 `对话交接文档-班主任工作台-20260825.md` 第 30 节
- 提示词只是索引；新对话必须重新核对当前源码、Git 和最新状态文档，并明确对话工具实际读取到的范围

## 已完成并进入当前源码

- 公共入口、兑换码登录、正式空白工作区、只读演示工作区和隐私页。
- 管理员账号、兑换码、设备、用户数据导出/删除与审计相关页面和接口。
- 原18个班主任工作模块全部保留，新增听写与复习，共19个模块及电脑端、手机端导航。
- 作业与考勤的搜索、筛选、多选、批量状态修改、保存后清空选择。
- 考勤主列表整合状态、备注和保存流程；值日教学日跟随课程日程自定义。
- 成绩新增/编辑考试、长期趋势、题目与知识点人工分析，以及试卷上传后的教师确认工作流。
- 独立健康与照护页、可搜索学生选择、自定义关注类型/场景、可选字段和监护人资料同步。
- 课堂随机点名与临时分组结果展示、历史和复制。
- 工作区乐观版本保存、本机草稿兜底、备份预检与恢复。
- HTTP/旧 WebView 环境下的客户端 ID 兼容降级，避免 `crypto.randomUUID()` 缺失导致白屏。

## 历史补录基线验证（本次新增验证见文首）

2026-09-11 针对历史补录基线重新执行：

- TypeScript：通过
- ESLint：通过
- `npm.cmd test`：29/29 通过，认证集成测试通过
- 静态布局审计：通过

此前全站可视化验证为 2026-09-09：

- 全站严格桌面/手机渲染审计：通过
- 考勤、课堂工具、健康与照护专项桌面/手机截图检查：通过
- 部署包解压及核心文件清单：通过

## 下一步

1. 回补实施包TASK-02遗漏的首页F-01/O-01账本并核对完成条件；F-02至F-23已有阶段证据，TASK-29已完成。
2. 按已完成页面的真实消费者清单推进TASK-30：逐页删除无消费者的旧DOM/CSS，并持续验证尚未清除的共享听写、门户和根样式边界。
3. 页面迁移完成后再处理共享保存controller和模块动态加载；使用慢速网络与真实手机决定超过500KB chunk的真实切分优先级。
4. 真实OCR、外部消息和线上部署仍按独立授权及数据安全条件处理，不能由本地测试或Git推送推断完成。

## 已知风险与未验证项

- 当前前端构建仍有超过 500KB 的 chunk 警告，功能不受阻，但手机弱网性能存在风险。
- 真实 JPG/PNG/PDF 的 AI 识别质量、超时与错误恢复未在正式 API Key 下验证。
- 完整“导出→清空/更换环境→恢复→再次保存”灾难恢复闭环仍需在隔离环境演练。
- 管理员永久删除等不可逆流程不应使用正式用户数据试错。
- 历史服务器使用 HTTP 兼容开关；功能兼容不等于传输安全，正式运营仍应使用 HTTPS。
- Git 推送状态和服务器部署状态必须分开记录。
# 2026-09-12 追加：线上基线与隔离准备已验证

线上导出88个受检文件对应0691aba，没有需要回灌的业务变化；完整结论见实施包evidence/online-baseline-and-save-20260912.md。保存层8项回归及CSS AST4项回归已加入主测试，构建、58/58测试、认证集成、TypeScript、完整ESLint通过，qa:strict本轮通过。独立浏览器证实限定旧控件CSS不会命中新控件；正式CSS尚未接线，根规则/portal仍待人工处理。未部署。继续TASK02档案/导入导出账本与页面迁移前置。
# 2026-09-12 追加：学生模块逐动作核验

F-02/O-02已展开到新增编辑、文本替换/追加、CSV导出、批量、档案、监护人/照护和删除确认；新页面尚未迁移。删除关联的两项越界清理缺陷已由失败测试复现后修正，档案和删除新增6项纯操作回归。生产构建、64/64自动测试、认证集成、TypeScript、完整ESLint、qa:strict通过；设计检测0项。旧档案仍无脏关闭保护，服务器失败仍由全局workspace草稿/重试承接，不应把弹窗按钮描述为服务器保存成功。
# 2026-09-12 追加：作业模块逐动作核验

F-03/O-03已展开到任务新增编辑删除、筛选分页、逐生/批量四态、公共学生备注和跟进名单。发现手机源码的编辑/删除任务分支没有任何edit触发器，当前实际不可达；新页面迁移必须补齐并实测。作业纯操作增加班级/任务归属校验，防止另一班ID污染任务或学生汇总。新作业页面尚未迁移。
# 2026-09-12 追加：考勤模块逐动作核验

F-04/O-04已展开，当前桌面/手机共用Attendance连续名单，没有请假详情浮层。UTC日期和一键正常清空既有备注问题已修；考勤纯写入覆盖跨班、历史日期和105人批次。TASK-20已将“已保存”与清选择延后至服务器确认，保存失败保留选择和草稿。请假扩展字段当前仅保留/写默认值，没有可编辑入口，不能沿用旧文档宣称完整请假流程。
# 2026-09-12 追加：听写全模块逐动作核验

F-05/O-05已覆盖任务、词库、参与者、批改、复听、错词、30天统计和家庭孩子。修复编辑未批改复习/归档任务丢sourceId与archived的问题，词库名称不再接受纯空格；生产构建、70/70自动测试、认证集成、TypeScript、完整ESLint、qa:strict通过，设计检测0项。新GradingView仍只在开发预览；生产其他子页仍用旧视图。只有批改有脏离开守卫，任务/词库/孩子表单仍可直接丢草稿，列为迁移缺口。
