# 变更日志

本文件按时间倒序记录用户可见功能、代码、配置、测试和项目记忆变化。2026-09-11 以前的条目依据现有源码、部署包、交接文档和 Codex 原始日志重建，不对应逐条历史 Git 提交。

## 2026-09-11

### 校园主题与听写复习（本地实现与验证）

- 建立共享主题与SVG组件、校园插画资源，独立实现听写编辑、词库、批改和个人筛选组件；不增加未完成的玻璃主题切换入口。
- 新增可选dictation数据，旧备份兼容，导出版本2并使用紧凑JSON；备份预检学生数改为所有班级总人数。
- 听写结果使用材料索引和确认状态，隔离家庭孩子与班级学生；服务器校验参与者归属和已批改材料快照。
- 听写提交改为服务器确认后更新正式结果，保存失败和并发冲突保留草稿与当前学生，确认成功才进入下一位；独立复听不覆盖历史。
- 接入首页、学生档案和可选作业关联；支持词库文本导入、参与者搜索分页、个人错词复习、历史和近30天加权统计。
- 抽取校园主题变量、公共按钮/指标条/分页/插画接口和独立听写页面，保留原18模块。玻璃主题仅预留，不显示切换入口。
- 保留5MB备份上限，听写提交4.8MB预检；两班各50人、100教学日、每日20词的模拟听写数据约1.43MiB，旧备份兼容与版本写入测试通过。
- TypeScript、ESLint、生产构建、37/37测试及认证集成通过；qa:strict的39项结果通过。浏览器覆盖班级/家庭隔离、105人搜索、保存失败、冲突与只读；代表桌面名单和手机批改经独立视觉复审修正后通过。
- 更新DESIGN与项目状态、长期决策；资料包与本机QA资料不入库。本条不代表服务器部署或线上验收，详细边界见PROJECT_STATUS。

### 建立持续项目记忆与 Git 工作流

- 新增根目录项目导航，以及 `PROJECT_STATUS`、`CHANGELOG`、`DEPLOYMENT_LOG`、`DECISIONS` 和开发导航。
- 更新 `AGENTS.md`：以后每个新对话自动读取项目状态；每次实际修改后自动更新记录、检查敏感文件、提交并推送。
- 扩充 `.gitignore`：部署包、压缩备份、QA 报告、对话恢复文件和本机设计审计资料不再进入新提交。
- 将 2026-07-31 后未持续提交的当前源码登记为一次历史补录基线，不伪造旧提交。
- 基线入库前重新执行 TypeScript、ESLint、静态布局审计和 `npm.cmd test`；29 项测试与认证集成均通过。
- 本地基线提交为 `41325fa`；旧内部 `origin` 因凭据不可用未能同步。
- 新增 GitHub 远端 `https://github.com/a18502656182-arch/teacher.git`。推送前检查完整 Git 历史，没有发现数据库、真实 `.env`、私钥或常见访问令牌；完整 `main` 已首次推送成功。
- GitHub 仓库为公开仓库；旧历史中的 15 个部署压缩包及当前约 10MB 的知识索引也会公开，后续禁止继续提交部署包、数据库、真实班级资料和密钥。

### 增加可复制的新对话交接提示词

- 在 `对话交接文档-班主任工作台-20260825.md` 第 30 节加入完整的新对话启动提示词，串联当前主对话、上一段主对话、项目文档、GitHub 基线、验证结果、部署边界和持续提交规则。
- 明确对话读取可能返回摘要或截断内容，不能仅凭提示词或聊天记忆宣称掌握全部事实；当前源码、`github/main` 和最新状态文档优先。
- 在项目导航和当前状态中补充该提示词入口，方便切换对话时直接复制。

## 2026-09-09（历史重建）

### 期中百人审计修复部署包

- 为 2026-09-06 审计后的当前源码重新完成 TypeScript、Lint、构建、29 项测试、认证集成与桌面/手机严格布局检查。
- 生成 `classroom-deploy-midterm-audit-fixes-20260909.tar.gz`，SHA-256 为 `F6D448BFDB59C9C02CC540AC7EC217688BF983A2F4941259ACDDFA20CCE1B016`。
- 记录中没有服务器部署完成证据，因此状态为“包已生成，部署待确认”。

## 2026-09-06（历史重建）

### 两班各 50 人的学期期中全流程审计与修复

- 使用两个班、每班 50 人的隔离测试数据，覆盖 18 个工作台模块、桌面/平板/手机、双班切换、搜索筛选、批量操作和错误分支。
- 修复移动端保存失败白屏，保留本机草稿并提供重试/载入最新版本。
- 课堂工具补齐真实分组结果、历史与复制；通知对象改为适合长名单的可搜索选择。
- 家校沟通新增记录不再预填虚构事实；通知删除改用统一危险确认。
- 考勤补充分页、搜索、状态筛选、批量操作和移动端适配。
- 新增学生、值日岗位和班干部岗位改为草稿确认后保存，避免生成半成品记录。
- 修复弹窗高度、可访问性、重复桌面/移动壳层以及相关 TypeScript 问题。

## 2026-09-05（历史重建）

### HTTP 与旧 WebView ID 兼容

- 定位考勤状态按钮白屏根因为非安全 HTTP 或旧 WebView 中缺少 `crypto.randomUUID()`。
- 增加共享 `makeId()` 降级方案，并处理考勤、日程、课堂工具、通知、学生档案和课程等同源写入流程。
- 增加禁用 `randomUUID` 条件下的回归检查。
- 生成 `classroom-deploy-http-id-compat-20260905.tar.gz`，SHA-256 为 `B650340EE46B4B7BD76AF1393EA608035B63C340F01DA759113E87E644E93133`。

## 2026-08-25 至 2026-08-29（历史重建）

### 工作台功能补齐与电脑/手机一致性

- 新增并深化考勤与请假、教师日程和工作留痕、学生档案、通知草稿、课堂工具、成绩长期趋势、题目/知识点分析与试卷识别确认流程。
- 新增独立“健康与照护”页面，并根据实际反馈精简字段、筛选、学生选择和弹窗布局。
- 重做考勤主名单、作业批量状态、值日教学日同步、侧栏完整展示及大量电脑/手机对齐问题。
- 建立正式空白工作区、账号隔离、设备限制、乐观保存、备份预检恢复和管理员管理能力。
- 详细过程仍可查阅根目录的专项计划、审核报告和 `对话交接文档-班主任工作台-20260825.md`。

## 2026-07-31 及以前

- Git 历史已记录初版工作台、SaaS 视觉体系、主要班务模块、积分/成长页面与严格布局审计。使用 `git log --oneline` 查看原始提交。
