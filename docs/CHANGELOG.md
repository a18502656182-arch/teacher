# 变更日志

## 2026-09-12：核实线上导出与提取保存操作

- 线上受检源码对应0691aba，保留本地开发增量，不恢复两份未引用残留CSS。
- 抽出workspace保存操作和类型；新增8项回归覆盖失败、冲突、排队、权限及容量。
- 修复听写成功且仍有一般未保存编辑时，本机草稿revision及合并数据未同步的问题；测试先复现再修复。根冲突UI改用已有state。
- 隔离归档不参与tsc/ESLint；原静态断言追随新源码位置，新增接线断言。没有生产操作。

## 2026-09-12：学生与作业操作去重及逐动作核验

- 学生两端批量编辑、作业两端逐生/批量状态接共享纯操作，主组件减少40行重复逻辑；界面和保存时点保持。
- 新增7项可执行回归，覆盖两班50人、105人批量、家庭不变、四态映射及旧可选结构；加入npm test。46项测试、生产构建及认证集成通过。
- 记录27项学生/作业操作、相关浮层、一般编辑与听写提交差异，明确旧CSS真实入口和全局焦点管理风险。新页面切换尚未完成，没有部署操作。

## 2026-09-12：继续听写逐动作核验和新视图开发

- 原Grading状态/effects/save原样迁入useGradingController，生产原视图与独立新视图共用逻辑。
- 增加校园批改视图、限高分页学生队列、家庭单列、手机选人焦点回退；主题Artwork登记原创候选词卡插画，玻璃仍仅预留。
- 本地105人/200词/家庭夹具验证；模拟保存失败重试、只读与未保存拦截通过。类型、ESLint、构建、39项既有测试、认证集成、qa:strict通过。
- 逐动作证据记录已测与未测；新视图未接生产路由，旧CSS退出及真实保存综合门槛待完成。没有上线操作。

## 2026-09-12：开始UI重构基础层与隔离组件验证

- 建立app/components/workbench的独立主题边界、CSS Modules控件与共享SVG；旧CampusIcon委托公共SVG，不改变业务或路由。
- 新增仅本地的tools/ui-preview，校园默认、玻璃内部材质探针、模拟保存失败及嵌套浮层，用于验证新基础层且不加载旧CSS。
- 新增只读AST交互扫描；1282交互、70浮层均为候选，TASK02仍待逐动作确认。实施包进度改为in-progress，未宣布页面迁移完成。
- TypeScript、ESLint、生产构建、39/39既有测试及qa:strict通过；浏览器检查桌面和390px内嵌手机表单及模拟草稿流程，局部finish review可阶段提交。未操作正式数据库/服务器，未生成部署包。

## 2026-09-12：建立UI彻底重构与双主题实施资料包

- 根据用户确认的全面界面重建方向，生成18份编号指南、33任务卡、23逐页卡、功能/浮层/素材/验收账本，以及本地参考查看器和独立代码样例。
- 明确逻辑保全、桌面/手机controller收敛、新旧CSS隔离与最终删除、C1–C6视觉门槛、玻璃仅架构预留。
- 复制原校园/玻璃12图及旧样板到本地资料包，记录哈希；生成只读完整性/任务依赖/源码漂移检查。没有修改运行页面或生产环境。
- 资料包校验和示例测试与网站验收分开；后续重构任务全部planned，不能复用历史通过结论。
- 本地ZIP包含121文件，完整性、解压、目录引用、33任务依赖、23范围与207验收场景检查通过；5项独立状态机样例测试和ESLint通过。没有重跑构建/网站QA，也没有将样例测试当网站通过证据。

本文件按时间倒序记录用户可见功能、代码、配置、测试和项目记忆变化。2026-09-11 以前的条目依据现有源码、部署包、交接文档和 Codex 原始日志重建，不对应逐条历史 Git 提交。

## 2026-09-12

### 按 C1–C6 完成校园插画全站构图重建

- 将旧工作台外壳替换为共享 `WorkspaceChrome`：桌面顶栏、四组可折叠导航、连续工作区和真实保存状态；手机保留紧凑班级/场景选择与五项高频底栏。19 个模块继续共用一套业务数据与组件树。
- 首页、学生名单、作业追踪和听写批改按参考构图重建为任务场景、名单/档案主从、任务/学生状态主从及桌面三栏/手机单学生流程。
- 新增学生档案、作业本、试卷复盘和课程计划四张独立校园 WebP；建立 dashboard、dictation、roster、homework、assessment、planning 等语义素材槽，导航图标统一为 SVG。
- 删除 `legacy-theme.css`；新增共享壳层与页面族样式，统一弹窗、输入、按钮和键盘焦点。其余页面族接入语义页头和插画，但仍保留部分历史业务结构与 `homework-bootstrap-shell` 兼容选择器，未宣称旧 CSS 已全部移除。
- 根据独立视觉终审压缩手机首页首屏、放大桌面教室场景、隐藏零选择时的批量状态动作，并让桌面听写词表独立滚动、确认和保存动作保持在首屏。
- 真实浏览器在隔离合成数据上验证听写选错词、确认和“保存并下一位”，正式结果仅在服务器确认后更新；未操作生产数据库或服务器。
- 更新静态与严格布局审计脚本，使其识别新的工作区内容和导航壳层，避免继续依赖已替换的 `.page-content` 与 `.side-nav`。
- TypeScript、ESLint、生产构建、39/39 自动化测试和认证集成通过；最终 `qa:strict` 覆盖 19 模块与 6 个宽度，115 项结果、117 张截图均无失败。构建仍保留超过 500KB 的 chunk 警告。
- 生成 `classroom-deploy-campus-rebuild-20260912.tar.gz`，完成路径安全、敏感内容排除、解压和构建入口检查；部署状态仍为本地待上传，不把生成部署包写成服务器已部署。

## 2026-09-11

### 建立校园插画全站构图重建规范（文档阶段）

- 用户确认要按 C1–C6 完整重建桌面与手机页面构图和视觉层次，不再接受仅换色、加小插画或继续覆盖旧后台结构。
- 新增 `docs/校园插画全站构图与视觉层次重建规范-20260911.md`，明确参考图约束等级、桌面/手机壳层、四个标杆页面、其余页面族、数据压力测试、分阶段实施和视觉验收门槛。
- 新增可直接发送给项目任务的 `docs/给项目对话的校园插画全站重建指令-20260911.md`，要求直接实施、保留业务边界、完成 C1–C6 对照截图并区分 Git 推送与服务器部署。
- 更新项目导航、当前状态和长期决策。本条只记录设计与实施文档，尚未修改业务页面，也未部署。

### 校园全站样式清理与弹窗核验（本地完成）

- 用户部署后反馈旧样式残留，重新按页面和编辑状态核验；原代表页面通过不代表所有弹窗通过。
- 抽取共用首页、班级切换、学生选择、弹窗键盘管理与复制帮助函数；校园样式拆为导航、控件、页面和手机文件。
- 首页采用原创教室场景插画，统一模块标题、指标条与输入控件；清理旧色值、装饰色条、阴影和手机套框。
- 修正新增班干部草稿编辑与选人、分组历史查看、嵌套弹窗顺序和关闭后残留inert；趋势与日程选人复用搜索选择器。
- 手机座位显示完整姓名并局部滚动；长文本框不再被单行输入高度覆盖。
- 统一入口与管理员页面的校园控件样式，补齐档案联系人和照护字段标签；考勤状态合并为分段控件，值日与周报去除重复套框。
- TypeScript、ESLint、生产构建、37/37测试、认证集成通过；19模块六宽度qa:strict共115项通过，新增长文本框真实DOM高度回归（六宽度与静态检查共7项）通过。认证测试首次被本地预览占用开发目录阻断，关闭预览后独立重跑通过。
- 独立视觉复审覆盖19模块桌面/手机及重点弹窗，提出的头像对比度、考勤按钮、值日套框、周报指标、选人标签五项问题已修正并复核通过。不代表所有状态组合或真实设备已穷尽验证。
- 本轮只使用临时合成数据，未操作正式数据库或生产服务器；完成核验后生成 `classroom-deploy-campus-ui-fullscan-20260911.tar.gz`，仅在本地校验，尚未上传或部署。

### 校园主题与听写复习（本地实现与验证）

- 建立共享主题与SVG组件、校园插画资源，独立实现听写编辑、词库、批改和个人筛选组件；不增加未完成的玻璃主题切换入口。
- 新增可选dictation数据，旧备份兼容，导出版本2并使用紧凑JSON；备份预检学生数改为所有班级总人数。
- 听写结果使用材料索引和确认状态，隔离家庭孩子与班级学生；服务器校验参与者归属和已批改材料快照。
- 听写提交改为服务器确认后更新正式结果，保存失败和并发冲突保留草稿与当前学生，确认成功才进入下一位；独立复听不覆盖历史。
- 接入首页、学生档案和可选作业关联；支持词库文本导入、参与者搜索分页、个人错词复习、历史和近30天加权统计。
- 抽取校园主题变量、公共按钮/指标条/分页/插画接口和独立听写页面，保留原18模块。玻璃主题仅预留，不显示切换入口。
- 保留5MB备份上限，听写提交4.8MB预检；两班各50人、100教学日、每日20词的模拟听写数据约1.43MiB，旧备份兼容与版本写入测试通过。
- TypeScript、ESLint、生产构建、37/37测试及认证集成通过；qa:strict的39项结果通过。浏览器覆盖班级/家庭隔离、105人搜索、保存失败、冲突与只读；代表桌面名单和手机批改经独立视觉复审修正后通过。
- 更新DESIGN与项目状态、长期决策；资料包与本机QA资料不入库。本条不代表服务器部署或线上验收，详细边界见PROJECT_STATUS。

### 建立持续项目记忆与 Git 工作流

- 新增根目录项目导航，以及 `PROJECT_STATUS`、`CHANGELOG`、`DEPLOYMENT_LOG`、`DECISIONS` 和开发导航。
- 更新 `AGENTS.md`：以后每个新对话自动读取项目状态；每次实际修改后自动更新记录、检查敏感文件、提交并推送。
- 扩充 `.gitignore`：部署包、压缩备份、QA 报告、对话恢复文件和本机设计审计资料不再进入新提交。
- 将 2026-07-31 后未持续提交的当前源码登记为一次历史补录基线，不伪造旧提交。
- 基线入库前重新执行 TypeScript、ESLint、静态布局审计和 `npm.cmd test`；29 项测试与认证集成均通过。
- 本地基线提交为 `41325fa`；旧内部 `origin` 因凭据不可用未能同步。
- 新增 GitHub 远端 `https://github.com/a18502656182-arch/teacher.git`。推送前检查完整 Git 历史，没有发现数据库、真实 `.env`、私钥或常见访问令牌；完整 `main` 已首次推送成功。
- GitHub 仓库为公开仓库；旧历史中的 15 个部署压缩包及当前约 10MB 的知识索引也会公开，后续禁止继续提交部署包、数据库、真实班级资料和密钥。

### 增加可复制的新对话交接提示词

- 在 `对话交接文档-班主任工作台-20260825.md` 第 30 节加入完整的新对话启动提示词，串联当前主对话、上一段主对话、项目文档、GitHub 基线、验证结果、部署边界和持续提交规则。
- 明确对话读取可能返回摘要或截断内容，不能仅凭提示词或聊天记忆宣称掌握全部事实；当前源码、`github/main` 和最新状态文档优先。
- 在项目导航和当前状态中补充该提示词入口，方便切换对话时直接复制。

## 2026-09-09（历史重建）

### 期中百人审计修复部署包

- 为 2026-09-06 审计后的当前源码重新完成 TypeScript、Lint、构建、29 项测试、认证集成与桌面/手机严格布局检查。
- 生成 `classroom-deploy-midterm-audit-fixes-20260909.tar.gz`，SHA-256 为 `F6D448BFDB59C9C02CC540AC7EC217688BF983A2F4941259ACDDFA20CCE1B016`。
- 记录中没有服务器部署完成证据，因此状态为“包已生成，部署待确认”。

## 2026-09-06（历史重建）

### 两班各 50 人的学期期中全流程审计与修复

- 使用两个班、每班 50 人的隔离测试数据，覆盖 18 个工作台模块、桌面/平板/手机、双班切换、搜索筛选、批量操作和错误分支。
- 修复移动端保存失败白屏，保留本机草稿并提供重试/载入最新版本。
- 课堂工具补齐真实分组结果、历史与复制；通知对象改为适合长名单的可搜索选择。
- 家校沟通新增记录不再预填虚构事实；通知删除改用统一危险确认。
- 考勤补充分页、搜索、状态筛选、批量操作和移动端适配。
- 新增学生、值日岗位和班干部岗位改为草稿确认后保存，避免生成半成品记录。
- 修复弹窗高度、可访问性、重复桌面/移动壳层以及相关 TypeScript 问题。

## 2026-09-05（历史重建）

### HTTP 与旧 WebView ID 兼容

- 定位考勤状态按钮白屏根因为非安全 HTTP 或旧 WebView 中缺少 `crypto.randomUUID()`。
- 增加共享 `makeId()` 降级方案，并处理考勤、日程、课堂工具、通知、学生档案和课程等同源写入流程。
- 增加禁用 `randomUUID` 条件下的回归检查。
- 生成 `classroom-deploy-http-id-compat-20260905.tar.gz`，SHA-256 为 `B650340EE46B4B7BD76AF1393EA608035B63C340F01DA759113E87E644E93133`。

## 2026-08-25 至 2026-08-29（历史重建）

### 工作台功能补齐与电脑/手机一致性

- 新增并深化考勤与请假、教师日程和工作留痕、学生档案、通知草稿、课堂工具、成绩长期趋势、题目/知识点分析与试卷识别确认流程。
- 新增独立“健康与照护”页面，并根据实际反馈精简字段、筛选、学生选择和弹窗布局。
- 重做考勤主名单、作业批量状态、值日教学日同步、侧栏完整展示及大量电脑/手机对齐问题。
- 建立正式空白工作区、账号隔离、设备限制、乐观保存、备份预检恢复和管理员管理能力。
- 详细过程仍可查阅根目录的专项计划、审核报告和 `对话交接文档-班主任工作台-20260825.md`。

## 2026-07-31 及以前

- Git 历史已记录初版工作台、SaaS 视觉体系、主要班务模块、积分/成长页面与严格布局审计。使用 `git log --oneline` 查看原始提交。
# 2026-09-12 追加：旧CSS选择器隔离工具

- 增加PostCSS/LightningCSS选择器AST迁移工具及独立浏览器开关探针；仅开发环境使用，未修改生产CSS入口。
- 4项CSS回归验证复杂选择器、文档根诊断、真实控件与全量旧声明保持；连同保存层回归共58项通过，认证集成/类型/ESLint/构建通过。
# 2026-09-12 追加：学生档案与关联删除边界

- 将学生档案表单控制器、纯数据应用和删除关联清理从旧大组件中提取，保持现有视图与workspace保存路径。
- 修复删除学生误删同一考试其他学生反思，以及误删其他场景空听写任务；新增6项跨班、家庭孩子、电话校验及关联清理回归。
