# Impeccable 全站体验改造执行计划

计划日期：2026-08-21

项目名称：班主任工作台

计划性质：可直接交给 AI 分阶段执行的前端改造方案。

问题基线：`上线前全站体验问题记录-班主任工作台-20260821.md`。

## 1. 改造目标

把当前可用但仍带有通用管理后台特征的界面，调整为适合班主任真实工作节奏、可以正式售卖的长期工作台：每位老师拥有独立用户、受保护的 workspace 和最多两台浏览器设备；打开页面后先看到需要处理的人和事，在少量操作内完成批量处理，编辑后明确知道是否已经写入自己的服务器数据，并能在手机、平板和桌面端稳定使用。

本轮不是重新发明业务，也不是单纯换颜色。核心顺序是：先建立用户身份、设备绑定和数据所有权，再消除数据丢失风险，然后调整信息架构和高频流程，统一弹窗、表单、颜色与页面细节，最后完成全站上线验收。

## 2. 执行边界

### 必须保持

- 现有 workspace 内部业务数据 JSON 格式和 15 个业务页面路由。
- 正式工作台继续使用 `/w/{workspaceToken}`，但 token 只作为定位符，不再作为授权凭据。
- 现有 15 个业务模块和真实业务能力。
- 课程日程的周一到周五按钮条。
- 值日岗位的周一到周五按钮条和台账状态按钮条。
- 作业详情学生高亮选择方式。
- 期末评语“学生列表 / 保存后的评语”两个入口。
- 已有电脑端 API key 读取方式，手机端 AI 编写继续复用同一配置。
- `/w/demo` 继续公开访问，但调整为只读演示环境。

### 明确禁止

- 不新增 npm 依赖，不执行 `npm install`。
- 不引入新的 UI 框架。
- 不为追求丰富而随机给列表项上色。
- 不把手机版重新做成电脑版表单的竖排版。
- 不把所有详情重新铺回主页面。
- 不把周一到周五按钮条和值日台账状态按钮条改成下拉。
- 不同时大改后端、数据结构和界面，避免无法定位回归来源。
- 不把手机号描述为已经验证的真实身份；第一版没有短信验证码，手机号只是与兑换码共同使用的账号标识。

### 本轮允许的基础设施改动

- 新增用户、兑换码、设备、会话和认证审计表。
- 给 `workspaces` 增加 `owner_user_id`。
- 新增认证与管理员接口。
- 对 workspace、资料库和 AI 等真实数据接口增加统一身份与所有权校验。
- 新增 Cookie、会话、设备和旧数据迁移逻辑。

## 3. 设计方向

访问模式：Operate，老师来这里是完成任务，不是浏览展示页。

结构原则：

- 首页围绕“今天要处理什么”。
- 学生问题围绕“这个学生发生了什么、下一步做什么”。
- 学期输出围绕“当前进度、待完成对象、已保存结果”。
- 设置与维护页面降低导航权重，但保留完整能力。

视觉原则：

- 主体保持清爽白底和深色文字，蓝色只承担主操作与当前选择。
- 增加克制的语义色：成长/完成用松绿色，待处理/异常用琥珀色，跟进/信息用湖蓝，学期输出用靛青，危险用红色，归档用中性灰。
- 普通列表不使用渐变；渐变只允许用于极少数顶部上下文区域，且必须保证文字对比度。
- 卡片只用于真正独立的对象或入口；工具栏、筛选区、页面分区不再包装成多层卡片。
- 手机端优先紧凑列表、分段控件和底部弹窗，桌面端优先表格/列表加详情面板。

## 4. 商业化用户、设备与数据隔离方案

### 4.1 用户看到的入口

公开首页只保留两个主要按钮：

```txt
进入我的工作台
查看演示
```

- “查看演示”直接进入只读 `/w/demo`。
- “进入我的工作台”先请求 `GET /api/auth/me`。
- 已有有效 session，或当前浏览器设备令牌仍有效时，直接进入当前用户 workspace。
- 没有可恢复身份时，在当前页面打开弹窗，不新增注册页和登录页。

进入弹窗固定文案：

```txt
进入班主任工作台
```

字段：

```txt
兑换码
手机号
```

主按钮：

```txt
进入
```

辅助文案：

```txt
首次进入会绑定当前设备，之后本设备可直接打开。
```

界面文案不使用“激活”。后台可以使用“兑换、绑定、会话”等技术概念，用户只感知“进入”。

### 4.2 设备绑定的准确边界

网页不能稳定识别一台物理电脑或手机。这里的“设备”实际指一个保存了持久设备 Cookie 的浏览器配置文件。

- 同一手机上的 Chrome 与微信内置浏览器会被视作两台设备。
- 清除 Cookie、使用无痕模式或重装浏览器后会被视作新设备。
- 不使用浏览器指纹绕过这一限制，避免隐私风险和误判。
- 管理员必须能查看设备名称、最近使用时间并解绑旧设备。
- 用户辅助文案保持简洁；在设备数量超限和帮助说明中再解释“浏览器设备”。

第一版每个兑换码默认最多绑定 2 个有效设备，适合“一台电脑 + 一部手机”。`max_devices` 保留为可配置字段。

### 4.3 核心业务流程

首次进入：

```txt
兑换码 + 手机号
→ 原子校验兑换码
→ 创建或认领用户
→ 创建或认领 workspace
→ 绑定当前浏览器设备
→ 创建 session
→ 设置安全 Cookie
→ 进入工作台
```

同一浏览器再次进入：

```txt
点击“进入我的工作台”
→ session 有效则直接进入
→ session 过期但设备令牌有效，则自动续建 session 后进入
→ 两者都无效才显示进入弹窗
```

换设备：

```txt
再次输入原兑换码 + 原手机号
→ 确认兑换码已属于该用户
→ 有空余设备名额则绑定新浏览器
→ 创建 session 并进入
```

设备超限：

```txt
当前兑换码已绑定 2 台设备，请联系管理员解绑旧设备。
```

退出当前设备：

- `POST /api/auth/logout` 同时撤销当前 session 和当前设备，清除两个 Cookie，并释放一个设备名额。
- 如果以后需要“仅退出但继续信任本设备”，应另设动作，第一版不混用两种含义。

### 4.4 数据模型

保留现有 workspace 业务 JSON，不拆分学生、作业等业务表。本阶段只增加用户归属与认证数据。

```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  phone TEXT NOT NULL UNIQUE,
  status TEXT NOT NULL DEFAULT 'active',
  created_at INTEGER NOT NULL,
  last_login_at INTEGER
);
```

```sql
CREATE TABLE redeem_codes (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  code_hash TEXT NOT NULL UNIQUE,
  code_hint TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'active',
  max_devices INTEGER NOT NULL DEFAULT 2,
  used_by_user_id INTEGER REFERENCES users(id),
  target_workspace_id INTEGER REFERENCES workspaces(id),
  used_at INTEGER,
  expires_at INTEGER,
  created_at INTEGER NOT NULL
);
```

`target_workspace_id` 是现有数据迁移所必需的补充字段：新兑换码为空，首次兑换时创建 workspace；迁移兑换码指向已有 workspace，老师首次进入时认领原有数据。

```sql
CREATE TABLE devices (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id),
  device_token_hash TEXT NOT NULL UNIQUE,
  device_name TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'active',
  created_at INTEGER NOT NULL,
  last_seen_at INTEGER NOT NULL,
  revoked_at INTEGER
);
```

```sql
CREATE TABLE sessions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id),
  device_id INTEGER NOT NULL REFERENCES devices(id),
  token_hash TEXT NOT NULL UNIQUE,
  expires_at INTEGER NOT NULL,
  created_at INTEGER NOT NULL,
  last_seen_at INTEGER NOT NULL,
  revoked_at INTEGER
);
```

```sql
CREATE TABLE auth_attempts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  lookup_hash TEXT NOT NULL,
  outcome TEXT NOT NULL,
  created_at INTEGER NOT NULL
);
```

`auth_attempts` 只保存经过 HMAC 的查询键和结果，不保存明文兑换码、完整手机号或原始 IP，用于服务重启后仍有效的限流和异常追踪。

`workspaces` 增加：

```sql
ALTER TABLE workspaces ADD COLUMN owner_user_id INTEGER REFERENCES users(id);
```

索引要求：

- `users(phone)` 唯一索引。
- `redeem_codes(code_hash)` 唯一索引。
- `devices(device_token_hash)` 唯一索引。
- `sessions(token_hash)` 唯一索引。
- `workspaces(owner_user_id)` 普通索引；第一版一个用户一个 workspace 时增加非空值唯一约束。
- `auth_attempts(lookup_hash, created_at)` 组合索引。

### 4.5 令牌与 Cookie 安全

- 兑换码使用密码学安全随机数生成，至少 128 bit 熵，展示时可分组方便输入。
- 数据库只保存兑换码的 HMAC-SHA-256 结果和末 4 位提示；明文只在管理员创建成功时返回一次。
- session token 与设备 token 至少 256 bit，数据库只保存 SHA-256 或 HMAC 结果。
- session Cookie：`HttpOnly`、生产环境 `Secure`、`SameSite=Lax`、`Path=/`，建议 30 天滑动过期。
- device Cookie：同样使用 `HttpOnly`、`Secure`、`SameSite=Lax`，有效期可设 365 天，直到管理员或用户撤销。
- 认证、保存和管理员写接口校验 `Origin`，只接受同源 JSON 请求。
- 日志禁止记录兑换码、session token、设备 token和完整手机号。
- `POST /api/auth/enter` 按兑换码查询键、手机号查询键和来源查询键限流；错误提示不区分“手机号存在”与“兑换码属于他人”，避免枚举用户。
- 兑换码兑换、用户绑定、workspace 归属和设备计数必须在同一数据库事务中完成，避免并发重复兑换或突破设备上限。

### 4.6 认证接口

#### `GET /api/auth/me`

- 检查 session Cookie。
- session 失效时检查持久设备 Cookie；设备有效则自动创建新 session。
- 成功返回用户基础状态、有效设备摘要和当前 workspace 路径。
- 未登录返回 401，不创建空用户。
- 已禁用用户或设备返回 403，并清除无效 Cookie。

建议响应：

```ts
type AuthMeResponse = {
  user: { id: number; phoneMasked: string };
  workspace: { token: string; path: string };
  device: { id: number; name: string };
};
```

#### `POST /api/auth/enter`

请求：

```ts
type EnterRequest = {
  redeemCode: string;
  phone: string;
};
```

规则：

- 手机号先去空格和分隔符，第一版按中国大陆 11 位手机号校验。
- 新兑换码 + 新手机号：创建用户、workspace、设备和 session。
- 迁移兑换码 + 新手机号：把指定旧 workspace 归属给新用户，不创建空 workspace。
- 已使用兑换码 + 同一手机号：在设备数量允许时绑定新设备。
- 已使用兑换码 + 不同手机号：返回通用“兑换码或手机号不正确”。
- 已存在用户已拥有 workspace，再输入另一张普通新建兑换码：第一版拒绝并提示直接进入，避免意外创建第二份数据；续费和多 workspace 另设产品规则。
- 超过设备数返回 409 和固定用户文案。

#### `POST /api/auth/logout`

- 撤销当前 session 与 device。
- 清除 session 和 device Cookie。
- 返回首页，不删除用户和 workspace 数据。

#### 管理员接口

- `POST /api/admin/redeem-codes`：生成新兑换码或指定已有 workspace 的迁移兑换码。
- `PATCH /api/admin/redeem-codes/{id}`：禁用、恢复或调整设备数。
- `PATCH /api/admin/users/{id}`：禁用或恢复用户。
- `GET /api/admin/users/{id}/devices`：查看设备名、状态和最近使用时间。
- `DELETE /api/admin/devices/{id}`：解绑设备并撤销其全部 session。

所有管理员接口继续使用独立 `ADMIN_KEY` 或后续管理员 session，不能复用普通用户 Cookie。现有 `/api/admin/workspaces` 在迁移完成后标记为内部兼容接口，不能继续创建无所有者的正式 workspace。

### 4.7 正式数据接口权限

统一实现 `requireWorkspaceOwner(request, token)`：

1. 读取并验证 session。
2. 验证用户状态为 active。
3. 验证 session 关联 device 为 active。
4. 查询 workspace，并验证 `owner_user_id === session.user_id`。
5. 验证 workspace 状态与到期时间。
6. 非所有者访问返回不泄露资源是否存在的通用 404 或 403。

必须接入：

- `GET /api/workspace/[token]`
- `PUT /api/workspace/[token]`
- `GET /api/knowledge/[token]`
- AI 评语和未来所有读取真实学生数据的接口。

演示例外：

- `/w/demo` 的 GET 返回固定演示数据，不关联任何真实用户。
- `/api/workspace/demo` 的 PUT 明确返回 403“演示数据不能保存”。
- 演示页面的保存按钮改为“演示模式”提示或禁用，不得产生看似成功的保存反馈。

### 4.8 旧数据迁移

当前已有 workspace 没有手机号，不能自动猜测所有者。

迁移流程：

1. 先生成数据库一致性备份，并验证可恢复。
2. 以可空字段方式增加 `owner_user_id` 和新认证表，旧系统暂时仍可读取。
3. 管理员为每个现有正式 workspace 生成带 `target_workspace_id` 的迁移兑换码。
4. 老师用迁移兑换码和手机号首次进入后，原 workspace 原子绑定到该用户。
5. 完成认领检查后，再启用正式数据接口的强制 session 所有权校验。
6. 未认领 workspace 在截止日期后暂停访问，不删除数据。
7. `demo` 永远保持 `owner_user_id = NULL`，并走只读例外逻辑。

部署必须分成“加表兼容”和“启用强制权限”两个包，不能在没有迁移兑换码的情况下直接切断现有用户。

### 4.9 最小前端改动

- `app/page.tsx` 删除功能总览、模块卡片和多余入口，首屏只保留品牌、简短说明以及“进入我的工作台 / 查看演示”两个按钮。
- 新增进入弹窗组件，桌面居中弹窗，手机底部弹窗；字段只有兑换码和手机号。
- 点击“进入我的工作台”先检查 `/api/auth/me`，检查期间按钮显示“正在进入”，不能先闪现空弹窗。
- 工作台手机顶部或“更多”中增加“我的工作台”，用于查看掩码手机号、当前设备和退出当前设备。
- 正式工作台收到 401 时回到首页并打开进入弹窗；403 或设备撤销时显示明确恢复入口。
- 当前错误状态页增加“重新输入兑换码”和“查看演示”。
- 不新增注册、登录、忘记密码页面。

## 5. 目标交互模型

### 保存模型

采用“模块提交即持久化 + 全局状态兜底”的单一真相模型。

1. 用户在模块弹窗点击“保存学生”“保存评语”“完成并归档”等按钮。
2. 前端生成下一份 workspace 数据，并立即调用统一持久化函数。
3. 按钮进入“保存中”，成功后显示“已保存”，失败时保留当前草稿并显示“重试”。
4. 顶部全局状态只显示“已同步 / 有未保存修改 / 保存失败”，不再与模块按钮形成两个互相矛盾的保存入口。
5. 用户关闭标签页或切换班级前，如果仍有未保存修改，必须拦截并说明风险。

实现建议：

- 抽取 `persistWorkspace(nextWorkspace)`，只负责 PUT、成功态和失败态。
- 抽取 `updateAndPersist(updater, options)`，计算下一状态后同时更新本地状态和服务器。
- 对连续输入使用本地草稿，不在每次按键时请求；只在明确提交、关闭编辑器或必要的防丢自动保存点写入。
- 使用 ref 保存最新 workspace，避免 React 状态更新时序导致提交旧数据。
- 全局 `save()` 作为失败重试和未提交编辑的兜底，而不是第二套保存含义。

### 首页与导航模型

桌面端导航分组：

- 今日：工作台、作业追踪、值日岗位。
- 学生：学生名单、成绩分析、积分评价、成长档案、家校沟通、考试反思。
- 输出：班级周报、期末评语。
- 班级设置：课程日程、座位分组、班干部、积分规则。

手机版底部保留：首页、学生、作业、成绩、更多。

“更多”内部按“学生工作 / 班级运行 / 学期输出 / 规则设置”分组。首页不再重复完整模块宫格，只保留最多 4 个最近使用或当前高频入口。

### 手机版高频模型

- 作业：默认全班已交或使用上一状态，老师只修改未交、待订正、已复查等异常。
- 值日：默认完成，点击异常岗位再选择返工并补备注。
- 成绩：默认查看未录和异常，录入同分学生时先多选，再出现底部批量录分。
- 长期列表：主页面只呈现搜索、核心筛选和列表；详情、编辑和低频操作进入底部弹窗。
- 批量栏：只有选择学生后出现，固定在底部导航上方，不遮挡列表最后一项。

## 6. 分阶段实施计划

### Phase 0：建立回归基线与变更保护

目标：在改代码前固定测试基线，避免后续视觉调整破坏用户已经确认的功能。

任务：

- 记录 15 个 `page=` 入口和关键弹窗清单。
- 保存桌面 1440×900、平板 768px、手机 390×844 的基线截图。
- 在 QA 脚本中保留横向溢出、文本溢出和弹窗遮挡检查。
- 为以下用户要求增加静态或交互断言：值日按钮条未改下拉、日程星期按钮条未改下拉、作业多选基于已选学生、评语编辑不出现学生和学期下拉。
- 确认当前构建与 QA 通过后再开始 Phase 1。

涉及文件：

- `scripts/layout-audit.mjs`
- `tests/rendered-html.test.mjs`
- 现有浏览器 QA 脚本或新增项目内无依赖脚本。

完成标准：基线结果可重复，后续每个阶段都能判断是否发生功能回退。

### Phase 1：建立用户身份、设备绑定和 workspace 所有权

目标：先把当前“知道链接即可读写”升级为可以商业销售的用户隔离系统，同时保持用户入口只有一次轻量输入。

任务：

- 新增认证数据库 schema、索引和可重复执行的迁移函数。
- 给数据库封装增加事务能力，确保兑换与设备计数原子执行。
- 新增 `lib/auth.ts` 或等价认证模块，集中处理哈希、Cookie、session、设备恢复和所有权校验。
- 实现 `/api/auth/me`、`/api/auth/enter`、`/api/auth/logout`。
- 实现兑换码生成、禁用用户和解绑设备的管理员接口。
- 首页改为两个按钮与进入弹窗，演示入口保持公开。
- 所有正式数据 API 接入 `requireWorkspaceOwner`。
- 把 demo 改为明确只读。
- 为已有 workspace 生成迁移兑换码并完成归属迁移工具。
- 建立认证限流、错误日志脱敏和失效 session 清理策略。

涉及文件：

- `db/schema.ts`
- `db/workspaces.ts`
- `db/index.ts`
- 新增 `db/auth.ts`
- 新增 `lib/auth.ts`
- `app/page.tsx`
- `app/globals.css`
- 新增 `app/api/auth/me/route.ts`
- 新增 `app/api/auth/enter/route.ts`
- 新增 `app/api/auth/logout/route.ts`
- 新增或扩展 `app/api/admin/*`
- `app/api/workspace/[token]/route.ts`
- `app/api/knowledge/[token]/route.ts`
- `app/api/ai/comment/route.ts`
- `app/w/[token]/ClassroomApp.tsx`

完成标准：两个不同用户不能互相读取或保存 workspace；同一浏览器可自动进入；第三个设备被正确拒绝；管理员解绑后可重新绑定；旧 workspace 数据不丢失。

### Phase 2：修复保存语义与数据安全

目标：消除“模块说已保存，但服务器尚未保存”的最高风险问题。

任务：

- 在 `ClassroomApp` 顶层建立统一持久化状态：`idle | dirty | saving | saved | error`。
- 抽取 `persistWorkspace` 和 `updateAndPersist`，避免模块各自拼接保存提示。
- 把“已保存，记得保存”统一替换为准确状态：成功后“已保存”，未持久化时“待同步”，失败时“保存失败，点击重试”。
- 所有模块提交按钮接入统一持久化链路。
- 加入 `beforeunload` 未保存拦截；切换班级、关闭弹窗和离开编辑器时处理未提交草稿。
- 保存失败不关闭弹窗、不清空输入、不覆盖上次服务器数据。
- 对批量录分、批量状态和删除后保存显示具体对象数量，便于老师确认。

重点页面：期末评语、家校沟通、考试反思、成绩分析、作业追踪。

涉及文件：

- `app/w/[token]/ClassroomApp.tsx`
- `app/workbench-repair.css`
- 必要时扩展现有 API 错误响应文案，但不改接口结构。

完成标准：页面上不再出现矛盾的两个“保存”；断网后数据仍保留在当前编辑器并可以重试。

### Phase 3：统一弹窗、操作层级和表单基础组件

目标：先修共享结构，再迁移单页，避免 15 个页面分别打补丁。

任务：

- 把 `MobileInfoSheet`、学生选择弹窗和桌面详情弹窗统一为共享 `DetailSheet` 行为。
- 每次实例生成唯一标题 ID。
- 支持 Esc 关闭、焦点锁定、关闭后恢复触发按钮焦点、背景 inert、点击遮罩关闭可配置。
- 弹窗头部只保留对象名称和状态；底部固定主操作。
- 抽取 `SheetSection`，用间距和标题分区，不再每区套卡片。
- 抽取 `ActionGroup`：主操作一个、次操作最多两个、低频操作进“更多”。
- 危险操作统一红色文字或危险按钮，并保持二次确认。
- 抽取持久表单帮助文本，label、错误、帮助信息不再只依赖 placeholder。
- 统一空状态、加载状态、失败重试和保存中状态。

建议内部组件：

- `DetailSheet`
- `SheetSection`
- `ActionGroup`
- `FieldMessage`
- `SaveStatus`
- `EmptyState`
- `StatusBadge`

完成标准：键盘可完整操作弹窗；同一页面打开嵌套详情时无重复 ID；手机弹窗底部按钮不会被底部导航遮挡。

### Phase 4：调整工作台首页与全站导航信息架构

目标：从模块目录改成老师每天的处理入口。

任务：

- 今日工作台首屏按优先级显示：今日待处理、异常学生、待跟进记录、今日值日。
- 每条待办直接进入对应对象和操作位置，不只跳到模块首页。
- 桌面侧栏按“今日 / 学生 / 输出 / 设置”分组，低频设置默认收起或视觉降级。
- 手机版首页删除与底部导航重复的完整入口集合。
- “更多”页面按业务场景分组，并显示最近使用，不使用 10 多个同权卡片。
- 建立学生中心跳转：从学生详情可看到作业、成绩、积分、沟通、反思和评语的摘要，并能进入对应记录。

完成标准：新用户不需要先理解模块边界，也能从今日待办或学生对象继续工作。

### Phase 5：高频页面效率重构

#### 4.1 作业追踪

- 主页面保留科目、时间、内容搜索，默认收起低频筛选。
- 作业详情顶部只显示任务名称、日期、科目和异常数量，不再放大面积状态卡。
- 提供“全班已交”或“沿用上次状态”的快捷起点，再处理异常学生。
- 学生行只显示当前状态和一个主要操作；完整状态选择在点击行或批量栏中完成。
- 多选后显示固定批量栏；保留整行高亮选中。
- 编辑作业、删除作业放入详情“更多”，不与学生处理抢主次。

#### 4.2 成绩分析

- 默认视图优先“未录入 / 异常分数 / 重点学生”，全班完整成绩作为次视图。
- 手机筛选弹窗只保留科目、分数区间、重点状态和排序，选项按当前考试真实科目生成。
- 批量录分只在多选学生后出现，并明确显示人数、科目、分数和覆盖影响。
- 单人录分进入底部弹窗，不在列表展开多科目输入。
- 桌面长建议改为可换行文本区或详情侧栏；长学期名显示完整值和 tooltip。

#### 4.3 值日岗位

- 保留周一到周五按钮条和台账状态按钮条。
- 今日检查默认按“完成”处理；异常时选择“需返工”并补充备注。
- 单岗位行只保留一个主操作，指定学生、编辑岗位、停用进入详情。
- 台账搜索、复制和备注编辑分级，复制放入更多。

#### 4.4 学生名单

- 普通浏览与批量选择模式明确区分，默认不显示批量按钮。
- 手机继续使用紧凑列表，不增加大头像框。
- 搜索与小组筛选保持同一行平均分；窄屏保证长占位文案不会挤压小组选择。
- 详情弹窗按“基础资料 / 联系方式 / 班级信息 / 关联记录”分区。

完成标准：32 名学生场景下，常见批量任务不需要逐人完成；主操作始终清楚。

### Phase 6：学生证据与长期输出页面重构

#### 5.1 成长档案

- 主页面只显示搜索、小组筛选、学生列表。
- 点击学生后，弹窗第一屏显示姓名、小组、最近记录和“添加成长记录”主按钮。
- 自动摘要改为次级入口，不得压过人工记录。
- 新增记录标题必须包含学生姓名；保存后回到该学生时间线顶部。
- 筛选记录类型和时间范围放在学生详情内部，不占学生列表首屏。

#### 5.2 家校沟通

- 列表优先显示待跟进，再显示已跟进和已归档。
- 新增/编辑弹窗按“学生与时间 / 沟通内容 / 家长反馈 / 后续跟进”分区。
- 日期和电话加入字段级验证；错误不清空已输入内容。
- 长摘要允许两行，超出后省略并在详情显示完整内容。
- 同步成长档案的结果必须明确显示成功或失败。

#### 5.3 考试反思

- 保留考试切换、学生状态和填写弹窗。
- 默认优先未填写与草稿，已完成折叠或进入次视图。
- 删除薄弱科、选择反思对象和成绩页入口的既定决定继续保持。
- 完成并归档到家校沟通时显示归档对象和结果。

#### 5.4 期末评语

- 手机继续使用“学生列表 / 保存后的评语”双入口，不在主页面铺全部内容。
- 主页面只选择一次学期；编辑弹窗不再出现学生或学期下拉。
- 学生列表显示姓名、学号、小组和填写状态，不显示成绩与作业。
- 默认优先未填写，但提供“全部 / 未填写 / 已保存”轻量筛选，避免 32 人形成压力墙。
- AI 编写只在编辑场景出现，读取电脑端已配置 API key；生成失败保留原草稿。
- 依据选择、AI 生成、人工编辑、保存四步在弹窗中形成清晰顺序。
- 保存后的评语支持搜索、按学期查看、点击继续编辑；复制当前列表放入更多。

#### 5.5 班级周报

- 保留“概览 / 周报库”正向结构。
- 减少内部卡片嵌套，用分区和列表行表达层级。
- 复制正文、归档等操作放在详情或编辑器底部。
- 周次和当前状态始终可见，避免编辑错周。

完成标准：30 条以上沟通、评语和周报仍能快速查找；老师无需在两个长区域之间上下滚动。

### Phase 7：管理型页面与桌面端精修

#### 6.1 课程日程

- 保留当前课表时间轴和星期按钮条。
- 月份与周次改成紧凑上下文栏，避免形成大表单卡。
- “配置”进入更多或独立设置弹窗；“编辑当天”作为当前周的主操作。
- 课程、活动、重点使用不同但固定的语义色，不按星期随机上色。

#### 6.2 座位分组

- 桌面和手机都以座位图为主视觉。
- 智能排座作为主操作；轮换、按座位分组、配置进入次级操作区。
- 点击座位进入学生选择或详情；不恢复底部搜索座位板块。
- 自动排座前说明影响范围，完成后支持一步撤销。

#### 6.3 班干部

- 手机“新增班委 / 新增小组长”保持等宽同高并组成一个按钮组。
- 复制聘任书、停用、删除进入岗位详情。
- 长任期允许换行或完整 tooltip，不在窄列中硬塞单行。
- 未设置小组长显示为紧凑提示，不做大面积空卡。

#### 6.4 积分评价与积分规则

- 积分评价只保留记分和常用规则；学生和规则选择使用适合大量选项的弹窗。
- 常用规则改为可搜索规则清单，分类不出现横向滚动。
- 积分规则桌面端取消筛选区域框套框，使用一条工具栏连接列表。
- 规则行层级固定为：场景/分类、规则名称与理由、分值、状态。
- 启用、停用、复制、删除进入详情；新增规则是唯一主按钮。

完成标准：低频管理页完整但不抢日常工作入口；桌面端无长文本截断和多层边框。

### Phase 8：视觉系统、文案与上线精修

任务：

- 在 `app/workbench-repair.css` 建立明确的语义色 token，替换随机或位置相关颜色。
- 统一主按钮、次按钮、文本按钮、危险按钮、选中状态、焦点环和禁用状态。
- 统一桌面表格行高、手机列表行高、表单 label、帮助文本和错误文本。
- 删除无意义的“本页概览”、功能解释和 AI 模板式长文案。
- 将“名、作、分”等占位式单字图标替换为统一入口语言。
- 修复 favicon 404。
- 检查中文长词、日期、学期名、学生姓名和 200 字以上文本的换行。
- 检查高对比度、灰度和 200% 缩放下的可读性。

完成标准：颜色丰富但语义固定；同一业务状态在全站保持一致；不再依靠渐变和多层卡片制造层级。

## 7. 公共组件与内部类型建议

允许新增前端内部类型，不影响 API：

```ts
type SaveState = "idle" | "dirty" | "saving" | "saved" | "error";

type ActionTone = "primary" | "secondary" | "text" | "danger";

type SemanticTone =
  | "neutral"
  | "info"
  | "success"
  | "warning"
  | "danger"
  | "output";

type PersistOptions = {
  successMessage?: string;
  closeOnSuccess?: boolean;
  preserveDraftOnError?: boolean;
};

type AuthState =
  | { status: "checking" }
  | { status: "anonymous" }
  | { status: "authenticated"; workspacePath: string }
  | { status: "blocked"; reason: string };
```

组件优先级：

1. `SaveStatus` 与统一持久化函数。
2. `DetailSheet` 与焦点管理。
3. `ActionGroup` 与危险操作规范。
4. `FieldMessage` 与字段验证。
5. `MobileEntityRow`、`StatusBadge`、`EmptyState`。
6. 页面专属结构，只有出现两次以上真实重复时再抽象。

## 8. 测试计划

### 每个阶段执行

```bash
npm.cmd run build
```

```bash
npm.cmd run qa:layout
```

```bash
npm.cmd run qa:strict
```

```bash
node E:/文档/Codex/小红书/.agents/skills/impeccable/scripts/detect.mjs --json app/w/[token]/ClassroomApp.tsx
```

### 浏览器视觉验收

视口：

- 手机：390×844。
- 平板：768×1024。
- 桌面：1280×800 与 1440×900。

数据量：

- 学生 32 人。
- 小组 8 个。
- 作业 30 项以上。
- 沟通记录 30 条以上。
- 评语 30 条以上。
- 积分规则 20 条以上。
- 考试 3 次以上。
- 多个学期。

页面：

- `dashboard`
- `students`
- `homework`
- `scores`
- `points`
- `rules`
- `growth`
- `weekly`
- `schedule`
- `seating`
- `duty`
- `cadres`
- `records`
- `reflection`
- `comments`

关键弹窗：

- 学生编辑与批量编辑。
- 作业详情、编辑作业、批量状态。
- 成绩筛选、单人录分、批量录分。
- 积分选择学生、选择规则。
- 成长档案详情与新增记录。
- 课程编辑、座位详情、值日指定学生。
- 班干部详情、家校沟通新增与编辑。
- 考试反思填写、期末评语编辑与 AI 生成。

### 功能场景验收

- 新兑换码首次进入后创建用户、workspace、设备和 session。
- 同一浏览器关闭后再次进入无需重复输入。
- session 过期但设备有效时可自动恢复 session。
- 第二台设备绑定成功，第三台设备返回指定超限文案。
- 管理员解绑旧设备后，新设备可绑定。
- 错误兑换码、错误手机号、过期码、禁用码和高频尝试均有安全反馈。
- 用户 A 修改 URL 为用户 B 的 workspace token 时无法读取或保存。
- 禁用用户、撤销设备、过期 session 后立即失去正式数据权限。
- demo 可公开读取但不能保存。
- 迁移兑换码认领已有 workspace 后，学生、作业、成绩和评语数据完整。
- 网络正常保存、慢速保存、保存失败、重试成功。
- 有未保存修改时刷新、关闭、切换班级。
- 批量操作 0 人、1 人、32 人。
- 删除前确认、取消删除、删除后保存失败。
- 空数据、单条数据、30 条以上数据。
- 超长姓名、超长学期、超长建议、200 字以上评语。
- 键盘 Tab 顺序、Esc 关闭、关闭后焦点恢复。
- 颜色关闭或灰度模式仍能识别状态。

## 9. 阶段验收门槛

每个 Phase 只有满足以下条件才能进入下一阶段：

- 构建、布局和严格 QA 全部通过。
- 本阶段涉及页面的桌面与手机截图已检查。
- 没有新增横向滚动、文字遮挡、框套框和重复主按钮。
- 用户已明确确认的功能与按钮条没有被改变。
- 保存失败不会丢失草稿。
- 正式数据接口不存在只校验 token、不校验 session 与 owner 的遗漏。
- 数据库迁移已在备份副本演练并验证可回滚。
- 不通过扩大 CSS 覆盖层级临时压住问题；新增样式必须有清晰作用域。

## 10. 推荐实施批次

为了便于回滚和部署，建议拆成 8 个修改包：

1. `auth-foundation-migration`：认证表、owner 字段、事务与认证工具，暂不强制拦截旧 workspace。
2. `auth-entry-isolation`：两按钮首页、进入弹窗、认证接口、设备限制、正式接口强制所有权和 demo 只读。
3. `save-safety`：保存语义、未保存拦截、失败重试。
4. `shared-sheet-form`：弹窗、焦点、操作层级、字段消息。
5. `navigation-dashboard`：工作台首页与导航分组。
6. `high-frequency-flows`：学生、作业、成绩、值日。
7. `evidence-output-flows`：成长、沟通、反思、评语、周报。
8. `management-visual-polish`：课程、座位、班干部、积分规则、全站视觉精修。

每个包都应单独构建、截图、验收和打包，不应在一个部署包中混入下一阶段未完成内容。

## 11. 完成定义

本计划完成不以“页面颜色变了”或“QA 通过”作为唯一标准，而以以下结果为准：

- 用户不需要注册页或登录页，只通过兑换码和手机号首次进入。
- 每个正式 workspace 都有明确所有者，正式数据接口不再信任 URL token。
- 同一浏览器可自动进入，设备超限、解绑和换设备流程可被管理员处理。
- 两个用户之间无法读取、修改或推断对方 workspace 数据。
- 老师能明确知道哪些内容已经写入服务器。
- 老师可以在课间用手机快速处理全班正常、少数异常的任务。
- 新用户可以从今日待办或学生对象完成工作，不必先理解 15 个模块的边界。
- 桌面端适合大量列表和长文本，手机端适合单手、分步和弹窗编辑。
- 长期积累多个学期、考试和记录后，页面仍然易查找、易继续、易回顾。
- 全站色彩更丰富但不花哨，每一种颜色都对应稳定业务含义。
- 关键弹窗满足键盘与读屏基本要求。
- 所有自动化、视觉和真实交互验收通过，且无已知 P1 问题遗留。

## 12. 2026-08-21 实施结果（HTTPS 暂缓版）

本计划除 HTTPS 证书和 Nginx 443 配置外已实施。当前 HTTP 认证只能作为测试或过渡版本，不能标记为安全商业正式版。

已完成：

- 新增用户、兑换码、浏览器设备、会话、登录尝试、AI 日用量表及 workspace 所有者字段。
- 首次兑换、设备计数、workspace 与 session 创建使用同一 SQLite 事务。
- 首页只保留“进入我的工作台”和“查看演示”，增加兑换码与手机号进入弹窗。
- 增加用户认证、管理员会话、兑换码、用户状态、设备解绑和续期接口及 `/admin` 页面。
- 正式 workspace、知识库和 AI 接口改为 session、用户、设备、所有权与有效期联合校验。
- demo 固定只读，禁止保存，AI 只返回静态示例。
- 增加显式 HTTP 临时开关；未设置 `AUTH_ALLOW_INSECURE_HTTP=true` 时 HTTP 进入会明确失败。
- 增加 AI 首次数据确认、关闭开关和每日 20 次额度。
- 保存改为服务器确认后提示；失败保留草稿，退出前保存失败会中止退出。
- 首页、导航、15 个业务页面、关键弹窗及桌面和手机版工作流按本计划完成精修。
- 弹窗补齐唯一标题、Esc、Tab 焦点锁定、背景 inert 和焦点恢复。

本地安全演练已通过：

- 第一、第二浏览器绑定成功，第三浏览器返回 409。
- 用户 A 访问用户 B workspace 返回 404。
- 同一未使用兑换码并发提交只允许一个用户成功。
- 缺少 Origin 的写请求返回 403。
- demo PUT 返回 403，demo AI 不产生外部请求。
- session 失效后可由有效设备令牌恢复。
- workspace 到期进入只读，管理员续期后恢复写入。
- 用户被禁用后认证立即失效。

最终验收：

- `npm.cmd test`：6/6 通过，包含完整生产构建。
- `npm.cmd run qa:layout`：通过。
- `npm.cmd run qa:strict`：通过。
- Impeccable 源码检测：0 项。
- 浏览器视觉检查：390×844、768×1024、1440×900；15 个页面无横向溢出，关键弹窗已检查。
- Impeccable URL 检测器因项目未安装 Puppeteer 无法执行；遵守“不新增依赖、不执行 npm install”，改用已连接浏览器完成真实渲染审核。

部署前仍需完成的外部操作：

- 在服务器生成并设置真实随机密钥和管理员公网 IP 白名单，不能使用 `.env.example` 占位值。
- 停止服务或使用 SQLite 在线备份后备份 `/www/classroom/data/classroom.db`，并验证备份可读取。
- 当前 HTTP 阶段设置 `AUTH_ALLOW_INSECURE_HTTP=true`；未来 HTTPS 就绪后改为 `false` 并启用 HTTP 到 HTTPS 跳转。
- 反向代理必须覆盖而不是透传客户端伪造的 `X-Forwarded-For`、`X-Forwarded-Host` 和 `X-Forwarded-Proto`。

## 13. 2026-08-24 数据可靠性与售后流程补强

在原计划基础上完成以下补强：

- 乐观并发版本、服务器历史版本、本机草稿恢复和冲突分流。
- 跨班课程表、座位、沟通和学生证据隔离。
- 管理员审计、搜索分页、用户完整导出、永久删除和手机号纠错。
- SQLite 一致性备份脚本、备份保留数量和迁移版本记录。
- 隐私与数据说明、AI 超时退额、关闭授权和失败保留草稿。
- 认证端到端自动测试与 15 页隐藏裁切检测。

用户明确暂缓：

- HTTPS 证书与 443/Nginx 配置。
- 正式新用户首次进入时去除演示学生数据。
- 兑换码销售订单、批次、渠道、退款和续费闭环。

上述三项不得在后续交接中标记为已完成；尤其当前 HTTP 模式仍只能作为测试或过渡环境。
