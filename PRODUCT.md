---
name: 班主任工作台
status: active-development
lastUpdated: 2026-09-13
---

# Product

## Platform

班主任工作台是面向中小学班主任的响应式 Web 应用，覆盖桌面、平板和手机浏览器。

- 桌面端用于批量维护、对比分析、长表单编辑和长期资料整理。
- 手机端用于课堂、办公室和家校沟通场景中的快速查询、标记、记录和跟进。
- `/w/demo` 是公开只读演示环境；正式工作台必须与已认证用户绑定。
- `/admin` 是兑换码、用户期限和设备管理入口，不属于教师日常工作台。

## Stack

- React 19、Next.js 16、Vinext、Vite、TypeScript。
- SQLite + Drizzle ORM 保存认证、用户、兑换码、设备、会话和 workspace 数据。
- 当前教师界面由 `app/components/workbench/WorkbenchShell.tsx` 统一承载壳层，`app/w/[token]/ClassroomApp.tsx` 编排页面控制器与按需模块，业务规则限定在页面组件及路由级 `ClassroomPages.module.css`；旧全局 UI 生成链已经退出。
- 当前项目不引入新的 UI 框架；视觉改造阶段不新增 npm 依赖，不执行 `npm install`。

## Users

### 主要用户

中小学班主任，典型班额约 30 至 50 人。用户需要同时处理教学、学生管理、家校沟通、班级事务和阶段性材料输出，时间零碎，重复录入成本高。

### 次要用户

- 使用管理后台生成和发放兑换码、处理续期及账号问题的运营人员。
- 通过演示环境了解产品能力的潜在购买用户。

### 不作为当前主要用户

- 学生和家长没有独立账号或门户。
- 年级主任、学校管理员和多教师协作尚不是当前稳定权限模型的一部分。
- 产品当前不是教务排课系统、在线教学平台或完整校级学生信息系统。

## Product Purpose

班主任工作的核心问题不是缺少单个表格，而是信息分散在纸张、聊天、电子表格和多个临时文件中，同一名学生的作业、成绩、积分、沟通和成长记录彼此割裂。

本产品要做到：

1. 让老师每天打开后首先知道“今天需要处理什么”。
2. 让老师需要了解一名学生时，能够回答“这个学生最近发生了什么”。
3. 让一次记录可以被后续工作复用，而不是在成长档案、周报、反思和评语中重复抄写。
4. 让批量工作适合 30 人以上班级，长期数据增长后仍可搜索、筛选、归档和回看。
5. 让保存结果可信，正式用户之间的数据严格隔离。

## Positioning

班主任工作台不是功能入口集合，而是一套围绕学生、班级和时间组织的长期工作系统。

产品定位：

> 一次记录，持续复用；把每天零散的班务，沉淀为可追踪的学生成长和班级工作记录。

与通用表格相比，本产品强调跨模块关联、状态跟进、批量操作和长期输出；与大型校级系统相比，本产品强调班主任个人工作效率、低学习成本和手机端现场操作。

## Operating Context

### 高频场景

- 早晨查看到校、值日和当天安排。
- 布置作业后持续处理未交、待订正和已复查学生。
- 考试后录入成绩、查看异常、填写反思和形成评语依据。
- 课堂或课后快速记积分、补成长证据和沟通跟进。
- 周末生成班级周报，学期末集中处理期末评语。

### 使用约束

- 老师经常在手机和电脑之间切换，同一 workspace 最多允许两个浏览器设备。
- 手机端操作常发生在短时间和单手环境中，必须优先完成搜索、筛选、选择和快速编辑。
- 学生、家长电话、健康和沟通内容属于敏感数据，不能因为宣传视觉或便利性而扩大暴露范围。
- 长期使用会产生多学期、多次考试、30 条以上沟通记录和大量学生证据，不能只按演示数据规模设计。

## Capabilities and Constraints

### 当前教师工作台能力

现有正式页面共 18 个：

1. `今日工作台`：汇总当前班级的待处理事项和常用入口，并以“现在与接下来”复用当天个人日程和考勤异常。
2. `学生名单`：维护学生资料、小组和批量名单；在学生详情维护住宿信息、标签、多位监护人及仅班主任可见的照护提醒。
3. `考勤与请假`：按日期记录正常、迟到、请假与缺勤，支持连续请假拆分、月度记录回看，并保留请假原因和确认状态。
3. `作业追踪`：维护作业任务及学生提交、订正和复查状态。
4. `成绩分析`：维护考试、单人录分、多选批量录分、基础成绩查看；趋势支持按学生/班级、科目、关键词和时间范围筛选，台账分页查看长期考试库。试卷与知识点分析支持人工建立项目，或将图片/PDF 交给服务器端已配置的识别适配器，老师核对后才写入统计。
5. `积分评价`：选择学生与规则进行记分。
6. `积分规则`：维护积分分类、规则、分值和状态。
7. `成长档案`：按学生汇总成绩、积分、作业、沟通和成长记录。
8. `健康与照护`：独立维护仅班主任可见的最小必要行动提示，按优先级、类型和场景筛选；支持搜索学生、选填自定义类型/场景，并在编辑时同步查看学生档案中的主要监护人联系方式；不作为公开学生标签，也不保存诊疗材料。
8. `家校沟通`：记录沟通内容、渠道、状态、家长反馈和后续跟进；可保存、复制通知草稿并人工记录回执，但不会自动向家长外发。
9. `考试反思`：按考试和学生维护反思状态及内容。
10. `班级周报`：按周汇总班级情况并管理周报库。
11. `期末评语`：按学期维护学生评语、草稿和已保存评语，并在编辑场景提供 AI 辅助。
12. `课程日程`：在同一入口维护学期、周次、周课表、班级活动、每日重点，以及教师个人日程与工作留痕。
13. `课堂工具`：随机点名（排除当天请假学生）和临时分组；两者不写入积分或学生评价，分组结果可回看。
14. `座位分组`：维护座位图、分组、轮换和座位条件。
15. `值日岗位`：维护岗位、每日检查、返工和检查台账。
16. `班干部`：维护班委、小组长、职责、任期和状态。

### 当前商业与认证能力

以下主干已经存在并经过项目内集成测试：

- 管理员可批量生成兑换码。
- 新用户可通过兑换码和中国大陆 11 位手机号创建用户、workspace、设备和会话。
- 同一浏览器可恢复进入，第二个浏览器可绑定，第三个浏览器会受到设备上限限制。
- workspace 的读取、保存和 AI 请求受 session、用户、设备及 owner 关系保护。
- 用户之间不能通过替换 workspace token 读取彼此数据。
- workspace 支持有效期、只读宽限期和管理员续期。
- workspace 保存已使用 revision 乐观锁；并发旧版本写入返回 409，服务端保留最近版本，前端保留同版本本地恢复草稿。
- `/w/demo` 可公开读取但不能保存，演示 AI 使用静态结果。

这些能力并不等于已经满足安全商业正式上线标准，见“Release Boundaries”。

### 视觉改造阶段的硬约束

- 不修改现有路由、workspace JSON 保存格式、数据库结构和认证权限规则。
- 不删除真实业务能力，不用视觉重构代替业务验证。
- 不新增 npm 依赖，不执行 `npm install`。
- 保留手机底部导航，不复制桌面侧栏到手机。
- 手机端禁止横向滚动、框套框和无限铺开的长详情。
- 多选后才出现批量操作；一个页面或弹窗只设置一个明确主操作。
- `值日岗位`的星期按钮条和台账状态按钮条保持按钮形式，不改成下拉。
- 保存、冲突、失败和只读状态不能通过样式隐藏或弱化。

### 候选能力，不得写成当前已实现

以下来自对标分析，是后续产品候选，不属于本轮纯视觉改造：

- 考勤与请假的附件。
- 教师个人课表与调课记录。
- 通知的真实外发渠道接入、已读回执自动获取与消息模板审批。
- 完整 workspace 导出、导入和按 workspace 恢复。
- 兑换码订单、批次、渠道、退款和续费商品闭环。

## Information Architecture

产品围绕三个主对象组织：

- `今天`：待处理、时间安排、异常和跟进。
- `学生`：名单、作业、成绩、积分、成长、沟通、反思和评语。
- `班级`：课程、座位、值日、班干部、规则和周报。

桌面端导航按“今日 / 学生 / 输出 / 班级设置”分组；手机端固定展示“首页 / 学生 / 作业 / 成绩 / 更多”。“更多”按业务场景分组，不重复展示无意义的完整入口宫格。

跨模块设计必须优先保持对象连续性：从某个学生进入成长、沟通、反思或评语时，系统应尽量保持学生上下文，避免用户反复重新选择。

## Brand Commitments

- 品牌名称固定为“班主任工作台”。
- 产品语气专业、清楚、支持老师做判断，不以系统口吻替老师给学生下结论。
- 视觉允许鲜明、彩色、适合宣传，但不能牺牲高密度工作效率和敏感数据的严肃感。
- Emoji 是品牌识别的一部分，必须与中文标签共同出现，不能独立承担含义。
- 不用“智能”“自动”“已同步”等空泛文案制造先进感；只有真实发生且用户需要知道的状态才展示。
- 不把每个页面都做成同一种统计卡模板，也不为了丰富颜色而把长表单和彩色看板混为一体。

## Evidence on Hand

项目决策依据包括：

- 当前代码、数据库结构、测试脚本和真实本地渲染页面。
- `同行产品对标与全站优化建议-班主任工作台-20260824.md`：21 张同行截图的功能、结构和视觉对比。
- `全站功能与商业兑换流程审核-班主任工作台-20260824.md`：认证、隔离、保存、设备、到期和售卖流程审核。
- `上线前全站体验问题记录-班主任工作台-20260821.md`：桌面与手机体验问题。
- `项目交接记录-班主任工作台-20260814.md`：历史改动和已确认交互约束。

竞品截图只能证明可见界面，不能证明其后台逻辑、安全性、数据准确性或商业效果。对标结论应作为设计证据，不应被当成未经验证的产品事实。

## Product Principles

1. **行动先于概览。** 首页和列表优先展示老师下一步能处理的事情，不堆没有行动意义的数字。
2. **学生是跨模块主对象。** 所有记录最终应能回到学生上下文，并说明来源和时间。
3. **一次记录，多处复用。** 成长档案、周报、反思和评语优先引用已有事实，而不是重复填写。
4. **异常优先，默认正常。** 作业、值日和成绩等高频流程优先处理少数异常，减少全班重复点击。
5. **手机重新编排，不缩小桌面。** 手机端根据现场任务重新组织步骤，详情进入底部弹窗。
6. **长期数据优先。** 任何列表和筛选都要按多个学期、多个考试、30 条以上记录和 8 个以上小组验收。
7. **保存可信。** 只有服务器确认后才能表示已保存；冲突和失败必须可见并可恢复。
8. **系统提供证据，不替老师定性。** 自动摘要和 AI 只能辅助表达，不应把固定阈值包装成客观学生结论。
9. **宣传页吸引，工作页克制。** 首页和分析页承担品牌表现；录入、设置和敏感详情承担效率和可信度。
10. **少即是有主次，不是少功能。** 低频能力进入更多菜单或详情区，核心能力仍应完整可达。

## Accessibility & Inclusion

- 正文对比度至少 4.5:1；大字、图标和控件至少 3:1。
- 所有状态必须有文字，不能仅依赖颜色、Emoji 或位置。
- 交互目标在手机端应满足舒适触控尺寸，关键操作不能只靠悬停。
- 弹窗需要标题关联、键盘焦点管理、Esc 关闭、焦点恢复和背景不可交互。
- 支持浏览器缩放、文本换行和 `prefers-reduced-motion`。
- 不默认公开排名、健康信息、家长电话或沟通详情；敏感字段按任务最小化展示。
- 系统文案避免羞辱性、绝对化和给学生贴永久标签的表达。

## Release Boundaries

当前仍存在已确认的商业正式上线阻断项或高风险项：

- HTTP 认证没有传输加密；正式售卖前仍需 HTTPS、Secure Cookie 和安全管理员凭据。
- 乐观锁已经阻止静默覆盖，但发生多设备冲突时仍没有字段级合并；整份 JSON 写入、结构校验和部分桌面/手机派生数据同步仍需继续加固。
- 兑换码尚缺订单、批次、渠道、退款、续费和管理员审计闭环。
- 完整数据导出、导入、备份验证和 workspace 恢复能力不足。
- 手机号误输修正、禁用后设备恢复及长期设备生命周期仍需完善。

因此，当前版本可以继续作为测试和过渡版本，但不能仅凭界面完成度宣称为安全商业正式版。
