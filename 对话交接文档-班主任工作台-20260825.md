# 班主任工作台对话交接文档

更新时间：2026-08-25

用途：给一个完全没有前文的新 Codex 对话接手使用。本文件记录当前任务、真实完成状态、未完成事项、下一步顺序、用户偏好、部署方式和必须避开的历史坑。

## 1. 新对话先看这里

项目名称固定为：**班主任工作台**。

项目目录：

```txt
E:\文档\Codex\小红书\班主任工作台网站
```

当前正在做的不是单独某个页面，而是两条相互关联的主线：

1. 把已经经过多轮修改的桌面端和手机版统一成稳定、好看、长期可维护的“彩色校园工作台”。
2. 保持已经落地的兑换码、用户隔离、管理员后台、自动保存和数据安全能力，为后续售卖做准备。

当前最直接的工作阶段是：**先稳定全站公共壳层、页头和页面布局，再新增同行对标中发现的功能缺口。**

最近一次用户反馈是桌面端座位分组页面滚动后，左侧底部出现大块白色空白。第一次修复错误地固定了整条侧栏，导致左侧功能列表不能滚动、底部功能看似消失。该回归现已修复：

- 左侧导航恢复独立滚动。
- 页面主体很长时，左侧深色背景仍连续到底。
- 没有删除任何导航功能。
- 自动化检查已经增加“导航可滚到底”和“左侧轨道不露白”两项断言。

截至本文件生成时没有代码层面的阻塞，当前卡点是：**等待用户确认最新桌面壳层和页头视觉是否可以作为新增功能前的稳定基线。**

## 2. 当前真实状态，不要把计划当成完成

### 已经落地并通过测试

- 公开首页只保留“进入我的工作台”和“查看演示”。
- 兑换码 + 手机号进入流程。
- 用户、兑换码、浏览器设备、会话和 workspace 所有权隔离。
- 每个正式用户拥有自己的 workspace，URL token 不再单独作为权限凭据。
- 管理员后台 `/admin`。
- 管理员可批量生成 6 至 8 位兑换码。
- 管理员可查看完整手机号、兑换码使用信息、用户期限和设备。
- 用户禁用、恢复、续期、设备解绑、数据导出和永久删除。
- 正式 workspace 权限守卫、只读宽限期和用户间数据隔离。
- demo 公开只读，禁止保存，AI 使用静态示例。
- AI 首次授权、每日额度、失败保留草稿和 demo 不外发。
- 乐观并发版本、服务器历史版本、本地草稿恢复和冲突处理。
- SQLite 一致性备份脚本和迁移版本记录。
- 15 个业务页面的桌面端和手机版核心能力。
- 统一的彩色语义、Emoji 导航、深青色桌面侧栏和页面级彩色标记。
- 15 个桌面页面使用统一的 `WorkbenchPageHeader`，避免全局栏和页面内容重复显示标题。
- 手机版主要采用列表、筛选和底部弹窗，不直接照搬桌面长表单。
- 关键弹窗具备标题 ID、Esc、焦点锁定、背景 inert 和焦点恢复。
- 最新侧栏长页面回归修复。

### 明确还没有完成

- HTTPS 证书、Nginx 443 和 HTTP 到 HTTPS 跳转。
- 当前 HTTP 认证只能作为测试或过渡版本，不能称为安全商业正式版。
- 正式新用户首次进入时去除演示学生数据。此项此前被用户明确暂缓。
- 兑换码销售订单、批次、渠道、退款、财务和完整续费运营闭环。
- 真正的物理硬件设备识别。当前限制边界是浏览器设备令牌，不是手机、电脑或 Pad 的硬件身份。
- 同行对标发现的新模块：请假考勤、教师个人日程、通用工作留痕、完整学生主档案、健康照护、纵向成绩趋势、试卷/知识点分析、教师课程表、随机点名、临时分组、通知中心等。
- 上述新模块当前只在项目文档中有建议，尚未写入业务代码和数据库。
- 最新 2026-08-25 页头统一及侧栏修复尚未生成独立部署包。

## 3. 当前技术结构

### 技术栈

- React 19
- Next.js 16 API/目录结构
- Vinext + Vite
- TypeScript
- SQLite + Drizzle ORM
- Cloudflare/Vite 相关运行能力
- 未引入新的 UI 框架

Node 版本要求：

```txt
>= 22.13.0
```

### 主要页面

路由入口：

```txt
/w/:token?page=...
```

15 个模块 ID：

```txt
dashboard   今日工作台
students    学生名单
homework    作业追踪
points      积分评价
rules       积分规则
growth      成长档案
weekly      班级周报
schedule    课程日程
seating     座位分组
duty        值日岗位
cadres      班干部
records     家校沟通
scores      成绩分析
reflection  考试反思
comments    期末评语
```

### 关键文件

```txt
app/w/[token]/ClassroomApp.tsx
```

全站主工作台、绝大多数桌面和手机版模块、保存状态与页面交互。目前文件非常大，Babel 在 lint 时会提示超过 500KB 并降级代码生成样式，但不是 lint 错误。后续新增大功能前应逐步拆分模块，不能继续无限堆入该文件。

```txt
app/workbench-repair.css
```

全站主要修复样式和最新视觉系统。文件同样很大，存在多个历史覆盖层。修改时必须检查选择器优先级和文件尾部最终规则，不能只改前面的旧规则。

```txt
app/w/[token]/WorkbenchPageHeader.tsx
app/w/[token]/WorkbenchPageHeader.module.css
```

统一桌面页头组件。当前支持 `sky / coral / jade / marigold / iris / lake / berry` 七种页面语义色。

```txt
app/w/[token]/CourseSchedule.tsx
```

课程日程独立组件。

```txt
lib/classroom.ts
db/schema.ts
db/index.ts
db/auth.ts
db/workspaces.ts
lib/auth.ts
```

数据类型、数据库结构、认证和 workspace 隔离核心。

```txt
app/api/auth/
app/api/admin/
app/api/workspace/[token]/
app/api/knowledge/[token]/
app/api/ai/
```

认证、管理员、正式数据和 AI 接口。

```txt
scripts/layout-audit.mjs
scripts/auth-ui-audit.mjs
scripts/backup-db.mjs
tests/
```

静态、真实浏览器布局、认证流程和数据库备份检查。

## 4. 已完成的产品与页面改造

### 4.1 全站基础体验

- 项目名称统一为“班主任工作台”。
- 桌面导航按“今日 / 学生 / 输出 / 班级设置”分组。
- 手机底部导航保留高频入口，其余放“更多”。
- 颜色从单一蓝色调整为深青壳层 + 蓝、珊瑚、翡翠、金黄、靛青、湖蓝、莓红等稳定语义色。
- Emoji 已作为导航和模块识别语言，但不应继续无节制增加。
- 顶部删除重复的全局页面标题、同步提示和“已同步”状态。
- 账户弹窗只保留用户真正关心的手机号与使用期限，不展示浏览器、AI 编写或退出解绑等冗余内容。
- 桌面端一个页面只保留一个页面级标题，主操作放页头，低频操作进入“更多”或详情。
- 手机端复杂详情优先使用底部弹窗，避免列表下面堆长详情。

### 4.2 学生名单

- 桌面端表格化，支持搜索、小组、每页数量和资料编辑。
- 手机端搜索和小组筛选同一行平均分配。
- 手机列表只保留姓名、学号、小组和关键状态。
- 多选改为清晰圆形选择控件，不使用大头像框。
- 点击学生进入详情/编辑，不把长资料铺在列表下面。

### 4.3 作业追踪

- 手机主页面增加科目、时间、内容搜索和筛选。
- 作业详情进入弹窗。
- 学生状态保留已交、未交、待订正、已复查。
- 学生多选后才出现批量状态操作。
- 恢复紧凑学生列表和整行高亮，不使用大头像卡。
- 桌面端历史作业台账和处理入口已整理。

### 4.4 成绩分析

- 支持单个学生录分。
- 支持先多选学生，再打开“批量录分”弹窗。
- 批量录分基于已选学生，可选择考试科目和统一分数。
- 手机版筛选逻辑重新按实际使用设计，不再简单搬运桌面控件。
- 支持科目、小组、分数区间、重点标记和排序。
- 当前仍缺纵向趋势、知识点/试卷分析和更强解释层。

### 4.5 积分评价与积分规则

- 积分评价主页面只保留记分和常用规则。
- 删除快捷查看和最近记录等低价值主板块。
- 选择学生使用弹窗，支持大量小组时用下拉筛选。
- 规则分类不再横向拖拉。
- 积分规则桌面筛选区域已移除明显框套框结构。
- 规则列表支持搜索、分类、状态、启停、复制和删除。

### 4.6 成长档案

- 桌面端为学生列表 + 当前学生详情。
- 手机端只显示学生列表，点击学生进入详情弹窗。
- 摘要、成长记录、证据和后续措施放在弹窗中。
- “添加成长记录”作为当前学生详情的主操作，自动摘要降为次级操作。
- 添加记录时必须清楚显示当前学生，不能让老师猜记录归属。

### 4.7 课程日程

- 支持学期、月份、周次、课程表、班级活动和每日重点。
- 删除重复首页、保存和多个配置按钮。
- 周一到周五仍使用按钮条。
- 用户明确要求：不要把日期/星期结构随意改成下拉。
- 最近的视觉问题示例是课程日程页头结构和课程区域风格不一致，现已纳入统一页头体系。

### 4.8 座位分组

- 座位图是页面核心。
- 支持座位调整、学生选择、分组管理、智能排座等已有能力。
- 手机端删除底部无意义的“搜索座位”板块。
- 最近修复：长座位图页面滚动时左侧不再露出白色空块，左侧菜单仍可独立滚动到底。

### 4.9 值日岗位

- 保留今日检查、一周值日、岗位设置和检查台账。
- 周一到周五按钮条保持原状。
- 台账状态按钮条保持原状。
- 这两处用户明确要求不能改成下拉。
- 删除重复复制按钮和无意义概览操作。

### 4.10 班干部

- “新增班委 / 新增小组长”保持等宽、等高并形成一个整体。
- 手机端重新适配按钮组。
- 复制聘任书等低频操作不与新增操作争抢位置。

### 4.11 家校沟通

- 桌面端修复表单空白、不对齐和松散分组。
- 手机端使用搜索、筛选、列表和详情弹窗。
- 新增与编辑共用弹窗。
- 记录按待跟进优先展示。
- 长摘要已增加换行和截断策略。

### 4.12 考试反思

- 桌面和手机都支持多次考试切换。
- 手机端不再只有“新增反思”，已同步学生状态和考试切换逻辑。
- 删除“薄弱科”。
- 删除“选择反思对象”和“成绩页”一行。
- 不把“成绩联动”作为实际标题。
- 当前文案为本次考试反思、学生复盘状态、已填写、草稿、未填写等业务语言。

### 4.13 期末评语

- 支持学期切换，与课程日程中的学期关联。
- 手机端采用类似周报的“学生列表 / 保存后的评语”两个入口。
- 学生列表支持小组筛选。
- 列表不展示成绩，改为小组信息。
- 删除“新增评语”，点击学生姓名直接编辑。
- 编辑弹窗不重复选择学生和学期。
- 编辑区删除成绩和作业的大块展示。
- AI 编写读取正式 workspace 已有配置和授权流程。

### 4.14 班级周报

- 保留“概览 / 周报库”结构，作为手机端入口模式的重要参考。
- 支持周次切换、关注学生、生成、编辑、归档和复制正文。

## 5. 商业化认证与数据隔离现状

### 用户进入流程

首次进入：

```txt
兑换码 + 手机号
→ 创建用户
→ 创建正式 workspace
→ 绑定当前浏览器设备
→ 创建 session
→ 进入工作台
```

同一浏览器再次进入：

```txt
有效 session 或有效设备令牌
→ 自动进入
```

换浏览器：

```txt
重新输入原兑换码 + 手机号
→ 未超过设备数时绑定新浏览器
```

当前设备限制是浏览器令牌，不是硬件指纹。不要再向用户表述成“绑定物理手机或电脑”。

### 管理员能力

- 环境变量中的管理员密码创建 2 小时 HttpOnly 管理会话。
- 支持批量生成短兑换码。
- 明文兑换码只应在必要场景展示，不应写入日志。
- 管理员页面按用户要求显示完整手机号，不打码。
- 支持查看用户、期限、设备和兑换关系。
- 支持续期、禁用、恢复、解绑、导出和永久删除。

真实管理员密码和密钥绝对不能写入仓库、交接文档、URL、localStorage 或日志。用户曾要求密码长度可为 8 位，代码测试的是最小长度和环境变量读取，不应硬编码具体密码。

### 当前安全边界

- HTTP 模式必须显式设置 `AUTH_ALLOW_INSECURE_HTTP=true`。
- 未设置时，HTTP 登录应明确失败，不能静默降级。
- HTTP 下 Cookie 不能使用 `Secure`，存在链路窃听风险。
- 未来启用 HTTPS 后将该开关改为 `false`，Cookie 自动使用 `Secure`。
- 所有正式保存、知识库和 AI 请求必须同时校验 session、用户、设备、workspace 所有权和期限。
- `/w/demo` 只能读，不能保存真实数据或调用外部 AI。

## 6. 当前工作区与版本注意事项

当前 Git 工作区非常脏，包含大量已修改文件、未跟踪新文件、历史部署包和项目文档。

接手者必须遵守：

- 不执行 `git reset --hard`。
- 不执行 `git checkout --` 回退用户内容。
- 不删除看似旧的 tar.gz、文档或未跟踪文件。
- 不把整个工作区格式化。
- 不在不了解来源时清理“无关改动”。
- 修改 touched file 前先读取当前内容和文件尾部覆盖规则。

Git 在当前机器可能提示 `dubious ownership`。只在单条命令中使用：

```txt
git -c safe.directory='E:/文档/Codex/小红书/班主任工作台网站' status --short
```

不要擅自写全局 Git 配置。

## 7. 最近一次修复的技术细节

### 问题

座位分组桌面页比左侧导航长。页面下滚时，左栏下方出现白色区域。

### 错误修复

曾将 `.sidebar` 改为 `position: fixed` 并固定整个视口。虽然白色空白消失，但嵌套导航不再按预期滚动，导致用户看不到底部功能。

### 当前正确方案

- 保留原 `.sidebar` 的 `position: sticky`、`height: 100dvh`。
- 保留 `.side-nav` 的独立 `overflow-y: auto`。
- 不固定整个 sidebar。
- 在桌面 `.app-shell` 上用左右分色背景延伸深青色轨道：左 236px 深青，右侧为工作台背景。
- 主内容继续正常文档滚动。

关键样式位置：

```txt
app/workbench-repair.css 约 19245 行后的桌面媒体查询
```

回归检查位置：

```txt
scripts/layout-audit.mjs 约 287 行
```

检查内容：

- `.side-nav.scrollTop` 能到达最大滚动值。
- 桌面 shell 存在连续左侧背景轨道。
- 座位页滚到底后截图仍保持深色左栏。

## 8. 已经踩过的坑，绝对不要再踩

### 8.1 不要为了消除左栏空白固定整条 sidebar

固定整条 sidebar 会破坏内部导航滚动。正确做法是保留 sticky + 独立导航滚动，用壳层背景延伸颜色。

### 8.2 不要把桌面页面直接搬到手机端

历史上成绩筛选、成长档案、作业详情、积分学生选择等都因此出现逻辑错误。手机版必须根据真实操作重新决定首屏、筛选、列表和弹窗顺序。

### 8.3 不要只改文案或内容，不看容器结构

曾出现筛选框被拆成上下两块、按钮大小不一致、框套框、右侧空白和页头错位。每次改 JSX 后必须用真实浏览器截图检查 390px、768px、1280px/1440px。

### 8.4 不要再给每个页面套“三格统计 + 搜索 + 卡片列表”

这会产生明显 AI 模板感。统计只能保留真正影响操作的内容，不同页面必须按业务任务形成不同结构。

### 8.5 不要让顶部全局导航和页面标题重复

桌面全局区域只负责工作台身份、班级、账户和导航。页面名称、描述和主操作由 `WorkbenchPageHeader` 负责，只出现一次。

### 8.6 不要用浅到看不清的渐变和低对比按钮

历史上浅蓝、浅绿渐变导致按钮文字和列表状态难读。现在要求稳定纯色/浅色底 + 深色文字，并保持业务语义色一致。

### 8.7 不要把功能说明当页面标题

例如“成绩联动”只是功能背景，不是用户看到的业务标题。页面语言要表达老师当前要做的事。

### 8.8 不要修改用户明确要求保留的按钮条

值日岗位的周一到周五按钮条、台账状态按钮条保持原状，不改成下拉。

### 8.9 不要新增依赖或执行 npm install

用户多次强调以前部署不需要 `npm install`。项目已有依赖足够完成当前工作。除非用户明确改变要求，否则不新增 npm 依赖，也不执行 `npm install`。

### 8.10 不要把浏览器设备说成物理硬件

网页无法可靠识别同一个物理手机/电脑。当前实现是浏览器令牌，清 Cookie、无痕模式或更换浏览器可能被视为新设备。

### 8.11 不要在 HTTP 环境宣称可以安全正式售卖

认证和隔离逻辑已实现，但没有 HTTPS 时兑换码、手机号和会话仍有传输风险。

### 8.12 不要在 demo 调用外部 AI 或保存数据

demo 必须保持固定只读，AI 返回静态示例。

### 8.13 不要用错误的服务器目录执行 tar 或 npm

服务器项目目录是：

```txt
/www/classroom
```

上传包通常先放到：

```txt
/www
```

从 `/www/classroom` 内执行 `tar ... classroom` 会报 `Cannot stat: No such file or directory`，因为当前目录里没有下一层 `classroom`。

在 `/www` 解压到项目目录后，必须先进入 `/www/classroom` 再执行构建；从 `/www` 执行 `npm run build` 会因为没有 `/www/package.json` 而失败。

### 8.14 不要假定本地服务一定是 3007

端口被占用时 Vite 会自动切换到 3008 等端口。必须读取终端实际显示的 `Local` 地址。

### 8.15 不要声称完成了视觉审核，除非真实页面已渲染

此前视觉检查失败的主要原因是本地服务未启动或浏览器入口不可用。当前仓库可以使用 `qa:strict` 通过 Chrome/Edge CDP 自动渲染，也可以指定截图目录后用 `view_image` 检查。

## 9. 用户明确偏好

- 项目名称不能忘：班主任工作台。
- 用户发服务器命令时，命令一行一个，方便单独复制。
- 不要默认给一长串多余部署命令。
- 用户说“只需要解压上传”时，不要追加备份、安装依赖或其他步骤。
- 用户要求备份时，必须根据当前服务器目录给正确命令。
- 不新增依赖，不执行 `npm install`。
- 修改后要主动给出部署包和上传/解压命令，但先确认用户是否需要备份。
- 桌面端强调工作效率，手机端强调单手、列表、筛选、底部弹窗和大量数据可用性。
- 不喜欢纯蓝、过于朴素或明显 AI 模板化的界面。
- 喜欢 Emoji + 丰富但不花哨的颜色，适合宣传截图，但不能牺牲可读性和专业感。
- 不喜欢框套框、横向滚动筛选、重复按钮、无意义概览和大段功能说明。
- 长期软件必须考虑多个学期、多次考试、30 条以上记录、32 名以上学生和 8 个以上小组。
- 多选操作必须先选择对象，再显示批量按钮。
- 低频的复制、删除、配置、导出不能抢占主操作位置。

## 10. 当前验证结果

2026-08-25 最新代码已执行：

```txt
npm.cmd test
```

结果：构建通过、13 项渲染/结构测试通过、认证集成测试通过。

```txt
npm.cmd run qa:layout
```

结果：通过。

```txt
npm.cmd run qa:strict
```

结果：桌面和手机版 15 页真实渲染布局检查通过。

```txt
npm.cmd run lint
```

结果：通过。仅有 Babel 提示 `ClassroomApp.tsx` 超过 500KB，属于维护风险，不是 lint 失败。

最新座位分组专项截图确认：

- 左侧导航滚动到底后可以看到班级周报、期末评语、课程日程、座位分组、班干部、积分规则等底部功能。
- 主体页面滚动后左侧仍是完整深青色背景。
- 右侧分组管理与座位图没有被本次修复遮挡。

## 11. 下一步推荐顺序

### 第一步：冻结当前公共壳层

先让用户确认以下内容：

- 桌面页头是否统一且不再重复。
- 左侧导航是否可以完整滚动。
- 15 个页面顶部不再出现异常高度、错位或大块空白。
- 390px 手机端页头、筛选和底部导航没有回归。

不要在用户尚未确认公共壳层前开始大规模新增模块，否则新功能会建立在不稳定布局上，返工成本更高。

### 第二步：生成最新部署包

当前最新公共页头和侧栏修复还没有独立包。用户确认后：

1. 只打包实际改动和运行所需文件。
2. 包名注明日期和用途，例如 `desktop-header-sidebar-baseline-20260825`。
3. 给出一行一个的上传后命令。
4. 若用户说不需要备份，只给解压、进入项目目录、构建和重启所需最少命令。

### 第三步：新增功能缺口

建议按业务价值和数据依赖分批，不要一次把所有功能塞进 `ClassroomApp.tsx`。

优先级建议：

1. 请假与考勤闭环。
2. 教师个人日程和通用工作留痕。
3. 学生主档案、紧急联系人和健康照护提醒。
4. 成绩纵向趋势、考试对比、试卷与知识点分析。
5. 教师课程表、随机点名和临时分组。
6. 通知、家长输出和更完整导入导出。

每个新模块都必须先确定：

- 数据模型是否按班级和学期隔离。
- 桌面端主工作流。
- 手机端独立工作流。
- 长期数据归档和筛选。
- 与学生、成绩、沟通、周报和评语的关联边界。
- 自动保存、冲突和恢复方式。
- 权限与正式 workspace 隔离。
- 真实浏览器验收场景。

### 第四步：商业正式上线补齐

功能完成后，正式售卖前仍必须：

1. 配置 HTTPS 和 443。
2. 关闭不安全 HTTP 认证开关。
3. 决定正式新用户使用空白模板还是初始化向导，不能继续默认带演示学生。
4. 补齐兑换码订单、批次、渠道、退款和续费运营闭环。
5. 做数据库恢复演练、用户 A/B 隔离演练和多设备并发保存演练。
6. 重新做全站桌面、平板、手机视觉与功能终审。

## 12. 新功能开发时的架构建议

`ClassroomApp.tsx` 已过大。后续新增功能时建议：

- 每个新模块使用独立组件文件。
- 数据操作抽到模块 hook 或 service，不继续混在渲染函数中。
- 通用页头、列表、筛选、状态标签和弹窗继续复用现有模式。
- 不要为追求“组件化”一次性重写全部旧代码；以新模块和高风险区域为边界渐进拆分。
- 保持现有 `ClassroomData` 保存格式兼容，只有明确的数据迁移方案时才修改结构。
- 修改数据库 schema 前先备份 `data/classroom.db` 并验证备份可读取。

## 13. 本地运行和检查

不执行安装依赖，直接使用现有环境。

开发服务：

```txt
npm.cmd run dev -- --host 127.0.0.1 --port 3007
```

若 3007 被占用，以终端实际 `Local` 地址为准。

构建：

```txt
npm.cmd run build
```

静态布局检查：

```txt
npm.cmd run qa:layout
```

真实渲染检查：

```txt
npm.cmd run qa:strict
```

完整测试：

```txt
npm.cmd test
```

Lint：

```txt
npm.cmd run lint
```

数据库备份：

```txt
npm.cmd run backup:db
```

## 14. 服务器部署路径与最少正确流程

服务器项目目录：

```txt
/www/classroom
```

假设压缩包已上传到 `/www`，正确解压形式：

```txt
tar -xzf 包名.tar.gz -C /www/classroom
```

然后必须进入项目目录：

```txt
cd /www/classroom
```

再构建：

```txt
npm run build
```

实际重启命令应沿用服务器当前进程管理方式，不要在不清楚 PM2/systemd 配置时猜测。

如果需要从 `/www` 备份整个项目，可使用：

```txt
tar -czf /www/classroom-backup-$(date +%Y%m%d-%H%M%S).tar.gz -C /www classroom
```

如果当前提示符已经在 `/www/classroom`，也不要直接写 `tar ... classroom`。

## 15. 相关文档阅读顺序

1. 本文件：当前真实状态和交接入口。
2. `彩色校园工作台视觉改造执行计划-班主任工作台-20260824.md`：当前视觉方向和分阶段方法。
3. `同行产品对标与全站优化建议-班主任工作台-20260824.md`：新增功能缺口和同行优点。
4. `全站功能与商业兑换流程审核-班主任工作台-20260824.md`：商业流程风险原始审核。注意其中部分数据可靠性问题后来已修复。
5. `Impeccable全站体验改造执行计划-班主任工作台-20260821.md`：认证、隔离和全站体验完整计划及 8 月 21/24 日实施结果。
6. `认证与隔离部署说明-班主任工作台-20260821.md`：环境变量和部署安全要求。
7. `项目交接记录-班主任工作台-20260814.md`：8 月 14 日以前的移动端改造历史。

## 16. 新对话可直接采用的工作说明

```txt
请先读取“对话交接文档-班主任工作台-20260825.md”，再按其中的相关文档顺序补充上下文。

项目目录是 E:\文档\Codex\小红书\班主任工作台网站，项目名称必须写“班主任工作台”。当前先稳定公共桌面壳层、统一页头和手机版回归，不要直接新增所有同行功能。保留工作区已有改动，不 reset、不 checkout、不清理未跟踪文件，不新增依赖，不执行 npm install。

修改前先用真实页面确认问题；修改后至少执行 npm.cmd run qa:layout、npm.cmd run qa:strict、npm.cmd run build 和 npm.cmd run lint。涉及认证、保存或数据库时执行 npm.cmd test。视觉判断必须基于真实渲染截图，不能只看源码。

特别注意：不要固定整个桌面 sidebar，也不要让 side-nav 独立滚动。侧栏应当作为页面文档流的一部分，完整展示全部功能；右侧工作区随侧栏高度适配，整页使用浏览器滚动。值日岗位的星期按钮条和台账状态按钮条不能改成下拉。
```

## 17. 接手判断原则


## 18. 2026-08-25 后续功能进度补充

“考勤与请假”已经从规划进入业务代码，成为第 16 个工作台模块：

- 桌面端和手机端均可进入，手机端从“更多 → 学生与跟进”进入。
- 数据以 `AttendanceRecord` 保存：班级、学生、日期、全天/上午/下午、正常/迟到/请假/缺勤、请假类型、原因、提交人、联系电话、确认/销假状态和备注。
- 支持按日期查看、一次将当前班级全部标为正常、逐个标记异常；手机端点学生或标记请假后才打开底部详情弹窗，避免详情表单占据首屏。
- 考勤记录随现有 workspace JSON 保存，继续受用户隔离、乐观版本控制、本地草稿恢复和服务器历史版本保护；删除学生时会清理其考勤记录。

尚未完成的考勤深化项：月历总览、学生成长档案内的完整考勤时间线、请假附件与跨日期连续请假自动拆分。不要把这些后续项表述为已上线。

## 19. 2026-08-25 教师日程与工作留痕补充

“课程日程”已在同一入口新增 `班级课表 / 我的日程与留痕` 切换，不新增同质一级导航：

- 新增 `TeacherAgendaItem` 和 `WorkLog`，均使用稳定的 `classId`；事项可关联 `studentId` 数组，删除学生时会清理关联而不删除老师的工作记录。
- 个人事项具备日期、起止时间、类型、标题、说明、地点、关联学生、状态和完成时间；支持新增、编辑、删除与按日期查看。
- 未取消事项可“完成并留痕”：状态变为已完成，并以 `agendaId` 生成一条工作记录，避免日程完成后只剩口头说明。也支持手动补记工作和耗时。
- 桌面端以当天安排和工作留痕两块数据纸张呈现；手机端在课程页内切换，使用底部弹层编辑，不复制桌面课表。
- 该功能继续随 workspace JSON 保存，受现有用户隔离、乐观版本、本地草稿和服务器历史版本机制保护；演示工作台仍只读且不保存。

尚未完成：个人课表与调课记录、日程自动汇总进今日工作台/周报、跨班日程视图、附件和提醒推送。它们仍是后续计划，不应被表述为已上线。

补充：今日工作台已复用当天未完成的个人日程，桌面显示“现在与接下来”，手机在“今天先处理”后显示紧凑时间清单；当天的考勤异常会一并提示。当前点击“查看日程”进入课程日程入口后仍需手动切换到“我的日程与留痕”，尚未实现带视图参数的直达链接。

## 20. 2026-08-25 学生档案、成绩趋势与课堂工具补充

- `学生名单`的“档案”入口在桌面与手机详情均可进入：可维护住宿信息和标签；监护人支持姓名、关系、电话、主要联系人和紧急顺位；照护信息明确标为仅班主任详情可见，不能铺在名单列表中。删除学生会一并移除其监护人与照护资料。
- `成绩分析`已加入按得分率归一化的历次成绩趋势：空白成绩表示未录入，不会按零分计算；至少两场已保存考试才比较变化。它不是题目级或知识点级诊断。
- 新增“课堂工具”一级能力，桌面在“班级设置”、手机在“更多 → 班级事务”可进入。随机点名会排除当天请假的学生，一轮抽完后要求显式重置，不会重复抽取；临时分组使用随机洗牌并保存分组结果。两者均不写入积分或学生评价。

## 21. 2026-08-25 通知草稿与回执补充

“家校沟通”页新增通知草稿区，桌面位于沟通台账之前，手机位于筛选区之前：

- 可针对全班家长或单个学生家长，按班级群、私聊或电话提醒保存通知标题和正文。
- “复制通知”仅调用浏览器剪贴板，并将草稿状态标记为“已复制”；老师仍须在实际渠道中自行发送。代码和界面都不能将此描述为“已发送”。
- 可人工登记已收到的回执说明，状态为“已记录回执”；当前没有外部消息渠道、已读回执或自动推送集成。
- 删除学生会从定向草稿对象中移除该学生；删除班级会移除该班的草稿、课堂工具结果、监护人、照护、日程和工作留痕。

本轮验证已执行：`npm.cmd test`（20 项测试与认证集成通过）、`npm.cmd run qa:strict`（全模块及通知页桌面/手机截图通过）、Impeccable layout detector（无发现）。截图位于 `reports/notification-drafts-20260825/`。

## 22. 2026-08-25 题目与知识点分析补充

“成绩分析”已新增题目与知识点分析区，桌面和手机均位于历次趋势之后：

- 每场考试可新增题号、题型或知识点名称，选择可选归属科目，并设置满分；随后可按学生逐项录入分数。
- 分析展示每个项目的录入覆盖人数和平均得分率，并明确把空白视为“未录入”而非零分。当前最低得分率仅是待老师研判的线索，文案明确要求结合题目难度、缺考和覆盖率，不能自动给班级或学生下结论。
- 项目与得分数据保存在对应 `ScoreExam.knowledgeItems` 中，随既有 workspace JSON 保存；删除学生时会清理其题目/知识点得分。
- 尚不包含试卷扫描、OCR、自动识别知识点、题库标准答案或跨卷自动等值分析。这些需要新的数据来源、授权与校验，不能因当前录入分析功能而宣称已具备。

本轮验证：`npm.cmd test`（21 项测试与认证集成通过）；`QA_PAGES=scores npm.cmd run qa:strict`（桌面、手机通过）；Impeccable layout detector 无发现。截图位于 `reports/score-item-analysis-20260825/`。

## 23. 2026-08-25 安全备份恢复与正式空白工作台补充

- 完整备份恢复保留原有“导出 JSON / 选择 JSON / 确认后替换”流程，并新增恢复前预检：限制 5MB，校验备份格式和版本、学生与班级 ID、班级名单结构及当前班级引用。预检通过后确认框会显示来源、导出时间、班级/学生/沟通记录/考试数量与当前工作台将被替换的范围；取消不会写入数据。
- 当前恢复仍是“完整替换”而非字段级合并。替换后的数据先进入既有本机草稿与乐观锁保存流程，发生服务器冲突时仍需导出草稿并载入最新版本核对。
- `enterWithRedeemCode` 创建正式用户时已改用 `createEmptyClassroomData()`：一个空的“我的班级”、空记录和空考试，不再复制 `defaultClassroomData` 的演示学生或敏感示例数据。`/w/demo` 仍独立使用演示数据。
- 本轮验证：`npm.cmd test`（23 项测试及认证集成通过）、`npm.cmd run qa:layout`、`npm.cmd run lint` 均通过；lint 仅保留既有的超大 `ClassroomApp.tsx` Babel 性能提示。

## 24. 2026-08-25 考勤连续请假与学生历史补充

- 请假编辑支持“请假至”；确认保存后按日期区间拆成每日独立 `AttendanceRecord`，从而继续适配原有按日统计、首页考勤异常和周报逻辑。单日请假默认起止同日。
- 学生档案详情新增最近 24 条考勤历史，展示日期、状态、时段及已保存的请假原因或备注；明确提示空白日期不等于缺勤。该历史不进入名单主表，只在班主任详情中显示。
- 考勤页已提供按月查看的记录总览：仅标记已保存日期与异常数量，明确“无记录不等于全员正常”；点击日期可回到对应日继续维护。附件上传仍未实现，因其涉及文件存储、访问控制和删除策略，不能以文本字段替代后宣称完成。
- 本轮已通过 `npm.cmd test`（23 项测试及认证集成）、`npm.cmd run qa:strict` 和 `npm.cmd run lint`。

## 25. 2026-08-25 成绩入口与全站关键操作审计

- 成绩分析的“新增考试”原先只藏在桌面端“更多”菜单以及移动端“切换考试”底部面板中。现已改为两端首屏显性操作：桌面端“切换考试 / 新增考试”并列；手机端当前考试卡片同样并列。编辑当前考试仍为次级操作，避免新增入口被隐藏。
- 对 17 个业务模块按“关键实体能否新增、日常记录能否录入、已有数据能否继续维护”逐项核查：学生、考勤、作业、积分及规则、成长记录、周报、课程日程、课堂工具、座位、值日、班干部、沟通记录、成绩、考试反思和期末评语均存在相应入口。考试反思和期末评语是先选择学生再填写/保存的编辑型流程，不能误判为缺失“新增”按钮。
- 本轮验证：`npm.cmd test`（24 项测试及认证集成通过）、`npm.cmd run qa:strict`、`npm.cmd run qa:layout`、`npm.cmd run lint` 均通过；Impeccable layout detector 无发现。成绩页截图位于 `reports/score-direct-entry-20260825/`。

## 26. 2026-08-25 成绩长期趋势与试卷识别工作流补充

- 成绩首屏不再把“编辑考试”藏在“更多”下拉里：桌面端显式为“切换考试 / 编辑考试 / 新增考试”，手机端当前考试卡片也提供这三项直接操作。
- 历次趋势已从平铺卡片改为独立工作视图：可选择班级整体或学生，按考试名称/日期/科目搜索，筛选最近 12 场、24 场或全部；长期台账每页 10 场，超过 36 场时图表只显示最近 36 场，完整记录仍在台账可翻页查看。空白始终表示未录入，不得按零分或自动下结论。
- 题目与知识点分析也改为独立工作视图。老师可手动建立项目，也可上传 JPG、PNG、WEBP 或 PDF 试卷（最大 12MB）；服务端适配器返回题号、题型、知识点、分值与置信度后，老师必须逐项核对并点击“确认并加入统计”，项目才进入按学生录分、覆盖率和得分率比较。试卷原文件不会写入 workspace JSON。
- 真实 AI/OCR 尚不随前端代码提供：部署者必须配置服务器环境变量 `EXAM_AI_ENDPOINT` 与 `EXAM_AI_API_KEY`，并部署能处理 multipart 文件的受控适配服务。密钥不能进入浏览器，也不能以虚构值替代；未配置时会报清晰错误而不是伪造结果。适配器不得收到工作台访问令牌。
- 移动端“切换考试”同样已加入关键词、科目、年月、排序和每页 12 场的分页，避免数百场考试被一次性渲染。

本轮验证已完成：`npm.cmd test`（26 项测试与认证集成）、`npm.cmd run qa:layout`、`npm.cmd run qa:strict`、`npm.cmd run lint` 均通过；Impeccable layout detector 无发现。各视图截图位于 `reports/score-workspace-20260825/`、`reports/score-trends-redesign-20260825/`、`reports/score-paper-analysis-20260825/`。

## 27. 2026-08-25 全站显示与关键流程复查

- 对 17 个模块在 1440px 桌面与 390px 手机宽度进行渲染复查，覆盖首页、学生、考勤、作业、积分、规则、成长、周报、课程日程、课堂工具、座位、值日、班干部、沟通、成绩、反思与评语。布局审计未发现页面级横向溢出、主体被裁剪或桌面侧栏独立滚动。
- 修复积分评价学生表的全选列：原来“全选”文字会挤入极窄的选择列，在特定缩放下视觉错位；现在只保留居中的复选框，以可访问名称“全选当前筛选学生”表达操作，并统一焦点样式和强调色。
- 修复“我的日程与工作留痕”的卡片头操作：新增事项/手动记录统一最小高度、内边距、边框和字重，避免在桌面或手机上像被压缩的文字块。
- 本轮静态检查也复核了现有关键实体的新增、日常录入和已存数据维护路径；未发现会阻断当前主流程的 P0/P1 逻辑错误。该结论不等于附件、提醒、真实 OCR 等尚未接入能力已经完成。

仍按计划未完成、且不应提前宣称上线的事项：

1. 真实试卷 OCR/大模型适配服务的部署、计费/配额策略、失败重试与文件留存/删除策略；当前只有安全的服务端接口和人工核对工作流。
2. 请假和日程附件的独立文件存储、访问控制、删除/保留政策；不能以 workspace JSON 文本字段替代。
3. 教师个人课表与调课记录、跨班日程视图、日程自动汇总进周报、提醒推送；今日工作台已显示当天日程，但这不等于这些能力完成。
4. 今日工作台“查看日程”带目标视图的直达参数；当前进入课程日程后仍需手动切到“我的日程与留痕”。
5. 更长期的商业化与运维能力：续费码核销、账户修复的细化流程，以及跨设备/跨版本数据冲突的模块级合并。当前已有账号隔离、设备上限、乐观锁、备份恢复和管理员导出/删除，不能把它们混同为完整商业闭环。

- 事实：代码、测试和真实页面已经验证的内容。
- 计划：文档中建议但尚未落地的内容。
- 用户偏好：必须保护，但如果与安全或真实技术边界冲突，应直接说明风险。
- 商业上线：认证功能“能运行”不等于 HTTP 环境“足够安全”。
- 视觉完成：自动化无溢出不等于审美已经通过，仍需用户对真实截图确认。
- 功能完整：导航入口存在不等于长期数据、归档、导出、权限和移动流程都已经完成。

新对话应从当前基线继续，不要重做已经完成的移动端历史修改，也不要把同行截图逐像素复制到本项目。正确方向是吸收同行的时间维度、信息密度、彩色识别和日常班务能力，同时保留本项目更完整的数据隔离、跨模块证据和长期输出体系。

## 28. 2026-08-25 独立“健康与照护”页面

- 原计划中的健康与照护此前只在学生档案详情里作为单条照护提醒维护，不能承担班级级的安排前核对。本轮新增为桌面侧栏与手机“更多”均可进入的一级页面；正式名称采用“健康与照护”，避免把学生以“特异体质”公开标签化。
- 页面不预置任何虚构健康资料。老师登记时只填写日常安排所需的最小信息：学生、关注类型、优先级、行动摘要、具体安排、可选注意事项、适用场景与最近确认日期。资料保存为仅班主任可见。
- 新页已提供：班级覆盖/优先处理概览；姓名、类型、优先级、场景筛选；可点击编辑的行动卡；新增、修改、删除以及编辑态的主要监护人联系方式。普通学生名单不会显示这些内容。
- 同行截图可参考信息层级，但没有照搬其渐变卡片和“已建档率”等易造成误解的展示。该页不替代医疗判断、学校应急流程或合规的医疗资料保管。
- 后续仍未完成：在座位编排、活动/日程与考试安排页面中提供只读的跨模块场景提示；这需要在确认学校权限、可见范围与应急流程后再接入，不能仅凭本页面的记录自动替老师作决定。
- 本页已加入布局审计的全站页面集；桌面与手机均验证了新页面的首屏无横向溢出，并验证“新增照护登记”会实际打开可编辑表单。

## 29. 2026-08-26 健康与照护登记精简

- 根据实际使用反馈，移除了页面内与顶部重复的“新增”按钮、无明确用户目标的行动提醒板，以及“下次复核日期”功能；筛选器补全为“搜索 / 优先级 / 关注类型 / 适用场景”的可见字段名称。
- 新增登记不再用长学生下拉框：可按姓名、学号或小组搜索并从结果中选择。关注类型可选内置类型或填写自定义类型；适用场景可勾选内置项、自定义填写，也允许全部留空。行动摘要、老师要做什么、需避免或注意三项均改为选填；唯一必选项是学生。
- 监护人展示按学生档案中的主联系人优先、紧急联系人顺序兜底，且只在编辑窗口显示。编辑窗口改为固定头部与底部操作栏、可滚动内容区，避免长表单将取消/保存按钮裁出视口。
