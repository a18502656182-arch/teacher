# 班主任工作台网站项目交接记录

更新时间：2026-08-14

## 2026-08-21 商业化与用户隔离计划更新

最新全站问题记录：

`E:\文档\Codex\小红书\班主任工作台网站\上线前全站体验问题记录-班主任工作台-20260821.md`

最新完整执行计划：

`E:\文档\Codex\小红书\班主任工作台网站\Impeccable全站体验改造执行计划-班主任工作台-20260821.md`

重要状态：兑换码、手机号、浏览器设备绑定、session 和 `workspace.owner_user_id` 目前只是已经确认并写入文档的下一阶段计划，尚未写入项目代码。当前正式 workspace API 仍然主要依赖 URL token，不能误判为已经具备可售卖的用户隔离能力。

计划中的公开首页只保留“进入我的工作台”和“查看演示”。首次进入使用兑换码和手机号，默认最多绑定 2 个浏览器设备；正式数据接口必须同时校验 session、设备状态和 workspace 所有权，`/w/demo` 保持公开只读。

## 新窗口接手时先读这一段

这是一个“班主任工作台网站”项目，当前主要工作是在不影响电脑版的前提下，持续重做和完善手机版。手机版目标不是简单把电脑版竖排，而是做成更像 App 的结构：底部导航、卡片、弹窗、筛选抽屉、横向 chip、批量操作条等。用户非常强调：功能必须严格参考电脑版同步，但呈现方式必须适配手机。

当前最新本地项目目录：

`E:\文档\Codex\小红书\班主任工作台网站`

当前最新部署包：

`E:\文档\Codex\小红书\班主任工作台网站\classroom-deploy-mobile40-homework-highlight-select-20260814.tar.gz`

最新状态：作业详情手机版里，学生批量选择已从小复选框改为“点击头像/学生行选择”，并且最终按用户要求去掉对勾，只保留整行高亮显示选中状态。

## 关键文件

- 主应用组件：
  `E:\文档\Codex\小红书\班主任工作台网站\app\w\[token]\ClassroomApp.tsx`
- 主样式文件：
  `E:\文档\Codex\小红书\班主任工作台网站\app\workbench-repair.css`
- 课程日程组件：
  `E:\文档\Codex\小红书\班主任工作台网站\app\w\[token]\CourseSchedule.tsx`
- 数据类型和工具：
  `E:\文档\Codex\小红书\班主任工作台网站\lib\classroom.ts`
- 数据库相关：
  `E:\文档\Codex\小红书\班主任工作台网站\db`
  `E:\文档\Codex\小红书\班主任工作台网站\data`
- 部署包固定放置目录：
  `E:\文档\Codex\小红书\班主任工作台网站`

## 技术和运行方式

- 前端/全栈框架：Vinext / Next 风格 app router
- 语言：TypeScript + React
- 样式：主要是手写 CSS，集中在 `app/workbench-repair.css`
- 数据库：SQLite，部署在服务器磁盘文件中，不是服务器内存
- 生产数据库路径：
  `/www/classroom/data/classroom.db`
- 生产项目目录：
  `/www/classroom`
- 生产 PM2 进程名：
  `classroom-web`
- 生产访问地址：
  `http://class.nailaobao.top/w/demo`

## 服务器信息和注意事项

用户有一台阿里云服务器，已经运行英语网站。这个班主任项目部署到同一台服务器的二级域名。

已知服务器信息：

- 公网 IP：`47.103.158.152`
- 域名：`nailaobao.top`
- 班主任项目二级域名：`class.nailaobao.top`
- 域名解析：`class` 的 A 记录指向 `47.103.158.152`
- 服务器内存曾检查：
  - 总内存约 `3.4Gi`
  - 可用内存约 `1.2Gi`
  - Swap `4.0Gi`
- 磁盘曾检查：
  - `/dev/vda3` 总 `59G`
  - 已用约 `17G`
  - 可用约 `40G`

非常重要：不要影响线上英语网站。

英语网站相关 PM2 进程包括：

- `naila-api`
- `naila-web`
- `naila-web-test`
- `naila-web-test-dev`
- `naila-web-test-prod`

其中 `naila-web-test-prod` 使用过 `3002` 端口，属于英语网站测试环境，后续可能还要用。不要停止、重启、覆盖、改 Nginx 指到这个进程。

班主任项目应使用独立目录 `/www/classroom`、独立 PM2 进程 `classroom-web`、独立端口。之前配置过程中倾向使用 `127.0.0.1:3012` 给 Nginx 反代。

## 部署方式

每次本地修改后都打包成 `.tar.gz`，用户手动上传到服务器 `/www/` 下，然后在 FinalShell 执行命令。

FinalShell 命令必须一行一个，用户不喜欢多行续写命令。

通用部署命令：

```bash
cd /www/classroom
```

```bash
tar -xzf /www/压缩包文件名.tar.gz -C /www/classroom
```

```bash
npm run build
```

```bash
pm2 restart classroom-web
```

当前最新包部署命令：

```bash
cd /www/classroom
```

```bash
tar -xzf /www/classroom-deploy-mobile40-homework-highlight-select-20260814.tar.gz -C /www/classroom
```

```bash
npm run build
```

```bash
pm2 restart classroom-web
```

## 本地验证命令

每次修改后尽量跑：

```powershell
npm.cmd run build
```

```powershell
npm.cmd run qa:layout
```

```powershell
node "E:\文档\Codex\小红书\.agents\skills\impeccable\scripts\detect.mjs" --json "app/w/[token]/ClassroomApp.tsx" app/workbench-repair.css
```

打包命令模板：

```powershell
tar -czf "E:\文档\Codex\小红书\班主任工作台网站\classroom-deploy-名称.tar.gz" -C "E:\文档\Codex\小红书\班主任工作台网站" app build data db drizzle lib scripts worker .openai .env.example .gitignore drizzle.config.ts next.config.ts package.json package-lock.json postcss.config.mjs tsconfig.json vite.config.ts
```

## 项目历史概览

### 1. 主题恢复和桌面端稳定

早期用户不想继续使用紫白色或 glass 主题，要求恢复最开始的单蓝色主题。

已完成：

- 删除主题按钮。
- 删除 glass 主题页。
- 恢复电脑端单蓝色主题。
- 电脑端当前基本可暂时保持，不作为主要改动对象。

### 2. 手机访问问题和服务器部署

一开始尝试让手机访问电脑本地服务，例如 `http://192.168.18.6:3000/w/demo`，但手机打不开。

排查后决定：

- 不再依赖电脑局域网访问。
- 改用阿里云服务器部署。
- 在同一台服务器上新增二级域名 `class.nailaobao.top`。
- 数据库先用 SQLite，原因是当前主要是文字类数据，成本低、部署简单、适合早期使用。

关于 SQLite 的结论：

- SQLite 是服务器磁盘上的数据库文件，不是占用内存当数据库。
- 使用效果对应用来说可以像普通数据库一样增删改查。
- 适合当前阶段和中小规模文字数据。
- 后期老师数量很多时，可以先通过磁盘扩容、备份策略、写入频率控制继续撑一段；规模明显上来后再迁移到 MySQL/PostgreSQL。

### 3. 服务器部署过程中的关键坑

曾出现访问 `/w/demo` 404，原因和端口/进程不对应有关。

重要结论：

- `3002` 是英语网站测试环境 `naila-web-test-prod`，不要占用。
- 班主任项目需要独立端口和独立 PM2 进程。
- Nginx 应只把 `class.nailaobao.top` 反代到班主任项目端口。
- 不要改 `nailaobao.top`、`www.nailaobao.top`、`test.nailaobao.top` 的线上英语网站配置。

### 4. 手机版整体重做方向

用户明确表示：手机版不能只是电脑版网页竖过来，需要类似 App 的结构。

已逐步建立的手机版设计方向：

- 底部导航：`首页 / 学生 / 作业 / 成绩 / 更多`
- 其余工具通过首页工具宫格和“更多”弹窗进入。
- 页面顶部是公共顶部区域，后来加入了“首页”返回按钮。
- 保存按钮只有在需要保存或保存中时才显示，避免无意义常驻。
- 不做单蓝色工具宫格，工具入口使用彩色但统一规整的图标样式。
- 不在手机页面堆大表格，优先用：
  - 卡片
  - 弹窗
  - 底部 sheet
  - chip 筛选
  - 搜索+筛选行
  - 批量操作条

## 各页面已完成/已改动内容

### 首页 / 工作台

已完成：

- 首页不再只显示少量快捷入口。
- 除班级管理之外，首页显示所有工具。
- 工具宫格改为彩色统一结构。
- 更多弹窗里的工具排列也要和首页一样整齐。
- 公共顶部加入返回首页按钮。
- 顶部原本只显示标题和无意义“已保存”按钮，后来调整为更克制的公共顶部。

用户偏好：

- 首页工具入口类似 App 的全部应用页，但要和当前系统整体风格匹配。
- 彩色可以，但结构必须整齐统一。

### 学生名单

已完成：

- 手机版学生列表不再显示无意义的完整操作区。
- 点击学生才看详情。
- 新增学生不要出现电脑端没有的积分、作业、考勤等字段。
- 学生详情中删除座位号。
- 手机版搜索框和小组筛选压缩为一行，并调整比例，避免小组过窄。
- 电脑版和手机版列表中删除座位信息。
- 电脑版学生名单增加批量选择和批量编辑能力。
- 手机端学生名单也增加批量选择能力。
- 支持批量编辑小组、性别、备注。
- 电脑端全选框对齐问题已修。
- 电脑端批量导入名单区域重新收紧，删除“32 名匹配 第 1/2 页”等无意义文案。

用户偏好：

- 不需要座位号，因为座位是几排几座，不是数字座位号。
- 不需要特别长的搜索框，能合并在一个区域就合并。
- 手机端筛选组件的 label 和输入框位置要统一。

### 作业追踪

已完成：

- 新增作业、编辑作业、保存等基础功能修过，早期有“点保存没反应”的问题，后续已修。
- 电脑端作业详情里的筛选项补了功能提示：
  - 学生搜索
  - 状态
  - 小组
- 手机版每个作业详情中同步电脑端筛选和批量选择功能。
- 手机版作业详情曾经照搬电脑端结构，用户认为繁琐，后来改成更手机化：
  - 删除顶部无意义作业信息卡。
  - 使用横向状态 chip 显示全部、已交、未交、待订正、已复查及数量。
  - 搜索和小组筛选在一行。
  - 批量栏显示已选人数、全选当前、批量改状态、加入跟进。
- 最新修改：
  - 删除每个学生前面的小复选框。
  - 一开始改为点头像选择，选中头像显示对勾。
  - 用户认为对勾多余，最新改为只保留整行高亮显示选中，头像不再显示对勾。

当前最新状态：

- 作业详情手机版的批量选择方式：点击头像/学生行区域触发选择，选中只通过整行浅蓝高亮表达。
- 最新包：`classroom-deploy-mobile40-homework-highlight-select-20260814.tar.gz`

### 成绩分析

已完成：

- 手机版成绩页同步多科成绩显示。
- 增加考试切换。
- 增加搜索。
- 增加筛选排序能力。
- 增加电脑端对应功能：
  - 学生搜索
  - 显示科目
  - 分数区间
  - 重点标记
  - 排序字段
  - 排序方式
  - 编辑筛选项
  - 批量标记重点
  - 取消重点
- 用户指出：
  - 每个学生列表只显示“标记/已标记”，不要显示平均分。
  - 平均分只放详细信息里。
  - 不显示薄弱项。
  - 筛选排序里的“选择科目”是按固定科目排序，不是只显示该科目；只显示科目的功能在外部科目卡片里已有。
- 后来修过列表页排版乱掉问题。

### 积分评价 / 积分规则

已完成：

- 修复积分规则和积分评价重复新增按钮，只保留一个。
- 手机版积分评价按电脑端功能严格同步过一轮。
- 积分页面不应混入作业和成绩内容。
- 给学生调整积分不能用普通下拉框，因为学生多时不好找。
- 后续积分评价做过重新设计，使手机端不只是堆电脑功能。

注意：

- 用户曾明确说：标准要参考“积分评价”和“成长档案”这类已经较完整同步的页面。
- 以后同步其它页面时，不能只是加新增按钮，要把电脑端功能完整迁移，同时改成手机端适配结构。

### 成长档案

已完成：

- 手机版成长档案曾和电脑端差距很大，后续按电脑端功能严格同步。
- 样式适配手机端，使用卡片、弹窗、筛选等方式承载功能。

### 课程日程

已完成：

- 手机版课程日程曾只是电脑端简化版，后来同步更多电脑端能力。
- 去掉顶部无意义“新增日程”按钮。
- “日程”小卡片中补充当前学期时间说明。
- 课程日程存在组件文件 `CourseSchedule.tsx`，改动时需同时注意主组件调用和 CSS。

用户曾问手机是否能承载电脑端复杂日程功能。结论是可以，但不能直接压缩大表格，要使用：

- 周/月切换
- chip
- 底部弹窗编辑
- 横向滚动时段
- 分层展示

### 座位分组

已完成：

- 手机版座位从简单列表改为更接近电脑端座位图的样式。
- 修过“修改座位布局点保存页面没了”的崩溃。
- 修过行列无法自定义输入的问题。
- 修过删除后行列固定异常的问题。
- 点击某个座位弹窗里增加学生选择能力。
- 学生选择不能直接展示全部，要通过筛选搜索方式选择。

用户理解：

- 手机屏幕小，但座位图可以做，只是需要缩放/横向滚动/弹窗承载编辑。

### 班级周报

已完成：

- 去掉周报页面中奇怪的“日程”按钮，之前点击后会跳转课程日程。
- 周报页面曾和电脑版差距很大，后来同步了本周关注学生、小组表现、成长记录等更多电脑端功能。
- 用户指出手机版里的“本周安排”和“周报生成”当时无意义不能互动，后续进行过调整。

### 值日岗位

已完成：

- 按电脑端功能同步值日岗位页面。
- 岗位检查中的完成/返工状态点击后需要有不同字样和颜色，已调整。

### 家校沟通 / 班干部

已完成：

- 按电脑端所有功能严格同步家校沟通页面。
- 按电脑端所有功能严格同步班干部页面。

### 考试反思 / 期末评语

已被纳入“所有工具”和“更多”入口。

已在前期多轮同步中处理过新增、编辑、详情等基础能力，但如果新窗口继续时需要重点复查：

- 是否存在“只有按钮没有反应”的地方。
- 是否存在电脑端功能缺失。
- 是否存在手机端结构只是电脑端压缩版。

## 用户明确偏好和判断标准

1. 电脑版暂时不大动，除非用户明确要求，或需要补齐两端都没有的基础功能。
2. 手机版必须像 App，不要像电脑网页竖排。
3. 功能必须严格同步电脑端，但布局不能照搬电脑端。
4. 手机端页面空间有限，要多用 tab、chip、弹窗、卡片切换、底部 sheet。
5. 列表里不要展开完整操作区，点击条目进入详情/编辑。
6. 无意义按钮和重复标题要删。
7. 常驻“已保存”按钮没有意义，只有脏数据/保存中时显示保存状态。
8. 工具入口可以彩色，但必须整齐统一，不能散乱。
9. FinalShell 指令必须一行一个。
10. 打包文件必须放在：
    `E:\文档\Codex\小红书\班主任工作台网站`
11. 千万不要影响线上英语网站。

## 最近几个部署包脉络

- `classroom-deploy-mobile33-header-summary-20260814.tar.gz`
  - 公共顶部区域整理。
- `classroom-deploy-mobile34-student-batch-home-20260814.tar.gz`
  - 首页/学生批量功能相关。
- `classroom-deploy-mobile35-student-toolbar-20260814.tar.gz`
  - 学生页面筛选和工具栏调整。
- `classroom-deploy-mobile36-roster-cleanup-20260814.tar.gz`
  - 学生名单和座位信息清理。
- `classroom-deploy-mobile37-homework-batch-20260814.tar.gz`
  - 作业页面批量能力。
- `classroom-deploy-mobile38-homework-sheet-mobile-20260814.tar.gz`
  - 作业详情弹窗手机化。
- `classroom-deploy-mobile39-homework-avatar-select-20260814.tar.gz`
  - 作业详情用头像选择，选中显示对勾。
- `classroom-deploy-mobile40-homework-highlight-select-20260814.tar.gz`
  - 最新：去掉对勾，只保留高亮显示选中。

## 当前最新进度

最新完成项：

- 手机版作业详情里的学生选择方式已从复选框改为更适合手机的选择方式。
- 按用户最新要求，选中学生时不显示对勾，只保留整行高亮。
- 最新包已生成并通过构建、布局 QA、界面检测。

最新验证结果：

- `npm run build`：通过
- `npm run qa:layout`：通过
- `impeccable detect`：无报错

最新部署包：

`E:\文档\Codex\小红书\班主任工作台网站\classroom-deploy-mobile40-homework-highlight-select-20260814.tar.gz`

## 新窗口继续建议

接下来最适合做的是继续逐页用手机实际截图校验，而不是只看代码。

优先检查：

1. 作业详情最新版高亮选择是否符合用户预期。
2. 作业详情的批量操作条是否仍然太拥挤，尤其在窄屏上“批量改状态”和“加跟进”是否挤。
3. 学生名单手机版批量选择是否也需要去掉显式复选框，改成类似“长按/点击头像/编辑模式高亮”的手机设计。
4. 继续审查考试反思、期末评语等较少被用户截图复查的页面，确认不是只加了按钮而缺少实际功能。
5. 所有手机页面公共顶部是否一致，避免重复标题区域。

## 新窗口可以直接使用的提示词

可以把下面这段发给新窗口：

“请读取 `E:\文档\Codex\小红书\班主任工作台网站\项目交接记录-班主任工作台-20260814.md`，继续班主任工作台网站项目。重点遵守：不影响电脑端和线上英语网站；手机版要像 App，功能严格同步电脑端但布局不能照搬；部署包必须放在 `E:\文档\Codex\小红书\班主任工作台网站`；FinalShell 指令一行一个。当前最新包是 `classroom-deploy-mobile40-homework-highlight-select-20260814.tar.gz`，最新完成的是作业详情手机版选择学生只保留高亮、不显示对勾。接下来根据我发的截图继续修改。”

## 2026-08-21 商业化隔离与全站体验改造

此段覆盖上方“当前最新进度”。当前项目已完成 HTTPS 暂缓版全站计划，不再以 `mobile40` 为最新基线。

核心完成项：

- 首页两入口、兑换码 + 手机号进入、同浏览器自动恢复。
- 一个用户一个正式 workspace，最多绑定 2 个浏览器设备。
- 正式数据接口不再把 URL token 当权限凭据，必须同时校验 session、用户、设备和 owner。
- `/admin` 支持生成兑换码、禁用/恢复用户、查看/解绑设备及续期一年。
- 一年有效期、30 天只读宽限期、宽限结束禁止访问。
- demo 公开只读，保存 403，AI 静态示例。
- AI 首次确认、每日 20 次、用户可关闭；正式 AI 使用已有 DeepSeek 配置。
- 服务器确认保存、失败保留草稿、未保存拦截和退出失败保护。
- 15 个页面与关键弹窗完成桌面、平板、手机视觉和交互验收。

安全边界：

- 当前 HTTP 模式只能测试或过渡，兑换码、手机号和会话可能被网络链路截获。
- HTTP 必须显式设置 `AUTH_ALLOW_INSECURE_HTTP=true`；未来 HTTPS 后改为 `false`，Cookie 自动增加 `Secure`。
- 管理后台必须配置 `ADMIN_ALLOWED_IPS`，反向代理必须覆盖可信转发头。

本地备份：

`E:\文档\Codex\小红书\班主任工作台网站\backups\classroom-before-auth-20260821-132058.db`

备份已通过 SQLite `PRAGMA quick_check`，结果为 `ok`。服务器正式数据库仍必须在部署前单独备份。

最终验证：

- `npm.cmd test`：6/6 通过。
- `npm.cmd run build`：通过。
- `npm.cmd run qa:layout`：通过。
- `npm.cmd run qa:strict`：通过。
- Impeccable 源码检测：0 项。
- 390、768、1440 三档真实浏览器检查通过。

部署注意：

- 部署包不得包含本地 `.env.local` 和 `data` 目录，避免覆盖服务器密钥与正式数据库。
- 数据库认证表和 `owner_user_id` 由运行时幂等迁移创建。
- 不执行 `npm install`；继续使用服务器现有依赖。
