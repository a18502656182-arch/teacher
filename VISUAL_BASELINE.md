# 班主任工作台视觉基线

## 2026-09-15：考勤C01候选，听写继续暂缓

已登记用户对作业P01的基本接受及未实施的控件比例优化；听写因功能未由用户验收暂缓。按设计顺序例外完成考勤源码功能盘点、只读合成截图及两张ImageGen候选，入口docs/page-designs/V3/attendance/record.json，候选仍待方向确认，关键状态/P01/D/R未完成。手机第四行布局和工具行遗漏明确不作为实现合同；完整功能见function-map。原四项未提交修改保留，未改正式页面，没有部署。

## 2026-09-15最新顺序例外

学生与作业P01已分别获单页D；作业输入/筛选框比例按用户允许暂留后期优化。听写因用户表示功能未验收而暂缓，deferred不等于通过。现允许先做考勤独立候选；旧V3 blocked指正式批次接入/产品进展，不阻止此次用户明确授权的提前设计。主账本workflow记录例外，所有R仍待完成。

## 2026-09-15当前状态补充

作业C01已获用户方向确认；P01隔离可运行稿及40张主状态图等待单页D审核。主账本只链接record/hash。学生P01历史单页批准保留，听写未出本轮设计，V2三页D齐前仍不接业务，R前不进V3。此轮没有共享视觉源码改动、没有部署。


## 当前生效流程：逐页独立设计后接入

以 `docs/逐页设计定稿与实施包-20260914/00-从这里开始.md` 为最新流程入口，取代下文先改产品再监工的旧循环。每页须独立桌面/手机和关键状态设计；候选确认后制作真实可运行稿，本批全部取得D设计批准才能接入，R实际批准后才进下一批。监工只可选辅助；不要求其审美PASS。保留V1/23fcdc5历史批准，当前从V2学生等页面设计开始；首页不作为全站设计批准。原图、事实、回归和持久证据要求继续有效。主账本已迁移schema 3，V2逐页record通过路径和哈希链接；旧字段保留历史含义。学生C01已获用户方向确认，P01隔离可运行设计97d2121已获学生单页设计批准；V2批次D未齐、R为空，作业进入C01候选、听写为inventory。当前证据与命令见docs/page-designs/V2/RECOVERY-20260914.md。下文旧循环及强制监工要求仅保留历史，现行规则以新包和schema 3为准。

最后更新：2026-09-14。

具体实施、逐页任务卡与独立监工协议见 `docs/重构执行与监工资料包-20260914/00-使用说明.md`。每页提交监工检查点，监工PASS只允许继续本门内下一页；V1-V7结束均仍须人工批准。V0已实施部分提交/证据约束；25950b7监工报告另发现资源及视觉配置、规范化路径去重缺口，本轮按R5补充针对性验证。检查器是辅助约束，不能称为不可绕过的权限系统，也不据此宣称所有缺口已消除。

本文件是所有教师工作台视觉修改的唯一恢复入口。聊天记录、摘要、旧验收报告和自动测试结果都不能替代本文件、参考图与当前实际截图。发生换对话、上下文压缩、长时间中断或共享样式修改后，必须重新读取本文件和 `trackers/visual-baseline.json`，并实际打开本门参考图。

## 当前事实

- 2026-09-13 的 TASK-16 曾将 C1-C6 误判为视觉通过；旧结论已经撤销，不能作为后续完成证据。
- 当前教师页面功能、保存链路与响应式测试已有既存证据，但视觉丰富度与参考构图仍需重新验收。
- 已确认的首个回归是路由级通用 `.campus-art` 固定尺寸覆盖页面专用铺满规则，使首页大场景退化为小配图；V1候选实现已修复根因，仍待监工与用户按提交验收。
- 家庭教育不属于本轮视觉纠偏范围，不得夹带修改或纳入通过数量。

## 永久参考

原始 C1-C6 保留在本地实施包中并由 SHA-256 校验；为避免换机、重新克隆或上下文压缩后只剩文字描述，Git 同时保存视觉等价的高质量 WebP 副本：

`docs/visual-baseline/references/`

原图路径、原图哈希、Git副本哈希、目标页面和视口全部登记在 `trackers/visual-baseline.json`。副本用于恢复视觉方向；原图存在时，最终同尺寸对照优先使用原图。

## 七个人工暂停门槛

| 门槛 | 页面 | 暂停规则 |
|---|---|---|
| V1 | 今日工作台（桌面＋手机） | 单页定准后立即暂停 |
| V2 | 学生名单、作业追踪、听写与复习 | 三页完成后暂停 |
| V3 | 考勤与请假、成长档案、健康与照护 | 三页完成后暂停 |
| V4 | 家校沟通、成绩分析、考试反思 | 三页完成后暂停 |
| V5 | 课程日程、课堂工具、座位分组 | 三页完成后暂停 |
| V6 | 值日岗位、班干部、积分规则 | 三页完成后暂停 |
| V7 | 积分评价、班级周报、期末评语 | 三页完成后暂停 |

每一门都必须在聊天中向用户展示本门桌面/手机实际图与参考对照，并等待用户明确表示“通过、可以继续”或等价确认。未确认时门槛只能是 `awaiting-user-review`，不得自动开始下一门。用户提出问题时，本门回到 `needs-fix`。

## 每次视觉任务的固定循环

1. 读取本文件、JSON账本、当前Git差异和本门参考图。
2. 明确本次只修哪些已登记差距；业务逻辑、API、数据库、保存时序和家庭教育保持不动。
3. 修改一个页面或一个共享根因；共享CSS不得一次性覆盖页面专用构图。
4. 运行 `npm run qa:visual-baseline`、匹配风险的测试及现有严格QA。
5. 在固定视口生成桌面与手机实际截图；不得用缩小整页或不同视口伪装同尺寸比较。
6. 将参考图与实际图并排检查：视觉焦点、插画面积/裁切、主从比例、字号梯度、色彩面积、留白用途、操作位置及长数据状态。
7. 更新JSON中的问题、状态、`currentEvidence`、`comparisons` 和提交号。普通当前截图不能充当对比证据；明显差距存在时只能记 `needs-fix`。
8. 完成本门全部页面后运行 `npm run qa:visual-gate -- --gate Vn`，向用户展示证据并暂停。
9. 用户确认后才记录批准日期与提交，并开始下一门。

## V1–V7持续网格检查（每次视觉修改必做）

逐项检查页面网格、上下模块列分界/列间距、标题/图标/表格/内容起点、内外边距、按钮基线和分离/连体容器表达。没有功能分组理由就统一；有功能依据的不对称需说明理由并以实际图验证，不能用“故意设计”解释未验证的不整齐。

核对桌面、手机、空态、长内容；不得为了等高制造大留白，不裁内容、不删除真实数据、不缩小整页、不牺牲手机重排。共享布局修改须复核所有受影响页面及已批准页面。提交固定视口、连续滚动及局部对照，明确已修对齐和保留差异。自动测试不替代人工对齐、裁切和构图判断。

## 不能证明视觉通过的证据

- 自动测试或构建通过；
- 页面无横向溢出；
- 路由、按钮和保存功能正常；
- 已使用校园配色或插画；
- 页面存在左右布局；
- 旧任务卡或旧报告写着“通过”。

这些证据仍然必要，但只能证明功能、结构或稳定性，不证明视觉达到参考图。

## 共享修改触发全量回归

修改以下任一范围后，所有已经由用户批准的门槛都必须重新截图复核；修改发生时立即撤销当前批准有效性，并将门槛及其页面状态改为 `needs-regression-review`，历史批准日期和提交号可保留作为审计记录：

- `ClassroomPages.module.css` 或其他路由级/全局CSS；
- `.campus-art`、主题变量、ThemeArtwork或图片资源；
- WorkbenchShell、内容宽度、断点和手机底栏；
- 公共页头、按钮、容器、图标、表格或弹窗规则；
- 会改变字体加载、图片裁切、缩放或构建CSS顺序的配置。

## 完成定义

一个页面只有同时满足以下条件才能记为 `user-approved`：功能回归通过、固定视口实际图存在、同尺寸对照图存在、机器门槛通过、无未解决P0/P1视觉问题，并且用户已经明确确认。全站视觉纠偏只有V1-V7全部获用户确认后才完成。
