# 班主任工作台彩色校园工作台视觉改造执行计划

日期：2026-08-24  
依据：`PRODUCT.md`、`DESIGN.md`、`同行产品对标与全站优化建议-班主任工作台-20260824.md`  
范围：桌面端与手机端前端视觉、信息层级和表现层组件  
状态：待实施

## 1. 目标

把当前偏通用后台、蓝色占比过高、页面公式重复的界面，升级为具有宣传辨识度且适合长期使用的“彩色班务手册”。

本轮需要同时达成两件事：

1. 宣传截图一眼能被记住：深墨青品牌框架、固定 Emoji、丰富校园色、不同页面有不同视觉主体。
2. 老师每天使用仍然高效：白底表格和表单、清楚状态、紧凑列表、稳定筛选、手机底部弹窗。

视觉改造不是产品扩功能阶段。本计划不实现考勤请假、健康照护、工作留痕、随机点名、试卷分析或兑换码商业闭环等新功能；这些继续保留在对标文档和商业审核文档中单独排期。

## 2. 必须保护的现有能力

改造期间以下内容视为回归保护线：

- 15 个页面路由和页面入口不变。
- workspace JSON 数据结构和服务端保存接口不变。
- 用户、兑换码、设备、session、owner 隔离和到期规则不变。
- `/w/demo` 保持公开只读，正式 workspace 保持认证保护。
- 单人录分、多选批量录分、作业批量状态、积分选择学生等核心流程不丢失。
- 手机端底部导航保留。
- `值日岗位`的星期按钮条和台账状态按钮条保留按钮形式。
- 已确认的搜索、小组筛选、多学期和多考试切换不因换皮消失。
- 保存失败、只读、冲突、未保存和演示模式提示必须继续可见。

任何视觉修改若需要改变上述逻辑，必须停止并拆成独立功能任务，不能在 CSS 或组件迁移中顺带完成。

## 3. 实施纪律

### 3.1 代码纪律

- 不新增 npm 依赖，不执行 `npm install`。
- 不全量重写 `ClassroomApp.tsx`，按公共壳层、组件和页面分批迁移。
- 新样式使用 `wb-*`、`module-*` 或现有明确页面命名空间，避免继续增加无边界的 `button`、`section`、`h2` 全局覆盖。
- 先建立 token 和兼容层，再替换页面，不在每个页面复制颜色与圆角常量。
- 避免依赖 DOM 偶然层级的超长选择器；组件状态通过语义 class 或 data attribute 表达。
- 不删除看似无用但尚未完成业务确认的事件处理、字段或状态。
- 手工改文件使用 `apply_patch`；格式化和已有 QA 命令可直接运行。

### 3.2 视觉纪律

- 单屏高饱和色面积保持约 20% 至 30%。
- 主操作只有一个，低频操作不与主操作同级。
- Emoji 始终配中文标签，不在正文和表格中随意散落。
- 首页、成绩、课程和成长四个宣传样板页允许更鲜明；录入、规则和敏感页面保持克制。
- 普通业务容器默认无阴影，不用卡片包卡片。
- 手机端禁止横向滚动，禁止把桌面表格和四列表单直接压缩。
- 对比度、触控尺寸、焦点和减少动效属于验收项，不是最后补丁。

### 3.3 分包与确认纪律

每个 Phase 单独形成可构建的提交范围或部署包。每一阶段完成后先出截图和测试结果，再进入下一阶段。四个样板页没有获得视觉确认前，不批量迁移剩余页面。

## 4. 影响文件与建议边界

### 主要文件

- `app/workbench-repair.css`：设计 token、壳层、公共组件和页面命名空间样式。
- `app/w/[token]/ClassroomApp.tsx`：导航映射、公共展示组件、页面结构 class 和必要的表现层拆分。
- `app/w/[token]/CourseSchedule.tsx`：课程日程样板页结构和模块化视觉。
- `app/globals.css`：只保留真正全局的基础重置、字体和画布规则。

### 文档与 QA

- `PRODUCT.md`：产品事实，不随视觉实现改写。
- `DESIGN.md`：目标视觉规范；实现中出现稳定的新规则时同步更新。
- `.impeccable/design.json`：在第一阶段 token 最终确定后生成，承载阴影、断点、动效和组件扩展，不提前复制未落地 token。
- `scripts/layout-audit.mjs`：只在现有检查无法覆盖新壳层时扩展，不降低原检查标准。

## 5. Phase 0：基线冻结与页面清单

### 目标

在改变任何界面前，固定功能和视觉基线，避免后续只能凭印象判断“是不是改坏了”。

### 工作内容

1. 记录当前工作区已有改动，不执行 reset、checkout 或无关清理。
2. 执行数据库备份脚本并确认备份文件可以读取。
3. 对 15 个页面生成 390px、768px、1280px、1440px 基线截图。
4. 补充关键弹窗截图：
   - 作业详情与批量状态。
   - 成绩筛选、单人录分和批量录分。
   - 积分选择学生。
   - 成长档案学生详情与添加记录。
   - 家校沟通详情与新增。
   - 考试反思填写。
   - 期末评语编辑和 AI 区域。
   - 课程编辑、座位详情、值日指定和班干部详情。
5. 为每页记录当前主操作、筛选、列表/表格、弹窗和空状态，作为功能对照表。
6. 记录构建、布局 QA、严格 QA、测试和 lint 当前结果；已有失败不得归咎于视觉改造。

### 命令

```bash
npm.cmd run backup:db
npm.cmd run build
npm.cmd run qa:layout
npm.cmd run qa:strict
npm.cmd test
npm.cmd run lint
```

截图可使用：

```bash
$env:QA_SCREENSHOT_DIR='.qa-shots\visual-baseline'
npm.cmd run qa:strict
```

### 完成门槛

- 15 页和关键弹窗都有基线证据。
- 当前测试结果已记录。
- 没有修改业务文件。

## 6. Phase 1：设计 token 与公共表现层

### 目标

先建立统一语法，不直接大改页面布局。完成后旧页面仍可工作，只是具备逐页迁移基础。

### 工作内容

1. 在 `app/workbench-repair.css` 建立 `DESIGN.md` 对应变量：
   - 品牌色、模块色、状态色和中性色。
   - 8px/10px/999px 圆角。
   - 4/8/12/16/24/32px 间距。
   - 字号、数字对齐、边框、阴影、焦点和动效。
2. 清理新代码对 Bootstrap 变量名和默认 `#0d6efd` 的依赖，但不一次删除所有兼容变量。
3. 建立公共表现层 class 或轻量组件：
   - `ModuleMarker`：Emoji + 中文标签 + 模块色。
   - `PageContext`：班级、学期、考试或周次上下文。
   - `ActionButtonGroup`：唯一主操作 + 次级操作。
   - `StatusBadge`：统一状态映射。
   - `MetricCard`：只用于真实关键数字。
   - `EntityRow`：手机对象列表行。
   - `SheetSection`：手机底部弹窗分区。
4. 统一按钮、输入框、下拉、标签、卡片、列表行、表格、弹窗和空状态基础样式。
5. 增加 `prefers-reduced-motion` 和可见焦点规则。
6. 生成 `.impeccable/design.json`，内容与已落地 token 保持一致。

### 禁止事项

- 不改变页面结构和信息顺序。
- 不在这一阶段给所有旧卡片强行加彩色渐变。
- 不通过全局 `!important` 覆盖解决局部冲突。

### 完成门槛

- 旧页面业务可正常操作。
- 新 token 可被壳层和样板页直接引用。
- 输入、按钮和状态对比度符合要求。
- 构建、`qa:layout` 和 `qa:strict` 通过。

### 输出包建议

`visual-foundation`

## 7. Phase 2：桌面壳层、手机导航与页头

### 目标

建立全站第一眼识别，同时解决桌面顶部与页面标题重复的问题。

### 桌面端

1. 左侧导航改为深墨青品牌框架。
2. “班”品牌标记保留，使用晴空蓝到鸢尾紫的克制品牌过渡；不扩大成宣传 Hero。
3. 15 个导航项使用固定 Emoji + 中文标签，按“今日 / 学生 / 输出 / 班级设置”分组。
4. 当前项使用模块色浅底、清楚文字和稳定位置标记，不让所有当前项都变成同一种蓝。
5. 班级选择压缩为清楚的上下文控件；新建/删除班级进入班级设置，不长期占据侧栏首屏。
6. 删除桌面顶部重复的模块名，只保留：
   - 当前班级与学期上下文。
   - 页面标题和可选模块 Emoji。
   - 一个主操作与一个“更多”。
7. 账户入口保持低频，不显示“已同步”“自动同步”等无意义常驻文案。

### 手机端

1. 保留固定底部导航，不增加左侧竖栏。
2. 映射为 `📊 首页 / 🎒 学生 / 📚 作业 / 📈 成绩 / ••• 更多`。
3. 当前项使用浅模块色底板和清楚文本。
4. “更多”重构为 3×N 工具网格，入口尺寸固定，按学生工作、输出、班级设置分组。
5. 页面顶部仅显示当前任务所需的标题、上下文和主操作，不重复桌面信息层级。

### 验收重点

- 在家校沟通、作业追踪、成长档案三页确认页头不再重复。
- 侧栏在 1280px 下不挤压表格关键列。
- 手机底部导航不遮挡弹窗按钮和长列表最后一项。
- Emoji 在 Windows 浏览器截图中与文字基线协调。

### 完成门槛

- 15 页都能通过新导航到达。
- 当前页面状态准确。
- 桌面与手机均无横向滚动。
- 账户、班级切换和更多菜单功能不丢失。

### 输出包建议

`shell-emoji-navigation`

## 8. Phase 3：样板页一——今日工作台与成绩分析

四个样板页分两轮完成。第一轮验证“宣传吸引力”和“数据操作效率”能否同时成立。

### 8.1 今日工作台

#### 目标构图

- 首屏标题为今天的班级工作，而不是功能介绍。
- 最多 4 张 2×2 关键行动卡，颜色来自不同语义，不重复蓝黄绿模板。
- 卡片只展示有行动价值的信息，例如待处理、异常学生、今日值日和接下来事项。
- 第二部分使用“现在 / 接下来 / 待跟进”的时间线或行动列表。
- 快捷入口限制在 6 个以内，使用 Emoji 彩色入口；其余进入更多。

#### 禁止事项

- 不把所有模块数量塞进仪表盘。
- 不展示无法点击、无法解释或不需要处理的统计。
- 不用八张同尺寸大卡占满首屏。

### 8.2 成绩分析

#### 目标构图

- 当前考试、学科和模式切换形成一条清楚的分析上下文。
- 关键指标只保留对判断有用的平均分、录入情况、分数段或异常人数。
- 图表使用鸢尾紫为主、青翠绿/金盏黄/珊瑚橙为辅的多色规则。
- 图表可定位到学生明细；未录、异常和重点学生优先。
- 学生列表保留单人录分和多选后批量录分。
- 手机筛选弹窗按实际逻辑组织，不出现桌面字段硬搬或无意义选项。

#### 禁止事项

- 不公开强化排名，不把学生价值简化为单一名次。
- 不增加没有明细和行动的装饰图表。
- 不让四个批量按钮长期挤在一行。

### 样板确认闸门 A

在继续下一阶段前必须提供：

- 390px、1280px、1440px 的首页与成绩截图。
- 首页第一屏和成绩分析第一屏的并排对比。
- 成绩筛选、单人录分和批量录分弹窗截图。
- 对比度、横向溢出、按钮主次和长文本检查结论。

用户未确认方向时，只修改这两个样板页，不迁移其他页面。

### 输出包建议

`showcase-dashboard-scores`

## 9. Phase 4：样板页二——课程日程与成长档案

第二轮验证“时间型页面”和“学生对象型页面”的设计语言。

### 9.1 课程日程

#### 目标构图

- 月份与周次压缩为紧凑上下文区，不形成框套框。
- 保留周一到周五/第 6 天按钮条，其尺寸、字体和选中态统一。
- 本周课表成为视觉主体，课程用稳定学科色、时间、节次和内容构成时间轴感列表。
- “编辑当天”是当前唯一主操作；重复配置、保存和首页按钮不出现。
- 桌面可展示周课表，手机按单日切换，不在手机强塞完整周表。

### 9.2 成长档案

#### 目标构图

- 桌面保持学生列表 + 学生详情；手机主页面只保留学生列表。
- 学生详情先显示当前学生、最近证据和“添加成长记录”主操作。
- 自动摘要降为次级能力，不能挡在添加记录之前。
- 点击添加记录时对象姓名始终明确，记录完成后回到该学生详情。
- 证据来源使用成绩、作业、积分、沟通等稳定模块色，不把整块内容染色。
- 长期记录采用时间线、筛选和分段加载，不无限铺开。

### 样板确认闸门 B

必须提供：

- 390px 和 1280px 课程日程截图。
- 课程编辑和当天切换状态截图。
- 390px 和 1280px 成长档案截图。
- 学生详情、添加成长记录和自动摘要次级入口截图。
- 说明四个样板页的构图差异，证明没有再次套用同一页面公式。

四个样板页全部确认后，才进入其余页面迁移。

### 输出包建议

`showcase-schedule-growth`

## 10. Phase 5：高频业务页迁移

### 10.1 学生名单

- 桌面保留高密度表格，彩色只用于学生缩写、状态和选择反馈。
- 手机搜索和小组筛选一行平均分；列表显示姓名、学号、小组和关键状态。
- 圆形选择控件清楚可识别，不恢复大头像框。
- 点击学生打开详情；多选后才显示批量操作。
- 新增学生是唯一主操作，导入、导出和名单维护进入更多或展开区。

### 10.2 作业追踪

- 桌面使用任务列表/归档 + 当前作业状态工作区。
- 手机主页面保留科目、时间和内容搜索，任务卡层级更清楚但不过大。
- 详情中作业摘要压缩，学生列表成为主体。
- 选中学生整行高亮，多选后显示批量状态。
- 科目使用稳定模块色，状态仍使用完成/待处理/风险语义色。

### 10.3 积分评价

- 页面只保留记分和常用规则。
- 学生选择进入弹窗，规则分类使用换行网格、下拉或筛选弹窗，不横向滚动。
- 常用规则更像紧凑规则清单，不做大卡片墙。
- 选择学生和规则后，记分提交是唯一主操作。

### 10.4 家校沟通

- 桌面列表按待跟进优先，搜索、类型和状态对齐。
- 手机使用紧凑沟通记录行，长摘要正确换行或省略后进入详情。
- 新增/编辑弹窗按“学生与时间 / 沟通信息 / 内容 / 后续跟进”分组。
- 莓果粉只用于模块识别和关键跟进，不把敏感详情做成宣传卡。

### 完成门槛

- 32 人、8 个小组、30 条沟通记录下仍可操作。
- 多选、批量和详情流程与基线一致。
- 每页第一屏主任务明确，没有重复大统计区。

### 输出包建议

`workflow-high-frequency-pages`

## 11. Phase 6：输出与管理页迁移

### 11.1 积分规则

- 去除筛选区域框套框。
- 关键词、分类和状态在同一工具栏；手机分类和状态使用下拉。
- 桌面优先规则表格，手机使用规则列表行。
- 编辑、复制、启停和删除进入详情，新增规则是唯一主操作。

### 11.2 班级周报

- 保留“概览 / 周报库”的已确认结构。
- 统一湖水青模块色、行距、按钮和详情弹窗。
- 复制正文放在详情或编辑区，不抢首页入口。

### 11.3 考试反思

- 保留考试切换和学生状态，优先未填写与草稿。
- 不恢复“薄弱科”“选择反思对象”“成绩页”或“成绩联动”标题。
- 手机点击学生进入填写弹窗，鸢尾紫只用于复盘状态和主操作。

### 11.4 期末评语

- 保留“学生列表 / 保存后的评语”两个入口和主页面学期切换。
- 学生列表保留小组筛选，点击姓名直接编辑。
- 编辑弹窗不出现学生下拉、学期下拉、成绩卡或作业卡。
- AI 只在编辑场景出现，依据、生成、草稿和保存主次清楚。

### 11.5 座位分组

- 座位图是视觉主体，智能排座为主操作。
- 手机删除低价值搜索座位板块，点击座位进入学生选择。
- 座位条件和换座记录进入详情，不与座位图竞争。

### 11.6 值日岗位

- 保留星期按钮条和台账状态按钮条，不改下拉。
- 岗位列表改为检查清单，默认完成、异常再标返工。
- 指定学生、编辑岗位和岗位详情进入弹窗。
- 移除重复复制按钮和无行动统计卡。

### 11.7 班干部

- `新增班委 / 新增小组长`在桌面与手机均等宽同高，作为一个按钮组。
- 岗位筛选使用稳定分段条，不再出现上下两组三格统计。
- 未设置小组长使用紧凑提示，不占大卡。
- 复制聘任书进入更多或详情。

### 完成门槛

- 所有 15 页完成新系统迁移。
- 设置型页面没有宣传页式大卡和高饱和彩色面积。
- 输出型页面的编辑、复制、AI 和保存层级准确。
- 座位、值日和班干部保留原有业务交互。

### 输出包建议

`remaining-workflow-pages`

## 12. Phase 7：全站视觉、功能与宣传验收

### 12.1 自动化检查

执行：

```bash
npm.cmd run build
npm.cmd run qa:layout
npm.cmd run qa:strict
npm.cmd test
npm.cmd run lint
```

不得通过删除断言、放宽横向溢出阈值或跳过页面来让 QA 变绿。

### 12.2 视口矩阵

- 390px：主流手机。
- 768px：平板与窄桌面。
- 1280px：普通桌面。
- 1440px：宣传和宽桌面。

15 个页面逐页检查，关键弹窗再次抽查。

### 12.3 数据规模

- 32 名学生。
- 8 个小组。
- 30 条以上评语。
- 30 条以上沟通记录。
- 20 条以上积分规则。
- 3 次以上考试。
- 多学期和多周次切换。

### 12.4 功能回归清单

- 导航、班级切换、账户和更多菜单。
- 搜索、筛选、排序、分页和清空条件。
- 单人编辑、多选、全选和批量操作。
- 保存成功、保存失败、冲突、只读和演示模式。
- 弹窗打开、关闭、焦点、Esc、底部固定操作和返回上下文。
- 手机与桌面对同一数据的写入结果一致。
- 正式 workspace 与 demo 的权限边界不变。

### 12.5 视觉检查清单

- 没有横向滚动条、裁切和底部导航遮挡。
- 没有框套框、重复页头和重复主按钮。
- 没有浅到看不清的按钮、标签、渐变和图表文字。
- 颜色语义一致，危险红未被装饰滥用。
- Emoji 映射一致，且始终配文字。
- 每页首屏能看懂用途和主要动作。
- 四个宣传样板页构图不同，但属于同一品牌系统。
- 其余工作页不因追求宣传效果而降低列表密度。
- Windows、Android 和 iOS Emoji 差异已通过固定宣传截图环境控制。

### 12.6 Impeccable 终审

执行两轮即可：

1. 第一轮集中发现层级、密度、颜色、可访问性、响应式和交互问题并修复。
2. 第二轮只做最终确认，不无限添加装饰和新概念。

终审发现的业务问题记录到独立功能清单，不混入视觉补丁。

### 输出包建议

`visual-final-qa`

## 13. 宣传截图交付

最终只选最能代表产品的四组画面：

1. `📊 今日工作台`：彩色关键行动 + 当天时间线。
2. `📈 成绩分析`：多色分布/趋势 + 可行动学生明细。
3. `🗓️ 课程日程`：彩色课表 + 当天活动。
4. `🌱 成长档案`：学生状态 + 最近证据时间线。

截图要求：

- 使用固定浏览器、操作系统、视口和演示数据。
- 不显示真实学生姓名、电话、健康或沟通隐私。
- 桌面和手机各至少一张。
- 不在截图后期篡改产品能力、数字或状态。
- 宣传文案只描述真实可用能力，不把候选功能写成已上线。

## 14. 风险与应对

| 风险 | 可能表现 | 应对 |
| --- | --- | --- |
| Emoji 变得幼稚 | 大量裸 Emoji、正文到处散落 | 固定映射、配文字、限制在导航和入口 |
| 色彩变得花哨 | 每张卡不同渐变、状态语义混乱 | 20-30 色量、一色一义、模块色与状态色分离 |
| 全局 CSS 回归 | 一个按钮修改影响 15 页 | 命名空间、token 兼容层、分包截图 |
| 宣传压过效率 | 表格被大卡替代、首屏信息减少 | 只让四个样板页承担宣传，管理页保持高密度 |
| 手机继续硬搬桌面 | 多列字段、按钮一行塞满 | 每页重新确认手机主任务，详情进底部弹窗 |
| 视觉改坏保存 | 事件、状态或禁用逻辑被重构 | 不改业务 handler，按基线逐流程回归 |
| 一次改太多无法判断 | 全站完成后才发现方向不对 | 两个样板确认闸门，未确认不扩散 |
| 竞品风格侵权或同质化 | 复制其藏蓝、比例和具体构图 | 只吸收 Emoji + 彩色方法，使用自有墨青和校园色谱 |

## 15. 最终完成定义

只有同时满足以下条件，视觉改造才算完成：

- `PRODUCT.md` 中的业务事实和保护线没有被破坏。
- `DESIGN.md` 的颜色、Emoji、结构、组件和可访问性规则已经落地。
- 15 个页面和关键弹窗在四类视口下通过检查。
- 四个样板页具有宣传记忆点，其余页面保持专业工作效率。
- 构建、布局、严格 QA、功能测试和 lint 结果已记录并处理。
- 没有通过新增依赖、删功能或放宽测试完成视觉目标。
- 商业上线阻断项仍被如实记录，没有因界面更精美而被误判为已经解决。
