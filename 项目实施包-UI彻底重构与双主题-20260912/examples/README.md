# 独立示例入口

阅读 ../14-代码样例使用说明.md。这里没有生产页面，不发请求，不读数据库。
contracts.ts.txt 与 theme-tokens.css.txt 只是契约建议，不能自动复制接入。
运行 node --test examples/save-transition.test.mjs（工作目录为本实施包）。
fixture-spec.json不是备份；source-symbol-index不是AST，也不保证列出全部事件。
