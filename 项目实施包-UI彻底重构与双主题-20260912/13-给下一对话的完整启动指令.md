# 给下一对话的启动指令

复制以下内容即可；不能只读这段而不读本包。

请接手班主任工作台的UI彻底重构实施。唯一目录：E:/文档/Codex/小红书/班主任工作台网站。
实施包：项目实施包-UI彻底重构与双主题-20260912/00-从这里开始.md。

先按根AGENTS读项目记忆，再按实施包00规定完整阅读编号文档，实际查看全部校园参考图、玻璃参考图和历史组件样板。主方向已经确认：校园插画完整首版，浅色玻璃只预留架构，不展示切换。不要重新发起风格选择。

本任务允许清除重建全部UI/DOM/CSS，但保留19模块、家庭听写、账户管理、所有既有动作、数据契约、只读、保存、版本冲突、草稿与备份。不允许换色覆盖旧结构，也不允许借重构删功能或改统计。先盘点桌面/手机的每个操作和浮层，再抽取业务操作、建立新组件与页面。当前源码比历史文档更能证明已有能力，实施包比旧UI更能约束目标设计。

核对Git、未提交修改、源码baseline和trackers/progress.json。从最早未完成且依赖满足的task实施，不重做已有有效证据的任务。目录中有.codegraph则先用，没有则rg。

按任务卡逐步执行，先形成C1–C6实际渲染基准，再迁移全部页面族。主题与resize不得清空草稿；新页面不能挂homework-bootstrap-shell。旧CSS先隔离，逐页迁移后删除，最终移除全局导入。保存失败不跳下一位，409不静默覆盖，家庭孩子不进班级。

每完成页同时验证桌面/手机/编辑/错误状态。更新任务、功能、弹窗、验收账本，更新项目CHANGELOG/PROJECT_STATUS和长期DECISIONS。只在验证通过后提交推送github/main。只生成本地部署包，未获明确部署授权不操作生产。

如果本包与后续用户新要求冲突，以新要求为准并记决策；如果文档遗漏旧操作，补到账本并保留。不要说‘自动测试通过所以视觉通过’。最后分别报告业务、视觉、架构、性能、未完成项、Git和部署状态。
