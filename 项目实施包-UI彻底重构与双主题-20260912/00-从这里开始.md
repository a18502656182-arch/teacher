# UI彻底重构与双主题：执行入口

版本：1.0，2026-09-12。交付性质：实施规范、任务清单、参考资源和独立代码样例。不是已经完成的重构，也不是部署包。

## 已确认的授权与目标

用户已接受保留全部业务能力、重新建立整个界面层的方案；本次要求先把具体实操方案生成到项目。打开本包不自动代表可以发布线上。新对话收到“按本包实施”或“继续重构”后，从任务进度第一个未完成项开始本地实施。仅收到“读取/检查”时先核验，不改运行页面。

校园插画是唯一首版目标，未来浅色玻璃仅预留完整架构。不得再次询问主题方向。19模块、家庭听写、账户、管理后台、保存与备份均保留。允许全部重建展示层，最终删除旧布局依赖。不得换成普通后台模板、卡片墙或只改色加图。

## 必读顺序

1. 项目根目录 AGENTS.md、项目导航.md、docs/开发导航与状态-20260911.md、PROJECT_STATUS、CHANGELOG最新部分、DECISIONS；部署任务另读DEPLOYMENT_LOG。
2. 本包00、01、02、03、04、05、06、07、08、09、10、11、12、13、14、15、16、17，按文件编号读。不能只读最后的启动词。
3. 实际打开 references/campus 下全部6张图。参考图由原包逐字节复制，有哈希。
4. 打开 references/glass 下全部6张图，理解未来材质及家庭信息结构，不实现玻璃页面。
5. 打开参考图查看器 index.html；阅读 references/README.md；阅读 historical/ 中原始参考纠错、构图规范；打开 historical/组件样板.html。旧样板只用于能力清单，不是新设计成品。
6. 阅读当前任务涉及的 pages/*.md；打开当前源码并查证每项旧操作，随后填 trackers/*.csv/json。
7. 需要实现公共接口时读 examples/README.md；样例不是生产组件，禁止整包复制到 app。

## 资料冲突规则

当前用户明确要求 > 当前项目安全和业务契约 > 本包重构目标 > 20260911构图规范 > 原资料包 > 当前旧UI和历史截图。源码证明“当前实现”，不能以源码旧布局否定用户的新设计目标；规范不能证明功能已经存在。

旧资料“缩减大场景”的建议不能抵消C1–C6的视觉构图要求；缩减假口号和无用装饰，保留校园识别和明确的插画体量。现有DESIGN.md记录的是被用户指出不足的实现，不是本轮不可变更的视觉上限。

## 先做这五件事

1. 核对Git与未提交修改，记入 trackers/progress.json。不要覆盖用户已有工作。
2. 比对 evidence/source-baseline.json 的源码哈希，漂移是提醒，不是重置理由。查当前 .codegraph 是否存在，存在先CodeGraph，否则rg。
3. 从 TASK-01 开始，核对两个端的旧操作，补全功能保全账本和弹窗账本。
4. 先做独立新界面基准，完成C1–C6对照，再按阶段迁移；不能先全站套共享页头。
5. 每完成任务更新进度、证据路径与项目记忆；后继任务不能把 planned 读成 done。

## 本包不包含

真实学生/家长资料、数据库、环境变量、账户token、聊天恢复、历史QA截图、部署产物。参考图包含示例内容，不得导入正式账户。
本包不能保证后续代理绝不犯错；它通过明确优先级、逐项任务、状态机和退出门槛减少歧义。遇到未列出的旧功能先加到保全账本，不得以“文档没写”为由删除。

## 可移植与公开仓库

原创实施文档、任务和代码样例可随Git；references中的用户图片、historical原资料和assets素材副本仅本地保存并纳入本地zip。仅克隆Git的对话可能缺这些图片，应使用完整zip或当前原资料包恢复引用，不能只读文字开始UI实施。
不要上传zip到公开仓库。截图/QA输出不在初版包中，也不应放入公开Git。

## 工具与技能冲突

本轮已经确认视觉方向，技能中的随机选风格/反复确认不适用。先遵守用户校园方向。AGENTS要求避免无意义卡片，允许确有独立任务意义的工作面；不能把该规则解释为必须去掉参考所有色块和插画。检验轮次不足时报告needs-fix，不以工具预算结束作为done依据。
