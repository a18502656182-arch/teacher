# 已定事项与真正待决策事项

已定：校园风格、保留所有业务、UI可彻底重构、单业务树、玻璃不公开、逐页移除旧CSS、必须真实浏览器、数据隔离、本地实施优先。不要重复询问。

默认实施决定：CSS Modules+语义token；新控制器复用现有数据函数；任务批改材料用顶部情境区/可展开详情而非强制第四列；默认名单突出任务所需字段，完整资料进入编辑模式；手机返回保留筛选；状态主题不进入业务备份。

仅以下情况需要具体新决策：
1. 桌面/手机当前统计口径互相冲突，无法由现有用户需求确定哪一个正确。
2. 功能保全要求必须改变API/数据库/权限或备份格式才可实现。
3. 用户新增实际门户、OCR供应商、收费、对外消息或生产操作范围。
4. 发现可能属于用户的未提交修改，无法安全分离。

一般像素、组件文件名、插画裁切、合理间距由执行者按实渲染决定，写入设计记录即可。上面的默认决定如确有可用性反证，可提出具体替代并记录，不将‘更省事’当依据。
本次包制作只定义后续工作，没有开始TASK-01的全量功能审计，也没有完成新UI；trackers全为planned是准确状态。
