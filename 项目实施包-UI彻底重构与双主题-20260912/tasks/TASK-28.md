# TASK-28 周报评语迁移

状态：done。前置任务：21,23,24,25,27。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：历史/依据/编辑预览；AI同意与失败；复制归档。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：weekly/comments。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：weekreport/comment5布局。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：真实依据、草稿、AI失败、长文。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`9620575`
- 修改文件：`app/w/[token]/ClassroomApp.tsx`、`scripts/layout-audit.mjs`、`tests/rendered-html.test.mjs`及本任务账本/证据。
- 保全行为/删除旧依赖：桌面和手机周报、评语写入均等待workspace服务器确认；同步失败保留本机内容、当前学生/周次/依据和编辑层；保存中防重复。周报补齐桌面切周/版本/归档和手机关闭的脏草稿保护；只读写入口原生禁用。共享`weekreport/comment5`样式的集中清除归TASK-30，本项未冒险删除仍有消费者的规则。
- 命令与实际结果：ESLint、生产构建、周报/评语聚焦回归、264/264全量自动化及认证集成通过。
- 参考/实渲染证据与视口：1280×720桌面和390×844手机实查；1440、1280、1057、768、390、360六档布局通过，报告`.qa-shots/task28-writing/layout-audit.json`；详见`../evidence/writing-implementation-20260913.md`。
- 未覆盖状态：物理手机软键盘、真实弱网、正式DeepSeek、真实双标签409与线上环境未验收；冲突继续使用workspace统一恢复层。
- 完成提交：本任务提交。
- 下一步：执行TASK-30，全量移除旧UI依赖。
