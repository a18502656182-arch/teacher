# TASK-17 接通首页学生作业全流程

状态：done。前置任务：16。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：将新代表视图接统一操作层；迁移所有旧菜单/浮层。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：真实业务可用核心页。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：删除对应旧DOM/规则。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：完整创建编辑保存失败、草稿、跨班。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`af5b33e`
- 修改文件：作业`operations.ts`与controller、操作/渲染契约测试、首页/学生/作业执行卡及状态证据文档
- 保全行为/删除旧依赖：三页既有新视图与全部入口保留；旧页面DOM已在TASK-12至14退出，本任务未重复删除共享CSS；作业编辑/删除/跟进补当前班唯一目标防线
- 命令与实际结果：浏览器入口/Drawer/只读验证通过；专项58/58及作业操作10/10通过；ESLint、六档`qa:strict`、生产构建、256/256自动化与认证集成通过
- 参考/实渲染证据与视口：`evidence/core-pages-integration-20260913.md`；桌面1280×720、手机390×844
- 未覆盖状态：正式账号真实断网交互、真实多标签409浏览器演练；由隔离保存测试覆盖逻辑，不宣称线上已验
- 完成提交：本任务提交
- 下一步：执行TASK-18听写词库与历史复习完整迁移
