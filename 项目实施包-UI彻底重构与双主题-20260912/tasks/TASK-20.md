# TASK-20 考勤完整迁移

状态：done。前置任务：16,17。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：日期/备注/状态/批量整合，手机同controller。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：attendance页面卡全部。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：旧attendance布局。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：105人、成功清空、失败保留、只读。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`fe5731f`
- 修改文件：`Attendance.tsx`、`Attendance.module.css`、`ClassroomApp.tsx`、`features/attendance/operations.ts`、相关测试与布局审计。
- 保全行为/删除旧依赖：日期/月历、四态、逐生备注、搜索筛选、跨页多选、批量状态与统一备注、一键正常均保留；桌面与手机共用控制器。旧考勤布局退出，页面外观不再依赖旧全局类。
- 命令与实际结果：ESLint通过；考勤与源码专项42/42；生产构建及全量259/259、认证集成通过；六档考勤布局审计通过。
- 参考/实渲染证据与视口：浏览器实查1280×720、390×844；另由严格布局覆盖1536×1024、1280×720、1100×760、768×1024、375×667、390×844。
- 未覆盖状态：当前产品没有独立请假详情字段编辑入口；本任务不虚构该业务。正式写入的网络失败由workspace单元/集成契约覆盖，浏览器只使用只读演示数据。
- 完成提交：本任务提交。
- 下一步：执行TASK-21成长健康沟通迁移，并连续推进至TASK-28。
