# TASK-04 抽取纯业务计算

状态：done。前置任务：02。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：移动normalize/日期/成绩/关联辅助，不改算法；替换import。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：lib/workbench纯函数和回归。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：删除原重复函数仅在消费者改完后。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：固定输入输出、缺失/零值/跨班回归。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`8765907`
- 修改文件：`workspace/normalize.ts`、日程/成绩读取模型、成长时间范围、主组件导入、两份聚焦测试及配套进度证据。
- 保全行为/删除旧依赖：多班投影、旧字段兼容、本机日期、成绩空白/零分和成长时间口径保持；主组件内对应重复函数删除。显式空作业与空周计划不再补示例数据。
- 命令与实际结果：TypeScript、全量ESLint、生产构建、217/217自动化测试及认证集成通过；计划检查通过，保留既有大chunk警告。
- 参考/实渲染证据与视口：本任务无DOM/CSS变动，视觉验收不适用；不能用本轮构建替代后续逐页浏览器验收。
- 未覆盖状态：服务器确认式通用保存控制器、页面级状态与视觉迁移、真实物理设备和线上环境均未在本任务覆盖。
- 完成提交：`c944929`
- 下一步：TASK-05抽取通用保存、草稿、失败重试与409控制器，并用E7/E8/E9验证。
