# TASK-31 主题架构与性能检查

状态：done。前置任务：30。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：内部glass token替换/无blur；拆分chunk和图片；检测remount。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：主题契约检查、性能实测。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：删除意外公开glass入口/重复加载。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：状态不丢；名单/200词/保存计时，首屏资源。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`ef41770`
- 修改文件：主题定义与契约测试、壳层/控件/弹窗及工作台样式、`ClassroomApp.tsx`动态分块、听写批改插画格式、严格布局审计同步、Duty编辑器类型完整性。
- 保全行为/删除旧依赖：公开主题仍固定校园，内部glass仍不公开且改为实色无blur；低频教师页面按需加载，手机已访问页面继续保活筛选与返回上下文；未修改家庭教育业务。
- 命令与实际结果：ESLint、生产构建、TypeScript通过；构建超500kB分块警告消失；69/69性能/主题/渲染聚焦回归和61/61主题/渲染/Duty回归通过；日程六档严格布局专项通过。
- 参考/实渲染证据与视口：主客户端JS 598589→332249字节，CSS 401636→309349字节；听写批改插画1886556→75016字节且原尺寸实看通过。390×844应用内浏览器实查学生页无横向溢出或错误日志。
- 未覆盖状态：开发浏览器访问首页/学生/作业/成绩四页时DOM峰值约2570节点；保活换取返回状态，不视为零成本。未测真实低端手机、3G、线上LCP/INP或网络保存延迟；家庭教育按用户要求不验收。
- 完成提交：本任务提交
- 下一步：执行TASK-32全站教师业务与视觉验收。
