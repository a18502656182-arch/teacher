# TASK-15 听写构图基准

状态：done。前置任务：10,11。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：C2/C6中央批改+队列，材料横幅/展开；显式核对。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：Grading新展示。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：旧批改三栏强制布局/重复banner。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：8/12/60/200词，保存可达，失败不跳。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`b27a745267d19e777a67ea4a09ab44f89f7d7cbd`。
- 修改文件：`dictation/Grading.tsx`、`Grading.module.css`、`Dictation.tsx`、`dictation.css`、展示契约测试及本任务证据/状态。
- 保全行为/删除旧依赖：保留三态、逐词错词、备注、显式核对、本机草稿、失败不跳、成功下一位、复习与只读；删除旧批改三栏、重复手机banner及全部旧批改专属选择器。
- 命令与实际结果：`npm test`生产构建、252/252自动化与认证集成通过；全量ESLint及六档`qa:strict`通过。
- 参考/实渲染证据与视口：`../evidence/dictation-grading-implementation-20260913.md`；1536×1024与390×844隔离合成批改实看。
- 未覆盖状态：物理手机、软键盘、真实弱网和线上环境未验收；任务/词库/错词/历史旧展示留TASK-18。
- 完成提交：本任务提交。
- 下一步：按用户最新指令不中途暂停，执行TASK-16并继续推进至TASK-28。
