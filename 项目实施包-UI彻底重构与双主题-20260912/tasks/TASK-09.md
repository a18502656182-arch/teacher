# TASK-09 建立新旧样式隔离

状态：done。前置任务：06,07。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：按03作用域隔离旧规则及portal；新模块独立预览；禁用旧CSS对照。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：迁移边界与开发预览。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：不做简单全文件字符串前缀替换。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：新页无旧计算样式污染，旧页仍可用。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`645c290`
- 修改文件：正式根布局改载AST生成的隔离旧样式；工作区增加legacy根；新增生成脚本、生命周期命令、完整隔离预览和静态/生成一致性测试；人工迁移8处文档级旧规则。
- 保全行为/删除旧依赖：十个旧CSS源及声明顺序全部保留给未迁移页；新ThemeBoundary子树明确排除。未删除仍有消费者的旧规则，也未改API、数据库或业务状态。
- 命令与实际结果：专项6/6、TypeScript、ESLint、生产构建、233/233自动化测试、认证集成、静态审计和115项六档视口`qa:strict`通过；仅有既有大chunk警告。
- 参考/实渲染证据与视口：1280×800与390×844加载完整旧CSS前后，旧输入样式实际变化，新输入计算样式逐项不变；无横向溢出和控制台warning/error。详见 `evidence/legacy-style-isolation-20260913.md`。
- 未覆盖状态：这是迁移边界而非旧CSS清零；修改旧源后需重跑生成命令或重启dev。物理手机、屏幕阅读器和生产环境未验。
- 完成提交：`94de104`
- 下一步：TASK-10制作首批校园插画母样与变体；不得提前公开玻璃主题。
