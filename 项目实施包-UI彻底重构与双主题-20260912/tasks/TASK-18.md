# TASK-18 听写词库与历史复习

状态：done。前置任务：15,16。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：创建/文本导入/课次/词库/错词/记录统计全部迁移。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：dictation完整模块。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：旧dictation.css剩余规则。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：快照、统计分母、复听新轮、30天窗口。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`dc7e25a`
- 修改文件：`dictation/Dictation.tsx`、`TaskEditor.tsx`、`WordLibrary.tsx`、`PersonFilter.tsx`及四张CSS Module；`lib/dictation.ts`；匹配测试与项目记录。
- 保全行为/删除旧依赖：保留创建、文本导入、课次、快照、复听、错词和30天统计；词库补脏离开保护；删除全局`dictation.css`。
- 命令与实际结果：ESLint、47项专项、六档`qa:strict`、生产构建、257/257自动化及认证集成通过。
- 参考/实渲染证据与视口：`evidence/dictation-library-history-implementation-20260913.md`；1280×720与390×844实渲染。
- 未覆盖状态：物理手机/软键盘和真实服务器写入未在本任务重复实测；不含OCR、自动报词或外部发送。
- 完成提交：本任务提交。
- 下一步：TASK-19家庭学习完整迁移。
