# TASK-08 建立浮层与搜索选人

状态：done。前置任务：05,07。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：Dialog/Drawer与子层焦点；搜索分页100+选人；草稿关闭保护。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：共享浮层、StudentPicker。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：逐消费者替换旧管理器后删除。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：嵌套关闭、inert恢复、失败保留、105人。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`274eeef60bb5e5339b38ebfee69ed796d32a41d3`
- 修改文件：新增共享 `ModalLayer`、`Drawer`、`DraftClosePrompt`、`StudentPicker` 及局部样式和测试；`Dialog` 改为兼容包装；`StudentLookupDialog` 改接共享选人器；补齐 `Field` ref、预览与旧焦点管理器的嵌套边界。
- 保全行为/删除旧依赖：保留原调用接口和单选行为；105人搜索、分页、多选草稿、失败保留、dirty确认、嵌套关闭和焦点回退均进入共享实现。未删除仍有消费者的旧浮层CSS，完整隔离留TASK-09。
- 命令与实际结果：TypeScript、全量ESLint、生产构建、231/231自动化测试、认证集成和全量`qa:strict`通过；定向测试42/42通过。仅有既有chunk体积警告；严格审计首次遇临时文件占用，重试通过。
- 参考/实渲染证据与视口：隔离预览在1280×800及390×844实际操作105人搜索、分页、跨页多选、失败保留、脏关闭和嵌套焦点；无控制台错误或横向溢出。详见 `evidence/overlay-student-picker-20260913.md`。
- 未覆盖状态：物理手机软键盘与屏幕阅读器未验；现有业务页的其余旧浮层及全局CSS仍由TASK-09和页面迁移退出。
- 完成提交：`f5d6c3c`
- 下一步：TASK-09建立新旧样式隔离，继续核对真实portal与旧选择器边界。
