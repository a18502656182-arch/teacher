# TASK-23 成绩分析迁移

状态：done。前置任务：17,08。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：考试/录入/趋势/题目AI候选独立视图；保留满分口径。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：scores完整页面。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：score5/MobileScores旧展示。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：零分缺失、满分、候选未确认、长图表。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`4501b57`。
- 修改文件：`ClassroomApp.tsx`、`ScoreItemAnalysis.tsx`、`tests/rendered-html.test.mjs`、成绩页执行卡、F-11/O-11 tracker、项目状态/日志/导航和本任务证据。
- 保全行为/删除旧依赖：保全考试库、逐生/批量录分、空白与真实零分、满分区间、重点与建议、趋势、人工分析项及教师确认 AI 候选；关键提交仅在 workspace 服务器确认后关闭或清选择，失败保留本机修改和当前上下文；演示/只读禁用成绩写入口。共享 `score5/MobileScores` 旧样式集中清理仍归 TASK-30，没有提前删除共享消费者。
- 命令与实际结果：ESLint 0 警告；生产构建通过；51/51 成绩与源码专项通过；成绩录入和试卷分析各六档布局审计通过；全量测试结果见本任务证据。
- 参考/实渲染证据与视口：浏览器实查 `1280×720` 桌面成绩录入/趋势/试卷分析和 `390×844` 手机名单/学生详情；演示只读按钮及输入均禁用。另由布局审计覆盖 1440、1280、1057、768、360、390 六档。
- 未覆盖状态：没有向正式环境上传真实试卷；真实 OCR 服务、费用/配额和文件留存策略仍是外部配置边界；已有高分遇满分下调不静默迁移；共享旧 CSS 删除归 TASK-30。
- 完成提交：本任务提交。
- 下一步：执行 TASK-24 考试反思迁移，并连续推进至 TASK-28。
