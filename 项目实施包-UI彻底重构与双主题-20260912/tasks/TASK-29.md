# TASK-29 入口账户管理员隐私迁移

状态：done。前置任务：08,11。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：统一控件但保持权限与真实说明，盘点所有管理浮层。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：entry/account/admin/privacy。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：旧public-entry已迁移规则。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：临时用户、设备、过期只读、备份恢复。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`6e10e2d`（任务包基线）；管理员阶段开始于 `8690ee5`。
- 修改文件：入口/隐私位于 `features/entry`，账户位于 `w/[token]/features/account`，管理员位于 `admin/features/admin`；对应主题槽、测试、页面级样式和证据同步更新。
- 保全行为/删除旧依赖：认证、账户、班级、备份、当前设备退出、管理员账户/兑换码/设备/续期/导出删除/审计均保留；删除 `public-entry.css`、`admin.css`、旧账户/班级重复视图及管理员内联展示逻辑。
- 命令与实际结果：各阶段TypeScript、全量ESLint和生产构建通过；最终197/197自动化测试、扩展认证集成及115项六宽度严格审计通过。
- 参考/实渲染证据与视口：`../evidence/entry-operations-20260913.md`、`../evidence/account-operations-20260913.md`、`../evidence/admin-operations-20260913.md`；入口/管理员1280×720和390×844，账户1280×800和390×844。
- 未覆盖状态：真实物理手机、软键盘、HTTPS、外部网络和线上环境；没有使用正式用户执行永久删除。
- 完成提交：`a799f6b`、`af28a20`、`14222f3`，任务记录随本次证据提交。
- 下一步：先回补首页F-01/O-01账本，再进入TASK-30全量移除旧UI依赖。

## 阶段记录（2026-09-13）

- 入口与隐私：`a799f6b`，证据 `../evidence/entry-operations-20260913.md`。
- 账户、班级与备份：`af28a20`，证据 `../evidence/account-operations-20260913.md`。
- 管理员：`14222f3`，证据 `../evidence/admin-operations-20260913.md`。入口、账户、管理员与隐私均已完成独立迁移，TASK-29关闭。
