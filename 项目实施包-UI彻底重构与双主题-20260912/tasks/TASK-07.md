# TASK-07 建立基础控件

状态：done。前置任务：06。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：按钮Field状态分段菜单加载空态，全部状态可操作；CSS Modules。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：ui基础组件检查器。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：迁移组件不消费旧controls.css。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：标签/焦点/disabled/busy/长按钮文本。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`b1eb7d2`
- 修改文件：`app/components/workbench/ui/Button.tsx`、`SelectionBar.tsx`、新增`Menu.tsx`与`FeedbackState.tsx`、`controls.module.css`、独立预览、基础控件契约测试及局部设计说明。
- 保全行为/删除旧依赖：Field/Input/Textarea/Select与StatusSegment继续使用既有CSS Module；Button补busy进度但保留原文字，SelectionBar补批量区域语义；菜单、加载和空态只提供展示与交互原语，不虚构保存或业务结果。新控件未消费旧`controls.css`，也未提前删除仍有消费者的旧规则。
- 命令与实际结果：TypeScript、全量ESLint、生产构建、229/229自动化测试、认证集成和115项六档视口`qa:strict`通过。构建仍报告既有500KB以上chunk警告。
- 参考/实渲染证据与视口：本地独立预览完成桌面与390×844内嵌CSS手机视口检查；长按钮完整换行且无横向溢出，菜单方向键循环和Escape焦点恢复通过，空态/加载态可读，切换内部主题探针后输入保持。详见`../evidence/ui-primitives-20260913.md`。
- 未覆盖状态：本项是控件级样板，不代表共享壳层或业务页构图达标；真实手机软键盘、屏幕阅读器和旧CSS混合环境仍由后继任务验证。根据用户要求在此设第一次可浏览暂停点。
- 完成提交：`2a55a85`
- 下一步：用户浏览确认后执行TASK-08浮层与100+学生搜索选择；TASK-11壳层和TASK-12首页分别设置后续视觉验收点。

实际完成证据：`../evidence/ui-primitives-20260913.md`。页面重构未完成，未验证项保持未完成。
