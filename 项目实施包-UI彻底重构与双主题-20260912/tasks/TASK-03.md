# TASK-03 建立隔离测试夹具

状态：done。前置任务：02。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：按当前schema生成E0–E11；临时数据库；记录端口PID；复用现有测试工厂。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：本地fixture工厂及场景说明。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：无。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：旧备份可预检，50+50/105/2孩子归属正确。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`bc41088`。
- 修改文件：`tests/fixtures/workspace-fixtures.mjs`、`tests/workspace-fixtures.test.mjs`、`package.json`及本任务证据/状态。
- 保全行为/删除旧依赖：E0至E11按当前数据字段生成；未改变产品schema、API、保存逻辑或数据库路径。
- 命令与实际结果：`node --test tests/workspace-fixtures.test.mjs` 10/10通过；`npm test` 生产构建、207/207自动化测试和认证集成通过。旧版/新版备份使用真实预检，听写使用真实校验，12场景写入临时SQLite并回读。
- 参考/实渲染证据与视口：`../evidence/fixture-environment-20260913.md`；本任务建立夹具，不把任何页面视口标为视觉通过。
- 未覆盖状态：E7/E8/E9仍需在对应浏览器任务使用认证、路由故障和双客户端实际触发；夹具契约不能代替页面验收。
- 完成提交：`ce66702`。
- 下一步：TASK-04抽取纯业务计算；M0关闭，M1开始。
