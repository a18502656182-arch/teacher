# TASK-32 全站业务与视觉验收

状态：done。前置任务：31。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：按10逐页逐状态，运行类型lint构建业务认证strict；修复登记缺陷。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：acceptance全量证据。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：不得删断言绕过失败。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：功能/视觉/响应式/架构分开通过。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`712ed16`
- 修改文件：最终教师验收证据、acceptance台账、项目状态与变更日志。
- 保全行为/删除旧依赖：当前完整业务测试覆盖E0—E11、各教师模块纯操作、保存失败/409/只读；旧CSS退出后所有教师导航页重新渲染。未修改家庭教育，家庭九行历史证据未改写成本轮结果。
- 命令与实际结果：`npm test`生产构建、260/260测试和认证集成通过；TypeScript与ESLint通过；`npm run qa:strict`静态项+19页×6视口共115项、失败0。
- 参考/实渲染证据与视口：1440×900、1280×900、1057×900、768×1024、360×780、390×844全部教师页通过；应用内浏览器最终实查桌面评语页无横向溢出、加载残留或错误日志。
- 未覆盖状态：物理手机、真实软键盘、屏幕阅读器、真实弱网、线上Web Vitals、正式AI/OCR及各业务页独立双标签409没有逐页重演；共享409契约与认证集成已覆盖。大型共享CSS/主组件和手机保活DOM仍是P2维护成本。
- 完成提交：本任务提交
- 下一步：执行TASK-33更新记忆、提交并生成受控部署包，不部署生产。
