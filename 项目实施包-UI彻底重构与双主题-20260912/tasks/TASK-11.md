# TASK-11 建立共享壳层

状态：done。前置任务：05,07,08,09。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：顶栏/四组19导航/手机返回和底栏，场景一次，保存真实。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：shell组件与单controller接线。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：替换旧DesktopHeader/MobileWorkbench外壳。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：全导航、切班守卫、resize保留草稿。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`ebc169f`
- 修改文件：新增`components/workbench/shell`目录与controller/history/catalog/CSS；接入`ClassroomApp`和`ThemeBoundary`；调整听写重复场景入口、窄桌面成长栏、手机toast及真实布局审计；新增壳层行为测试。
- 保全行为/删除旧依赖：四组19模块继续进入原业务组件；桌面/手机共用场景、班级、保存与冲突状态。删除旧`WorkspaceChrome.tsx`和`MobileWorkbench`外壳；未删除仍被未迁移业务页消费的legacy源规则。
- 命令与实际结果：TypeScript、相关ESLint、生产构建、237/237自动化、认证集成和115项`qa:strict`通过；独立review为`ship-with-notes`，0 blocker/0 major。
- 参考/实渲染证据与视口：1280×720、390×844实看新壳层；严格审计覆盖1440×900、1280×900、1057×900、768×1024、360×780、390×844。详见`../evidence/shared-workbench-shell-20260913.md`。
- 未覆盖状态：真实物理手机与软键盘未验；自定义返回和原生Back混用的历史语义、最多19页保活的长期成本留TASK-31量化；业务页A场景仍按后续任务验收。
- 完成提交：源码`5f16aeb`；文档提交见Git历史。
- 下一步：按用户要求停在真实本地页面；确认壳层方向后执行TASK-12完整首页。
