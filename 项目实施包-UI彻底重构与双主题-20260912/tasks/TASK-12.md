# TASK-12 首页构图基准

状态：done。前置任务：10,11。不得用历史通过替代前置门槛。

## 操作
1. 读取00、07、相关pages卡与trackers，核对当前HEAD及用户未提交差异。
2. 本任务具体实施：按pages/dashboard完成C1/C5独立基准，空态与满数据均设计。
3. 对每个涉及页面依次完成view、状态、动作、浮层、手机布局；功能保全账本逐行定位新入口。
4. 预期产物：dashboard新视图与对照。拟建文件参照03，命名可微调但职责不可混淆。
5. 退出/删除范围：旧Dashboard布局待接线后退出。删除共享规则前检查未迁移消费者，禁止重置用户改动。
6. 本项验证：1536/390/360参考对照；首项操作可达。源码调整后运行匹配风险的测试；纯文档项验证引用和状态一致即可。
7. 写入实际证据、问题、下一动作；完成条件不满足标needs-fix。更新项目记忆，验证后提交推送。

## 不允许
跳过前置、整段复制旧布局到新文件、扩大到新增业务、改API/数据库作为UI捷径、把样例代码当真实保存、在正式数据库造测试。

## 完成记录（实施者填写）
- 开始HEAD：`1d144b8`
- 修改文件：新增`features/dashboard`视图、局部样式与纯读取模型；`ClassroomApp`接入桌面/手机同一首页并补学生上下文导航；新增首页模型测试并更新结构契约。
- 保全行为/删除旧依赖：保留日程、作业、家校、听写、值日、学生成长和家庭入口；删除旧`Dashboard.tsx`、`dashboard.css`及legacy生成入口。首页仍无写入或自有浮层。
- 命令与实际结果：生产构建、242/242自动化、认证集成、ESLint、115项`qa:strict`和`git diff --check`通过；独立复核首轮3项P1已修复，终轮为`ship`且0 P0/P1。
- 参考/实渲染证据与视口：1536×900、390×844、360×800实看；四个主要待办分别进入正确模块，学生事实提醒进入指定学生成长档案，360px无横向溢出或控制台错误。详见`../evidence/dashboard-implementation-20260913.md`。
- 未覆盖状态：E0/E6由隔离夹具、模型测试与CSS规则验证，未写正式数据库做浏览器截图；未做物理手机和屏幕阅读器测试。首页无编辑、保存失败或冲突状态，按源码职责记不适用。
- 完成提交：主体源码`b9036a7`，复核修复`015daa3`；文档提交见Git历史。
- 下一步：按用户要求停在真实本地首页；确认后执行TASK-13学生构图基准。家庭学习暂不扩展。
