# TASK-19 家庭学习完成复核

日期：2026-09-13。开始HEAD：`084ff5d`。本任务承接已实现的家庭场景，不重做提交`872dee7`中的孩子切换、今日任务、档案、错词复习和共享批改。

## 当前复核与修正

- 家庭任务继续按稳定`childId`精确读取；只读演示的零孩子场景没有借用32名班级学生，桌面和手机都保留五个家庭子页。
- `familyOverview`改为复用全听写统一的`recentTasks`，不再维护第二套`Date + toLocaleDateString`算法；新增未来与第31天排除断言。
- 孩子档案离开守卫从“仅dirty且非busy”改为“dirty或busy”，保存进行中不再短暂允许切场景或离页。
- 家庭页头移除装饰渐变，继续使用校园语义色和本地`family`插画；旧家庭条件化平铺分支保持退出。
- TASK-18已经删除共享旧`dictation.css`；家庭自定义任务、词库和批改现均由局部CSS Module承担。

## 验证

- 1280×720与390×844只读演示：家庭场景、零孩子空态、五个子页、班级/家庭切换可见；手机无重复业务页头或整页横向溢出。
- ESLint通过；家庭+听写19/19专项通过；六档`qa:strict`通过。
- 完整`npm test`：生产构建、258/258自动化和认证集成通过；保留既有大chunk警告。

## 边界

- 两个同名孩子、归档、任务创建、批改、真实409和dirty确认的可写证据沿用当前源码对应的既有隔离核验，不把旧截图冒充本轮新执行。
- 物理手机、软键盘、弱网和生产账号未验；本任务未连接生产数据库，也未部署。
