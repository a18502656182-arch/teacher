# TASK-15 听写批改构图基准证据

## 实施结果

- `Grading.tsx` 改为中央逐词核对区与右侧学生队列，不再使用旧左材料栏、中央批改栏、右名单三栏强制布局。
- 材料变为单一可展开横幅，8、12、60、200词均可完整访问；没有在手机重复渲染第二份任务横幅。
- 当前学生、参与状态、备注、错词、显式核对、保存/放弃和个人错词复习保留。桌面名单每页20人；手机通过共享 `Drawer` 切换学生，固定保存区位于底部导航之上。
- 失败仍保留当前学生、错词、备注、确认状态和本机草稿；服务器确认成功后才推进下一位。演示/只读只允许查看和打印。
- 新展示由 `Grading.module.css` 接管；`dictation.css` 中旧 `.dictation-grading`、材料栏、名单栏、词项和保存栏规则已删除，其余任务/词库/历史规则留给 TASK-18。

## 验证

- 隔离合成预览（不读写正式数据库、不进入产品路由开关）：1536×1024 实际选择200词并操作第200项，保存按钮可见；模拟失败后状态提示“保存失败”且仍为合成学生01，取消失败后重试才进入下一位。
- 390×844 实测200词：页面 `scrollWidth === clientWidth`，无横向溢出；保存区可见，学生队列抽屉可搜索、分页、关闭。
- E5自动化覆盖8/12/60/200词；E7/E8/E9契约覆盖只读、503与409。新增展示契约断言确认可展开材料、显式核对、手机固定保存、Drawer及旧批改选择器退出。
- `npm test`：生产构建、252/252自动化与认证集成通过。
- `npm run lint -- --no-cache`：通过。
- `npm run qa:strict`：1440×900、1280×900、1057×900、768×1024、360×780、390×844全站审计通过。
- Impeccable检查器只报设计系统偏离建议；已将颜色、字号和圆角全部收敛到 `DESIGN.md` 现有校园语义值，未新增依赖或主题入口。

## 边界

- 演示工作区本身没有听写任务，因此真实产品路由只验证了空态；批改交互使用隔离合成预览验证，没有向演示或正式数据库写测试数据。
- 物理手机、软键盘遮挡、真实弱网和线上环境未验收。
- 任务、词库、错词与记录统计仍沿用旧展示，按计划由 TASK-18 完整迁移。

## 下一步

执行 TASK-16，同尺寸并排检查 C1–C6，集中修复标杆比例、字阶、插画与状态差异；按用户最新指令不中途暂停，继续推进至 TASK-28。
