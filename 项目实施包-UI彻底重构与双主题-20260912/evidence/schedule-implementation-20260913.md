# 课程与个人日程实施证据（2026-09-13）

范围：TASK-25，F-15 / O-15。开始 HEAD：`d633844`。

## 本轮完成

- 桌面课程页的学期、周课表、班级活动和每日重点提交统一等待 workspace 保存结果；失败保留当前编辑器和本机修改，成功才关闭或更新基线。
- 桌面与手机个人日程、工作留痕、完成并留痕、删除操作等待服务器确认；失败不丢当前上下文，也不冒充外部发送。
- 手机课程、活动、重点和学期配置采用同一确认语义，保存中防重复；演示/只读模式显式禁用所有写入口。
- 保持既有跨月、1—7 教学日、周外活动保留、跨班隔离和完成留痕幂等操作层，不改 API、数据库和数据契约。

## 验证

- `node --test tests/rendered-html.test.mjs tests/schedule-operations.test.mjs`：50/50 通过。
- `node scripts/layout-audit.mjs`，页面 `schedule`：360、390、768、1024、1280、1440 六档通过，报告 `.qa-shots/task25-schedule/layout-audit.json`。
- 应用内浏览器：1280×720 桌面及 390×844 手机实际打开班级课表、个人日程与留痕；写入口只读状态正确，无横向溢出或控制台异常。
- 全量 ESLint、生产构建、自动化与认证集成结果见本任务提交前的项目状态记录。

## 未覆盖与后续边界

- 未使用真实班级、正式数据库、线上环境或外部消息服务。
- 100+ 日程、物理手机软键盘及真实弱网未验收。
- `courseplan-*`、`agenda-*` 与手机公共旧样式仍有其他迁移页面消费者，集中退出归 TASK-30，不能在本页任务中抢先删除。
