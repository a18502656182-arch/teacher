# TASK-22 积分与规则迁移证据（2026-09-13）

开始HEAD：`18dcb0c`。

## 当前行为

- 桌面与手机均保留学生筛选/多选、规则选择、自定义正负分值、执行人、说明、批量提交、排行/事件和撤销。
- 积分提交先应用workspace草稿并等待服务器确认；成功才清选择和说明，失败保留本次上下文。保存中禁重复提交，演示/只读禁用选择、提交和撤销。
- 规则新增、编辑、启停、复制和删除等待服务器确认；失败保留编辑内容或本机修改。删除规则不会删除历史积分，规则变更不会追改历史快照。
- 纯操作层继续校验活动班、学生ID、启用规则和非零分值；105人选择去重且跨班写入拒绝。

## 验证

- 浏览器：1280×720和390×844实查积分与规则布局、演示只读状态、正负规则和历史信息。
- `node --test tests/points-rules-operations.test.mjs tests/rendered-html.test.mjs`：45/45通过。
- `QA_PAGES=points,rules npm run qa:strict`：六档视口通过。
- ESLint和生产构建通过；构建只有既知大chunk提示。
- `npm test`：生产构建、261/261测试和认证集成通过。

## 边界

浏览器未写正式数据库，真实失败与冲突由workspace保存契约和源码断言覆盖。`pointdesk-*`、`ruledesk-*`共享样式集中移除仍属于TASK-30，本任务未虚假宣称已删除。
