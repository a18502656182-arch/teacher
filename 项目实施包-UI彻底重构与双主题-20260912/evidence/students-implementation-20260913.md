# TASK-13 学生构图基准实施证据

日期：2026-09-13。开始 HEAD：`40d7b9f`。未执行生产部署，未使用正式数据库或真实学生资料。

## 实施结果

- `StudentsView` 与 `useStudentsController` 接管桌面和手机学生页；旧 `Students` / `MobileStudents` 已从 `ClassroomApp.tsx` 删除。
- 桌面按 C3 使用连续的名单主表与 350px 右侧单学生事实区；手机使用名单到单学生 Drawer 的顺序推进。
- 新增、单学生编辑、批量编辑、文本名单预检/追加/替换、CSV 导出、关联删除、完整档案共用同一班级数据与操作边界。
- 主表只显示电话“已维护/未填写”，完整电话只在单学生编辑和受控档案内显示；只读/演示状态禁用写入口但保留查询、详情和导出。
- `StudentProfile` 改用共享原生 Dialog；脏关闭进入放弃确认，监护人、照护、听写和考勤历史均保留。旧学生页在 `page-families.css` 的构图规则已删除；未迁移页面仍需的共享旧 CSS 未扩大清理范围。
- 导入和新建不再预填虚构积分或成绩，初值为 0；删除继续调用 `removeStudentRelations` 清理当前班关联。

## 验证

- `npm test`：246/246 通过，认证集成通过；新增学生读取模型覆盖 0/50/105 人、长姓名、缺失电话、名单解析和跨班同步。
- `npm run lint`：通过。
- `npm run build`：通过；保留既有大于 500KB chunk 警告，不视作本任务已解决。
- `npm run qa:strict`：通过。
- 浏览器实看：1536×1024 桌面默认/编辑态；390×844 手机默认、单学生 Drawer 与导入预检。编辑态 AX 树未出现完整家长电话输入；导入两行显示“预检：2 人”。
- Impeccable 独立终审：0 个 P0/P1，结论通过。手机复选目标约 44×68px；无卡中卡、横向溢出或按钮截断。
- 检测器：侧边色条 warning 已修为普通边框；胶囊圆角等 advisory 有明确身份标记用途，不构成阻断。

## 未覆盖与边界

- 正式账号的真实网络失败与 409 未在本任务制造；页面仍由共享 workspace 草稿、重试和载入最新版本承接，不能据本地 UI 结果宣称服务器已保存。
- CSV 只验证当前班生成逻辑和浏览器入口，未把下载文件提交仓库。
- 真实生产部署仍需用户单独授权。

下一步：按用户临时规则暂停；收到继续指令后执行 TASK-14 作业构图基准。
