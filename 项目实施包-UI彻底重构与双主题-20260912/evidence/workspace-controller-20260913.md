# TASK-05 工作区保存控制器证据（2026-09-13）

## 实施范围

- 开始HEAD：`c944929`；代码完成提交：`7617c9f`。
- 新增 `app/w/[token]/workspace/useWorkspaceController.ts`，集中维护工作区加载、草稿恢复、dirty、保存状态、错误、409冲突、revision、保存队列、自动保存、离开守卫和备份数据读取。
- `ClassroomApp.tsx` 改为消费controller，删除同职责状态、ref、effect和闭包。按当前Git内容逐行计数，主组件由5327行降至5230行；此前TASK-04文档记录的5574/5191为转录错误，已一并纠正。
- `workspace/operations.ts` 继续明确区分一般 `updateData/save` 与听写专用 `commitWorkspace`。听写失败不写入正式结果，待提交数据进入内存及完整备份；听写待提交时离开守卫拒绝退出。
- 公开只读演示不再接受旧页面的一般本地写入。到期正式账号仍允许查看与导出并拒绝写入。

## 自动验证

- `npx tsc --noEmit --pretty false --incremental false`：通过。
- `npm run lint`：通过。
- `node --test tests/workspace-operations.test.mjs tests/rendered-html.test.mjs`：46/46通过。
- `npm test`：生产构建通过，220/220自动化测试通过，认证集成通过；仍有既有500KB以上chunk警告。
- 新增回归覆盖本机存储不可用、听写409保留待提交草稿、待提交听写阻止离开和演示一般写入口只读。

## 隔离浏览器验证

所有正式账号场景使用系统临时目录SQLite、合成手机号/兑换码和两班各50人夹具，只监听本机127.0.0.1；未读取或修改仓库正式数据库。结束后已停止测试服务、关闭测试页并删除临时目录。

- E8保存失败：在考勤页停止本地路由后把第二名学生改为请假；页面保留请假状态，顶部变为“保存失败”，错误明确说明本机修改保留并提供重试。恢复服务后点击重试变为“已保存”，刷新仍为请假。
- E9并发冲突：两个标签从同一revision加载。标签A保存后返回200；标签B使用旧revision保存返回409。标签B保留自己的本机修改，并显示导出当前草稿、载入最新版本和关闭入口。
- E7只读：把临时工作区期限设为前一日后刷新，页面显示“只读宽限期”；点击第三名学生的迟到按钮后仍保持正常，出现只能查看和导出的说明，未发出PUT。
- 演示只读：打开 `/w/demo?page=attendance`，点击首名学生迟到后仍保持正常，并显示“当前为只读演示，数据不会被修改”。控制台错误与警告为0。

## 仍需后续处理

- 考勤等若干旧页面仍在本地应用数据后立即显示模块内“已保存”提示；当全局请求随后失败时会形成文案冲突。共享controller已提供权威全局状态，但各页面需在TASK-17至TASK-28迁移时让清空、关闭和成功提示等待对应的服务器确认。
- 本项没有改DOM或CSS，不能计作页面视觉迁移。真实物理手机、软键盘、真实弱网、HTTPS和线上环境仍未验收。
