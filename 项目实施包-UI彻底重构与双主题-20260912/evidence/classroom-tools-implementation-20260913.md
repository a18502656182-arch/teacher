# 课堂工具实施证据（2026-09-13）

范围：TASK-26，F-16 / O-16。开始 HEAD：`840ef1e`。

## 本轮完成

- 随机点名继续保持页面瞬时状态：按真实候选抽取、排除当天请假、不重复、可重置，不写积分或学生评价。
- 临时分组接入 workspace 保存结果；服务器确认后给出完成提示，失败保留本机分组结果和当前历史选择，保存中防止重复生成。
- 演示/只读模式显式禁用分组数和生成入口，但不阻止不落库的随机点名、重置及历史复制。
- 保持人数不足时只生成非空组、当前班历史隔离、移除学生不泄露他班姓名和 105 人不遗漏不重复等操作层契约。

## 验证

- `node --test tests/rendered-html.test.mjs tests/classroom-tools-operations.test.mjs`：49/49 通过。
- `node scripts/layout-audit.mjs`，页面 `tools`：360、390、768、1024、1280、1440 六档通过，报告 `.qa-shots/task26-tools/layout-audit.json`。
- 应用内浏览器：1280×720 桌面及 390×844 手机实查；只读分组入口禁用，手机随机点名可正常生成瞬时真实结果。
- 全量 ESLint、生产构建、自动化与认证集成结果见项目状态。

## 未覆盖与后续边界

- 未写正式数据，未操作生产环境；物理手机、真实弱网未验收。
- 页面没有对话框或抽屉，关闭脏草稿不适用；历史使用原生 `details`。
- 共享旧工具样式退出归 TASK-30。
