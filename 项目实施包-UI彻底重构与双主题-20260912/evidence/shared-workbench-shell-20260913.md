# TASK-11 共享工作台壳层实施证据

## 结果

源码提交`5f16aeb`建立唯一`WorkbenchShell`、19模块目录、四组导航和单一壳层controller。桌面为72px顶栏、244px侧栏（窄桌面220px）；手机为明确返回、首页/学生/作业/成绩/更多五项底栏。“更多”只列底栏之外15项并使用共享原生Drawer。班级、班级/家庭场景、真实保存/只读/失败/冲突状态均由既有controller传入，没有新增假保存。

旧`WorkspaceChrome.tsx`、`DesktopHeader`、`WorkspaceNav`和`MobileWorkbench`外壳退出。未迁移业务页仍在legacy根内使用原内容与必要旧样式；新顶栏、导航、手机栏和Drawer分别位于next主题边界，不把整套旧DOM复制到新文件。

## 导航与状态

- 四组目录覆盖19个模块且无重复、无遗漏；功能账本F-01至F-23的业务入口和状态均未被删除或升级为“已迁移”。
- controller统一处理桌面、手机、URL、浏览器popstate和听写离开保护。自定义返回使用模块轨迹连续退回，不再在两个模块间循环或增加history项。
- 手机访问过的模块保持挂载，返回时保留内部筛选与分页；壳层按模块记录并恢复页面/内容滚动。390×844实测学生搜索草稿在390→1280→390后仍保留。
- 听写内部和家庭页重复场景按钮已移除；运行时审计要求首页及听写实际可见场景入口恰好一个。
- toast位于手机底栏上方且不截获点击；运行时逐按钮检查底栏没有被业务浮条遮挡（模态层打开时按原生阻断语义排除）。

## 验证

- `npm test`：生产构建、237/237自动化及认证集成通过。仅保留既有500KB以上chunk警告。
- `npm run qa:strict`：静态检查读取新`shell.module.css`，运行时六档视口×19页及共享检查全部通过；视口为1440×900、1280×900、1057×900、768×1024、360×780、390×844。
- TypeScript与相关ESLint通过；`git diff --check`通过。
- 1280×720实看顶栏、四组连续侧栏、19入口、班级/场景/保存状态及无横向溢出；390×844实看五项底栏、返回、Drawer、模块URL和resize草稿。最终截图复查dashboard、dictation、growth，听写手机页无顶部遮挡，场景只出现一次。
- 独立review初次发现More重复、返回循环、听写场景重复和静态审计读取死CSS四项major；修复后复核为`ship-with-notes`，0 blocker、0 major。

## 未覆盖边界

自定义header返回采用`replaceState`，与浏览器原生Back交替时仍可能经过旧历史项；主流程正确，但混合序列留后续加集成覆盖。访问模块最多保活19个手机页面，长期DOM/effect成本由TASK-31量化。守卫失败与原生popstate的组合没有完整浏览器自动化；当前由纯轨迹测试、听写守卫回归、源码检查和实际交互共同覆盖。真实物理手机、软键盘、HTTPS与生产环境未验；没有部署或生成部署包。
