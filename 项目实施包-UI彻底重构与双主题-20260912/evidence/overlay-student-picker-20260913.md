# TASK-08 共享浮层与搜索选人实施证据

## 范围与提交

- 开始HEAD：`274eeef60bb5e5339b38ebfee69ed796d32a41d3`。
- 源码提交：`f5d6c3c feat: add shared overlays and student picker`。
- 新增 `ModalLayer`、`Drawer`、`DraftClosePrompt` 与 `StudentPicker`；`Dialog` 收敛为共享原生顶层的兼容包装，旧 `StudentLookupDialog` 改为新选人器适配入口。
- 没有修改API、数据库、认证、部署配置或正式数据；未删除仍有消费者的旧浮层CSS，完整新旧样式边界归TASK-09。

## 保全行为

- 原生 `showModal` 保持背景不可交互；关闭后焦点回到实际触发器，子层关闭不会误关父层。
- Escape、遮罩和关闭按钮统一进入关闭请求；busy阻止关闭，dirty草稿进入明确的放弃确认。
- 105人名单使用线性名册、搜索与分页，不把百人名单改成按钮墙或同时渲染全部学生。
- 多选草稿支持跨页保留；异步提交失败保留搜索、页码和选择，成功后才提交给调用者。

## 实际验证

- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- 浮层、选人器及适配入口定向测试：42/42通过。
- `npm test`：生产构建、231/231自动化测试及认证集成通过；仅保留既有500KB以上chunk警告。
- `npm run qa:strict`：首次受临时文件占用影响，未改代码直接重试后全量无失败。
- 隔离预览 `127.0.0.1:4207`：1280×800与390×844均验证105人搜索、分页、跨页多选、异步失败保留、脏关闭确认、嵌套关闭和焦点回退；控制台无错误，页面无横向溢出，手机底部操作区可达。
- 独立finish reviewer结论：`ship`；未发现需要阻断TASK-09的缺口。

## 边界

上述手机结果是390×844浏览器CSS视口，不等同于物理手机软键盘或屏幕阅读器验收。现有生产消费者通过兼容适配器获得新选人器，但旧页面其余浮层和全局样式仍需TASK-09及后续页面迁移逐项退出；不能据此宣布全站portal或旧CSS清零。
