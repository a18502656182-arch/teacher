# TASK-12 首页构图基准实施证据

日期：2026-09-13。主体源码提交：`b9036a7`；独立复核修复：`015daa3`。

## 实施边界

- 新增 `features/dashboard/DashboardView.tsx`、局部 CSS Module 与纯读取模型；首页继续只做事实汇总和模块导航，没有新增业务、接口、数据库或保存动作。
- 删除旧 `app/components/campus/Dashboard.tsx`、`dashboard.css` 及其 legacy CSS 生成入口。家庭学习入口保留为低优先级侧栏，数据与班级名册隔离。
- 作业未完成数以当前班任务和当前名册 ID 的交集计算；沟通、个人事项和值日均按当前班读取。学生提醒只来自带日期的考勤异常、待跟进沟通或未完成作业，并显示日期与来源。
- 当天课程与活动、教师事项合并排序；周日不再错误映射为周五。空数据不补造课程、任务、岗位、学生提醒或家庭档案。

## 实际验证

- 应用内浏览器：1536×900、390×844、360×800 实看首页；手机首项操作在首屏可达，360px 整页无横向溢出。
- 逐项点击“日程跟进 / 作业跟进 / 家校跟进 / 听写批改”，URL 与目标模块标题一致。
- 360×800 补充交互：`董思涵，2026-07-17，作业，数学《计算练习第3页》· 未交` 点击后进入 `?page=growth`，显示“董思涵 · 成长档案”；`scrollWidth=clientWidth=360`，控制台无错误。
- 空态与长数据由 E0/E6 隔离夹具、纯读取模型测试及 CSS 换行/收缩规则覆盖；没有向正式数据库写测试数据。首页无编辑器、保存、失败或冲突状态，A-01-05/07/08 按源码职责记为不适用；只读模式保留查看与导航且不产生写入。

## 命令结果

- `npm test`：242/242 通过，认证集成通过，生产构建通过。
- `npm run lint`：通过。
- `npm run qa:strict`：115 项六档布局审计通过。
- `git diff --check`：通过。
- Impeccable 静态 detector 仅报告既有设计系统建议，没有阻断项。独立复核先发现重复点击同一学生、历史考勤行动语义和嵌套`main`三项P1；`015daa3`修复并补测试后复核为`ship`，0 P0/P1。

## 剩余边界

- 未做真实物理手机、屏幕阅读器或软键盘测试；首页本身无输入控件。
- E0/E6 通过隔离夹具和自动化验证，未把夹具灌入正式工作区做浏览器截图。
- 学生、作业、听写仍按 TASK-13 至 TASK-16 建立其各自构图基准；家庭学习按用户当前优先级暂不扩展。
