# TASK-31 主题架构与性能实测

## 主题与状态

- `resolvePublicTheme()`继续固定返回校园主题；内部glass状态仍为planned，没有公开入口或校园插画回退。
- 两套主题的材质能力均改为实色、`blur: false`和`0px`，应用CSS内不再存在`backdrop-filter`或`filter: blur`。
- ThemeBoundary不使用React key，主题变量更新不会主动重挂工作台。契约测试覆盖公开主题、完整token、缺图为空和无重挂载。

## 构建前后

| 项目 | TASK-30后基线 | TASK-31后 | 变化 |
|---|---:|---:|---:|
| 主工作台客户端JS | 598589 B | 332249 B | -44.5% |
| 主工作台CSS | 401636 B | 309349 B | -23.0% |
| 听写批改场景图 | 1886556 B PNG | 75016 B WebP | -96.0% |

低频学生、作业、考勤、健康、日程、课堂工具、座位、值日、班干部、通知、账户及成绩子视图通过`React.lazy`按需加载；构建不再报告大于500kB的客户端chunk。WebP保持1666×944，原尺寸实看未发现构图或文字安全区损坏。

## 数据规模与浏览器证据

- 105人读取与筛选测试通过；本机本轮对应测试约1.5ms与0.9ms，数值只代表当前开发机的纯计算。
- 两班各50人、100教学日、每日20词的1,496,331字节合成数据，生成/校验/序列化24.8ms；SQLite WAL合成20版写入与回读214.3ms。均不含网络，不能解释为线上保存延迟。
- 8/12/60/200词夹具和容量校验通过。
- 390×844应用内浏览器实际进入按需学生页，横向溢出为0，错误日志为0。访问首页/学生/作业/成绩四个手机页面时DOM峰值约2570节点；保活成本已量化，换取返回时筛选和分页组件状态不被卸载。

## 验证与边界

ESLint、生产构建、TypeScript、主题/渲染/性能聚焦测试通过；按需加载引入后，严格QA改为等待业务模块和日程子视图完成加载，不放宽原业务断言，日程六档专项通过。未建立真实低端手机、3G、线上LCP/INP或实际网络保存基线。按用户要求没有修改或验收家庭教育。
