# TASK-23 成绩分析迁移证据（2026-09-13）

开始 HEAD：`4501b57`。

## 实施结果

- 桌面与手机继续使用同一当前班成绩数据和操作层，保全考试增改删/切换、逐生与批量录分、重点与建议、满分区间、趋势以及试卷分析。
- 新增、编辑、删除考试，批量录分、批量重点、满分区间、人工分析项和 AI 候选确认接入 `save(): Promise<boolean>`；服务器确认后才关闭编辑器或清空选择，失败保留本机修改与当前上下文。
- 成绩页和试卷分析补齐演示/只读边界。演示仍能搜索、筛选、切换考试、查看趋势和分析记录，但不能改分、改建议、改考试或发送试卷。
- AI 提取仍只是待核对候选，只有教师确认后才进入统计；未配置识别服务时不伪造结果。

## 数据与风险边界

- 空白继续表示未录入；真实 `0` 分计入录入覆盖；不同满分按得分率进入长期趋势。
- 105 人批量、跨班考试/学生拦截和重复确认幂等由成绩操作层专项覆盖。
- 没有对正式数据库、线上环境或真实学生资料执行测试；没有上传真实试卷。
- `score5-*` 与旧手机成绩样式仍有共享消费者，集中退出留到 TASK-30，不能把本任务误写为已删除。

## 验证

- `npm run lint -- --max-warnings=0`：通过，0 警告。
- `npm run build`：通过。
- `node --test tests/rendered-html.test.mjs tests/score-operations.test.mjs`：51/51 通过。
- `QA_PAGES=scores node scripts/layout-audit.mjs`：成绩录入六档视口通过。
- `QA_PAGES=scores QA_SCORES_VIEW=analysis node scripts/layout-audit.mjs`：试卷分析六档视口通过。
- 真实浏览器：1280×720 桌面成绩录入、趋势、试卷分析；390×844 手机成绩名单和学生详情。演示只读写入口均显示 disabled，未见横向溢出或遮挡。
- 全量 `npm test`：262/262 通过，认证集成通过。
