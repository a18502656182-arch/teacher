# TASK-18 听写词库与历史复习实施证据

日期：2026-09-13。开始HEAD：`dc7e25a`。结论：本地源码与预览通过，未操作生产环境。

## 实际改动

- 任务、词库、错词本和记录统计改为局部 CSS Module；删除最后一张全局 `dictation.css`，批改页继续复用 TASK-15 的中央工作面。
- 任务建立/编辑保留文本导入、词库、课次、作业关联、历史错词、参与者搜索分页和最多200词限制；保存失败保留同一任务ID，成功才进入批改。
- 词库建立/编辑保留最多10,000词、512KB TXT导入和独立材料快照；新增离开/刷新保护、取消确认和失败草稿保留。
- 错词本保留来源、个人复习和“后续答对不抹除历史”；记录页保留全班/个人、首次/复习拆分和按实际参与词次加权。
- 抽取 `recentTasks`，明确近30天为包含当天与前29个自然日，未来任务与第31天不进入统计。
- 手机隐藏重复业务页头，保留壳层标题；筛选、统计带、连续任务行和词库表在窄屏重排，不复制第二套业务树。

## 保全与边界

- 材料与参与者由 `newTask` 深拷贝形成任务快照；修改词库不会追改既有任务。
- 请假、未参加、待批改均不进入错误率分母；复听由 `newTask` 生成新ID，原轮次结果不覆盖。
- 演示页面为只读，只用于检查入口、布局和空态，没有向正式数据库写测试数据。
- 家庭页仍复用相同任务、词库和批改能力；家庭专属档案与今日构图的完整验收属于TASK-19。

## 验证

- 浏览器：`1280×720` 检查任务、词库、错词、记录四个入口和空态；`390×844` 检查壳层单页头、筛选和统计重排，均无整页横向溢出。
- `npm run lint`：通过。
- `node --test tests/dictation.test.mjs tests/rendered-html.test.mjs`：47/47通过。
- `npm run qa:strict`：六档严格布局审计通过。
- 完整 `npm test`：生产构建、257/257自动化及认证集成通过；仅保留既有的大chunk构建警告。

## 未宣称覆盖

- 只读演示不能验证真实服务器写入；保存失败、409和同ID重试由既有workspace与听写控制器自动化覆盖。
- 未在物理手机或软键盘环境验证TXT选择器；浏览器390×844只证明当前可见布局。
- 本任务没有实现自动报词、OCR或外部发送。
