# 班干部：逐页执行卡

状态：implemented。模块ID：cadres。源码相对 app/w/[token]（明确app/lib前缀除外）：Cadres.tsx、Cadres.module.css、features/cadres/operations.ts；接线位于ClassroomApp.tsx。
参考：责任编排派生。派生页面使用对应页面族构图和校园token，不把缺少专属图解释为可回退旧UI。

## 保全动作（种子，实施前展开）
新增岗位/小组长、任命选人、职责任期评价、聘任书/复制既有动作、删除。
逐个查旧handler、字段、权限和两端实现，补feature-preservation.csv。每个实际浮层补overlay-inventory.csv，不能只凭本卡认定无其他功能。

## 布局落地
桌面：岗位列表→选中职责与任期编辑。
手机：岗位→详情→选人编辑。
主操作由本页任务决定，顶部只一个；低频操作入有名称的菜单；插画按06的角色与文字安全区制作，空数据不给假数据填充。

## 修改步骤
1. 查当前源码中本卡符号，记录旧状态与调用链；read-only模式查看两端可见入口。
2. 将筛选/选中/草稿/提交收敛到feature controller；复用既有业务计算，不把旧DOM复制进新容器。
3. 新建features/cadres下视图与局部样式，使用共享组件实现上述布局；接到同一workspace操作层。
4. 依次实现初始页、详情、创建、编辑、长内容、空态、只读、失败状态；不适用项写明依据。
5. 验证主题与宽度变化不重置草稿；操作的服务端确认/本地应用状态不混淆。
6. 在独立新界面及混合迁移环境各看一次，确认旧CSS不参与此页外观。
7. 删除/替换：cadre3旧布局。核对其他消费者后才删共享规则。
8. 更新账本，运行本页业务回归、桌面手机截图和受影响共享组件检查；提交并记录。

## 必须实测
状态：无学生、长职责、空任期。
专项：取消新增无半成品；搜索选人；班委与小组长范围正确。
另查：长数据、空值、返回/取消、焦点、保存中防重复、失败保留、只读和跨班隔离（适用时）。浏览器不能写正式数据。

## 完成判据
全部保全动作有新入口与证据；桌面/手机/主要浮层可用；参考气质和比例达标；旧布局退出；测试没有通过削弱断言变绿。结果写当前commit，不复用旧报告标pass。

## 2026-09-12执行结果

- 桌面与手机已改用同一独立校园班干部页面；班委/小组长、当前班学生范围、空岗、任期、职责、状态、评价、小结、复制和删除共用当前班操作层。正式空工作区不再生成示例干部，岗位和明确小组号按班级保存。
- 新增班干部语义插画槽与原创透明水彩素材；旧 `cadre3-*` / `mobile-cadres-*` DOM已移除，死规则留待TASK-30统一核对删除。共享学生选择器portal与最终焦点回退仍是明确缺口。
- 甲班105人、乙班50人、待任命班委、第27组组长、长职责、空任期、脏取消、只读、保存失败、真实409以及360/390/768手机宽度均在隔离环境验证；完整测试182/182、认证集成、TypeScript、全量ESLint和115项六宽度严格审计通过。
- 详细证据见 `evidence/cadre-operations-20260912.md`；A-19-01至09记录本次提交。

## 2026-09-13 完成补强

岗位新增、编辑和删除等待workspace确认；失败保留本机修改与编辑表单，成功才关闭，保存期间禁用表单与关闭动作。候选范围、脏取消与共享选人器保持。桌面/手机实查及六档布局通过；死旧规则归TASK-30。证据见`../evidence/management-implementation-20260913.md`。
