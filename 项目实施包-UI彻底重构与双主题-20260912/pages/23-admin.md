# 管理员：逐页执行卡

状态：migrated-and-verified。模块ID：admin。旧源码：app/admin/page.tsx;app/admin.css；新实现：app/admin/features/admin/AdminConsole.tsx、AdminUserDialog.tsx、useAdminConsole.ts、api.ts、types.ts、AdminConsole.module.css。服务端仍使用app/api/admin/*。
参考：紧凑管理表格。派生页面使用对应页面族构图和校园token，不把缺少专属图解释为可回退旧UI。

## 保全动作（种子，实施前展开）
兑换码、用户、电话、设备、续期、导出删除、工作区/审计既有能力。
逐个查旧handler、字段、权限和两端实现，补feature-preservation.csv。每个实际浮层补overlay-inventory.csv，不能只凭本卡认定无其他功能。

## 布局落地
桌面：检索列表→用户/设备详情→明确危险确认。
手机：可读列表→详情，敏感字段必要呈现。
主操作由本页任务决定，顶部只一个；低频操作入有名称的菜单；插画按06的角色与文字安全区制作，空数据不给假数据填充。

## 修改步骤
1. 查当前源码中本卡符号，记录旧状态与调用链；read-only模式查看两端可见入口。
2. 将筛选/选中/草稿/提交收敛到feature controller；复用既有业务计算，不把旧DOM复制进新容器。
3. 新建features/admin下视图与局部样式，使用共享组件实现上述布局；接到同一workspace操作层。
4. 依次实现初始页、详情、创建、编辑、长内容、空态、只读、失败状态；不适用项写明依据。
5. 验证主题与宽度变化不重置草稿；操作的服务端确认/本地应用状态不混淆。
6. 在独立新界面及混合迁移环境各看一次，确认旧CSS不参与此页外观。
7. 删除/替换：旧admin展示样式，保留API。核对其他消费者后才删共享规则。
8. 更新账本，运行本页业务回归、桌面手机截图和受影响共享组件检查；提交并记录。

## 必须实测
状态：无权限、过期、长列表、操作失败。
专项：只用临时数据库；服务端权限仍有效；不可逆不试正式用户。
另查：长数据、空值、返回/取消、焦点、保存中防重复、失败保留、只读和跨班隔离（适用时）。浏览器不能写正式数据。

## 完成判据
全部保全动作有新入口与证据；桌面/手机/主要浮层可用；参考气质和比例达标；旧布局退出；测试没有通过削弱断言变绿。结果写当前commit，不复用旧报告标pass。

## 2026-09-13 实施记录

- 实现提交：`14222f3`；证据：`../evidence/admin-operations-20260913.md`。
- 管理员会话、账户检索、手机号、设备、续期、状态、导出/永久删除、兑换码和审计均有新入口；接口、管理员权限和审计契约保持不变。
- 页面改为 `features/admin` 内的页面级组件、controller和CSS Module。旧内联页面和全局 `admin.css` 已退出；共享原生对话框统一详情与危险操作，手机长内容和页脚可达。
- 浏览器使用隔离临时数据库验证1280×720与390×844、空态、30条分页、编辑/取消/确认、失败保留和无横向溢出。生产构建、197项测试、扩展认证集成、全量ESLint和115项六宽度严格审计通过。
- 管理员页面不属于工作区只读模式；真实物理手机、HTTPS、外部网络和线上部署不属于本记录的已验证范围。
