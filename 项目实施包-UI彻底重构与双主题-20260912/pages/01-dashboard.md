# 首页：逐页执行卡

状态：implemented。模块ID：dashboard。源码相对 app/w/[token]（明确app/lib前缀除外）：features/dashboard/DashboardView.tsx;features/dashboard/read-model.ts;ClassroomApp.tsx:MobileHome。
参考：C1/C5。派生页面使用对应页面族构图和校园token，不把缺少专属图解释为可回退旧UI。

## 保全动作（种子，实施前展开）
真实待办跳转、课程日程、听写摘要、值日、学生近況、家庭入口。
逐个查旧handler、字段、权限和两端实现，补feature-preservation.csv。每个实际浮层补overlay-inventory.csv，不能只凭本卡认定无其他功能。

## 布局落地
桌面：场景与待办并排→课程/听写→本周事务；空态收缩。
手机：薄横幅→待办→课程→常用入口；首项操作首屏可达。
主操作由本页任务决定，顶部只一个；低频操作入有名称的菜单；插画按06的角色与文字安全区制作，空数据不给假数据填充。

## 修改步骤
1. 查当前源码中本卡符号，记录旧状态与调用链；read-only模式查看两端可见入口。
2. 将筛选/选中/草稿/提交收敛到feature controller；复用既有业务计算，不把旧DOM复制进新容器。
3. 新建features/dashboard下视图与局部样式，使用共享组件实现上述布局；接到同一workspace操作层。
4. 依次实现初始页、详情、创建、编辑、长内容、空态、只读、失败状态；不适用项写明依据。
5. 验证主题与宽度变化不重置草稿；操作的服务端确认/本地应用状态不混淆。
6. 在独立新界面及混合迁移环境各看一次，确认旧CSS不参与此页外观。
7. 删除/替换：Dashboard旧布局与MobileHome重复装饰。核对其他消费者后才删共享规则。
8. 更新账本，运行本页业务回归、桌面手机截图和受影响共享组件检查；提交并记录。

## 必须实测
状态：暂无任务、无日程、有逾期、长标题。
专项：点击每个待办进入正确模块；空日程不影响课程存在性；数字来源逐项核对。
另查：长数据、空值、返回/取消、焦点、保存中防重复、失败保留、只读和跨班隔离（适用时）。浏览器不能写正式数据。

## 完成判据
全部保全动作有新入口与证据；桌面/手机/主要浮层可用；参考气质和比例达标；旧布局退出；测试没有通过削弱断言变绿。结果写当前commit，不复用旧报告标pass。

## 2026-09-13 盘点记录

- 盘点提交：`bc41088`；证据：`../evidence/dashboard-inventory-20260913.md`。
- 桌面与手机共用 `Dashboard`，已有作业/考勤/听写导航、教师事项、听写摘要、值日、学生近况和家庭入口；手机只增加共享账户入口。首页无自有写入和浮层。
- 已确认课程表未进入今日安排、作业状态未按当前名册二次过滤、沟通待跟进未按班级过滤、学生近况缺少事实来源和学生上下文等数据缺口。
- 当前仍使用 `components/campus/Dashboard.tsx` 与全局 `dashboard.css`；A-01保持未运行，待TASK-03夹具和TASK-12/TASK-17正式迁移后验收。

## 2026-09-13 实施记录

- 主体源码提交：`b9036a7`；复核修复：`015daa3`；证据：`../evidence/dashboard-implementation-20260913.md`。
- 桌面与手机共用新的局部样式首页，按班主任当天工作顺序展示日程、作业、家校、听写、课程/班务、值日和学生事实；家庭学习入口降为次要但未删除。
- 作业、沟通、事项、值日和学生事实均按当前班/当前名册读取；课程按真实星期进入当天日程，空数据不补造事实。学生提醒显示日期与来源并携带稳定学生ID进入成长档案。
- 旧`Dashboard.tsx`、`dashboard.css`及legacy生成入口已退出；A-01已按实际视口、隔离夹具和源码职责登记。
