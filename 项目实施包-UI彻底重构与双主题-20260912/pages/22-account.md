# 账户与班级管理：逐页执行卡

状态：migrated-and-verified。模块ID：account。旧源码相对 app/w/[token]（明确app/lib前缀除外）：ClassroomApp.tsx:AccountDialog/MobileWorkbench;ClassSwitcher.tsx；新实现：features/account/AccountCenter.tsx、useAccountCenter.ts、operations.ts。
参考：共享对话框。派生页面使用对应页面族构图和校园token，不把缺少专属图解释为可回退旧UI。

## 保全动作（种子，实施前展开）
账户有效期、退出、班级新增编辑删除、备份导出/预检恢复。
逐个查旧handler、字段、权限和两端实现，补feature-preservation.csv。每个实际浮层补overlay-inventory.csv，不能只凭本卡认定无其他功能。

## 布局落地
桌面：账号/班级上下文分别展示，危险操作低强调。
手机：抽屉/全屏表单统一行为。
主操作由本页任务决定，顶部只一个；低频操作入有名称的菜单；插画按06的角色与文字安全区制作，空数据不给假数据填充。

## 修改步骤
1. 查当前源码中本卡符号，记录旧状态与调用链；read-only模式查看两端可见入口。
2. 将筛选/选中/草稿/提交收敛到feature controller；复用既有业务计算，不把旧DOM复制进新容器。
3. 新建features/account下视图与局部样式，使用共享组件实现上述布局；接到同一workspace操作层。
4. 依次实现初始页、详情、创建、编辑、长内容、空态、只读、失败状态；不适用项写明依据。
5. 验证主题与宽度变化不重置草稿；操作的服务端确认/本地应用状态不混淆。
6. 在独立新界面及混合迁移环境各看一次，确认旧CSS不参与此页外观。
7. 删除/替换：旧账户浮层/重复mobile账号。核对其他消费者后才删共享规则。
8. 更新账本，运行本页业务回归、桌面手机截图和受影响共享组件检查；提交并记录。

## 必须实测
状态：过期只读、备份错误、空班/删班。
专项：删除关联和恢复确认；权限/会话不变；不展示假续费闭环。
另查：长数据、空值、返回/取消、焦点、保存中防重复、失败保留、只读和跨班隔离（适用时）。浏览器不能写正式数据。

## 完成判据
全部保全动作有新入口与证据；桌面/手机/主要浮层可用；参考气质和比例达标；旧布局退出；测试没有通过削弱断言变绿。结果写当前commit，不复用旧报告标pass。

## 2026-09-13 实施记录

- 实现提交：`af28a20`；证据：`../evidence/account-operations-20260913.md`。
- 桌面和手机改为同一个原生 `AccountCenter`。账户读取、班级增改切删、完整备份和当前设备退出均有唯一入口；演示与到期只读状态使用同一结构。
- 班级操作提取为纯函数并补齐两班各50人、空班、新班和删除关联回归。旧 `ClassSwitcher`、旧账户弹窗、手机重复账户/班级层及其全局样式已删除。
- 删除和退出采用对话框内确认，保存失败保留工作区草稿；有效与无效备份均在隔离浏览器实测。TypeScript、全量ESLint、生产构建、196项测试、认证集成和115项六宽度严格审计通过。
- 管理员页面、真实物理手机、弱网、HTTPS和线上部署不属于本记录的已验证范围。
