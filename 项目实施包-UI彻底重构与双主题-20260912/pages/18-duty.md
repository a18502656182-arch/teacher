# 值日岗位：逐页执行卡

状态：implemented。模块ID：duty。源码相对 app/w/[token]（明确app/lib前缀除外）：Duty.tsx、Duty.module.css、features/duty/operations.ts；接线位于ClassroomApp.tsx。
参考：责任编排派生。派生页面使用对应页面族构图和校园token，不把缺少专属图解释为可回退旧UI。

## 保全动作（种子，实施前展开）
教学日轮换、小组、固定学生、岗位编辑、检查台账。
逐个查旧handler、字段、权限和两端实现，补feature-preservation.csv。每个实际浮层补overlay-inventory.csv，不能只凭本卡认定无其他功能。

## 布局落地
桌面：周期→岗位/当天名单→记录。
手机：当天名单→岗位详情/人员选择。
主操作由本页任务决定，顶部只一个；低频操作入有名称的菜单；插画按06的角色与文字安全区制作，空数据不给假数据填充。

## 修改步骤
1. 查当前源码中本卡符号，记录旧状态与调用链；read-only模式查看两端可见入口。
2. 将筛选/选中/草稿/提交收敛到feature controller；复用既有业务计算，不把旧DOM复制进新容器。
3. 新建features/duty下视图与局部样式，使用共享组件实现上述布局；接到同一workspace操作层。
4. 依次实现初始页、详情、创建、编辑、长内容、空态、只读、失败状态；不适用项写明依据。
5. 验证主题与宽度变化不重置草稿；操作的服务端确认/本地应用状态不混淆。
6. 在独立新界面及混合迁移环境各看一次，确认旧CSS不参与此页外观。
7. 删除/替换：duty3旧布局。核对其他消费者后才删共享规则。
8. 更新账本，运行本页业务回归、桌面手机截图和受影响共享组件检查；提交并记录。

## 必须实测
状态：6/7教学日、未分组、固定人员。
专项：跟随课程配置；轮换不覆盖固定名单；记录日期正确。
另查：长数据、空值、返回/取消、焦点、保存中防重复、失败保留、只读和跨班隔离（适用时）。浏览器不能写正式数据。

## 完成判据
全部保全动作有新入口与证据；桌面/手机/主要浮层可用；参考气质和比例达标；旧布局退出；测试没有通过削弱断言变绿。结果写当前commit，不复用旧报告标pass。

## 2026-09-12执行结果

- 桌面与手机已改用同一独立校园值日页面；岗位检查、本周安排、岗位设置和检查台账共用当前班操作层。6/7教学日、非连续组号、未分组、固定/临时负责人、检查快照、恢复自动、台账备注、跨班、只读、失败重试和真实409均在隔离环境验证。
- 新增值日语义插画槽与独立水彩清洁角素材；旧 `duty3-*` / `mobile-duty-*` DOM已移除，死规则留待TASK-30统一核对删除。岗位编辑脏关闭和共享学生选择portal仍是明确缺口，空班视觉A-18-03保持未运行。
- 详细证据见 `evidence/duty-operations-20260912.md`；A-18-01、02、04至09记录本次提交。

## 2026-09-13 完成补强

轮换、检查、指定、岗位与台账写入等待workspace确认；岗位编辑增加脏草稿关闭确认，失败保留当前编辑器，保存中防重复。桌面/手机实查与六档布局通过；死旧规则归TASK-30。证据见`../evidence/management-implementation-20260913.md`。
