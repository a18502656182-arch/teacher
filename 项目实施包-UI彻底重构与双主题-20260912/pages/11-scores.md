# 成绩分析：逐页执行卡

状态：inventoried-with-gap。模块ID：scores。源码相对 `app/w/[token]`（明确 `app/lib` 前缀除外）：`ClassroomApp.tsx:Scores/MobileScores`、`ScoreTrends.tsx`、`ScoreItemAnalysis.tsx`、`features/scores/operations.ts`。

参考：教学分析派生。派生页面使用对应页面族构图和校园 token，不把缺少专属图解释为可回退旧 UI。

## 当前真实入口和动作

桌面和手机共用当前班考试数据，但目前仍是两套历史视图。共有动作如下：

- 无考试：真实空状态可新增第一场考试；不再用学生综合分生成一场假考试。
- 考试管理：新增、编辑、删除、切换；考试库可按名称/科目/日期搜索，按科目、年份、月份筛选，按日期排序并分页。
- 成绩录入：逐生逐科录入或清回空白；学生搜索、小组、科目、分数区间、重点标记和排序；当前筛选全选；批量统一录分、批量标记/取消重点；逐生建议。
- 统计：录分进度、已录平均、全科录完学生的总分分布、各科已录成绩平均；空白不进入分母，真实零分进入统计。
- 长期趋势：搜索并选择当前班学生，按时间范围、科目和考试分页查看相对满分的得分率。
- 题目/知识点：教师手工新增分析项、逐生录分/清空和删除；上传 JPG/PNG/WEBP/PDF 后只保存服务返回的 AI 候选，教师确认后才并入正式统计。
- 满分与区间：按科目设置满分及自定义分数段，总分区间独立保存。

## 已完成的业务修正

1. 新考试及新增科目为每名学生建立空记录，不再预写 `0`。
2. 真实零分计入录入人数；未录为空值，不生成完整总分，也不进入科目平均和分布分母。
3. 桌面和手机录分、批量录分、考试管理、人工分析项和试卷候选确认接同一当前班操作层；他班考试、学生和候选不能越界修改。
4. 成绩输入支持清回未录入；批量录分成功后桌面和手机都清空选择。
5. 编辑考试保留仍存在科目的真实成绩，新科目保持空白；删除考试只清理该考试的反思记录。删除最后一场考试后，考试反思显示真实空状态，不能再写入不存在的占位考试。
6. AI 候选只有 `待核对` 状态可确认；重复确认幂等，不会重复加入知识点统计。
7. 旧无 `classId` 考试仍可读取，规范化加载时写入当时活动班级快照；本机日期不再经过 UTC 截断。

## 当前保全边界

- 页面仍使用 `score5-*` 与旧 `MobileScores` DOM/CSS；独立校园成绩页、CSS 隔离和视觉验收尚未完成。
- 普通工作区编辑会先应用到本机草稿并关闭弹层，服务器失败由全局保存/恢复处理；尚未保留单个考试弹层上下文，也没有脏关闭提示。
- 将科目满分调低后，已有高分不会自动迁移或拦截；必须在后续数据契约决策后处理，不能静默改历史成绩。
- 删除人工分析项没有危险确认。上传失败可显示原因，但真实 OCR 服务、费用/配额、重试及文件留存删除策略仍未完成。
- `Student.score` 还是必填数字。当前仅在至少录入一科时同步为已录平均；清空最后一科不能把摘要表示为“未录”，需在后续模型迁移中解决。
- 100+ 名单的操作层已测；长科目、上传失败、弹层焦点、只读、保存失败与 409、桌面/手机视觉仍需随正式新页面实测。

## 后续页面迁移步骤

1. 新建 `features/scores` 下的 controller、校园桌面/手机视图和局部样式，复用现有操作层，不复制历史 handler。
2. 桌面采用考试上下文、录入/趋势/分析分视图；手机采用考试选择、当前视图和局部横滚。所有批量入口必须显示范围与人数。
3. 为无考试、部分录入、真实零分、不同满分、长科目、上传失败、只读和保存冲突建立可见状态。
4. 为考试管理、批量录分、满分区间、学生选择及试卷候选建立统一 Dialog/Sheet；验证脏关闭、焦点圈、背景锁定与手机键盘。
5. 新页面在独立环境及混合迁移环境各验一次；确认旧 `score5-*`/`MobileScores` 选择器不再命中后才删除旧 DOM/CSS。

## 完成判据

功能账本所有动作有新入口及证据；桌面、手机和主要浮层实测通过；空白/零分/未完成分母语义不回退；AI 候选确认边界保持；旧布局退出；测试没有通过削弱断言变绿。当前状态不满足这些最终判据。
