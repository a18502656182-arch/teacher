# 课堂工具：逐页执行卡

状态：implemented。模块ID：tools。源码相对 app/w/[token]（明确app/lib前缀除外）：ClassroomTools.tsx；共享操作：features/tools/operations.ts；局部样式：ClassroomTools.module.css。
参考：专注工具派生。派生页面使用对应页面族构图和校园token，不把缺少专属图解释为可回退旧UI。

## 保全动作（种子，实施前展开）
随机点名、请假排除、不重复、重置、临时分组、结果复制、历史。
逐个查旧handler、字段、权限和两端实现，补feature-preservation.csv。每个实际浮层补overlay-inventory.csv，不能只凭本卡认定无其他功能。

## 布局落地
桌面：选择工具→主要结果→历史次级入口。
手机：专注结果区+主要操作。
主操作由本页任务决定，顶部只一个；低频操作入有名称的菜单；插画按06的角色与文字安全区制作，空数据不给假数据填充。

## 修改步骤
1. 查当前源码中本卡符号，记录旧状态与调用链；read-only模式查看两端可见入口。
2. 将筛选/选中/草稿/提交收敛到feature controller；复用既有业务计算，不把旧DOM复制进新容器。
3. 新建features/tools下视图与局部样式，使用共享组件实现上述布局；接到同一workspace操作层。
4. 依次实现初始页、详情、创建、编辑、长内容、空态、只读、失败状态；不适用项写明依据。
5. 验证主题与宽度变化不重置草稿；操作的服务端确认/本地应用状态不混淆。
6. 在独立新界面及混合迁移环境各看一次，确认旧CSS不参与此页外观。
7. 删除/替换：旧工具卡片布局。核对其他消费者后才删共享规则。
8. 更新账本，运行本页业务回归、桌面手机截图和受影响共享组件检查；提交并记录。

## 必须实测
状态：全员请假、人数不足、全轮完成、多组。
专项：真实随机结果；排除正确；不自动记积分；复制历史内容正确。
另查：长数据、空值、返回/取消、焦点、保存中防重复、失败保留、只读和跨班隔离（适用时）。浏览器不能写正式数据。

## 完成判据
全部保全动作有新入口与证据；桌面/手机/主要浮层可用；参考气质和比例达标；旧布局退出；测试没有通过削弱断言变绿。结果写当前commit，不复用旧报告标pass。

## 2026-09-12 实际核验

已展开随机点名、当天请假排除、不重复、全轮完成、重置、临时分组、历史和复制。共享操作层修复 UTC 日期、切班串本轮、错误请假计数、抽取顺序展示和空组；两班隔离合成工作区实际验证桌面/手机、只读、人数不足、多组、历史复制和全员请假。

本卡仍未完成：生产页面继续使用 `.classroom-tools` / `.tool-*` 旧卡片结构，正式校园专注构图、旧 CSS 退出、操作级服务器确认、物理手机与弱网尚未验收，所以 `A-16-*` 不改为通过。证据见 `../evidence/classroom-tools-operations-20260912.md`。

## 2026-09-13 迁移完成

随机点名继续作为不落库的课堂瞬时工具；临时分组等待workspace服务器确认，失败保留本机结果和历史选择，保存中防重复。演示/只读模式禁用分组设置和生成，但随机点名、重置及复制仍可用。桌面、手机实查及六档布局通过；共享旧工具样式退出归TASK-30。证据见`../evidence/classroom-tools-implementation-20260913.md`。
